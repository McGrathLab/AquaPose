---
phase: 108-branch-reconciliation-repo-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - CHANGELOG.md
  - CLAUDE.md
  - .planning/
  - docs/
  - .github/workflows/docs.yml
  - src/aquapose/core/context.py
  - src/aquapose/engine/observers.py
  - src/aquapose/synthetic/fish.py
  - src/aquapose/synthetic/scenarios.py
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "D-01/FOUND-03: `main`'s 4 commits are ancestors of `dev` via a real merge commit — `git log --oneline dev..main` is empty."
    - "D-01: `dev`'s 1496 published commits are untouched — no rebase, no cherry-pick, no force, no history rewrite."
    - "D-04/D-05: the `main` branch ref and GitHub's default-branch setting are unchanged by this plan."
    - "D-02: every one of the 113 conflicts was resolved by a stated, path-scoped policy — never by `-X ours`, `-X theirs`, or `-s ours`."
    - "HAZARD 2 CLOSED: none of the 29 superseded paths that exist only on `main` survive the merge — `src/aquapose/core/midline/**`, `src/aquapose/training/yolo_{obb,pose,seg}.py`, `core/tracking/ocsort_wrapper.py`, `core/association/refinement.py`, `scripts/perf_validate.py`, and the 14 matching test files are absent from `dev`."
    - "The merge changed nothing under `src/`, `tests/`, or `scripts/` — dev's production tree is byte-identical across the merge commit."
    - "`.planning/` KEEP DECISION: main's 665-file historical project-record archive is deliberately kept — it is the only surviving source for 108-04's v2.2 backfill. `.planning/research/**` (5 files dev deleted) stays deleted."
    - "D-03: dev's package version is `1.2.0-dev.0` — ahead of the shipped `1.1.1`, still on the prerelease channel."
    - "D-10 groundwork: `CHANGELOG.md` carries main's shipped `v1.1.0` and `v1.1.1` sections, which dev never had."
    - "D-17/FOUND-02: `.github/workflows/docs.yml` on dev triggers on push to dev."
    - "D-13: the four dead `.rst` files are gone and main's 8-file API set is present."
    - "D-15/D-16: `docs/conf.py` carries `napoleon_use_ivar`, `autodoc_mock_imports`, `_resolve_release()`; `pyproject.toml`'s docs env is `detached = true`."
    - "THIS PLAN OWNS a66287a's four `src/` docstring hunks. The dev-side resolution drops them, so this plan re-applies them explicitly; 108-05 does not re-derive them."
    - "Nothing was pushed. The phase's single push is deferred to 108-05 (T-108-03)."
  artifacts:
    - path: "pyproject.toml"
      provides: "version 1.2.0-dev.0 plus a detached docs env with numpy"
      contains: "detached = true"
    - path: ".github/workflows/docs.yml"
      provides: "push trigger on main and dev"
      contains: "branches: [main, dev]"
    - path: "docs/conf.py"
      provides: "forward-ported Sphinx repair from a66287a"
      contains: "napoleon_use_ivar"
    - path: ".planning/milestones/v2.2-phases/29-guidebook-audit/29-01-SUMMARY.md"
      provides: "the v2.2 phase record 108-04 reconstructs its milestone entry from"
    - path: "src/aquapose/core/context.py"
      provides: "a66287a's escaped cache-glob docstring"
  key_links:
    - from: "dev"
      to: "main"
      via: "merge commit"
      pattern: "git log --oneline dev..main -> empty"
    - from: "pyproject.toml [tool.hatch.envs.docs]"
      to: "docs/conf.py autodoc_mock_imports"
      via: "detached env relies on mocks instead of installed deps"
      pattern: "detached = true"
---

<objective>
Bring main's 4 commits onto dev with a real merge (D-01), resolving all **113** conflicts by a
stated path-scoped policy (D-02), **removing the 29 superseded files git adds silently with no
conflict signal**, and re-applying the four `src/` docstring hunks from `a66287a` that a dev-side
resolution necessarily drops.

Purpose: dev becomes the single source of truth with true ancestry to main, so every future
main-to-dev sync is conflict-free (FOUND-03). FOUND-02's workflow file and FOUND-01's `conf.py`
repair arrive as merge content and are verified rather than re-authored.

Output: one merge commit plus one follow-up commit on dev; `pyproject.toml` at `1.2.0-dev.0` with
a detached docs env; `docs/` and `.github/workflows/docs.yml` matching main's repaired tree;
main's historical `.planning/` archive tracked; dev's `src/` and `tests/` trees unchanged apart
from four one-line docstring edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**No worktree isolation.** This plan runs on the main working tree with HEAD on `dev`. It performs
branch surgery and file deletions. Do not run it in a detached worktree and do not run any task in
parallel with another.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-01-BLOCKER.md

**Verified merge facts, re-derived 2026-08-17 with `git merge-tree --write-tree --name-only dev main`.
These supersede every conflict-surface claim in the superseded plan, and correct two counts in the
BLOCKER: there are 28 `src/`+`tests/` silent additions (not 23), plus `scripts/perf_validate.py`.**

Merge base `cdd9a74`. dev HEAD `c25d5b8`. main `2aba732`, four commits ahead: `19ea21e` (a 903-file
squashed snapshot of dev's ~March tree with no shared blob ancestry, which is why almost everything
collides), `9aa1fd6`, `a66287a`, `2aba732`.

Tracked-file counts: main 935, dev 323, base 67. Dry-run merged tree: **1022** files.

**Conflict surface — exactly 113 paths (99 add/add, 8 content, 6 modify/delete):**

| Count | Path set | Resolution |
|---|---|---|
| 62 | conflicts under `src/` | **dev side** |
| 36 | conflicts under `tests/` | **dev side** |
| 5 | `.planning/GUIDEBOOK.md`, `MILESTONES.md`, `PROJECT.md`, `ROADMAP.md`, `STATE.md` | **dev side** (dev is 1496 commits newer) |
| 1 | `CLAUDE.md` | **dev side** |
| 1 | `.gitignore` | **dev side**, then re-apply main's two-line `_readthedocs/` hunk |
| 1 | `CHANGELOG.md` | **main side** — main is a verified strict superset: it prepends the shipped `v1.1.1` and `v1.1.0` sections above a byte-identical copy of dev's file. A dev-side pick would erase the release history D-10 depends on. |
| 1 | `.planning/REQUIREMENTS.md` (deleted in main, modified in dev) | **keep dev's version** via `git add` |
| 5 | `.planning/research/ARCHITECTURE.md`, `FEATURES.md`, `PITFALLS.md`, `STACK.md`, `SUMMARY.md` (deleted in dev, modified in main) | **keep deleted** via `git rm` |
| 1 | `pyproject.toml` | **hand-resolve** per the table below |

**Zero conflicts occur under `docs/` or `.github/`.** Those paths merge cleanly to main's blobs
because dev never touched them since the base. The dry run confirms the merged `docs/conf.py`,
`docs/index.md`, and `.github/workflows/docs.yml` come out byte-identical to main's, that
`docs/api/` becomes main's 8-file set, and that dev's dead
`docs/api/mesh.rst`, `optimization.rst`, `segmentation.rst`, `utils.rst` are removed by the merge
itself (D-13 satisfied without a manual delete).

**HAZARD 2 — 29 paths git adds SILENTLY, with no conflict, because they exist in main but in
neither dev nor the merge base.** These are modules dev deliberately refactored away. Left in
place they are dead code that fails only at import time. None of the 29 exists on disk today.

    src/aquapose/core/association/refinement.py
    src/aquapose/core/midline/__init__.py
    src/aquapose/core/midline/backends/__init__.py
    src/aquapose/core/midline/backends/pose_estimation.py
    src/aquapose/core/midline/backends/segmentation.py
    src/aquapose/core/midline/crop.py
    src/aquapose/core/midline/midline.py
    src/aquapose/core/midline/orientation.py
    src/aquapose/core/midline/stage.py
    src/aquapose/core/midline/types.py
    src/aquapose/core/tracking/ocsort_wrapper.py
    src/aquapose/training/yolo_obb.py
    src/aquapose/training/yolo_pose.py
    src/aquapose/training/yolo_seg.py
    scripts/perf_validate.py
    tests/unit/core/association/test_refinement.py
    tests/unit/core/midline/test_direct_pose_backend.py
    tests/unit/core/midline/test_midline_stage.py
    tests/unit/core/midline/test_orientation.py
    tests/unit/core/midline/test_pose_estimation_backend.py
    tests/unit/core/midline/test_segmentation_backend.py
    tests/unit/segmentation/__init__.py
    tests/unit/segmentation/test_affine_crop.py
    tests/unit/segmentation/test_dataset.py
    tests/unit/segmentation/test_detector.py
    tests/unit/test_midline.py
    tests/unit/tracking/test_ocsort_wrapper.py
    tests/unit/training/test_yolo_pose.py
    tests/unit/training/test_yolo_seg.py

Superseded on dev by `src/aquapose/core/types/midline.py`,
`src/aquapose/evaluation/stages/midline.py`, `src/aquapose/training/yolo_training.py`, and the
detection/reconstruction backends.

**`.planning/` main-only file set — 665 paths — KEEP DECISION (explicit, not incidental).**
On main these break down as `milestones/**` 561, `quick/` 48, `todos/` 19, `debug/` 19,
`inbox/` 13, plus `PIPELINE.md`, `RETROSPECTIVE.md`, `SOURCES.md`, `coco-interchange-plan.md`,
`seed-pseudo-labeling.md`. **All 665 are kept.** Reasons:

1. `main:.planning/milestones/v2.2-phases/29-*` through `33-*` hold the PLANs, SUMMARYs, CONTEXTs
   and VERIFICATIONs for phases 29 through 33.1. Those directories **do not exist on dev at all** —
   neither tracked nor on disk. They are the only surviving source material for 108-04's v2.2
   Backends backfill (D-21). Dropping them makes that requirement unachievable.
2. `milestones/v1.0-phases` through `v3.5-phases` are the same archive for every earlier milestone,
   and `RETROSPECTIVE.md` is read by GSD planning itself.
3. D-18's stated rationale for un-ignoring `.planning/` is that it makes the project record
   reproducible; dropping the record contradicts it.
4. Cost is bounded and verified: 6.0 MB, 656 `.md` plus 17 `.gitkeep`, 1 `.txt`, 1 `.svg`,
   1 `.json`; largest blob 184 KB; and `pyproject.toml:57` already excludes `.planning/` from the
   sdist so none of it ships to PyPI.
5. Deciding per-subdirectory which historical scratch "deserves" tracking is exactly the
   archaeology D-21 rules out.

Zero filename collisions exist between these 665 paths and anything on disk (verified) — dev's
`.planning/todos/` uses `completed/` and `pending/` subdirectories while main's is flat, so both
sets coexist. `.planning/` is still in `.gitignore` at this point; that does not block the merge,
because the merged paths become **tracked** and gitignore never applies to tracked files. Plan
108-03 removes the rule.

**Expected post-merge tracked-file count: 1022 minus 29 (hazard-2 purge) minus 5 (research kept
deleted) = 988.**

**`pyproject.toml` resolution targets:**

| Region | dev side | main side | Resolution |
|---|---|---|---|
| `project.version` (line 7) | `1.1.0-dev.7` | `1.1.1` | **`1.2.0-dev.0`** (D-03) |
| `project.license` (line 11) | `{text = "MIT"}` | `{text = "MIT"}` | Leave MIT — plan 108-02 owns the relicense |
| MIT OSI classifier (line 18) | present | present | Leave — 108-02 owns it |
| `project.dependencies` | grown (`timm`, `shapely`, `pytorch-metric-learning`, `igraph`, `leidenalg`, `ultralytics`, ...) | older | **Keep dev's verbatim** |
| `[tool.hatch.envs.docs]` (line 83) | no `detached`, no `numpy` | `detached = true` plus a three-line explanatory comment, and `numpy` appended to deps | **Take main's** (D-15) |
| `[tool.hatch.envs.docs.scripts] build` | identical on both | identical | either |
| `[tool.semantic_release]` tables | newer | stale | **Keep dev's** |
| everything else | newer | 1496 commits stale | **Keep dev's** |

**a66287a's four `src/` docstring hunks — THIS PLAN OWNS THEM (task 3).** All four files are in the
113-conflict set and resolve dev-side, so the merge drops the fixes. dev still has the unrepaired
text at these exact locations:

| File | Current dev text | Repair |
|---|---|---|
| `src/aquapose/core/context.py:54` | the cache-path Args entry names `*_cache.pkl` bare, which RST parses as emphasis | wrap the glob in double backticks |
| `src/aquapose/synthetic/fish.py:37` | the curvature Args entry names `radius=1/|curvature|` bare, which RST parses as a substitution reference | wrap the expression in double backticks |
| `src/aquapose/engine/observers.py:110` | `Delivery order:` is immediately followed by the numbered list with no blank line | insert one blank line between them |
| `src/aquapose/synthetic/scenarios.py:57` | the crossing half-angle sentence is immediately followed by the bullet list with no blank line | insert one blank line between them |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the merge and resolve all 113 conflicts by the stated path-scoped policy</name>
  <files>pyproject.toml, .gitignore, CHANGELOG.md, CLAUDE.md, .planning/, src/, tests/</files>
  <read_first>
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-01-BLOCKER.md` in full — the
      reason this plan exists and the record of the aborted first attempt.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` sections D-01, D-02,
      D-03, D-04, D-05, D-15.
    - `pyproject.toml` (current dev state) — line 7 `version`, line 11 `license`, line 18 the MIT
      classifier, the `dependencies` array, and `[tool.hatch.envs.docs]` at line 83.
    - `git show main:pyproject.toml` — the source-of-truth analog for the docs env block:
      `detached = true`, its three-line explanatory comment, and `numpy` appended to the
      dependency list.
    - `git show a66287a -- pyproject.toml .gitignore` — the exact two hunks being forward-ported.
  </read_first>
  <action>
    Record `PRE=$(git rev-parse HEAD)` and `MAIN_PRE=$(git rev-parse main)` and write both into the
    SUMMARY. Confirm `git rev-parse --abbrev-ref HEAD` is `dev` and `git status --porcelain` lists
    only the two untracked paths `11.0` and `~/` — plan 108-03 owns those, leave them alone.
    Confirm `git stash list` shows the pre-existing unrelated `stash@{0}`; do not touch it and do
    not run any `git stash` command anywhere in this plan.

    Run `git merge --no-ff main -m "merge: reconcile main into dev (1.1.1 release history + a66287a Sphinx repair)"`.
    It will stop with 113 conflicts. That is expected. Do **not** run `git merge --abort`.

    Forbidden throughout this plan: `git rebase`, `git cherry-pick`, `-X ours`, `-X theirs`,
    `-s ours`, `--force`, `--force-with-lease`, `push --force`, and any checkout of, commit to, or
    push to `main`. dev has 1496 published commits (D-01, T-108-01) and main is frozen (D-04,
    D-05, T-108-02).

    Apply the resolution policy one path set at a time. `git checkout --ours` or `--theirs` applied
    to an explicitly enumerated path set is a stated policy, not a blanket side-pick, and is what
    D-02 asks for.

    1. **dev side, `src/` and `tests/`:** feed `git diff --name-only --diff-filter=U -- src tests`
       into `git checkout --ours --` and then `git add --`. Expect 98 paths.
    2. **dev side, top-level docs:** same treatment for `CLAUDE.md` and `.gitignore`. Then
       re-apply main's a66287a hunk to `.gitignore` by hand: directly after the existing
       `docs/_build/` line under the `# Sphinx documentation` heading, add a comment line reading
       `# Read the Docs output dir (created by .readthedocs.yaml build commands)` followed by the
       rule `_readthedocs/`. Leave the `.planning/` rule in place — plan 108-03 removes it, and its
       removal must be that plan's only `.gitignore` change.
    3. **main side, `CHANGELOG.md`:** `git checkout --theirs -- CHANGELOG.md` then `git add`. This
       is the only main-side content pick in the plan and it is deliberate.
    4. **dev side, `.planning/` docs:** `--ours` then `add` for `.planning/GUIDEBOOK.md`,
       `.planning/MILESTONES.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and
       `.planning/STATE.md`.
    5. **keep dev's version:** `git add .planning/REQUIREMENTS.md`. main deleted it; dev's modified
       version is already in the tree and is the one that survives.
    6. **keep deleted:** `git rm -f` the five `.planning/research/*.md` paths. Do not restore them.
       This is an explicit "dev deleted these on purpose" decision, not a mechanical resolution.
    7. **hand-resolve `pyproject.toml`**, never with a side-pick. Produce a file that is dev's
       content in full except for two changes: set `version = "1.2.0-dev.0"` on line 7 (D-03); and
       replace the `[tool.hatch.envs.docs]` block with main's version of it — main's three-line
       explanatory comment, the `detached = true` key, and main's dependency list ending with the
       `numpy` entry. Keep dev's `dependencies` array verbatim; it must still contain `timm`,
       `shapely`, `pytorch-metric-learning`, `igraph`, `leidenalg`, and `ultralytics`. Keep
       `license = {text = "MIT"}` and the MIT OSI classifier untouched — plan 108-02 owns the
       relicense and owns this file from wave 3 onward. Keep dev's `[tool.semantic_release]` tables
       and the `[tool.hatch.build.targets.sdist]` exclude list unchanged. Keep the existing
       `[tool.hatch.envs.docs.scripts] build` line. Then `git add pyproject.toml`.

    Do **not** commit yet — task 2 must purge the silent additions inside the same merge. Do
    **not** push.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only --diff-filter=U)" &amp;&amp; git rev-parse -q --verify MERGE_HEAD &gt;/dev/null &amp;&amp; grep -q 'version = "1.2.0-dev.0"' pyproject.toml &amp;&amp; grep -q 'detached = true' pyproject.toml &amp;&amp; grep -q '^_readthedocs/$' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=U` outputs zero lines — no unresolved conflict remains.
    - `git rev-parse -q --verify MERGE_HEAD` exits 0 — the merge is still in progress, uncommitted.
    - `git rev-parse main` equals the recorded `MAIN_PRE` value.
    - `grep -c 'version = "1.2.0-dev.0"' pyproject.toml` returns `1`.
    - `grep -c 'detached = true' pyproject.toml` returns `1`.
    - `grep -cE '^ *"numpy",$' pyproject.toml` returns `1`.
    - `grep -q 'timm' pyproject.toml`, `grep -q 'pytorch-metric-learning' pyproject.toml`, and `grep -q 'leidenalg' pyproject.toml` each exit 0.
    - `grep -c 'text = "MIT"' pyproject.toml` returns `1` — 108-02 still owns the relicense.
    - `grep -c '^_readthedocs/$' .gitignore` returns `1` and `grep -c '^\.planning/$' .gitignore` returns `1`.
    - `grep -c '^## v1.1.1 ' CHANGELOG.md` returns `1`, `grep -c '^## v1.1.0 ' CHANGELOG.md` returns `1`, and `grep -c '^## v1.1.0-dev.7 ' CHANGELOG.md` returns `1`.
    - `git ls-files .planning/research | wc -l` returns `0`.
    - `git ls-files .planning/REQUIREMENTS.md | wc -l` returns `1`.
    - `git ls-files .planning/milestones/v2.2-phases | wc -l` is greater than `35` — the v2.2 archive 108-04 depends on is staged.
    - `git reflog dev | grep -ciE 'rebase|cherry-pick'` returns `0`.
  </acceptance_criteria>
  <done>All 113 conflicts are resolved by the stated path-scoped policy, `pyproject.toml` is hand-resolved to `1.2.0-dev.0` with a detached docs env, the five `.planning/research/` files stay deleted, main's `.planning/` archive is staged, and the merge is staged but not committed.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the 29 silently-resurrected paths, scan for secrets, and commit the merge</name>
  <files>src/, tests/, scripts/</files>
  <read_first>
    - The HAZARD 2 block in this plan's `<context>` — the authoritative 29-path list.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-01-BLOCKER.md` section
      "Hazard 2" — the supersession mapping explaining why each path is dead on dev.
    - `.pre-commit-config.yaml` — note that `detect-secrets` runs with `--baseline .secrets.baseline`
      and has **no** `^\.planning/` exclude, while `trailing-whitespace`, `end-of-file-fixer`,
      `check-yaml`, and `check-added-large-files` all do.
    - `.secrets.baseline` — the existing allowlist.
    - `git ls-files src/aquapose/core/types/midline.py src/aquapose/evaluation/stages/midline.py src/aquapose/training/yolo_training.py` — the live replacements that make the resurrected modules dead.
  </read_first>
  <action>
    Still inside the unfinished merge. Derive the silent-addition set mechanically rather than
    transcribing it: `git diff --cached --diff-filter=A --name-only $PRE -- src tests scripts`
    lists exactly the paths the merge adds under those trees relative to pre-merge dev. It must
    print **29** paths and they must match the HAZARD 2 list exactly. If the count or the contents
    differ, stop and report — do not improvise.

    Remove every one of them from both the index and the working tree with `git rm -f`, feeding the
    derived list through `xargs`. Then remove the directories left empty: `src/aquapose/core/midline/`,
    `tests/unit/core/midline/`, and `tests/unit/segmentation/`. Check `tests/unit/tracking/` and
    `tests/unit/training/` before touching them — both retain surviving tests and must stay.

    Do **not** remove anything under `docs/`. The four `.rst` files the merge adds there
    (`engine`, `evaluation`, `synthetic`, `training`) are wanted (D-13).

    Prove the merge is content-neutral for production code before committing:
    `git diff --cached --name-only $PRE -- src tests scripts` must print zero lines.

    Scan the 665 newly tracked `.planning/` files, which have never been through the secret hooks.
    Run `hatch run pre-commit run detect-secrets --all-files`. **If it reports any finding, stop and
    report to the user — do not edit `.secrets.baseline` to silence it.** Also run
    `grep -rniE '(api[_-]?key|client[_-]?secret|access[_-]?token|password)[[:space:]]*[:=]' .planning/`
    and `grep -rlE 'ghp_|github_pat_|BEGIN [A-Z ]*PRIVATE KEY' .planning/`; any hit is a hard stop
    for user review. Confirm `find .planning -type f -size +1M` prints nothing.

    Complete the merge with `git commit --no-edit`. Let the pre-commit hooks run; do **not** use
    `--no-verify`. If a hook rewrites a merged `.planning/` file, re-stage and re-run rather than
    bypassing.

    Do **not** push. `release.yml` runs `semantic-release version` on every push to dev with
    `contents: write`; the phase's single push is deferred to plan 108-05 so no prerelease tag can
    be cut against an MIT-declared tree (T-108-03).
  </action>
  <verify>
    <automated>test "$(git ls-files | wc -l)" -eq 988 &amp;&amp; test "$(git ls-files src/aquapose/core/midline tests/unit/core/midline tests/unit/segmentation scripts/perf_validate.py | wc -l)" -eq 0 &amp;&amp; test -z "$(git log --oneline dev..main)" &amp;&amp; hatch run python -c "import aquapose"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse -q --verify MERGE_HEAD` exits non-zero — the merge is committed, not in progress.
    - `git log --oneline -1 --merges` names the new merge commit and `git rev-list --parents -n1 HEAD | wc -w` returns `3`.
    - `git log --oneline dev..main` outputs zero lines (FOUND-03).
    - `git rev-parse main` equals the recorded `MAIN_PRE` value.
    - `git ls-files | wc -l` returns `988`.
    - `git ls-files src/aquapose/core/midline | wc -l` returns `0`.
    - `git ls-files src/aquapose/training/yolo_obb.py src/aquapose/training/yolo_pose.py src/aquapose/training/yolo_seg.py | wc -l` returns `0`.
    - `git ls-files src/aquapose/core/tracking/ocsort_wrapper.py src/aquapose/core/association/refinement.py | wc -l` returns `0`.
    - `git ls-files tests/unit/core/midline tests/unit/segmentation tests/unit/test_midline.py tests/unit/tracking/test_ocsort_wrapper.py tests/unit/training/test_yolo_pose.py tests/unit/training/test_yolo_seg.py tests/unit/core/association/test_refinement.py | wc -l` returns `0`.
    - `git ls-files scripts/perf_validate.py | wc -l` returns `0`.
    - `test -e src/aquapose/core/midline` and `test -e tests/unit/segmentation` each exit non-zero — nothing lingers untracked on disk either.
    - `git diff --name-only $PRE HEAD -- src tests scripts` outputs zero lines — the merge changed no production code.
    - `git ls-files src/aquapose/training/yolo_training.py src/aquapose/core/types/midline.py src/aquapose/evaluation/stages/midline.py | wc -l` returns `3` — the live replacements survived.
    - `hatch run python -c "import aquapose"` exits 0.
    - `hatch run pre-commit run detect-secrets --all-files` exits 0 and `git diff --exit-code HEAD -- .secrets.baseline` exits 0 — the baseline was not widened.
    - `find .planning -type f -size +1M` outputs zero lines.
    - `git status --porcelain` lists only `11.0` and `~/`.
  </acceptance_criteria>
  <done>The merge is committed with true ancestry, dev fully contains main, all 29 superseded paths are absent from both the index and the working tree, dev's production code is byte-identical across the merge, and no secret entered history.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the merged docs/CI artifacts and re-apply a66287a's four dropped docstring hunks</name>
  <files>docs/conf.py, docs/index.md, docs/api/index.rst, .github/workflows/docs.yml, src/aquapose/core/context.py, src/aquapose/engine/observers.py, src/aquapose/synthetic/fish.py, src/aquapose/synthetic/scenarios.py</files>
  <read_first>
    - `docs/conf.py` (post-merge) alongside `git show main:docs/conf.py` — the source-of-truth analog.
    - `.github/workflows/docs.yml` (post-merge) alongside `git show main:.github/workflows/docs.yml`.
    - `docs/index.md` (post-merge) — main's version restores the `reports/z_uncertainty_report`
      toctree entry, which a66287a added specifically to silence a `-W` fatal orphan-document warning.
    - `git show a66287a -- src/` — the four exact one-line hunks this task re-applies, with their
      before/after text.
    - `src/aquapose/core/context.py` around line 54, `src/aquapose/synthetic/fish.py` around
      line 37, `src/aquapose/engine/observers.py` around line 110, and
      `src/aquapose/synthetic/scenarios.py` around line 57 — read each before editing.
    - `.claude/rules/code-style.md` — docstrings are Google style; these repairs must preserve it.
  </read_first>
  <action>
    First, verification. The merge delivered these; the job is to confirm they survived, and to
    repair only by restoring main's blob with `git checkout main -- <path>` if one drifted.

    1. `docs/conf.py` byte-identical to `git show main:docs/conf.py`, containing
       `napoleon_use_ivar`, `autodoc_mock_imports`, `def _resolve_release`, the aliased
       `importlib.metadata` version import, and the version-as-string assignment. Plan 108-05 will
       extend `autodoc_mock_imports`; do not extend it here.
    2. `docs/api/` contains exactly `calibration.rst`, `core.rst`, `engine.rst`, `evaluation.rst`,
       `index.rst`, `io.rst`, `synthetic.rst`, `training.rst`, and none of `mesh.rst`,
       `optimization.rst`, `segmentation.rst`, `utils.rst`.
    3. `.github/workflows/docs.yml` byte-identical to main's, with `branches: [main, dev]` under
       `on: push:` and the `pull_request` block retained. This is what satisfies FOUND-02.
    4. `docs/index.md` toctree lists `reports/z_uncertainty_report`.

    Do **not** modify `.github/workflows/release.yml`, `publish.yml`, `test.yml`, or
    `slow-tests.yml`. Do **not** add the docs build to `.pre-commit-config.yaml` — D-17 rejects it
    explicitly on push-latency grounds.

    Second, re-apply a66287a's four `src/` docstring hunks, which the dev-side conflict resolution
    necessarily dropped. This plan owns them; 108-05 must not re-derive them. Each is a
    single-line, text-only edit inside a docstring:

    - `src/aquapose/core/context.py` — in `load_stage_cache`'s Args entry for `path`, wrap the
      `*_cache.pkl` glob in double backticks so RST stops reading the leading asterisk as emphasis.
    - `src/aquapose/synthetic/fish.py` — in `FishConfig`'s `curvature` Args entry, wrap the
      `radius=1/|curvature|` expression in double backticks so RST stops reading the pipes as a
      substitution reference.
    - `src/aquapose/engine/observers.py` — in `EventBus`'s delivery docstring, insert a blank line
      between the `Delivery order:` lead-in and the numbered list that follows it.
    - `src/aquapose/synthetic/scenarios.py` — in `crossing_paths`'s docstring, insert a blank line
      between the crossing half-angle sentence and the bullet list that follows it.

    Change no code, no signatures, no imports, and no prose meaning — these are RST-escaping and
    whitespace fixes only. Run `hatch run lint` and `hatch run format --check` (or
    `hatch run pre-commit run --files` on the four paths) to confirm ruff is satisfied.

    Commit as `fix(108-01): forward-port a66287a docstring repairs dropped by the merge`. Do not
    push.
  </action>
  <verify>
    <automated>git diff --quiet main -- docs/conf.py .github/workflows/docs.yml docs/index.md &amp;&amp; test "$(ls docs/api/*.rst | wc -l)" -eq 8 &amp;&amp; grep -q '``\*_cache.pkl``' src/aquapose/core/context.py &amp;&amp; grep -q '``radius=1/|curvature|``' src/aquapose/synthetic/fish.py &amp;&amp; hatch run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code main -- docs/conf.py` exits 0.
    - `git diff --exit-code main -- .github/workflows/docs.yml` exits 0.
    - `git diff --exit-code main -- docs/index.md` exits 0.
    - `grep -c 'napoleon_use_ivar' docs/conf.py` returns `1`; `grep -q 'def _resolve_release' docs/conf.py` and `grep -q 'autodoc_mock_imports' docs/conf.py` each exit 0.
    - `grep -q 'branches: \[main, dev\]' .github/workflows/docs.yml` exits 0.
    - `git ls-files docs/api | wc -l` returns `8`.
    - `git ls-files docs/api/mesh.rst docs/api/optimization.rst docs/api/segmentation.rst docs/api/utils.rst` outputs zero lines.
    - `grep -q 'reports/z_uncertainty_report' docs/index.md` exits 0.
    - `grep -c '``\*_cache.pkl``' src/aquapose/core/context.py` returns `1` and `grep -c '[^`]\*_cache.pkl[^`]' src/aquapose/core/context.py` returns `0`.
    - `grep -c '``radius=1/|curvature|``' src/aquapose/synthetic/fish.py` returns `1`.
    - `awk '/Delivery order:/{getline; if ($0 ~ /^[[:space:]]*$/) print "OK"}' src/aquapose/engine/observers.py` prints `OK`.
    - `awk '/derived from ``difficulty``:/{getline; if ($0 ~ /^[[:space:]]*$/) print "OK"}' src/aquapose/synthetic/scenarios.py` prints `OK`.
    - `git diff --name-only HEAD~1 HEAD -- src | wc -l` returns `4` — exactly the four files, nothing else under `src/`.
    - `git diff HEAD~1 HEAD -- src | grep -cE '^[+-][[:space:]]*(def |class |import |from |return |@)'` returns `0` — no code changed.
    - `hatch run lint` exits 0.
    - `hatch run python -c "import aquapose.core.context, aquapose.engine.observers, aquapose.synthetic.fish, aquapose.synthetic.scenarios"` exits 0.
    - `git status --porcelain` lists only `11.0` and `~/`.
    - `git log --oneline origin/dev..dev | wc -l` is greater than `0` — nothing was pushed.
  </acceptance_criteria>
  <done>The merged Sphinx config, docs index, API `.rst` set, and docs workflow are present on dev and byte-identical to main's verified-green versions; a66287a's four docstring repairs are re-applied with no code change; nothing is pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `dev` -> `origin/dev` | 1496 published commits; any history rewrite here is destructive to a shared remote |
| local repo -> `main` / `origin/main` | a frozen release branch (D-04) and GitHub's default branch (D-05); no write is authorized |
| `main`'s squashed `19ea21e` tree -> dev's working tree | 703 files of ~5-month-old content enter the index; 29 of them are superseded modules that git adds with no conflict signal |
| previously-untracked `.planning/` archive -> public git history | 665 files never scanned by the pre-commit secret hooks become permanently published |
| repo push -> GitHub Actions | `release.yml`, `test.yml`, `docs.yml` all fire on push to `dev`; `release.yml` holds `contents: write` via `RELEASE_TOKEN` and creates commits and tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | published `origin/dev` history | mitigate | Plan mandates `git merge --no-ff main` only; `git rebase`, `git cherry-pick` of the 4 commits, and any force push are explicitly forbidden in every task action; acceptance asserts `git reflog dev` records no rebase or cherry-pick entry and that HEAD has exactly two parents |
| T-108-02 | Tampering | frozen `main` branch ref | mitigate | No task checks out, commits to, or pushes `main`; `MAIN_PRE` is recorded before the merge and re-asserted equal in tasks 1 and 2 |
| T-108-03 | Elevation of Privilege | `release.yml` (`contents: write`, `RELEASE_TOKEN`) | mitigate | The push is deferred out of this plan entirely to 108-05, after the relicense lands, so no prerelease tag can be cut against an MIT-declared tree |
| T-108-04 | Tampering | CI workflow trigger surface | mitigate | Only `docs.yml` changes, and only by taking main's already-reviewed blob; task 3 pins it byte-identical to `main` and forbids edits to the other four workflows and to `.pre-commit-config.yaml` |
| T-108-05 | Repudiation | merge provenance | accept | A real `--no-ff` merge commit records true ancestry between the branches; this is the intended audit trail |
| T-108-29 | Tampering | silent resurrection of 29 superseded modules with no conflict signal | mitigate | Task 2 derives the addition set with `git diff --cached --diff-filter=A` against the recorded pre-merge SHA, requires the count to be exactly 29, `git rm -f`s all of them, and asserts by explicit `git ls-files` counts that each named module family is absent from the index and from disk; a further assertion requires `git diff $PRE HEAD -- src tests scripts` to be empty |
| T-108-30 | Tampering | a blanket side-pick silently reverting 1496 commits of production code | mitigate | D-02 forbids `-X ours`/`-X theirs`/`-s ours` as a whole-tree strategy; resolution is applied to enumerated path sets only, and `CHANGELOG.md` is the sole main-side content pick, justified by a verified superset check |
| T-108-31 | Information Disclosure | credentials inside the 665 newly tracked `.planning/` archive files | mitigate | Task 2 runs `pre-commit run detect-secrets --all-files` before completing the merge, plus explicit greps for `api_key`/`client_secret`/`access_token`/`password`/PEM headers/`ghp_`/`github_pat_`, with a hard stop on any hit and a prohibition on widening `.secrets.baseline` |
| T-108-32 | Denial of Service | repository bloat from the merged archive | mitigate | The kept set is 6.0 MB of markdown with a 184 KB largest blob; task 2 asserts `find .planning -type f -size +1M` prints nothing before committing |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. The merge only edits `pyproject.toml` declarations; `hatch run python -c "import aquapose"` and `hatch run lint` use the already-provisioned default env against an unchanged dependency set |
</threat_model>

<verification>
- `git log --oneline dev..main` returns zero lines (FOUND-03).
- `git rev-parse main` unchanged from the recorded pre-merge value (D-04, D-05).
- HEAD is a two-parent merge commit; `git reflog dev` shows no rebase or cherry-pick.
- `git ls-files | wc -l` returns 988; none of the 29 hazard-2 paths is tracked or on disk.
- `git diff $PRE HEAD -- src tests scripts` was empty at the merge commit; the only later `src/` change is the four docstring files.
- `pyproject.toml` version is `1.2.0-dev.0` with `detached = true` and `numpy` (D-03, D-15).
- `CHANGELOG.md` carries the shipped `v1.1.0` and `v1.1.1` sections (D-10 groundwork).
- `.github/workflows/docs.yml` has `branches: [main, dev]` under `on: push` (FOUND-02, D-17).
- `docs/api/` holds exactly main's 8-file set; the four dead `.rst` files are gone (D-13).
- `docs/conf.py` and `docs/index.md` are identical to main's (D-15, D-16 groundwork).
- a66287a's four docstring repairs are present; `hatch run lint` passes.
- `.planning/milestones/v2.2-phases/` is tracked, unblocking 108-04's v2.2 backfill.
- `.planning/research/` is still deleted; `.planning/REQUIREMENTS.md` is dev's version.
- Nothing has been pushed to any remote.

</verification>

<success_criteria>
- dev fully contains main via a real merge commit; no history was rewritten.
- main and the GitHub default-branch setting are untouched.
- Zero superseded modules were resurrected; production code is unchanged by the merge.
- `pyproject.toml` is at `1.2.0-dev.0` with a detached docs env and dev's dependency list intact; license fields are still MIT for 108-02 to change.
- The Sphinx repair, the docs push trigger, and a66287a's docstring fixes are all present on dev.
- The historical `.planning/` archive is tracked and secret-clean.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-01-SUMMARY.md` when done.
Record: the recorded `PRE` and `MAIN_PRE` SHAs, the merge commit SHA, the exact count of conflicts
resolved per path set, the 29 purged paths, the final `git ls-files` count, and the
detect-secrets result.
</output>
