---
phase: 113-concepts-tutorial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started/index.md
  - docs/getting-started/installation.md
  - docs/index.md
  - pyproject.toml
autonomous: true
requirements: [DOCS-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader can follow docs/getting-started/installation.md from top to bottom without opening any source file and end up with AquaPose importable and a torch whose CUDA status they can state (DOCS-03)."
    - "installation.md documents the GPU/CUDA caveat concretely: it names https://pytorch.org/get-started/locally/ as the source of the platform-correct torch command, gives the torch.cuda.is_available() check, and states the 6.4 GB GPU footprint observed on the reference GTX 1660 SUPER run (D-08, D-10)."
    - "installation.md discloses in the install step itself that `pip install aquapose` does not resolve on PyPI yet and names the phase that publishes it (D-09)."
    - "installation.md lists the three first-run blockers: ffmpeg on PATH for viz MP4 export, ~600 MB free for generated refractive LUTs plus 215 MB for the tutorial dataset, and a CUDA GPU (D-10)."
    - "installation.md points contributors at docs/contributing.md for the developer/hatch path instead of restating it, and points at docs/reference/cli.rst for command options instead of restating them (D-07)."
    - "The Getting Started section is reachable from the docs root: docs/index.md carries a Getting Started card and a root toctree entry, and docs/getting-started/index.md links the installation page (D-07)."
    - "hatch run docs:build (sphinx-build -W --keep-going) exits 0 with docs/_build/html/getting-started/installation.html produced."
    - "The default hatch environment no longer pins a third-party wheel index, and the environment still resolves a CUDA-enabled torch on this machine (D-08)."
  artifacts:
    - path: "docs/getting-started/index.md"
      provides: "Getting Started section landing page (grid-item-card grid + hidden toctree), mirroring docs/reference/index.md"
      contains: "grid-item-card"
    - path: "docs/getting-started/installation.md"
      provides: "End-user install guide: install, torch-for-your-platform, CUDA verification, prerequisites, troubleshooting"
      min_lines: 60
    - path: "docs/index.md"
      provides: "Root card grid + toctree entry for the Getting Started section"
      contains: "getting-started/index"
    - path: "pyproject.toml"
      provides: "Default hatch env with no third-party wheel-index pin (D-08)"
  key_links:
    - from: "docs/index.md"
      to: "docs/getting-started/index.md"
      via: "grid-item-card :link: getting-started/index plus a root toctree entry"
      pattern: "getting-started/index"
    - from: "docs/getting-started/index.md"
      to: "docs/getting-started/installation.md"
      via: "grid-item-card :link: installation plus a hidden toctree entry"
      pattern: "installation"
    - from: "docs/getting-started/installation.md"
      to: "docs/reference/cli.rst"
      via: "cross-link to the Phase 112 CLI reference instead of restating options"
      pattern: "reference/cli"
  prohibitions:
    - requirement_id: DOCS-03
      category: transparency
      statement: "MUST NOT present `pip install aquapose` as a command that works today without disclosing on the same page that the package is not yet published to PyPI (D-09 accepted risk)."
    - requirement_id: DOCS-03
      category: safety
      statement: "MUST NOT give install instructions that can silently leave the reader with a non-CUDA or otherwise unusable torch without telling them how to detect it and what to do about it."
---

<objective>
Create the user-facing **Getting Started** docs section (D-07) and land its first
page — the end-user installation guide (DOCS-03) — then make the documented
install path true by dropping the third-party wheel-index pin from the default
hatch environment (D-08).

This is the phase **tracer**: it wires one complete path through every layer this
phase touches — narrative page authoring → new section index → root docs IA →
the `sphinx-build -W --keep-going` gate → the packaging config the guide
describes — and proves it end to end before any expansion page (concepts,
tutorial) is written.

Purpose: DOCS-03 — a new user can install AquaPose from written instructions,
including the GPU/CUDA caveat, without reading source.
Output: `docs/getting-started/{index.md,installation.md}`, a wired
`docs/index.md`, and a `pyproject.toml` whose default env no longer pins a
CUDA-12.1 wheel index.
</objective>

## Artifacts this phase produces

New symbols created by this phase (excluded from source-drift verification —
they do not exist in the codebase before this phase):

| Artifact | Kind | Plan |
|---|---|---|
| `docs/getting-started/` | new docs directory | 01 |
| `docs/getting-started/index.md` | new docs page (section index) | 01 |
| `docs/getting-started/installation.md` | new docs page | 01 |
| `docs/getting-started/concepts.md` | new docs page | 04 |
| `docs/getting-started/tutorial.md` | new docs page | 07 |
| `getting-started/index` | new root-toctree entry in `docs/index.md` | 01 |
| Getting Started grid card | new `grid-item-card` in `docs/index.md` | 01 |
| (removed) `[tool.hatch.envs.default.env-vars]` | deleted config table in `pyproject.toml` | 01 |
| `TestDepositDocCorrections` | new test class in `tests/scripts/test_package_tutorial_dataset.py` | 03 |
| Zenodo concept + version DOI | new external identifier recorded in the deposit README and tutorial | 06, 07 |

No new CLI flags, functions, config keys, or dependency groups are introduced by
this phase.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-concepts-tutorial/113-CONTEXT.md
@.planning/phases/113-concepts-tutorial/113-PATTERNS.md
@docs/reference/index.md
@docs/index.md
@docs/contributing.md
@pyproject.toml
</context>

<!-- planner-discipline-allow: UV_EXTRA_INDEX_URL -->
<!-- planner-discipline-allow: cu121 -->
<!-- planner-discipline-allow: env-vars -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a new user installs AquaPose from the docs" — one path through every layer</name>
  <read_first>
    - `docs/reference/index.md` — the Phase 112 section-index shape D-07 mirrors exactly (grid-item-card grid + hidden toctree). Copy this structure, retarget the cards.
    - `docs/index.md` — the root card grid and root toctree this page must be wired into. Match the existing `grid-item-card` / `:link:` / `:link-type: doc` syntax verbatim.
    - `docs/contributing.md` — the existing Development Setup section. This page stays the *contributor* path; the new install guide serves *users*. Link to it, do not duplicate it.
    - `docs/reference/config.md` (first ~20 lines) — the established cross-link idiom ("For the full auto-generated API documentation ... see the [engine module reference](../api/engine.rst)"). Reuse this idiom rather than restating reference material.
    - `docs/conf.py` — confirms the MyST + sphinx-design + Furo extension set the new page relies on; also confirms the docs env is detached, so the page must not require importing `aquapose` at build time.
    - `pyproject.toml` lines 60-100 — the `docs` hatch env and its `build` script (`sphinx-build -W --keep-going -b html docs docs/_build/html`), which is the gate this task must leave green.
    - `.planning/phases/113-concepts-tutorial/113-CONTEXT.md` — decisions D-07 (new Getting Started section), D-09 (`pip install aquapose` kept, accepted risk), D-10 (full prerequisites).
    - `README.md` — its "GPU Support" section holds the existing `nvrtc: error: failed to open libnvrtc-builtins.so` troubleshooting note; carry that note into the install guide's troubleshooting section (the README rewrite itself is Phase 114 and is out of scope here).
  </read_first>
  <files>docs/getting-started/index.md, docs/getting-started/installation.md, docs/index.md</files>
  <action>
Create the Getting Started section and its first real page, and wire it into the
docs root, so that one complete authoring→section→IA→build path is proven before
any other page is written.

**`docs/getting-started/index.md`** — copy the exact structure of
`docs/reference/index.md`: an H1, a one-line lede, a `::::{grid} 1 2 2 3` /
`:gutter: 3` block of `grid-item-card`s, and a hidden `{toctree}` with
`:maxdepth: 2` and `:hidden:`. In THIS task the section contains exactly ONE
page, so emit exactly one card (`Installation`, `:link: installation`,
`:link-type: doc`) and exactly one toctree entry (`installation`). Do NOT add
cards or toctree entries for `concepts` or `tutorial` — those files do not exist
yet and `sphinx-build -W` fails on an unresolved toctree reference. Plans 04 and
07 add their own entries when their pages land.

**`docs/getting-started/installation.md`** — the end-user install guide
(DOCS-03). Write it as MyST `.md` (D-07/Phase-112 D-12: `.md` for narrative).
Cover, in this order:

1. *Who this page is for* — one short paragraph: this is the user path; link to
   `../contributing.md` for the contributor/hatch path. Do not restate the hatch
   dev-setup commands.
2. *Install AquaPose* — `pip install aquapose` (D-09). In the same step,
   plainly disclose that AquaPose is not yet published to PyPI, that publication
   lands in Phase 114, and give the working interim command (install from the
   GitHub repository `McGrathLab/AquaPose`). This disclosure is mandatory — see
   the `must_haves.prohibitions` entry on this plan.
3. *Install PyTorch for your platform* (D-08) — do not ship a torch command.
   Send the reader to <https://pytorch.org/get-started/locally/> to get the
   command matching their OS, package manager, and CUDA version, and explain
   why: AquaPose does not pin a CUDA build, so the reader chooses the wheel that
   matches their driver.
4. *Verify the install* — a `python -c` one-liner that prints `torch.__version__`
   and `torch.cuda.is_available()`, plus an explicit statement of what each
   outcome means: `True` means the pipeline will run on GPU; `False` means torch
   installed but has no CUDA support and the reader must reinstall from the
   pytorch.org selector, because the reference tutorial run is a GPU workload.
5. *Prerequisites* (D-10) — a section covering the three things that actually
   break a first run: **ffmpeg on PATH** (`aquapose viz` shells out to it for MP4
   export); **disk headroom** — approximately 600 MB for the refractive lookup
   tables, which are generated by `aquapose prep generate-luts` and are not
   shipped, plus 215 MB for the tutorial dataset; and **GPU footprint** — the
   reference tutorial run fit in 6.4 GB on a GTX 1660 SUPER, a usable practical
   floor. State that the pipeline fails fast with a "LUTs not found" error if the
   LUT generation step is skipped.
6. *Troubleshooting* — at minimum the `nvrtc: error: failed to open
   libnvrtc-builtins.so` symptom carried over from the README's GPU Support
   section, described as a torch/driver CUDA-version mismatch resolved by
   reinstalling torch from the pytorch.org selector; plus the "ffmpeg not found"
   symptom.
7. *Next steps* — cross-link forward to the tutorial and back to
   `../reference/cli.rst` for the full command surface and `../reference/config.md`
   for configuration fields. Because `concepts.md` and `tutorial.md` do not exist
   yet, write these as prose references to the Getting Started section (plain
   text or a link to `index`), never as a MyST `{doc}` role or relative link to a
   non-existent file — `sphinx-build -W` treats an unresolved reference as an
   error.

Use fenced `bash` blocks with inline `#` comments for every runnable step
(the idiom already used in `docs/contributing.md`), with narration in prose
between fences rather than as large comment blocks.

**`docs/index.md`** — add a `Getting Started` `grid-item-card` as the FIRST card
in the existing grid, before the `Reference` card, with `:link: getting-started/index`
and `:link-type: doc`, and a one-line description naming install, concepts and
tutorial. Add `getting-started/index` as the first entry in the root `{toctree}`,
before `reference/index`. Change nothing else in this file.

Do not create `concepts.md` or `tutorial.md` in this task.
  </action>
  <verify>
    <automated>hatch run docs:build && test -f docs/_build/html/getting-started/installation.html && test -f docs/_build/html/getting-started/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run docs:build` exits 0 (this runs `sphinx-build -W --keep-going`, so any warning is a failure).
    - `docs/_build/html/getting-started/installation.html` and `docs/_build/html/getting-started/index.html` both exist after the build.
    - `grep -c 'grid-item-card' docs/getting-started/index.md` is exactly 1.
    - `grep -c 'getting-started/index' docs/index.md` is at least 2 (one card `:link:`, one toctree entry).
    - `grep -q 'pytorch.org/get-started/locally' docs/getting-started/installation.md` succeeds.
    - `grep -q 'torch.cuda.is_available' docs/getting-started/installation.md` succeeds.
    - `grep -q 'ffmpeg' docs/getting-started/installation.md` succeeds.
    - `grep -q '600 MB' docs/getting-started/installation.md` succeeds (LUT disk headroom, D-10).
    - `grep -q '215 MB' docs/getting-started/installation.md` succeeds (dataset size, D-10).
    - `grep -q 'GTX 1660 SUPER' docs/getting-started/installation.md` succeeds (GPU floor, D-10).
    - `grep -q 'PyPI' docs/getting-started/installation.md` succeeds (D-09 disclosure present).
    - `grep -q 'McGrathLab/AquaPose' docs/getting-started/installation.md` succeeds (correct repo org).
    - `grep -q 'contributing' docs/getting-started/installation.md` succeeds (contributor path is linked, not duplicated).
    - `grep -q 'reference/cli' docs/getting-started/installation.md` succeeds (CLI options are cross-linked, not restated).
    - `hatch run docs:build` passing is itself the proof that every toctree entry and `{doc}` reference in the new section resolves — `sphinx-build -W` errors on any unresolved reference, so a forward reference to a page that does not exist yet cannot slip through.
    - `wc -l docs/getting-started/installation.md` reports at least 60 lines.
  </acceptance_criteria>
  <done>A reader landing on the docs root sees a Getting Started card, follows it to a section index, follows that to a complete installation guide covering install, platform torch, CUDA verification, prerequisites and troubleshooting — and the whole path builds clean under `sphinx-build -W --keep-going`.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the CUDA-12.1 wheel-index pin from the default hatch environment (D-08)</name>
  <precondition>The current default hatch environment reports a CUDA-enabled torch — `hatch run python -c "import torch; print(torch.cuda.is_available())"` prints `True` before this task starts. Record the reported torch version as the baseline.</precondition>
  <reversibility rating="costly">One line in `pyproject.toml`, but it changes what every fresh `hatch env create` produces for every developer and all six CI matrix jobs; reverting means re-pinning and re-establishing that GPU wheels still resolve (D-08).</reversibility>
  <read_first>
    - `pyproject.toml` lines 26-100 — the `[project] dependencies` list (which declares `torch>=2.0`), the `[tool.hatch.envs.default]` table with `installer = "uv"` and its `dependencies`, the `[tool.hatch.envs.default.env-vars]` table being removed, and `[tool.hatch.envs.default.scripts]`. Read these before editing so the edit is surgical.
    - `.github/workflows/test.yml` — the `pre-commit`, `typecheck`, and `test` matrix jobs ({ubuntu,windows}-latest x Python 3.11/3.12/3.13). Confirms no runner has a GPU, which is D-08's justification.
    - `.github/workflows/slow-tests.yml` — the second GPU-less workflow.
    - `docs/getting-started/installation.md` (written in Task 1) — the guide whose torch instructions this change makes true.
    - `.planning/phases/113-concepts-tutorial/113-CONTEXT.md` — D-08 in full, including the recorded evidence that this pin caused three of six CI matrix jobs to fail at "Create environment" with a 503 from the pinned index while fetching a pure-Python package.
  </read_first>
  <files>pyproject.toml</files>
  <action>
Delete the entire `[tool.hatch.envs.default.env-vars]` table from `pyproject.toml`
— both the table header and its single `UV_EXTRA_INDEX_URL` key, whose value
points uv at the `cu121` PyTorch wheel index (D-08). The table has exactly one
key, so the header is removed with it; do not leave an empty table behind.

Change nothing else in `pyproject.toml`. Specifically leave untouched:
`[project] dependencies` (which keeps `torch>=2.0`), `[tool.hatch.envs.default]`
with `installer = "uv"` and its `dependencies` list (`pytest`, `pytest-cov`,
`ruff`, `pre-commit`, `basedpyright`), `[tool.hatch.envs.default.scripts]`, the
`[tool.hatch.envs.docs]` table, and the `[tool.hatch.envs.release]` table.

After the edit, confirm the default environment still resolves a working CUDA
torch on this machine. This matters beyond this plan: Plan 05 executes the
tutorial end-to-end on a GPU using this same environment, so a silent downgrade
to a CPU-only torch here would surface as a confusing failure two waves later.
Run the full fast test suite and the torch CUDA probe. If `torch.cuda.is_available()`
now reports `False`, or torch fails to import, STOP and surface the result rather
than continuing — do not paper over it by re-adding the pin or by installing a
different torch build without recording why.

Then re-read `docs/getting-started/installation.md` and confirm its torch section
is consistent with the new reality: AquaPose pins no CUDA build, the reader picks
their wheel at pytorch.org. Adjust the wording there if the page still implies a
bundled CUDA build.
  </action>
  <verify>
    <automated>! grep -q 'UV_EXTRA_INDEX_URL' pyproject.toml && ! grep -q 'cu121' pyproject.toml && hatch run python -c "import torch; print(torch.__version__, torch.cuda.is_available())" && hatch run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'UV_EXTRA_INDEX_URL' pyproject.toml` is 0.
    - `grep -c 'cu121' pyproject.toml` is 0.
    - `grep -c 'env-vars' pyproject.toml` is 0.
    - `grep -q 'installer = "uv"' pyproject.toml` still succeeds (the surrounding table survived the edit).
    - `grep -q 'torch>=2.0' pyproject.toml` still succeeds (the project dependency was not touched).
    - `hatch run python -c "import torch; print(torch.cuda.is_available())"` prints `True`.
    - `hatch run test` exits 0.
    - `hatch run docs:build` still exits 0.
    - The torch version reported after the change is recorded in the plan SUMMARY alongside the pre-change baseline captured by the precondition.
  </acceptance_criteria>
  <done>The default hatch environment declares no third-party wheel index, the fast test suite is green, torch still reports CUDA available on this machine, and the installation guide's torch instructions describe what the packaging actually does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs reader → package registry | The install guide directs a reader to run a package-manager command against an external registry. Wrong or ambiguous instructions cause the reader to install something other than AquaPose. |
| build env → third-party wheel index | The default hatch env directed `uv` at a non-PyPI index for every package resolution. This task removes that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-01 | Spoofing | `docs/getting-started/installation.md` install step | high | mitigate | Name the exact distribution (`aquapose`) and the exact repository (`McGrathLab/AquaPose`) with a full https URL; disclose that the PyPI name is not yet claimed by this project so a reader who hits an unexpected package on PyPI treats it as a red flag rather than as AquaPose. |
| T-113-02 | Tampering | `[tool.hatch.envs.default.env-vars]` third-party index | high | mitigate | Removing the index pin (D-08) stops `uv` from querying `download.pytorch.org` for *every* package, including pure-Python ones — it eliminates a standing substitution surface and the observed 503 flakiness. |
| T-113-03 | Information disclosure | install guide troubleshooting text | low | accept | Troubleshooting examples quote only generic loader/driver error strings; no host paths, usernames, or credentials appear. |
| T-113-04 | Denial of service | docs build gate | low | accept | `sphinx-build -W --keep-going` is a local/CI gate on static markdown; failure blocks a docs publish, not a service. |
| T-113-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Not applicable to this plan** — no task runs a package-manager install. The only packaging change *removes* an index. No `## Package Legitimacy Audit` is required because no install task exists; `pip install aquapose` appears only as documented prose for the reader, never as an executed command. |

ASVS level 1; blocking severity: high. T-113-01 and T-113-02 are both mitigated within this plan, so no high-severity threat is left open.
</threat_model>

## Edge Coverage (spec-less probe fallback)

This phase has no SPEC.md, so the deterministic edge probe ran instead. This plan
owns 1 of the 5 probe-surfaced rows.

| Requirement | Category | Probe status | Disposition here |
|---|---|---|---|
| DOCS-03 | unclassified | unresolved | **FLAGGED ASSUMPTION** (below) |

### Flagged assumption — DOCS-03 (unclassified probe row)

The probe could not classify a boundary condition for DOCS-03 and returned it
`unresolved`. Per the fallback protocol an unclassified row is surfaced, never
auto-resolved and never dropped.

**The assumption this plan makes:** the install guide's success condition is
"AquaPose imports and the reader can state their torch CUDA status", and a reader
on a machine with **no CUDA GPU at all** is out of scope for DOCS-03 — the guide
tells such a reader how to detect the condition (`torch.cuda.is_available()`
returns `False`) and states the GPU floor, but the phase does not commit to a
supported CPU-only execution path, because no source artifact
(ROADMAP, REQUIREMENTS, CONTEXT.md) specifies one. If a CPU-only path is
required, it is unplanned work and needs a developer decision.

<verification>
1. `hatch run docs:build` exits 0 — the inherited hard gate from Phases 110/112 is intact.
2. `docs/_build/html/getting-started/installation.html` and `.../index.html` exist.
3. The root docs page links the new section: `grep -c 'getting-started/index' docs/index.md` >= 2.
4. `grep -c 'cu121' pyproject.toml` is 0 and `grep -c 'UV_EXTRA_INDEX_URL' pyproject.toml` is 0.
5. `hatch run test` exits 0.
6. `hatch run python -c "import torch; print(torch.cuda.is_available())"` prints `True`.
7. `hatch run ruff format --check src/ tests/ scripts/` exits 0 (D-16 regression guard — do not reintroduce formatting drift).
</verification>

<success_criteria>
- DOCS-03 is satisfiable by reading one page: install, platform torch, CUDA verification, prerequisites, troubleshooting.
- The Getting Started section exists and is reachable from the docs root under `sphinx-build -W`.
- The default hatch environment pins no third-party wheel index, and this machine still has a CUDA-enabled torch for Plan 05's verification run.
- No page in this plan restates CLI options or config fields already shipped in Phase 112 — they are cross-linked.
</success_criteria>

<output>
Create `.planning/phases/113-concepts-tutorial/113-01-SUMMARY.md` when done.
Record in it: the torch version before and after the `pyproject.toml` change, the
exact `docs/index.md` card ordering chosen (Claude's Discretion per CONTEXT), and
whether the README's `nvrtc` troubleshooting note was carried into the install
guide (also Claude's Discretion).
</output>
