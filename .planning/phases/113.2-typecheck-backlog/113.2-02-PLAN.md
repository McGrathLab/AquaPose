---
phase: 113.2-typecheck-backlog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquapose/core/reid/embedder.py
  - src/aquapose/training/reid_training.py
autonomous: true
requirements: [QA-06]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`hatch run typecheck` reports 0 errors for `src/aquapose/training/reid_training.py`, down from 5."
    - "`ReidConfigLike` accepts both a frozen dataclass and a mutable one, because it declares read-only properties rather than writable attributes (per the short-tail analysis in CONTEXT)."
    - "The only suppression directives added anywhere in this phase are the two in `reid_training.py`, each naming `pytorch_metric_learning` and the reason it is a third-party inference defect rather than a defect in this codebase (D-05)."
    - "`hatch run test` still reports `1403 passed, 3 skipped, 17 deselected`, with no test file edited."
    - "`tests/unit/core/test_import_boundary.py` still passes — `core/reid/embedder.py` gains no forbidden import."
  artifacts:
    - src/aquapose/core/reid/embedder.py
    - src/aquapose/training/reid_training.py
  key_links:
    - "`training/reid_training.py::_EmbedderConfig` -> `core/reid/embedder.py::ReidConfigLike` — the structural-typing edge that currently fails; a frozen dataclass cannot satisfy a protocol declaring writable attributes."
    - "`engine.config.ReidConfig` -> `ReidConfigLike` — the protocol's other implementer; loosening to read-only properties must not break it, since a plain writable attribute satisfies a read-only property but not vice versa."
    - "`training/reid_training.py` -> `pytorch_metric_learning.losses.MultiSimilarityLoss` — an untyped third-party `__init__` whose `alpha=2`/`beta=50` defaults make basedpyright infer `int`; the only legitimate suppression site in the phase."
---

<objective>
Close the five-error `training/reid_training.py` cluster: one genuine protocol-variance
defect in this codebase, and four errors that are a third-party type-inference artifact.

These two look alike in the error output (`reportArgumentType` in one file) and are opposite
in nature. Separating them is the point of this plan: one gets a real fix, the other gets the
phase's *only* permitted suppression, tightly bounded and reasoned inline.

Purpose: prove that the phase can tell "our annotation is wrong" apart from "the library
has no annotations", instead of reaching for the same blunt instrument for both.
Output: `reid_training.py` at 0 errors, with exactly two suppression directives in the whole phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113.2-typecheck-backlog/113.2-CONTEXT.md
@CLAUDE.md
@.claude/rules/code-style.md
</context>

<measured_baseline>
Re-measured at commit `d3e6073`: `hatch run typecheck` reports **98 errors, 0 warnings**.
`training/reid_training.py` carries 5 of them, in two unrelated groups:

| Site | Error | Root cause |
|---|---|---|
| `reid_training.py:197:29` | `_EmbedderConfig` not assignable to parameter `config` of type `ReidConfigLike` | Protocol declares writable attributes; the dataclass provides read-only ones. **Our defect.** |
| `reid_training.py:475:50` / `:475:60` | `float` not assignable to `alpha` / `beta` of type `int` | `pytorch_metric_learning` is untyped. **Third-party defect.** |
| `reid_training.py:811:50` / `:811:60` | same | same |

Verified on this tree, not assumed:

- `pytorch_metric_learning` resolves to `site-packages/pytorch_metric_learning/__init__.py` — it is third-party, not vendored.
- `inspect.signature(losses.MultiSimilarityLoss.__init__)` is `(self, alpha=2, beta=50, base=0.5, **kwargs)` — **no annotations at all**. basedpyright infers `int` for `alpha` and `beta` purely from the integer default values, under the default `useLibraryCodeForTypes`. The library documents these as float hyperparameters; the inferred `int` is an artifact of untyped source, not a real contract.
- `ReidConfigLike` lives at `src/aquapose/core/reid/embedder.py:17-24` and declares five bare attribute annotations (`model_name`, `batch_size`, `crop_size`, `device`, `embedding_dim`), which a `Protocol` treats as **writable**. A frozen dataclass therefore fails it.
</measured_baseline>

<tasks>

<task type="auto">
  <name>Task 1: Make ReidConfigLike declare read-only properties so frozen implementers satisfy it</name>
  <files>src/aquapose/core/reid/embedder.py</files>
  <read_first>
    - `src/aquapose/core/reid/embedder.py` lines 1-60 — the `ReidConfigLike` protocol declaration and `FishEmbedder.__init__`, which is the only consumer and reads all five attributes without writing any of them.
    - `src/aquapose/training/reid_training.py` around lines 985-1016 — `_EmbedderConfig` (the frozen dataclass that fails the protocol) and `_make_embedder_config`, its only constructor.
    - The `ReidConfig` definition in `src/aquapose/engine/config.py` — the protocol's other implementer, named in the `ReidConfigLike` docstring. Confirm it still satisfies the loosened protocol; a plain writable attribute satisfies a read-only property, so it should, but verify rather than assume.
    - `.claude/rules/code-style.md` — Google-style docstrings and required type hints.
  </read_first>
  <action>
    Change each of the five `ReidConfigLike` members from a bare attribute annotation to a
    read-only property declaration (a `@property` returning the same type, with the body
    elided as a protocol stub). Keep the member names, types and ordering exactly as they are
    — this is a variance fix, not a rename. Update the protocol docstring to state that
    implementers may be frozen, which is why the members are read-only.

    Confirm `FishEmbedder` only reads these members; if any code path assigns to one through a
    `ReidConfigLike`-typed reference, stop and surface it rather than working around it — that
    would mean the protocol genuinely needs write access and the diagnosis is wrong (D-06).

    This edit is in `core/`, so re-run the import-boundary guard; no import changes are
    expected, but the guard is cheap and this phase does not skip it.
  </action>
  <verify>
    <automated>test "$(hatch run typecheck 2>&1 | grep -F 'reid_training.py:197' | grep -c ' - error: ')" = "0"</automated>
    <automated>hatch run test -k "import_boundary or embedder or reid"</automated>
    <automated>hatch run test 2>&1 | tail -1 | grep -q '1403 passed, 3 skipped, 17 deselected'</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(hatch run typecheck 2>&1 | grep -F 'reid_training.py:197' | grep -c ' - error: ')" = "0"` succeeds — the `_EmbedderConfig` -> `ReidConfigLike` assignment now typechecks.
    - `test "$(hatch run typecheck 2>&1 | grep -F 'core/reid/embedder.py:' | grep -c ' - error: ')" = "0"` succeeds — the edit introduces no error in the file it changes (this file had 0 errors at baseline).
    - `hatch run typecheck 2>&1 | grep -oP 'src/aquapose/\S+?\.py(?=:\d+:\d+ - error)' | sort -u` lists only files from the measured 9-file baseline — loosening the protocol has not broken `engine/config.py` or any other implementer.
    - `test "$(grep -c '@property' src/aquapose/core/reid/embedder.py)" = "5"` succeeds — all five members converted, none missed.
    - `hatch run test -k "import_boundary or embedder or reid"` passes.
    - `hatch run test 2>&1 | tail -1` contains `1403 passed, 3 skipped, 17 deselected`, and `git diff --name-only d3e6073 -- tests/` lists at most `tests/unit/core/test_h5.py`.
    - The plan SUMMARY records whether `engine.config.ReidConfig` was re-verified against the loosened protocol.
  </acceptance_criteria>
  <done>`ReidConfigLike` declares five read-only properties; `reid_training.py:197` is clean; both implementers still satisfy it; suite unchanged.</done>
  <reversibility rating="reversible">A protocol variance change with two known implementers and no data or wire-format consequence; reverting is a single-file edit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Bound the pytorch_metric_learning inference defect to two reasoned suppressions</name>
  <files>src/aquapose/training/reid_training.py</files>
  <read_first>
    - `src/aquapose/training/reid_training.py` lines 470-478 and 806-814 — the two identical `losses.MultiSimilarityLoss(alpha=2.0, beta=50.0, base=0.5)` construction sites.
    - `src/aquapose/training/reid_training.py` lines 8-27 — the import block, confirming `from pytorch_metric_learning import losses, miners` is a plain third-party import with no local shim.
    - `.planning/phases/113.2-typecheck-backlog/113.2-CONTEXT.md` decision D-05 — the exact conditions under which a targeted suppression is permitted, and the requirement that each names the library and the reason.
  </read_first>
  <action>
    Add one line-scoped basedpyright suppression directive at line 475 and one at line 811.
    Use the `pyright:` form scoped to `reportArgumentType`, not the mypy-style `type:` form
    the file already uses four times at lines 126, 614, 619 and 622 — basedpyright treats the
    bracket contents of the mypy form as opaque and suppresses the whole line, so only the
    `pyright:` form is genuinely rule-scoped. Accompany each with an inline reason naming
    `pytorch_metric_learning`, stating that its
    `MultiSimilarityLoss.__init__` carries no annotations and that basedpyright infers `int`
    for `alpha` and `beta` from the integer default values, while the library documents them
    as float hyperparameters. Two directives cover all four errors, since each line carries
    two.

    Do not change the argument values. Passing integer literals would also silence the
    checker, but it would encode a float hyperparameter as an integer in source and obscure
    the fact that the constraint is fictional — the suppression is the honest fix here, and it
    is exactly the case D-05 carves out. Record in the plan SUMMARY that this is the phase's
    entire suppression budget: two directives, both in this file, both attributable to one
    untyped third-party constructor.
  </action>
  <verify>
    <automated>test "$(hatch run typecheck 2>&1 | grep -F 'training/reid_training.py:' | grep -c ' - error: ')" = "0"</automated>
    <automated>hatch run test 2>&1 | tail -1 | grep -q '1403 passed, 3 skipped, 17 deselected'</automated>
    <automated>hatch run lint && hatch run format --check</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(hatch run typecheck 2>&1 | grep -F 'training/reid_training.py:' | grep -c ' - error: ')" = "0"` succeeds — the file is fully clean, 5 -> 0.
    - `test "$(grep -rEho '(type|pyright|basedpyright):[[:space:]]*ignore' --include=*.py src/aquapose/ | wc -l)" = "73"` succeeds — exactly 2 directives added against the measured baseline of 71 across all of `src/aquapose/`.
    - `test "$(grep -cE '(type|pyright|basedpyright):[[:space:]]*ignore' src/aquapose/training/reid_training.py)" = "6"` succeeds — the 4 pre-existing directives at lines 126, 614, 619 and 622 plus exactly the 2 new ones; the delta lands in this file and nowhere else.
    - `test "$(grep -cE 'pyright:[[:space:]]*ignore\[reportArgumentType\]' src/aquapose/training/reid_training.py)" = "2"` succeeds — both new directives use the basedpyright rule-scoped form, which is distinguishable from the 4 pre-existing mypy-style `type:` directives and is the only form basedpyright actually scopes by rule.
    - `test "$(grep -c 'pytorch_metric_learning' src/aquapose/training/reid_training.py)" -ge "4"` succeeds — the two existing import lines plus an inline reason naming the library beside each of the two directives.
    - `grep -n 'MultiSimilarityLoss(alpha=2.0, beta=50.0, base=0.5)' src/aquapose/training/reid_training.py` still matches both lines — argument values unchanged, so training behavior is untouched (D-06).
    - `hatch run test 2>&1 | tail -1` contains `1403 passed, 3 skipped, 17 deselected`.
    - `hatch run lint` and `hatch run format --check` exit 0.
  </acceptance_criteria>
  <done>`training/reid_training.py` is at 0 typecheck errors; the repo-wide ignore-directive count is 73 (baseline 71 + exactly 2), both new ones rule-scoped and attributed to `pytorch_metric_learning`.</done>
  <reversibility rating="reversible">Two comment lines; removing them restores the exact prior state and the four errors.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `training/` -> `pytorch_metric_learning` | Untrusted-by-the-typechecker third-party surface: no annotations, no `py.typed`, types inferred from library source. |
| `training/` (and `engine/`) -> `core/reid/embedder.py::ReidConfigLike` | Structural-typing contract crossing two layers; loosening it widens what any caller may pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113.2-04 | Tampering | Suppression directives becoming an unbounded escape hatch — the phase's central failure mode | high | mitigate | Repo-wide directive count is gated to exactly 73 (baseline 71 + 2), both new ones rule-scoped to `reportArgumentType`, both in one file, each carrying an inline reason naming the third-party library. `113.2-06-PLAN.md` re-asserts the same count over the finished phase. |
| T-113.2-05 | Elevation of Privilege | Loosening `ReidConfigLike` widens the set of objects any caller may pass into `FishEmbedder` | low | mitigate | Read-only properties are strictly *narrower* on the write axis than writable attributes: the change removes a capability rather than granting one. Acceptance criteria re-verify both known implementers and require no new error site anywhere. |
| T-113.2-06 | Tampering | Silencing the checker by changing hyperparameter values (`2.0` -> `2`) instead of the annotation, altering training numerics | medium | mitigate | Acceptance criterion greps that both call sites still read `alpha=2.0, beta=50.0, base=0.5` verbatim. |
| T-113.2-SC | Tampering | Supply chain — package-manager installs | high | accept | No `pip`/`npm`/`cargo` install tasks. `pytorch_metric_learning` is an existing pinned dependency; this plan adds a comment beside a call to it and installs nothing. |

ASVS level 1, blocking threshold `high`. The one `high`-severity threat (T-113.2-04) is dispositioned `mitigate` with a hard counted gate, not accepted.
</threat_model>

<artifacts_this_phase_produces>
This plan produces **no new symbols and no new files**. It changes the shape of one existing
symbol and adds two comments:

| Symbol / file | Module path | Kind |
|---|---|---|
| `ReidConfigLike.model_name` / `.batch_size` / `.crop_size` / `.device` / `.embedding_dim` | `aquapose.core.reid.embedder` | existing protocol members, converted from writable attributes to read-only properties |
| `src/aquapose/training/reid_training.py` | — | two rule-scoped suppression directives with inline reasons; no symbol change |

Nothing is added to `tests/`. The phase-wide artifact list is consolidated in `113.2-06-PLAN.md`.
</artifacts_this_phase_produces>

<verification>
- `training/reid_training.py` reports 0 typecheck errors; `core/reid/embedder.py` reports 0.
- Repo-wide ignore-directive count is exactly 73 (measured baseline 71 + 2).
- Both `MultiSimilarityLoss` call sites still pass `alpha=2.0, beta=50.0, base=0.5`.
- `hatch run test` reports `1403 passed, 3 skipped, 17 deselected` with no test edited.
- `hatch run test -k import_boundary` passes.
- `hatch run lint` and `hatch run format --check` exit 0.
</verification>

<success_criteria>
The `reid_training.py` cluster is closed, with the protocol defect fixed at root cause and
the third-party inference artifact bounded to two reasoned, rule-scoped suppressions that are
countable and attributable.
</success_criteria>

<output>
Create `.planning/phases/113.2-typecheck-backlog/113.2-02-SUMMARY.md` when done.
Record: the re-verification result for `engine.config.ReidConfig` against the loosened
protocol, and the exact text of the two inline suppression reasons.
</output>
