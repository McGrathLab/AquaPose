---
phase: 109-correctness-green-test-suite-config-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquapose/engine/config.py
  - src/aquapose/training/run_manager.py
  - tests/unit/training/test_run_manager.py
autonomous: true
requirements: [QA-03]
must_haves:
  truths:
    - "keypoint/pose and detection weights_path resolve via one convention: relative paths resolve against project_dir, absolute paths honored as-is (D-03)"
    - "A config using the legacy model_path field is rejected with a 'did you mean weights_path?' error is NO LONGER emitted — model_path is simply an unknown field after alias removal (D-04)"
    - "run_manager writes weights_path with forward slashes so detection.weights_path / midline.weights_path are platform-neutral (D-07)"
    - "No @pytest.mark.skip or xfail was added to any test in this plan"
  artifacts:
    - path: "src/aquapose/engine/config.py"
      provides: "Unified project_dir-relative weights_path resolution; model_path alias removed"
      contains: "det_kwargs"
    - path: "src/aquapose/training/run_manager.py"
      provides: "Forward-slash-normalized weights_path writes in update_config_weights"
      contains: "def update_config_weights"
  key_links:
    - from: "src/aquapose/engine/config.py load_config"
      to: "det_kwargs / pose_kwargs weights_path"
      via: "project_dir-relative resolution (layer 3.5)"
      pattern: "weights_path"
    - from: "src/aquapose/training/run_manager.py update_config_weights"
      to: "config[section]['weights_path']"
      via: "forward-slash normalization before write"
      pattern: "weights_path"
---

<objective>
Unify how model-weights paths resolve across the detection and pose (midline)
backends, and remove the pre-1.0 `model_path` → `weights_path` back-compat alias.
This is the QA-03 foundation and also carries D-07 (forward-slash normalization of
written weights paths in `run_manager`), fixing the 2 `test_update_config_weights_*`
Windows path-separator failures.

Purpose: A single, least-surprising path convention (relative → `project_dir`,
absolute honored as-is) so downstream detection and midline backends consume the
same rule, and written config paths are platform-neutral. This is the config
surface every other plan may re-run the suite against, so it lands first (Wave 1,
but its file is exclusive — no other same-wave plan touches `config.py`).

Output: Updated `config.py` (alias removed, resolution verified for both backends),
updated `run_manager.py` (forward-slash writes), updated `test_run_manager.py`
(the 2 separator assertions now pass on Windows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md
@.planning/GUIDEBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the model_path alias and unify weights_path resolution in config.py</name>
  <files>src/aquapose/engine/config.py</files>
  <read_first>
    - src/aquapose/engine/config.py — the `_RENAME_HINTS` dict (line ~593, the `"model_path": "weights_path"` entry to delete) and the "layer 3.5" project_dir-relative resolution block (lines ~733-753, which already resolves `det_kwargs`/`pose_kwargs` `weights_path` against project_dir)
    - .planning/GUIDEBOOK.md §11 (Configuration System) — the `defaults → YAML → CLI → freeze` precedence the resolution must honor; path resolution belongs at the load/entrypoint layer, not inside stages
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md — decisions D-03 (relative→project_dir, absolute honored) and D-04 (remove alias, clean break)
  </read_first>
  <action>
    Delete the `"model_path": "weights_path"` entry from `_RENAME_HINTS` (D-04) so
    `model_path` becomes an ordinary unknown field (the strict-reject path in
    `_filter_fields` still raises `ValueError`, just without the "did you mean" hint).
    Confirm the layer-3.5 resolution block already covers BOTH `det_kwargs` and
    `pose_kwargs` `weights_path` fields relative to `project_dir` and honors absolute
    paths via the `Path(val).is_absolute()` guard (D-03) — if the pose backend field is
    the `midline`-mapped `pose_kwargs`, it is already listed; verify no second weights
    field on either sub-config is left unresolved. Do NOT add back-compat handling for
    `model_path`. Keep resolution at the load layer (GUIDEBOOK §11) — do not push it
    into stages. Add/adjust the module or function docstring note that both detection
    and pose weights_path resolve by the same project_dir-relative convention.
  </action>
  <verify>
    <automated>hatch run test tests/unit/engine -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/aquapose/engine/config.py | grep -c '"model_path"'` returns 0 (the alias entry is gone)
    - A YAML/CLI override giving a relative `detection.weights_path` and `midline.weights_path` with a set `project_dir` resolves both to `project_dir / <relative>` (absolute paths pass through unchanged)
    - `hatch run test tests/unit/engine -q` exits 0 with no test skipped/xfailed
  </acceptance_criteria>
  <done>The model_path alias is deleted; both detection and pose weights_path resolve via the unified project_dir-relative convention; engine unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize written weights_path to forward slashes in run_manager (D-07)</name>
  <files>src/aquapose/training/run_manager.py, tests/unit/training/test_run_manager.py</files>
  <read_first>
    - src/aquapose/training/run_manager.py — `update_config_weights` (lines ~223-261); the failing write is `config[section]["weights_path"] = str(weights_path)` at line ~250, which yields backslashes on Windows
    - tests/unit/training/test_run_manager.py — `TestUpdateConfigWeights::test_update_config_weights_obb` (line ~251) and `::test_update_config_weights_pose` (line ~270); both write `Path("/new/best.pt")` and assert the stored value equals `/new/best.pt`
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md — D-07 (normalize written weights_path to forward slashes so it is platform-neutral; same QA-03 surface as D-03/D-04)
  </read_first>
  <action>
    In `update_config_weights`, when writing `config[section]["weights_path"]`, normalize
    the string to forward slashes so the persisted YAML value is platform-neutral — use
    `Path(weights_path).as_posix()` (which renders `/new/best.pt` on all platforms)
    rather than `str(weights_path)`. Apply the same normalization to the `click.echo`
    confirmation line so the printed path matches what was written. Do NOT alter the
    section-mapping logic (`obb→detection`, `pose→midline`, `seg→segmentation`) or the
    field name `weights_path`. This is the QA-03 write surface — the field name is already
    `weights_path` (not the removed `model_path`), so no alias interplay. Root-cause note
    (D-02 discipline) for the two fixed tests: "backslash separators leaked into the
    persisted YAML on Windows; genuine platform bug, fixed in production code via
    as_posix()." Leave the test assertions expecting `/new/best.pt` unchanged — they now
    pass because production behavior is corrected.
  </action>
  <verify>
    <automated>hatch run test tests/unit/training/test_run_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_update_config_weights_obb` and `test_update_config_weights_pose` pass on the current platform (persisted value is `/new/best.pt`, forward slashes)
    - The fix lives in production code (`run_manager.py`), not by weakening the test assertions
    - `hatch run test tests/unit/training/test_run_manager.py -q` exits 0 with no skip/xfail added
  </acceptance_criteria>
  <done>update_config_weights writes forward-slash weights paths; both TestUpdateConfigWeights separator tests pass; a one-line root-cause note is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
Internal correctness/config work only. No new external attack surface, no new
inputs crossing a trust boundary, no dependency installs.

| Boundary | Description |
|----------|-------------|
| YAML config → frozen PipelineConfig | Already-trusted local config file; path resolution does not broaden what is read |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01-01 | Tampering | project_dir-relative path join | accept | Paths come from the user's own local config; resolution does not follow attacker-controlled input beyond the existing behavior. No PII, no network. |
</threat_model>

<verification>
- `hatch run test tests/unit/engine tests/unit/training/test_run_manager.py -q` exits 0
- The `model_path` alias entry is absent from `config.py`
- Written weights paths use forward slashes
</verification>

<success_criteria>
QA-03 resolution convention is unified (relative→project_dir, absolute honored) for
both detection and pose backends; the model_path alias is removed; run_manager writes
platform-neutral weights paths; the 2 separator tests pass; no skip/xfail introduced.
</success_criteria>

<output>
Create `.planning/phases/109-correctness-green-test-suite-config-consolidation/109-01-SUMMARY.md` when done, including the D-02 one-line root-cause notes for the two fixed separator tests.
</output>
