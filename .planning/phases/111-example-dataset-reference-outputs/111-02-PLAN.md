---
phase: 111-example-dataset-reference-outputs
plan: 02
type: execute
wave: 2
depends_on: [111-01]
files_modified:
  - scripts/package_tutorial_dataset.py
  - tests/scripts/test_package_tutorial_dataset.py
autonomous: false
requirements: [DATA-02]
user_setup:
  - service: gpu
    why: "Running the real pipeline (aquapose run, diagnostic mode) + viz requires a CUDA GPU and the ~150-200 MB deposited clip + models"
    env_vars: []
    dashboard_config:
      - task: "Ensure a CUDA-capable GPU is available; reference-output regeneration runs the production YOLO OBB + pose backends"
        location: "Local machine (dev branch)"

must_haves:
  truths:
    - "Running the packager with --regenerate-outputs runs the pipeline on the DEPOSITED trimmed clip + DEPOSITED config with the D-02 canonical models and produces reference_outputs/outputs.h5"
    - "reference_outputs/ contains a 3D animation (animation_3d.html), an overlay mosaic (overlay_mosaic.mp4), and timing.txt"
    - "Reference outputs are regenerated on the dev branch with the current production models, not stale March runs"
  artifacts:
    - path: "scripts/package_tutorial_dataset.py"
      provides: "regenerate_reference_outputs() — invokes aquapose run (diagnostic) then aquapose viz on the deposited tree"
      exports: ["regenerate_reference_outputs"]
    - path: "aquapose-tutorial-data/reference_outputs/outputs.h5"
      provides: "HDF5 pipeline output regenerated from the deposited clip"
      contains: "outputs"
  key_links:
    - from: "scripts/package_tutorial_dataset.py"
      to: "aquapose run --config <deposit>/config.yaml --mode diagnostic"
      via: "console-script subprocess invocation"
      pattern: "diagnostic"
    - from: "scripts/package_tutorial_dataset.py"
      to: "aquapose viz --animation --overlay"
      via: "console-script subprocess invocation"
      pattern: "viz"
---

<objective>
Deliver DATA-02: implement `regenerate_reference_outputs()` in the packaging
script so that `--regenerate-outputs` runs the REAL pipeline against the deposited
trimmed clip + deposited config (with the D-02 canonical models) on `dev`, then
runs `aquapose viz` to produce the 3D animation and overlay mosaic, and captures
timing — populating `reference_outputs/{outputs.h5, animation_3d.html,
overlay_mosaic.mp4, timing.txt}` (D-08).

Generating from the DEPOSITED tree (not the full 5-minute source clip) is what
makes the tutorial reproducible: a downstream user running the same config against
the same clip reproduces these exact reference outputs.

Purpose: Reference outputs make the Phase 113 tutorial verifiable against what the
code actually emits.
Output: The `regenerate_reference_outputs` function + the populated
`reference_outputs/` directory in the deposit tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-example-dataset-reference-outputs/111-CONTEXT.md
@.planning/phases/111-example-dataset-reference-outputs/111-PATTERNS.md
@.planning/phases/111-example-dataset-reference-outputs/111-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement regenerate_reference_outputs() — run pipeline (diagnostic) + viz + timing against the deposited tree</name>
  <files>scripts/package_tutorial_dataset.py, tests/scripts/test_package_tutorial_dataset.py</files>
  <read_first>
    - scripts/package_tutorial_dataset.py (the skeleton from Plan 01 — the stub `regenerate_reference_outputs` and the console-script discovery + subprocess error-handling helpers to reuse)
    - tools/smoke_test.py (console-script discovery: `Path(sys.executable).parent / "aquapose"`, `.exe` on win32, fallback `python -c "from aquapose.cli import main; main()"`; PATTERNS.md cites lines 229-288)
    - src/aquapose/cli.py (the `run` command: `--config`, `--set output_dir=...`, `--mode diagnostic`; the `viz` command: positional run arg, `--animation`, `--overlay`, `--output-dir`; lines 41-144 and 418-568)
    - src/aquapose/evaluation/viz/animation.py and .../viz/overlay.py (what generate_animation / generate_overlay produce — HTML animation, MP4 overlay mosaic)
    - .planning/phases/111-example-dataset-reference-outputs/111-CONTEXT.md (D-08 reference outputs; D-02 canonical models; Claude's Discretion: HTML animation default)
  </read_first>
  <action>
    Replace the Plan-01 stub `regenerate_reference_outputs(deposit_dir: Path) -> None`
    with a real implementation. Build the `aquapose` invocation prefix via the
    console-script discovery idiom from `smoke_test.py` (`Path(sys.executable).parent /
    "aquapose"`, add `.exe` on win32, fallback to `[sys.executable, "-c", "from
    aquapose.cli import main; main()"]`).

    Step A — run the pipeline in diagnostic mode against the deposited tree. Build
    `[*invoke, "run", "--config", str(deposit_dir / "config.yaml"), "--set",
    f"output_dir={deposit_dir / 'reference_outputs'}", "--mode", "diagnostic"]`.
    Run it from `cwd=deposit_dir` (so the relative `project_dir: .` resolves), wrap
    the `subprocess.run(..., capture_output=True, text=True)` in the smoke_test
    error-handling idiom, and time it with `time.perf_counter()` around the call.
    The diagnostic run produces `outputs.h5` and the per-chunk pickle cache that
    `viz` consumes.

    Step B — generate the 3D animation + overlay mosaic. Resolve the produced run
    directory under `reference_outputs/` (the run dir that `aquapose run` created;
    resolve it the same way `viz` does — a positional run arg, or omit to let
    `resolve_run` pick the latest under project_dir). Build `[*invoke, "viz",
    <run_dir>, "--animation", "--overlay", "--output-dir",
    str(deposit_dir / "reference_outputs")]`. `--animation` yields interactive HTML
    (Claude's-Discretion default per D-08); `--overlay` yields the mosaic MP4. Run
    from `cwd=deposit_dir`, same error handling.

    Step C — normalize output filenames + write timing. After viz, rename/copy the
    produced artifacts to the D-06 canonical names inside `reference_outputs/`:
    `outputs.h5`, `animation_3d.html`, `overlay_mosaic.mp4`. Write `timing.txt` with
    the wall-clock pipeline duration captured in Step A (and viz duration if useful),
    one labeled line each. Print a summary block listing the four artifacts and their
    sizes.

    Wire this into `main` so the `--regenerate-outputs` flag calls
    `regenerate_reference_outputs(output_dir)` AFTER the tree + config are assembled
    and BEFORE the final checksum manifest (which Plan 03 owns — do not emit the
    final manifest here).

    Add a unit test that monkeypatches `subprocess.run` to a fake returning
    returncode 0 and asserts: (a) the pipeline command contains `run`, `--config`,
    `--mode`, `diagnostic`; (b) the viz command contains `viz`, `--animation`,
    `--overlay`; (c) `timing.txt` is written. Do NOT run the real GPU pipeline in the
    unit test — that is the Task 2 human checkpoint.
  </action>
  <verify>
    <automated>hatch run test tests/scripts/test_package_tutorial_dataset.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/package_tutorial_dataset.py` contains `def regenerate_reference_outputs(` with no `pass`-only stub body.
    - The pipeline subprocess argv (asserted via monkeypatched `subprocess.run`) contains `run`, `--config`, `--mode`, `diagnostic`, and a `--set output_dir=...reference_outputs` token.
    - The viz subprocess argv contains `viz`, `--animation`, `--overlay`.
    - A `timing.txt` write is exercised in the test.
    - `hatch run test tests/scripts/test_package_tutorial_dataset.py` passes (mocked subprocess; no GPU).
  </acceptance_criteria>
  <done>`regenerate_reference_outputs` runs the diagnostic pipeline + viz against the deposited tree and writes the four canonical reference-output artifacts + timing; mocked-subprocess unit tests are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Regenerate reference outputs on GPU against the deposited clip and verify they are current-model outputs</name>
  <what-built>regenerate_reference_outputs() runs the real diagnostic pipeline + viz on the deposited trimmed clip using the D-02 canonical models. This checkpoint executes it on a GPU (it cannot run in CI/unit tests) and verifies the four reference-output artifacts are freshly produced on dev.</what-built>
  <how-to-verify>
    1. On a machine with a CUDA GPU, run: `hatch run python scripts/package_tutorial_dataset.py --source-dir D:\AquaPose_Zenodo_staging\YH --output-dir .\aquapose-tutorial-data --regenerate-outputs`. (If the tree already exists from 111-01, this re-runs assembly then regenerates outputs.)
    2. Confirm `aquapose-tutorial-data/reference_outputs/` contains: `outputs.h5`, `animation_3d.html`, `overlay_mosaic.mp4`, `timing.txt`.
    3. Open `animation_3d.html` in a browser — confirm it renders 3D fish midlines that move over the 30s clip (9 fish, D-07 n_animals).
    4. Play `overlay_mosaic.mp4` — confirm the 2D reprojection overlay looks reasonable across the 12 camera panes.
    5. `python -c "import h5py; f=h5py.File('aquapose-tutorial-data/reference_outputs/outputs.h5'); print(list(f.keys()))"` — confirm it opens and has expected top-level groups (midlines/tracks).
    6. Confirm `timing.txt` reports a plausible wall-clock pipeline duration.
    7. Confirm you are on `dev` (`git branch --show-current`) so DATA-02's "regenerated on dev with current production models" is satisfied — NOT stale March runs.
  </how-to-verify>
  <resume-signal>Type "approved" once the four reference-output artifacts are freshly regenerated on dev and render correctly — or describe issues (e.g. animation empty, wrong fish count, pipeline error) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deposited clip + models -> pipeline execution | The pipeline runs the AGPL Ultralytics backends on the deposited assets; outputs become part of the public deposit |
| reference_outputs/ -> public DOI | outputs.h5 / animation / overlay become public (Plan 03 uploads) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-05 | Tampering | reference outputs provenance | mitigate | Regenerate against the DEPOSITED clip + DEPOSITED config on `dev` with the D-02 canonical models — not stale March `main`-era runs; the Task-2 checkpoint asserts branch=dev and fresh artifacts, keeping the tutorial verifiable. |
| T-111-06 | Information Disclosure | outputs.h5 content | accept | Contains only 3D fish midline coordinates + timing — no PII, no credentials. Low-value; the human checkpoint inspects content before Plan 03 publishes. |
| T-111-07 | Denial of Service (self) | GPU pipeline run | accept | Runs on a 30s / ~900-frame clip (D-04) — bounded runtime; not a public-facing service. |
| T-111-SC | Tampering | pip/package installs | accept | No new package installs — reuses the existing `aquapose` runtime + ffmpeg. No RESEARCH.md package audit required. |
</threat_model>

<verification>
- `hatch run test tests/scripts/test_package_tutorial_dataset.py` green (mocked subprocess).
- `hatch run check` clean on the modified script + test.
- Human checkpoint (Task 2): four reference-output artifacts freshly regenerated on dev, rendering correctly.
</verification>

<success_criteria>
- `--regenerate-outputs` runs the real diagnostic pipeline + viz against the
  deposited tree and produces `reference_outputs/{outputs.h5, animation_3d.html,
  overlay_mosaic.mp4, timing.txt}` on `dev` with the D-02 canonical models.
- DATA-02 satisfied: reference outputs are current-model, deposited-clip
  reproducible.
</success_criteria>

<output>
Create `.planning/phases/111-example-dataset-reference-outputs/111-02-SUMMARY.md` when done.
</output>
