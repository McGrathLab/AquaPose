---
phase: 113.1-pre-release-bug-fixes
plan: 07
subsystem: infra
tags: [pillow, packaging, pyproject, claude-md, guidebook, agent-context]

# Dependency graph
requires:
  - phase: 113.1-01
    provides: "The third PIL import site (_resolve_sibling_image in prep.py) that raised the pillow declaration to a supply-chain concern"
  - phase: 113.1-03
    provides: "The section 6 GUIDEBOOK correction and its drift guard test, plus the filed section 3/4/7/8/14 drift todo this plan's note references"
provides:
  - "An explicit pillow>=10.0 runtime dependency in pyproject.toml, closing the accidental-transitive-resolution gap ahead of Phase 114's pip install aquapose release"
  - "A CLAUDE.md that no longer instructs discuss-phase to ingest .planning/GUIDEBOOK.md as authoritative context"
affects: [114-release-packaging, gsd-discuss-phase]

actuals:
  tokens: 742
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - pyproject.toml
    - CLAUDE.md
    - .planning/todos/done/2026-09-02-pillow-undeclared-dependency.md

key-decisions:
  - "pillow>=10.0 chosen as a modern floor above every real constraint (code's own 7.0.0 API floor, ultralytics>=7.1.2, torchvision!=8.3.*,>=5.3.0) rather than the bare minimum, matching the project's other unpinned-ceiling dependency style"
  - "Verified the transitive floors by reading installed package metadata (importlib.metadata.requires) inside the existing hatch env rather than recreating it, per the CUDA-preservation fence"
  - "CLAUDE.md's Agent-Specific Instructions section removed in full (its only content was the discuss-phase GUIDEBOOK hook) and replaced with a short explanatory note naming the drift todo by filename, so a future maintainer does not reinstate the hook without context"
  - "GUIDEBOOK.md content itself left untouched deliberately (user decision, 2026-09-02) - only the ingestion path closed, drift todo stays pending"

patterns-established: []

requirements-completed: [QA-05]

coverage:
  - id: D1
    description: "pillow declared as an explicit pyproject.toml runtime dependency with a floor compatible with both transitive constraints and the code's own API usage"
    requirement: "QA-05"
    verification:
      - kind: other
        ref: "python3 -c \"import tomllib...\" pillow membership check (exit 0)"
        status: pass
      - kind: other
        ref: "hatch run lint (exit 0)"
        status: pass
      - kind: other
        ref: "hatch run docs:build (exit 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "CLAUDE.md no longer instructs any agent to ingest GUIDEBOOK.md as authoritative; GUIDEBOOK.md content and its drift todo are left untouched"
    requirement: "QA-05"
    verification:
      - kind: other
        ref: "grep -n \"Agent-Specific Instructions\" CLAUDE.md (no match)"
        status: pass
      - kind: other
        ref: "git diff --stat .planning/GUIDEBOOK.md (empty)"
        status: pass
      - kind: unit
        ref: "tests/unit/engine/test_pipeline.py::test_build_stages_sequence_matches_guidebook"
        status: pass
    human_judgment: false

duration: 2min
completed: 2026-09-02
status: complete
---

# Phase 113.1 Plan 07: Pillow Dependency + GUIDEBOOK Ingestion Hook Summary

**Declared `pillow>=10.0` as an explicit `pyproject.toml` runtime dependency and removed the `CLAUDE.md` instruction that told `discuss-phase` to ingest `.planning/GUIDEBOOK.md` as authoritative context.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-09-02T21:15:01Z
- **Completed:** 2026-09-02T21:16:32Z (implementation); test suite and summary authoring followed
- **Tasks:** 2
- **Files modified:** 3 (`pyproject.toml`, `CLAUDE.md`, one todo moved pending → done)

## Accomplishments

- `pillow>=10.0` added to `[project] dependencies` in `pyproject.toml`. The floor was chosen above every real constraint: the code's own API floor is 7.0.0 (`UnidentifiedImageError`, imported in `src/aquapose/training/prep.py:19`), `ultralytics` declares `pillow>=7.1.2`, and `torchvision` declares `pillow!=8.3.*,>=5.3.0` — both read directly from installed package metadata via `importlib.metadata.requires(...)` inside the existing hatch environment, not assumed or recalled. Installed version in that environment is 12.1.1.
- `CLAUDE.md`'s `## Agent-Specific Instructions` section (its only content: a `discuss-phase` hook instructing agents to read `.planning/GUIDEBOOK.md` as discussion context) was removed in full and replaced with a short note recording that the guidebook is a human-oriented reference with known drift in sections 3, 4, 7, 8, and 14, naming the drift todo by filename.
- The pillow todo was moved from `.planning/todos/pending/` to `.planning/todos/done/` with a `## Fixed` note.

## Task Commits

Each task was committed atomically:

1. **Task 1: Declare pillow as a runtime dependency** - `aa80121` (feat)
2. **Task 2: Stop CLAUDE.md feeding GUIDEBOOK.md into planning as authoritative** - `3b0f88f` (docs)

**Plan metadata:** (this commit, follows)

## Files Created/Modified

- `pyproject.toml` - Added `"pillow>=10.0"` to the `dependencies` array, next to `h5py>=3.9`, matching the neighbouring bare-lower-bound convention
- `CLAUDE.md` - Removed `## Agent-Specific Instructions` (discuss-phase GUIDEBOOK hook); replaced with a short explanatory note under `## Notes on .planning/GUIDEBOOK.md`
- `.planning/todos/done/2026-09-02-pillow-undeclared-dependency.md` - Moved from `pending/`, with a `## Fixed` note naming this phase/plan and the declared floor

## Decisions Made

- **Floor choice (`>=10.0`):** matches the plan's directive — a modern floor above all three real constraints (code API floor 7.0.0, `ultralytics>=7.1.2`, `torchvision!=8.3.*,>=5.3.0`) rather than the bare minimum, consistent with how other scientific dependencies in this manifest are pinned (bare `>=`, no upper cap).
- **Verification method:** read installed package metadata (`importlib.metadata.requires('ultralytics')`, `...('torchvision')`) inside the current, already-working hatch environment rather than recreating it — per the phase's standing fence against `hatch env create/prune/remove` (risk of silently losing CUDA torch).
- **GUIDEBOOK scope discipline:** per the user's 2026-09-02 decision, only the ingestion path in `CLAUDE.md` was closed. `.planning/GUIDEBOOK.md` content itself (sections 3, 4, 7, 8, 14) was left untouched, and its drift todo (`2026-09-02-guidebook-sections-3-4-7-8-14-drift-beyond-section-6.md`) stays pending. This is a deliberate half-measure, not an oversight — fixing the content here would have substituted executor judgment for a stated user choice.
- **Section 6 left alone:** the drift guard test `test_build_stages_sequence_matches_guidebook` (added by 113.1-03) was not touched and still passes — section 6 remains correct and pinned.

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched their `<action>` and `<acceptance_criteria>` blocks precisely; no auto-fixes, no architectural questions, no scope expansion.

## Issues Encountered

None. The tracer feedback gate (Task 1 is `type="tracer"`) was re-verified end-to-end after commit under active auto-chain mode (`AUTO_CHAIN=true`) before proceeding to Task 2 — all three verify steps (pillow membership, `hatch run lint`, `hatch run docs:build`) passed.

## Regression Gate

`hatch run test` (full suite, excludes `@slow`/`@e2e`) run after both tasks: **1400 passed, 3 skipped, 17 deselected, 0 failures** in 11.11s. Per the sibling-plan note, the suite was already fully green going into this plan (113.1-06 fixed the last standing failure first in the wave); this plan introduced zero regressions.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `pillow` is now an explicit, correctly-floored dependency ahead of Phase 114's `pip install aquapose` release work.
- `CLAUDE.md` no longer feeds a known-drifted document into `discuss-phase` planning context; the drift itself remains a tracked, pending todo for whoever picks it up (not scoped to any phase currently on the roadmap).
- Both plan 05 (import boundary, `113.1-05-PLAN.md`) and this plan are now the only remaining incomplete plans in the phase's wave 2 — plan 05's status is unaffected by this plan's work.

---
*Phase: 113.1-pre-release-bug-fixes*
*Completed: 2026-09-02*

## Self-Check: PASSED

All claimed files found on disk; both task commit hashes (`aa80121`, `3b0f88f`) found in git log.
