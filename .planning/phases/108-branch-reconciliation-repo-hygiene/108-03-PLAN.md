---
phase: 108-branch-reconciliation-repo-hygiene
plan: 03
type: execute
wave: 3
depends_on: ["108-02"]
files_modified:
  - .gitignore
  - tests/integration/segmentation/__init__.py
  - .planning/todos/
autonomous: false
requirements: [FOUND-04]

must_haves:
  truths:
    - "D-18: `.planning/` is tracked normally — `git check-ignore .planning/STATE.md` exits non-zero and GSD commits no longer need `git add -f`."
    - "D-18/D-19: the `.gitignore` diff for this plan is a single deletion — the `.planning/` rule — with no rule added."
    - "D-18: the whole project record is tracked: the 665-file historical archive the 108-01 merge delivered plus the 6 previously invisible `.planning/todos/` files."
    - "D-20: the dead `tests/integration/segmentation/` package is gone, and no other module was swept."
    - "D-19/FOUND-04: `git status --porcelain` on `dev` is empty — no `11.0`, no `~/` directory."
    - "OI-02: the 192 MB SAM2 clone under the stray tilde directory — including its `checkpoints/` model weights — was moved aside with explicit user confirmation, not destroyed silently."
    - "FOUND-04 / ROADMAP criterion 4: the four leftover top-level `src/aquapose/{reconstruction,segmentation,tracking,visualization}` directories — which hold only `__pycache__` and are what criterion 4 actually refers to — are gone from the working tree, while the live `src/aquapose/core/` modules of the same names remain and still import."
    - "108-01's merge results survive: none of the 29 purged paths reappeared and `git ls-files scripts/perf_validate.py` is still empty."
  artifacts:
    - path: ".gitignore"
      provides: "build-artifact coverage without the .planning/ contradiction"
      contains: "runs/"
    - path: ".planning/todos/pending/2026-03-06-fix-core-import-boundary-violation-in-frame-source.md"
      provides: "proof the previously invisible todo files are now tracked"
  key_links:
    - from: ".gitignore"
      to: ".planning/"
      via: "absence of an ignore rule"
      pattern: "git check-ignore .planning/STATE.md -> exit 1"
    - from: "pyproject.toml [tool.hatch.build.targets.sdist]"
      to: ".planning/"
      via: "sdist exclude — what makes un-ignoring safe"
      pattern: "exclude = \\[\".planning/\""
---

<objective>
Resolve the `.gitignore`/tracked-files contradiction by un-ignoring `.planning/` (D-18), delete the
dead `tests/integration/segmentation` package (D-20), and clear the working tree of `11.0` and the
stray `~/` directory (D-19), with an explicit user gate before touching the 192 MB SAM2 model
weights inside it (OI-02).

Purpose: satisfies FOUND-04. **ROADMAP criterion 4 is factually wrong** — `git ls-files` tracks no
`runs/`, no `tmp/`, no `yolo26n*.pt`, and no vendored SAM2, and `.gitignore` already covers all of
them. A fresh clone of `dev` is already clean. The same criterion's "dead
`reconstruction`/`segmentation`/`tracking` directories" resolve to four *untracked,
`__pycache__`-only* leftovers at the **top level** of `src/aquapose/`; those are removed here. The
same-named packages under `src/aquapose/core/` are **live production code** and must not be touched.

Output: `.gitignore` without the `.planning/` rule, the previously invisible `.planning/todos/`
files committed, the empty `tests/integration/segmentation/` package deleted, the four stale
top-level `src/aquapose/` directories gone, and a clean `git status`.

**Do not edit `pyproject.toml`** — plan 108-02 owns it for the rest of the phase. **Do not push** —
the phase's single push happens in plan 108-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**No worktree isolation.** This plan runs sequentially on the main working tree with HEAD on `dev`
and performs filesystem deletions outside the repository. Do not run it in a detached worktree.
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md

**Verified facts, re-derived 2026-08-17. These correct the file counts in the superseded plan,
which were written before the 108-01 merge fact-check and were wrong by roughly 580 files.**

- `.gitignore` lines 94–100 are `tmp/`, `scripts/`, `baseline_tracking_output/`, `*.pt`, `*.mp4`,
  `runs/`, `.planning/`. **Only the last one is removed by this plan.** Note `scripts/` is ignored
  while `scripts/detect_swaps.py` is tracked — that is a pre-existing, deliberate arrangement and
  is **not** in scope; do not touch it. Plan 108-01 added `_readthedocs/` higher in the file; do
  not remove it.
- **Post-108-01 `.planning/` state:** 704 tracked files (dev's original 39 plus the 665-file
  historical archive the merge delivered), 710 files on disk. Only **6** files are untracked and
  invisible: `.planning/todos/completed/` (1 file) and `.planning/todos/pending/` (5 files).
  Un-ignoring surfaces those six, plus whatever SUMMARY files this phase's own execution creates.
  There is no 589-file audit to perform — plan 108-01 already ran `detect-secrets` across the
  entire merged archive before committing it.
- Note the merge also brought main's flat `.planning/todos/*.md` set (19 files, all
  `2026-02-*`/`2026-03-0*` dated). dev's disk uses `completed/` and `pending/` subdirectories.
  Both coexist with zero filename collisions; reconciling the two layouts is **not** in scope.
- `pyproject.toml:57` has `exclude = [".planning/", ".claude/", ".hooks/"]` under
  `[tool.hatch.build.targets.sdist]` — this is what makes D-18 safe. No packaging change needed,
  and this plan must not edit that file.
- `.pre-commit-config.yaml` runs `detect-secrets` with `--baseline .secrets.baseline` on all staged
  files with **no** `.planning/` exclude. `trailing-whitespace`, `end-of-file-fixer`, `check-yaml`,
  and `check-added-large-files` all *do* exclude `^\.planning/`.
- `git ls-files tests/integration` returns exactly three paths: `tests/integration/__init__.py`,
  `tests/integration/segmentation/__init__.py`, and
  `tests/integration/test_calibration_cross_validation.py`. Nothing in the repo imports or
  references `integration.segmentation`. The directory also holds a stale `__pycache__/` containing
  a `test_yolo_sam_integration` artifact from a test file that no longer exists.
- **What ROADMAP criterion 4's dead directories actually are:** four *top-level* leftovers at
  `src/aquapose/reconstruction`, `src/aquapose/segmentation`, `src/aquapose/tracking`, and
  `src/aquapose/visualization`. Each was verified to contain **zero non-`.pyc` files** and **zero
  tracked paths**. They are stale build residue from packages that moved under `core/` or were
  removed in v3.7. The live production modules are `src/aquapose/core/reconstruction/` and
  `src/aquapose/core/tracking/` — different paths, do not touch them. Tracked top-level members of
  `src/aquapose/` are exactly `calibration`, `core`, `engine`, `evaluation`, `io`, `synthetic`,
  `training`, `cli.py`, `cli_utils.py`, `logging.py`, `__init__.py`, `py.typed`.
- Untracked-and-unignored working-tree paths: `11.0` (an 11 KB pip install log from
  `pip install boxmot`, dated 2026-02-27) and `./~` — a literal tilde directory, **192 MB**,
  containing a full SAM2 clone at `./~/PycharmProjects/sam2` with a `checkpoints/` directory of
  downloaded model weights.
- A pre-existing unrelated `stash@{0}` (base `3e802c0`) exists. Leave it alone; run no `git stash`
  command in this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-ignore .planning/ and commit the previously invisible files</name>
  <files>.gitignore, .planning/todos/</files>
  <read_first>
    - `.gitignore` lines 50–102 — confirm the `_readthedocs/` rule 108-01 added in the Sphinx
      section is present and that line 100 is exactly `.planning/`; note that lines 94–99
      (`tmp/`, `scripts/`, `baseline_tracking_output/`, `*.pt`, `*.mp4`, `runs/`) all stay.
    - `pyproject.toml` lines 55–60 — the `[tool.hatch.build.targets.sdist]` exclude list, the
      source of truth for why un-ignoring is safe. **Read only; do not edit this file.**
    - `.pre-commit-config.yaml` — note that `detect-secrets` has no `^\.planning/` exclude while the
      four `pre-commit-hooks` entries do.
    - `.secrets.baseline` — the existing allowlist.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-01-SUMMARY.md` — the record of
      the merge, including the detect-secrets result already obtained over the 665 archive files.
    - `.planning/STATE.md` Blockers/Concerns section — records this contradiction as a known blocker.
  </read_first>
  <action>
    Delete the single line `.planning/` from `.gitignore`. Change nothing else in that file. Per
    D-19, add **no** new ignore rules — in particular do not add `~/` or a pip-log pattern, and do
    not remove `scripts/` or `_readthedocs/`.

    Then stage the newly visible files: `git add -A .planning/`. Confirm from
    `git status --porcelain .planning/` beforehand and `git diff --cached --name-only` afterwards
    that the set is the six `.planning/todos/completed/` and `.planning/todos/pending/` files plus
    any SUMMARY files this phase has produced so far — **nothing else**. If the count is materially
    larger, stop and report: it means the 108-01 merge did not land as planned.

    Audit before committing:
    1. `hatch run pre-commit run detect-secrets --all-files`. **If it reports any finding, stop and
       report to the user — do not update `.secrets.baseline` to silence it.**
    2. `grep -rniE '(api[_-]?key|client[_-]?secret|access[_-]?token|password)[[:space:]]*[:=]' .planning/todos/`
       and `grep -rlE 'ghp_|github_pat_|BEGIN [A-Z ]*PRIVATE KEY' .planning/todos/`. Any hit is a
       hard stop for user review.
    3. Informational only, not a blocker: count files containing absolute Windows paths with
       `grep -rl 'C:.Users' .planning/ | wc -l` and record the number in the SUMMARY. Local Windows
       paths naming the already-public author are not a leak; do not scrub them.
    4. `find .planning -type f -size +1M` must print nothing.

    Commit as `chore(108-03): un-ignore .planning/ and track the remaining project record`. This
    commit must **not** require `git add -f` — ending that friction is the point of D-18. Do not
    push.
  </action>
  <verify>
    <automated>! git check-ignore -q .planning/STATE.md &amp;&amp; test "$(grep -c '^\.planning/$' .gitignore)" -eq 0 &amp;&amp; test "$(git ls-files .planning | wc -l)" -ge 710 &amp;&amp; test -z "$(git status --porcelain .planning)" &amp;&amp; git diff --quiet HEAD -- pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore .planning/STATE.md` exits non-zero.
    - `grep -c '^\.planning/$' .gitignore` returns `0`.
    - `grep -c '^runs/$' .gitignore` returns `1`, `grep -c '^tmp/$' .gitignore` returns `1`, `grep -c '^\*\.pt$' .gitignore` returns `1`, `grep -c '^scripts/$' .gitignore` returns `1`, `grep -c '^_readthedocs/$' .gitignore` returns `1`.
    - `git diff HEAD~1 HEAD -- .gitignore | grep -c '^+[^+]'` returns `0` — deletion only, no rule added (D-19).
    - `git diff HEAD~1 HEAD -- .gitignore | grep -c '^-[^-]'` returns `1` — exactly one rule removed.
    - `git ls-files .planning | wc -l` returns at least `710`.
    - `git ls-files .planning/todos | wc -l` returns at least `25` (main's 19 flat files plus dev's 6 subdirectory files).
    - `git ls-files .planning/milestones/v2.2-phases | wc -l` is greater than `35` — the archive 108-04 needs is still tracked.
    - `git status --porcelain .planning/` outputs zero lines.
    - `hatch run pre-commit run detect-secrets --all-files` exits 0.
    - `git diff --exit-code HEAD -- .secrets.baseline` exits 0 — the baseline was not widened.
    - `find .planning -type f -size +1M` outputs zero lines.
    - `git diff --exit-code HEAD~1 HEAD -- pyproject.toml` exits 0 — 108-02 owns that file.
    - `git ls-files scripts/perf_validate.py src/aquapose/core/midline tests/unit/segmentation | wc -l` returns `0` — 108-01's purge still holds.
  </acceptance_criteria>
  <done>`.planning/` is no longer ignored, the six previously invisible todo files are tracked in a normal commit that needed no `git add -f`, detect-secrets passes, and `.secrets.baseline` was not widened.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead tests/integration/segmentation package</name>
  <files>tests/integration/segmentation/__init__.py</files>
  <read_first>
    - `tests/integration/segmentation/__init__.py` — confirm it is empty or contains only a
      docstring, and that the directory holds no test modules (only a stale `__pycache__/`).
    - `tests/integration/__init__.py` and `tests/integration/test_calibration_cross_validation.py`
      — the sibling files that stay.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` section D-20 — the
      scope fence: exactly two dead artifacts, no broader sweep.
  </read_first>
  <action>
    Confirm nothing references the package:
    `grep -rn "integration.segmentation\|integration/segmentation" --include='*.py' --include='*.toml' --include='*.cfg' --include='*.ini' .`
    must return no hits outside `.planning/`. If it returns a hit, stop and report.

    Run `git rm -r tests/integration/segmentation`, then remove the leftover
    `tests/integration/segmentation/__pycache__/` and the now-empty directory from disk.

    **Do not remove anything else.** `src/aquapose/core/reconstruction/` and
    `src/aquapose/core/tracking/` are live production modules despite ROADMAP criterion 4 calling
    them dead — leave them untouched. `docs/api/mesh.rst`, `optimization.rst`, `segmentation.rst`,
    and `utils.rst` are the other dead artifact and were already removed by the 108-01 merge;
    verify they are absent rather than deleting them again. D-20 caps dead-code removal at exactly
    these two things — do not start a broader sweep.

    Do not attempt to fix any failing test. The suite is red with 8 known failures and stays red
    through this phase; Phase 109 owns that.

    Confirm collection still works: `hatch run test --collect-only -q tests/integration` exits 0.

    Commit as `chore(108-03): remove dead tests/integration/segmentation package`. Do not push.
  </action>
  <verify>
    <automated>test ! -e tests/integration/segmentation &amp;&amp; hatch run test --collect-only -q tests/integration &gt;/dev/null &amp;&amp; test "$(git ls-files tests/integration | wc -l)" -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `test -e tests/integration/segmentation` exits non-zero.
    - `git ls-files tests/integration/segmentation` outputs zero lines.
    - `git ls-files tests/integration | wc -l` returns `2`.
    - `hatch run test --collect-only -q tests/integration` exits 0.
    - `git ls-files docs/api/mesh.rst docs/api/optimization.rst docs/api/segmentation.rst docs/api/utils.rst` outputs zero lines.
    - `git ls-files docs/api | wc -l` returns `8`.
    - `git ls-files src/aquapose/core/reconstruction | wc -l` is greater than `0` and `git ls-files src/aquapose/core/tracking | wc -l` is greater than `0` — live code untouched.
    - `git diff --name-status HEAD~1 HEAD | grep -c '^D'` returns `1` and `git diff --name-only HEAD~1 HEAD | wc -l` returns `1` — exactly one file deleted, nothing else changed.
    - `git ls-files src/aquapose/core/midline tests/unit/segmentation scripts/perf_validate.py | wc -l` returns `0` — 108-01's purge still holds.
  </acceptance_criteria>
  <done>The empty `tests/integration/segmentation` package and its stale `__pycache__` are deleted, integration test collection still succeeds, and no live module or additional file was removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Clear the working tree — confirm before touching the SAM2 model weights</name>
  <files>11.0, ./~, src/aquapose/reconstruction, src/aquapose/segmentation, src/aquapose/tracking, src/aquapose/visualization</files>
  <read_first>
    - `git status --porcelain` — the authoritative list of what is untracked and unignored.
    - For each of `src/aquapose/reconstruction`, `src/aquapose/segmentation`,
      `src/aquapose/tracking`, `src/aquapose/visualization`: run
      `find <path> -type f ! -name '*.pyc'` and `git ls-files <path>`. Both must print nothing
      before you delete. If either prints anything, stop.
    - `ls src/aquapose/core` — confirm `reconstruction/` and `tracking/` live here and are the
      production modules that must survive.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` `<open_items>` OI-02
      and section D-19 — OI-02 explicitly qualifies D-19 and requires a user confirmation here.
    - The contents of the tilde directory: `du -sh './~'` (expected ~192 MB) and
      `find './~' -maxdepth 5 -type d -name checkpoints -exec du -sh {} \;` plus a listing of the
      files inside it. You must be able to tell the user how much data and which weight files are
      at stake before anything moves.
  </read_first>
  <action>
    Automate everything first, then pause. In order:

    1. Confirm the four top-level leftovers are pure residue: for each of
       `src/aquapose/reconstruction`, `src/aquapose/segmentation`, `src/aquapose/tracking`, and
       `src/aquapose/visualization`, `find <path> -type f ! -name '*.pyc'` must print nothing and
       `git ls-files <path>` must print nothing. **If either prints anything, stop and report — do
       not delete.** Then `rm -rf` those four paths. Do not touch
       `src/aquapose/core/reconstruction` or `src/aquapose/core/tracking`.
    2. `rm -f ./11.0` — an 11 KB never-tracked pip install log, safe to delete unconditionally (D-19).
    3. Measure what is at stake before moving anything: `du -sh './~'` and
       `find './~' -maxdepth 5 -type d -name checkpoints -exec du -sh {} \;`, and list the files
       inside each `checkpoints` directory with their sizes.
    4. **Move, do not delete** (OI-02): `mkdir -p ../aquapose-quarantine-108` then
       `mv './~' ../aquapose-quarantine-108/tilde`. Never run `rm -rf` against the tilde directory
       or anything under it at this step — it holds SAM2 model weights whose removal is required by
       no success criterion.
    5. Confirm the move preserved the clone:
       `test -d ../aquapose-quarantine-108/tilde/PycharmProjects/sam2`, list its `checkpoints/`
       contents, and confirm `du -sh ../aquapose-quarantine-108/tilde` matches the size measured in
       step 3.
    6. Confirm the live modules still import:
       `hatch run python -c "import aquapose.core.reconstruction, aquapose.core.tracking"`.
    7. Per D-19, add no `.gitignore` entries for `~/` or pip logs.

    Then pause and ask the user what should happen to the quarantined copy. Only run
    `rm -rf ../aquapose-quarantine-108/` if the user explicitly replies "delete". If they reply
    "relocate <path>", `mv` it there. If "keep", leave it in place.

    All four deleted paths were untracked, so no commit is required by this task. If nothing needs
    committing, say so explicitly rather than creating an empty commit.
  </action>
  <what-built>
    Everything automatable has already been done.

    The four stale top-level directories `src/aquapose/reconstruction`, `segmentation`, `tracking`,
    and `visualization` were removed after confirming each holds zero non-`.pyc` files and zero
    tracked paths. This is what ROADMAP criterion 4 actually meant, and it cost nothing because they
    were pure `__pycache__` residue. `src/aquapose/core/reconstruction/` and
    `src/aquapose/core/tracking/` were left untouched and still import.

    `11.0` — an 11 KB pip install log from `pip install boxmot`, never tracked — was deleted with
    `rm -f`.

    The stray literal-tilde directory (192 MB) was **moved, not deleted**, to a location outside the
    repository: `../aquapose-quarantine-108/tilde/`. This preserves `~/PycharmProjects/sam2` and its
    `checkpoints/` directory of downloaded SAM2 model weights intact.

    Nothing has been destroyed. `rm -rf` was deliberately not used on the tilde directory: OI-02
    records that these weights may not be trivially re-obtainable offline, and that their removal is
    required by **no** success criterion — the directory was never tracked by git, so a fresh clone
    of `dev` is already clean without it.

    The task output above states the measured size of the quarantined directory and the exact
    contents of its `checkpoints/` directory.
  </what-built>
  <how-to-verify>
    1. Confirm the repo is clean: run `git status --porcelain` in the project root. It must print
       nothing at all.
    2. Confirm nothing was lost: run
       `ls -lh ../aquapose-quarantine-108/tilde/PycharmProjects/sam2/checkpoints`. It should list
       the SAM2 weight files that were previously inside the repository directory. The measured
       total size is in the task output above.
    3. Decide what happens to the quarantined copy and tell me which:
       - **keep** — leave it at `../aquapose-quarantine-108/` indefinitely (default; costs only disk)
       - **relocate** — give me a path to move it to (for example your real `~/PycharmProjects/sam2`)
       - **delete** — I will `rm -rf ../aquapose-quarantine-108/` and the weights are gone permanently

    Do not answer "delete" unless you are sure you can re-download those checkpoints.
  </how-to-verify>
  <acceptance_criteria>
    - `git status --porcelain` outputs zero lines.
    - `test -e ./11.0` exits non-zero.
    - `test -e './~'` exits non-zero — no literal tilde directory remains in the repo root.
    - Before the user's decision: `test -d ../aquapose-quarantine-108/tilde/PycharmProjects/sam2` exits 0 — the clone survived the move.
    - `git diff --exit-code HEAD -- .gitignore` exits 0 for this task — D-19: no defensive ignore rules were added for `~/` or pip logs.
    - `git ls-files | grep -c '^11\.0$'` returns `0` and `git ls-files | grep -c 'sam2'` returns `0` — confirming neither was ever tracked.
    - `test -e src/aquapose/reconstruction`, `test -e src/aquapose/segmentation`, `test -e src/aquapose/tracking`, and `test -e src/aquapose/visualization` each exit non-zero.
    - `test -d src/aquapose/core/reconstruction` and `test -d src/aquapose/core/tracking` each exit 0 — live modules preserved.
    - `hatch run python -c "import aquapose.core.reconstruction, aquapose.core.tracking"` exits 0.
    - `git ls-files | wc -l` differs from 108-01's recorded 988 only by the files this plan added and the one it deleted; state the arithmetic explicitly in the SUMMARY.
    - `git ls-files src/aquapose/core/midline tests/unit/segmentation scripts/perf_validate.py | wc -l` returns `0` — 108-01's purge still holds.
    - The user's disposition choice (keep / relocate / delete) is recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain)" &amp;&amp; test ! -e ./11.0 &amp;&amp; test ! -e './~' &amp;&amp; test ! -e src/aquapose/reconstruction &amp;&amp; test ! -e src/aquapose/segmentation &amp;&amp; test ! -e src/aquapose/tracking &amp;&amp; test ! -e src/aquapose/visualization &amp;&amp; test -d src/aquapose/core/reconstruction &amp;&amp; test -d src/aquapose/core/tracking &amp;&amp; hatch run python -c "import aquapose.core.reconstruction, aquapose.core.tracking"</automated>
  </verify>
  <resume-signal>Reply "keep", "relocate &lt;path&gt;", or "delete" — or describe issues.</resume-signal>
  <done>The repository working tree is clean, `11.0` is gone, the tilde directory no longer sits in the repo, the SAM2 checkpoints are preserved at a quarantine path, the live `core/` modules still import, and the user has explicitly chosen the quarantined copy's fate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untracked local filesystem -> destructive shell commands | the tilde directory holds 192 MB of SAM2 model weights that no success criterion requires deleting |
| previously-ignored `.planning/` -> public git history | files never scanned by the pre-commit secret hooks become permanently published |
| `.secrets.baseline` -> detect-secrets gate | widening the baseline to make a commit pass would silently disable the control |
| ROADMAP criterion 4's wording -> live production code | four *top-level* residue directories share names with three *live* `core/` packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-12 | Denial of Service | local SAM2 model weights inside the tilde directory | mitigate | Task 3 is a blocking human checkpoint; the action moves the directory to `../aquapose-quarantine-108/` instead of `rm -rf`, measures its size and checkpoint contents first, and asserts the clone survives the move before the user is asked; deletion only ever happens on an explicit "delete" reply |
| T-108-13 | Information Disclosure | credentials or tokens inside newly tracked `.planning/` files | mitigate | Task 1 runs `pre-commit run detect-secrets --all-files` plus explicit greps for `api_key`/`client_secret`/`access_token`/`password`/PEM headers/`ghp_`/`github_pat_`, with a hard stop on any hit; 108-01 already applied the same gate to the 665 merged archive files before committing them |
| T-108-14 | Tampering | `.secrets.baseline` allowlist | mitigate | The action forbids baselining a finding to make the commit pass; acceptance asserts `git diff --exit-code HEAD -- .secrets.baseline` exits 0 |
| T-108-15 | Information Disclosure | absolute local paths in `.planning/` naming the developer's machine | accept | The paths name the already-public repository author and a local Windows home directory; they expose no credential, host, or private endpoint. Counted and recorded in the SUMMARY for visibility, not scrubbed |
| T-108-16 | Tampering | live production modules misidentified as dead by ROADMAP criterion 4 | mitigate | Task 2 scopes deletion to one file, greps for references first, and asserts exactly one file was deleted and that `src/aquapose/core/{reconstruction,tracking}` still have tracked files. Task 3's removal of the same-named *top-level* leftovers is gated on each holding zero non-`.pyc` files and zero tracked paths, and acceptance re-imports both live modules to prove they survived |
| T-108-34 | Tampering | silently regressing 108-01's merge purge or 108-02's relicense | mitigate | Every task asserts `git ls-files src/aquapose/core/midline tests/unit/segmentation scripts/perf_validate.py` is empty, and tasks 1 and 2 pin their exact changed-file sets so no unrelated file can drift |
| T-108-17 | Denial of Service | repository size / large files entering history | mitigate | Task 1 asserts `find .planning -type f -size +1M` prints nothing before committing; the archive is markdown with a 184 KB largest blob |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. `hatch run test --collect-only` and the import checks use the already-provisioned default env against an unchanged dependency set |
</threat_model>

<verification>
- `git check-ignore .planning/STATE.md` exits non-zero (D-18).
- The `.gitignore` diff for this plan is exactly one removed line and zero added lines (D-19).
- `git ls-files .planning | wc -l` is at least 710 and `git status --porcelain .planning/` is empty.
- `pre-commit run detect-secrets --all-files` exits 0 with an unmodified `.secrets.baseline`.
- `tests/integration/segmentation` no longer exists; `src/aquapose/core/{reconstruction,tracking}` do (D-20).
- The four stale top-level `src/aquapose/` directories are gone and both live `core/` modules import.
- `git status --porcelain` is empty (FOUND-04).
- The SAM2 clone survives at the quarantine path unless the user explicitly says otherwise (OI-02).
- 108-01's 29-path purge and `docs/api` 8-file set still hold; `pyproject.toml` is unmodified; nothing pushed.
</verification>

<success_criteria>
- GSD planning commits no longer require `git add -f`.
- The complete project record — merged archive plus the previously invisible todos — is in git history.
- The working tree is clean without any irreversible loss of local model assets.
- Dead-artifact removal stayed inside the D-20 fence.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-03-SUMMARY.md` when done.
Record: the newly tracked file count and the arithmetic against 108-01's 988, the absolute-path
grep count, the measured size and checkpoint contents of the quarantined tilde directory, and the
user's verbatim disposition choice for it.
</output>
