---
phase: 112-config-cli-reference
plan: 04
type: execute
wave: 3
depends_on: [112-02, 112-03]
files_modified:
  - docs/reference/index.md
  - docs/index.md
  - docs/api/cli.rst
  - docs/api/engine.rst
autonomous: true
requirements: [DOCS-05, DOCS-06]
must_haves:
  truths:
    - "A new top-level Reference section appears in the docs sidebar, holding the CLI Reference and Config Reference pages"
    - "The Reference section is reachable from the docs landing page and precedes the API Reference section"
    - "The Phase 110 api/cli.rst and api/engine.rst pages carry reciprocal cross-links to the new Reference pages, unchanged otherwise"
    - "hatch run docs:build exits 0 with zero warnings — every new page is in a toctree, no broken cross-refs, no orphan pages"
  artifacts:
    - path: "docs/reference/index.md"
      provides: "Reference section landing page with grid cards and hidden toctree listing cli and config"
      contains: "toctree"
    - path: "docs/index.md"
      provides: "Reference grid-card and top-level toctree entry (above api/index)"
      contains: "reference/index"
  key_links:
    - from: "docs/index.md"
      to: "docs/reference/index.md"
      via: "grid-item-card :link: reference/index and hidden toctree entry"
      pattern: "reference/index"
    - from: "docs/reference/index.md"
      to: "docs/reference/cli.rst and docs/reference/config.md"
      via: "hidden toctree listing cli and config"
      pattern: "config"
    - from: "docs/api/cli.rst"
      to: "docs/reference/cli.rst"
      via: ":doc: cross-reference to the CLI Reference page"
      pattern: "reference/cli"
---

<objective>
Wire the two Reference pages into the docs IA and enforce the build-green gate. Create the Reference section landing page, add a Reference card + toctree entry to the top-level docs index (above the API Reference section per D-10), add reciprocal cross-links onto the kept Phase 110 `api/cli.rst` and `api/engine.rst` pages (D-13), and verify `hatch run docs:build` exits 0 with zero warnings under `-W`. Closes DOCS-05 and DOCS-06.

Purpose: The CLI and Config pages (Plans 02/03) exist but are orphaned until they are in a toctree — `-W` fails on orphan pages and broken cross-refs. This plan is the integration + gate wave: it makes the pages reachable, completes the bidirectional cross-linking to the API surface, and proves the whole tree still builds clean.
Output: `docs/reference/index.md` created; `docs/index.md`, `docs/api/cli.rst`, `docs/api/engine.rst` modified; `hatch run docs:build` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-config-cli-reference/112-CONTEXT.md
@.planning/phases/112-config-cli-reference/112-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Reference section landing page and wire it into the top-level toctree</name>
  <read_first>
    - docs/index.md (the full file — the grid-card pattern at lines 5-29 and the hidden toctree at lines 31-38; this is the exact analog to copy)
    - docs/reference/cli.rst (from Plan 02 — confirm the page title/target name `cli`)
    - docs/reference/config.md (from Plan 03 — confirm the page title/target name `config`)
    - .planning/phases/112-config-cli-reference/112-PATTERNS.md (the `docs/reference/index.md` and `docs/index.md` pattern sections — grid-card + hidden-toctree structure and placement)
  </read_first>
  <action>Create `docs/reference/index.md` (MyST) mirroring the `docs/index.md` grid-card + hidden-toctree structure. Give it a `# Reference` title and a short intro line. Add two `:::{grid-item-card}` cards inside a `::::{grid} 1 2 2 3` block: "CLI Reference" with `:link: cli` `:link-type: doc`, and "Config Reference" with `:link: config` `:link-type: doc` (`colon_fence` and `sphinx_design` are already enabled in conf.py, so no config change is needed). Below the grid, add a hidden `{toctree}` (`:maxdepth: 2`, `:hidden:`) listing exactly `cli` and `config` (relative page names, no extension). Then edit `docs/index.md`: add a third `:::{grid-item-card}` titled "Reference" with `:link: reference/index` `:link-type: doc` and a one-line description (e.g. CLI commands and configuration fields for looking up any command or setting), mirroring the existing "API Reference" card exactly. In the hidden top-level toctree (lines 31-38), add the entry `reference/index` placed ABOVE `api/index` so the user-facing Reference section precedes the auto-generated module API in the sidebar (D-10 signal). Do NOT remove or reorder the existing `api/index`, `contributing`, or `reports/z_uncertainty_report` entries.</action>
  <verify>
    <automated>test -f docs/reference/index.md && grep -q "reference/index" docs/index.md && grep -Eq "^cli$|^\s+cli$" docs/reference/index.md && grep -q "config" docs/reference/index.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/index.md` exists with a `# Reference` title, two grid cards (CLI Reference → `cli`, Config Reference → `config`), and a hidden toctree listing `cli` and `config`.
    - `docs/index.md` contains a "Reference" grid-item-card with `:link: reference/index` and a hidden-toctree entry `reference/index` placed above `api/index`.
    - The existing `api/index`, `contributing`, and `reports/z_uncertainty_report` toctree entries remain present and in order after `reference/index`.
  </acceptance_criteria>
  <done>The Reference section is reachable from the landing page, lists both pages in its toctree, and precedes the API Reference in the top-level sidebar order.</done>
</task>

<task type="auto">
  <name>Task 2: Add reciprocal cross-links to the kept API pages and enforce the build-green gate</name>
  <read_first>
    - docs/api/cli.rst (Phase 110 page — the `.. automodule:: aquapose.cli` block; KEEP as-is per D-13, add only a one-line cross-ref)
    - docs/api/engine.rst (Phase 110 page — the `automodule` blocks incl. the engine.config module; KEEP as-is per D-13, add only a one-line cross-ref)
    - docs/reference/cli.rst (Plan 02 — target of the api/cli.rst cross-link)
    - docs/reference/config.md (Plan 03 — target of the api/engine.rst cross-link)
    - pyproject.toml (line 99 — the `build = "sphinx-build -W --keep-going -b html docs docs/_build/html"` gate)
    - .planning/phases/112-config-cli-reference/112-PATTERNS.md (the cross-linking shared-pattern section — RST `:doc:` role usage)
  </read_first>
  <action>Add exactly one cross-reference line to each kept API page (D-13 — do NOT trim or alter the existing `automodule` blocks). In `docs/api/cli.rst`, add a `:doc:` cross-ref pointing to the user-facing CLI Reference, e.g. a line reading ``For the command-line usage reference, see :doc:`../reference/cli`.`` placed after the title (before or after the automodule block, as a standalone paragraph). In `docs/api/engine.rst`, add a `:doc:` cross-ref to the config reference, e.g. ``For the tiered configuration-field reference, see :doc:`../reference/config`.`` Do NOT modify any `automodule` directive, `:members:`/`:exclude-members:` option, or existing content on these two pages. Then run the full build gate `hatch run docs:build` and drive warnings to zero: resolve any orphan-page warning (ensure both Reference pages are reachable via the toctree from Task 1), any broken `:doc:`/`:link:` cross-ref, and any `sphinx-click` import warning (if one surfaces, extend `autodoc_mock_imports` per Plan 01's established pattern — but this should already be clear from Plan 01). The build MUST exit 0 with no warnings.</action>
  <verify>
    <automated>hatch run docs:build; echo "BUILD_EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/api/cli.rst` contains a `:doc:` cross-reference to `../reference/cli`, with its original `automodule` block(s) unchanged.
    - `docs/api/engine.rst` contains a `:doc:` cross-reference to `../reference/config`, with its original `automodule` block(s) unchanged.
    - `hatch run docs:build` exits 0 with zero warnings — no orphan-page warning for the Reference pages, no broken cross-reference, no unmocked-import warning from the `sphinx-click` render.
    - The rendered build contains a CLI Reference page (from the `.. click::` directive) and a Config Reference page under a "Reference" sidebar section.
  </acceptance_criteria>
  <done>Both API pages cross-link to their Reference counterparts (originals otherwise untouched), and the full docs tree builds green under `sphinx-build -W --keep-going`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new docs pages → published docs site | Newly-authored pages join the toctree and (in Phase 114) the published Read the Docs site. |

Documentation-only integration. No runtime surface, no user input, no network endpoints introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-04-T | Tampering | docs build integrity | mitigate | The `sphinx-build -W --keep-going` gate (this plan's Task 2 verification) promotes any warning — broken cross-ref, orphan page, unmocked import from the `sphinx-click` render — to a build failure, so a broken/corrupt docs tree cannot ship. |
| T-112-04-I | Information disclosure | cross-link targets and card text | mitigate | Cross-links and card descriptions reference only in-repo doc pages; no external URLs, credentials, or sensitive paths added. |
| T-112-SC | Tampering | `sphinx-click` extension (inherited) | mitigate | Covered by Plan 01; this plan re-exercises the extension via the full-build gate, providing end-to-end confirmation the supply-chain addition renders cleanly under `-W`. |
</threat_model>

<verification>
- `docs/reference/index.md` exists; `docs/index.md` has the Reference card + `reference/index` toctree entry above `api/index`.
- `docs/api/cli.rst` and `docs/api/engine.rst` carry reciprocal `:doc:` cross-links with their automodule blocks intact.
- `hatch run docs:build` exits 0 with zero warnings — CLI Reference and Config Reference render under a Reference sidebar section.
</verification>

<success_criteria>
The Reference section is fully wired into the docs IA, the two Reference surfaces and the two Phase 110 API pages cross-link bidirectionally, and the complete docs tree builds green under `-W` — closing DOCS-05 and DOCS-06.
</success_criteria>

<output>
Create `.planning/phases/112-config-cli-reference/112-04-SUMMARY.md` when done.
</output>
