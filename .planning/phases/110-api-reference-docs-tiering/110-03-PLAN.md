---
phase: 110-api-reference-docs-tiering
plan: 03
type: execute
wave: 2
depends_on: ["110-01", "110-02"]
files_modified:
  - docs/api/index.rst
  - docs/api/core.rst
  - docs/conf.py
autonomous: false
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "docs/api/index.rst has two sidebar-visible sections 'Core Pipeline' and 'Research Utilities', each with its own toctree (D-04)"
    - "The Core Pipeline toctree lists tier-one pages in the D-06 5-stage narrative order (types → detection → tracking → association → pose → reconstruction, framed by calibration/runtime/engine/io/cli)"
    - "Every public module in src/aquapose/ (the ~120-module inventory) is reachable from docs/api/index.rst via a toctree path, verified by an automated cross-check"
    - "sphinx-build -W --keep-going exits 0 over the expanded tree (no orphan pages, no missing-toctree warnings, no import errors)"
    - "The old flat docs/api/core.rst stub is not left orphaned (removed or emptied out of the build)"
  artifacts:
    - path: "docs/api/index.rst"
      provides: "two-section tiered toctree index"
      contains: "Core Pipeline"
    - path: "docs/conf.py"
      provides: "autodoc_mock_imports (extended only if a newly-surfaced module needs it)"
      contains: "autodoc_mock_imports"
  key_links:
    - from: "docs/api/index.rst"
      to: "docs/api/core/*.rst and tier-two pages"
      via: "two toctree blocks"
      pattern: "core/detection"
    - from: "hatch run docs:build"
      to: "sphinx-build -W --keep-going"
      via: "docs env script"
      pattern: "sphinx-build -W"
---

<objective>
Wire the tier-one (plan 01) and tier-two (plan 02) pages into a curated two-section
API index (D-04, D-06, D-08), retire the old flat stubs so nothing is orphaned, run a
coverage cross-check proving every public module is reachable (DOCS-02), and drive
`hatch run docs:build` (sphinx-build -W --keep-going) to a clean exit (SC#3).

Purpose: This is the integration + acceptance plan. It closes DOCS-01 (visible
two-tier split) and DOCS-02 (full coverage) and enforces the hard build gate.
Output: rewritten docs/api/index.rst, retired core.rst stub, any needed conf.py
mock-import additions, a green -W build, and a coverage report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-api-reference-docs-tiering/110-CONTEXT.md
@.planning/phases/110-api-reference-docs-tiering/110-PATTERNS.md
@.planning/phases/110-api-reference-docs-tiering/110-01-SUMMARY.md
@.planning/phases/110-api-reference-docs-tiering/110-02-SUMMARY.md
@docs/api/index.rst
@docs/conf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the two-section index and retire the old core.rst stub</name>
  <read_first>
    - docs/api/index.rst (current single flat toctree — replace entirely)
    - .planning/phases/110-api-reference-docs-tiering/110-PATTERNS.md (the two-toctree index blueprint and the note that underlined section headings — not `rubric` — create sidebar entries, required by D-04)
    - .planning/GUIDEBOOK.md §6 (the 5-stage narrative order for D-06: Detection → 2D Tracking → Cross-Camera Association → Midline → Reconstruction)
    - .planning/phases/110-01-SUMMARY.md and 110-02-SUMMARY.md (the exact page filenames each authoring plan produced)
    - docs/api/core.rst (the old flat stub that must not remain orphaned)
  </read_first>
  <action>
    Rewrite docs/api/index.rst as two sections, each an underlined heading (so it
    appears in the sidebar per D-04) followed by its own `.. toctree:: :maxdepth: 2`:

    - Section "Core Pipeline" toctree in D-06 order:
      calibration, core/types, core/detection, core/tracking, core/association,
      core/pose, core/reconstruction, core/runtime, engine, io, cli.
    - Section "Research Utilities" toctree:
      evaluation, training, synthetic, core/reid.

    Use underlined section headings (not `.. rubric::`) so each tier is a sidebar
    entry (D-04 requires the split visible before clicking in). No :glob:, no
    :recursive:, no :hidden: (matches existing index.rst conventions).

    Retire the old flat docs/api/core.rst: it is superseded by core/types, core/runtime,
    and the per-stage pages. Remove it from the build so sphinx-build -W does not flag it
    as an orphan document. Preferred: delete docs/api/core.rst (git rm). If deletion is
    undesirable, instead ensure it is not referenced by any toctree AND carries an
    `:orphan:` field — but deletion is the clean choice since its content is now fully
    covered by the per-package pages.

    Confirm no other page (evaluation.rst, training.rst) is double-listed: the tier-two
    section indexes appear in index.rst's Research Utilities toctree, and their own
    sub-pages appear only in their local toctrees (not in index.rst).
  </action>
  <verify>
    <automated>python -c "import pathlib; idx=pathlib.Path('docs/api/index.rst').read_text(encoding='utf-8'); assert 'Core Pipeline' in idx and 'Research Utilities' in idx, 'sections missing'; order=['calibration','core/types','core/detection','core/tracking','core/association','core/pose','core/reconstruction','core/runtime','engine','io','cli']; pos=[idx.find(x) for x in order]; assert all(p!=-1 for p in pos), 'missing tier-one entry'; assert pos==sorted(pos), 'D-06 order wrong'; assert idx.count('.. toctree::')==2, 'expected exactly two toctrees'; assert not pathlib.Path('docs/api/core.rst').exists() or ':orphan:' in pathlib.Path('docs/api/core.rst').read_text(), 'old core.rst neither removed nor orphaned'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/api/index.rst contains exactly two ".. toctree::" blocks, one under an underlined "Core Pipeline" heading and one under an underlined "Research Utilities" heading
    - The Core Pipeline toctree entries appear in this exact relative order: calibration, core/types, core/detection, core/tracking, core/association, core/pose, core/reconstruction, core/runtime, engine, io, cli
    - The Research Utilities toctree lists evaluation, training, synthetic, core/reid
    - docs/api/core.rst is either deleted or carries an ":orphan:" field and is absent from every toctree
    - No page is listed in more than one toctree across the docs/api tree
  </acceptance_criteria>
  <done>index.rst presents the two sidebar-visible tiers in D-06 order; the old flat core.rst is retired so nothing is orphaned.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage cross-check + green -W build</name>
  <read_first>
    - docs/conf.py (autodoc_mock_imports list, lines ~102-125; extend alphabetically only if -W reports an import error for a newly-surfaced module)
    - pyproject.toml [tool.hatch.envs.docs.scripts] (build = sphinx-build -W --keep-going -b html docs docs/_build/html)
    - .planning/phases/110-api-reference-docs-tiering/110-CONTEXT.md <code_context> (the 120-module inventory that defines the DOCS-02 acceptance bar)
    - all docs/api/**/*.rst produced by plans 01 and 02 (the automodule targets to cross-check against src/)
  </read_first>
  <action>
    Coverage cross-check (DOCS-02 gate): enumerate every non-private public module under
    src/aquapose/ (all .py except __init__.py and underscore-prefixed files such as
    evaluation/viz/_frames.py and _loader.py), derive each dotted module path, and assert
    every one appears as an `.. automodule::` target somewhere under docs/api/**/*.rst.
    Emit the list of any modules present in src/ but absent from the rst tree; that list
    MUST be empty. Underscore-private modules are the only permitted absences. Record the
    covered/total count in the summary.

    Build gate (SC#3): run `hatch run docs:build` (sphinx-build -W --keep-going). It MUST
    exit 0. If it fails on an import error for a newly-surfaced module, add the offending
    top-level package name to autodoc_mock_imports in docs/conf.py (preserve alphabetical
    order) and re-run. If it fails on a duplicate-object or ambiguous-reference warning
    from a symbol re-exported by both a package __init__ and its submodule, resolve by the
    minimal fix (e.g. drop the redundant top-level automodule members or add a narrow
    :no-index: on the submodule's re-exported symbol) and document the fix in the summary.
    Do NOT silence warnings by relaxing -W or removing pages from the toctree.

    Capture the final sphinx-build exit code and warning count for the summary.
  </action>
  <verify>
    <automated>python -c "import pathlib,ast; root=pathlib.Path('src/aquapose'); mods=[]; [mods.append('aquapose'+str(p.relative_to(root)).replace('\\\\','/').replace('/', '.').removesuffix('.py')) for p in root.rglob('*.py') if p.name!='__init__.py' and not p.name.startswith('_')]; rst=' '.join(f.read_text(encoding='utf-8') for f in pathlib.Path('docs/api').rglob('*.rst')); missing=[m for m in mods if f'automodule:: {m}' not in rst]; print('MISSING:',missing); exit(1) if missing else print(f'COVERAGE OK: {len(mods)} modules all present')"</automated>
    <automated>hatch run docs:build</automated>
  </verify>
  <acceptance_criteria>
    - The coverage cross-check reports zero non-private src/aquapose modules missing from the docs/api automodule tree (only __init__.py and underscore-prefixed modules may be absent)
    - core/association/*, core/tracking/*, core/types/*, all backends packages, cli.py, io/*, and evaluation/viz/* are all present in the rendered tree (explicit DOCS-02 named modules)
    - `hatch run docs:build` exits 0 (sphinx-build -W --keep-going clean; no orphan, no missing-toctree, no import, no duplicate-object warnings)
    - Any conf.py change is limited to appending mock-import package names in alphabetical order; -W is not relaxed and no page is dropped from a toctree to reach green
  </acceptance_criteria>
  <done>Coverage cross-check passes with an empty missing-list and the -W build exits 0 over the full expanded module tree.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Rebuilt API reference: two sidebar sections ("Core Pipeline" / "Research Utilities"),
    tier-one pages in the 5-stage pipeline order, tier-two pages carrying the honest
    "Research utility — not part of the supported pipeline API." note, and full coverage
    of every public module. `sphinx-build -W --keep-going` exits clean.
  </what-built>
  <how-to-verify>
    1. Open docs/_build/html/api/index.html (or run `hatch run docs:build` then open it).
    2. Confirm the sidebar shows two distinct sections "Core Pipeline" and "Research
       Utilities" before clicking into any page (D-04).
    3. Confirm the Core Pipeline entries read in pipeline order (Detection → 2D Tracking →
       Cross-Camera Association → Pose/Midline → Reconstruction, framed by calibration,
       runtime, engine, io, cli) (D-06).
    4. Open a tier-two page (e.g. Evaluation, Training) and confirm the note reads exactly
       "Research utility — not part of the supported pipeline API." (D-05).
    5. Spot-check that a previously-invisible submodule now renders — e.g. Cross-Camera
       Association shows clustering/recovery/scoring/validation, and Evaluation
       Visualisation shows animation/overlay (DOCS-02).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks off (missing section, wrong order, wrong/missing note, missing module).</resume-signal>
</task>

</tasks>

<verification>
- Two-section index in D-06 order (task 1 verify).
- Coverage cross-check: zero non-private modules missing (task 2 verify command 1).
- hatch run docs:build exits 0 (task 2 verify command 2).
- Human confirms the rendered tiering, ordering, note wording, and a formerly-invisible submodule now appears.
</verification>

<success_criteria>
The rendered API reference visibly separates tier-one from tier-two with honest labels
(DOCS-01), every public module is reachable and rendered (DOCS-02), and
sphinx-build -W --keep-going exits clean over the expanded tree (SC#3).
</success_criteria>

<output>
Create `.planning/phases/110-api-reference-docs-tiering/110-03-SUMMARY.md` when done.
Record: the final index structure, the coverage count (covered/total, list any
permitted private omissions), any conf.py mock-import additions, any duplicate-object
fixes applied, and the final sphinx-build exit code + warning count.
</output>
