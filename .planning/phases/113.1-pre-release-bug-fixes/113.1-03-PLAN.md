---
phase: 113.1-pre-release-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/GUIDEBOOK.md
  - tests/unit/engine/test_pipeline.py
  - .planning/todos/pending/2026-02-28-regenerate-golden-regression-test-data-for-v2-1.md
  - .planning/todos/pending/2026-02-28-move-lut-generation-to-pre-pipeline.md
  - .planning/todos/done/2026-02-28-regenerate-golden-regression-test-data-for-v2-1.md
  - .planning/todos/done/2026-02-28-move-lut-generation-to-pre-pipeline.md
autonomous: true
requirements: [QA-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/GUIDEBOOK.md` section 6 states the real production stage order — Detection, Pose, 2D Tracking, Cross-Camera Association, Reconstruction — and every claim in it was checked against `build_stages` in `src/aquapose/engine/pipeline.py`, not against another prose document (D-14)."
    - "Section 6 documents the 4-stage synthetic-mode variant (SyntheticDataStage, TrackingStage, AssociationStage, ReconstructionStage), which it does not mention at all today (D-14)."
    - "The Pose stage is described as enriching `Detection` objects in place with no dedicated `PipelineContext` output field, matching `_STAGE_OUTPUT_FIELDS[\"PoseStage\"]` being an empty tuple — not as a stage that writes a context field."
    - "The PipelineContext Data Flow table in section 6 has a Pose row and its rows are in the corrected order."
    - "A test asserts that the stage-name sequence returned by `build_stages` appears verbatim in GUIDEBOOK.md section 6, for both `mode=\"production\"` and `mode=\"synthetic\"`, so the doc cannot silently rot again (D-15)."
    - "The guard test extends the existing `build_stages` test infrastructure in `tests/unit/engine/test_pipeline.py` — the monkeypatched stage `__init__` plus `load_config(cli_overrides=...)` pattern — rather than duplicating a second config-construction fixture."
    - "The two stale todos are in `.planning/todos/done/` with a note that names which of the two reasons applies and cites the evidence, and that evidence was re-verified against the working tree during this plan rather than copied from CONTEXT.md (D-03)."
    - "The Zenodo/DOI todo is still in `.planning/todos/pending/` and untouched, and `grep -q 'doi\\.org' docs/getting-started/tutorial.md` still FAILS (D-05)."
    - "No pending todo outside the two named in D-03 was moved, edited, or closed (D-02)."
  artifacts:
    - path: ".planning/GUIDEBOOK.md"
      provides: "A section 6 whose stage order, per-stage subsections, synthetic-mode variant and data-flow table match `build_stages`"
      contains: "SyntheticDataStage"
    - path: "tests/unit/engine/test_pipeline.py"
      provides: "`test_build_stages_sequence_matches_guidebook`, a parametrized guard over production and synthetic modes"
      contains: "GUIDEBOOK"
    - path: ".planning/todos/done/2026-02-28-regenerate-golden-regression-test-data-for-v2-1.md"
      provides: "The obsolete golden-regression todo, filed with a re-verified evidence note"
      contains: "Filed"
    - path: ".planning/todos/done/2026-02-28-move-lut-generation-to-pre-pipeline.md"
      provides: "The already-implemented LUT-generation todo, filed with a re-verified evidence note"
      contains: "Filed"
  key_links:
    - from: "tests/unit/engine/test_pipeline.py"
      to: ".planning/GUIDEBOOK.md"
      via: "the test builds the arrow-joined display-name sequence from `build_stages` output and asserts it is a substring of the guidebook, so source is the authority and the doc is the thing checked"
      pattern: "GUIDEBOOK"
    - from: "tests/unit/engine/test_pipeline.py"
      to: "src/aquapose/engine/pipeline.py"
      via: "reuses the existing monkeypatched-stage-__init__ plus load_config(cli_overrides=...) construction from `test_build_stages_stop_after`"
      pattern: "build_stages"
  prohibitions:
    - requirement_id: QA-05
      category: transparency
      statement: "MUST NOT correct the guidebook against another prose document. `docs/getting-started/concepts.md` happens to be right, and copying from it would produce a correct page by luck while leaving the verify-against-source discipline unpracticed — which is the exact habit whose absence created this todo."
    - requirement_id: QA-05
      category: transparency
      statement: "MUST NOT file a todo to done/ without recording, in the file itself, the evidence that justifies closing it. A todo closed on an unstated rationale is indistinguishable from a todo abandoned, and the next reader has no way to tell which happened."
    - requirement_id: QA-05
      category: transparency
      statement: "MUST NOT let the guard test pass by asserting the doc against a hardcoded string alone. If both sides of the comparison are literals in the test, the test pins the test — the sequence must be derived from a real `build_stages` call so a future pipeline reorder turns it red."
---

<objective>
Stop bad context from reaching future planning agents.

`CLAUDE.md` instructs `discuss-phase` to read `.planning/GUIDEBOOK.md` as
authoritative project context, and section 6 documents a pre-v3.7 stage order
with a Midline stage running after association. Phase 113's plan acceptance
criteria were written against that stale order and had to be deviated from at
execution time — this is not an inert stale doc, it is an input to planning.

Correct section 6 against `build_stages` (D-14), add the cheap guard that keeps
it correct (D-15), and file the two stale todos with their evidence (D-03).

Purpose: QA-05 success criteria 4 and 6.
Output: a guidebook section that matches the code, a test that goes red if
either drifts, and a todo backlog whose closed items say why they were closed.
</objective>

## Artifacts this phase produces

Created by **this plan**:

- `.planning/GUIDEBOOK.md` — a corrected section 6 with a `### Stage 2 — Pose`
  subsection, renumbered Tracking/Association/Reconstruction subsections, a new
  `### Synthetic Mode` subsection documenting the 4-stage variant, and a Pose row
  in the PipelineContext Data Flow table.
- `tests/unit/engine/test_pipeline.py` — new test
  `test_build_stages_sequence_matches_guidebook` and its stage-class-name to
  guidebook-display-name mapping.
- `.planning/todos/done/2026-02-28-regenerate-golden-regression-test-data-for-v2-1.md`
  and `.planning/todos/done/2026-02-28-move-lut-generation-to-pre-pipeline.md` —
  moved from `pending/`, each with an appended `## Filed` note.
- New todos in `.planning/todos/pending/`: one recording that the
  `calibrate-keypoints` config update is a non-atomic read-modify-write, and one
  recording that `pillow` is imported directly by three `training/` modules but
  is not a declared dependency in `pyproject.toml`. Conditionally, one recording
  wider GUIDEBOOK drift if Task 1's bounded scan finds more than two drifted
  sections.

Produced by the rest of the phase (for cross-reference): the
`calibrate-keypoints` writer and parser changes plus their test fixtures
(113.1-01); the `n_animals` scaffold and guard changes (113.1-02);
`src/aquapose/core/types/video_discovery.py` and
`tests/unit/core/test_import_boundary.py` (113.1-04); the recorded corrected YH
t-values (113.1-05).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113.1-pre-release-bug-fixes/113.1-CONTEXT.md
@.planning/phases/113.1-pre-release-bug-fixes/113.1-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the guidebook's stage order is the code's stage order" — one path only (D-14, D-15)</name>
  <read_first>
    - `src/aquapose/engine/pipeline.py` lines 265-300 — the `build_stages` docstring, which states the v3.7 ordering and the synthetic-mode branch. This is the authority. Read it, then read the code below it too; per the phase standing lesson, a docstring is evidence but not proof.
    - `src/aquapose/engine/pipeline.py` lines 390-440 — the actual construction: the `config.mode == "synthetic"` branch building a 4-item list, and the production branch building the 5-item list. Confirm the class order in each list with your own eyes.
    - `src/aquapose/engine/pipeline.py` line 322 — the `STAGE_NAMES` map, the canonical short names for each stage class.
    - `tests/unit/engine/test_pipeline.py` lines 231-313 in full — `test_build_stages_stop_after` and `test_build_stages_stop_after_invalid`. These already solve the hard part: they monkeypatch `__init__` on `DetectionStage`, `PoseStage`, `ReconstructionStage`, `SyntheticDataStage`, `AssociationStage`, `TrackingStage` and `VideoFrameSource` to avoid file I/O, then build a config with `load_config(cli_overrides={...})`. Reuse that construction; do not write a second fixture.
    - `src/aquapose/engine/pipeline.py` lines 365-390 — `_check_luts_if_needed` (loads LUTs when an `AssociationStage` is present and `config.calibration_path` is truthy) and `_check_model_weights` (raises when a configured weights path does not exist). The existing tests pass `calibration_path` as `"."`; confirm what that does before choosing what your config passes, and choose values that keep the test hermetic and fast.
    - `.planning/GUIDEBOOK.md` lines 105-115 — the section 6 heading and the stale flow line at 108, the one line this task replaces.
    - `.planning/todos/pending/2026-09-02-guidebook-pipeline-stage-order-is-stale.md` — the todo's terminal gate.
  </read_first>
  <files>.planning/GUIDEBOOK.md, tests/unit/engine/test_pipeline.py</files>
  <action>
Wire ONE path end to end: the code's real production stage order, through a
derived display-name sequence, into the guidebook, asserted by a runnable test.
Nothing else in section 6 changes in this task.

Replace the stale flow line under the section 6 heading with a fenced block
containing exactly this line, arrows included, and nothing else:

  Detection -> Pose -> 2D Tracking -> Cross-Camera Association -> Reconstruction

using the same U+2192 arrow character the file already uses elsewhere rather than
the ASCII form written above. Verify each name against `build_stages` and
`STAGE_NAMES` before you write it. Do NOT read `docs/getting-started/concepts.md`
for this — it is another prose document, it happens to be correct, and copying
from it would produce the right words for the wrong reason.

Then add `test_build_stages_sequence_matches_guidebook` to
`tests/unit/engine/test_pipeline.py`, for `mode="production"` only in this task.
Build the config the same way `test_build_stages_stop_after` does — the same
monkeypatched stage `__init__` list and the same `load_config(cli_overrides=...)`
call. Do not introduce a second config fixture and do not copy the config dict
into a module-level constant; extend what is there.

Define a module-level mapping from stage class name to the display name used in
the guidebook: `DetectionStage` to `Detection`, `PoseStage` to `Pose`,
`TrackingStage` to `2D Tracking`, `AssociationStage` to
`Cross-Camera Association`, `ReconstructionStage` to `Reconstruction`, and
`SyntheticDataStage` to `Synthetic Data` (the last is unused until Task 2 but
belongs in the mapping now). Look up each returned stage's class name in the
mapping and fail with an explicit assertion naming the unmapped class if it is
missing, rather than letting a `KeyError` surface.

Join the display names with the same separator the guidebook uses — a U+2192
arrow with a single space on each side — and assert the resulting string is a
substring of `.planning/GUIDEBOOK.md`, read via
`Path.read_text(encoding="utf-8")`. The explicit encoding matters: this project
has already been bitten twice by platform-default decoding, and the guidebook
contains non-ASCII arrows. Resolve the guidebook path from the test file's own
location by walking up to the repository root, not from the current working
directory, so the test passes regardless of where pytest is invoked from.

The assertion direction is load-bearing: the expected sequence is DERIVED from a
real `build_stages` call and the guidebook is the thing checked. Do not hardcode
the expected sequence as a literal and compare it to another literal — that pins
the test, not the pipeline.
  </action>
  <verify>
    <automated>hatch run test tests/unit/engine/test_pipeline.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run test tests/unit/engine/test_pipeline.py` exits 0 with the new test collected and passing, and the two pre-existing `build_stages` tests still passing unmodified.
    - The corrected production flow line exists verbatim: `grep -Fq 'Detection → Pose → 2D Tracking → Cross-Camera Association → Reconstruction' .planning/GUIDEBOOK.md` succeeds.
    - The stale order is gone: `grep -c '→ Midline →' .planning/GUIDEBOOK.md` returns 0.
    - The expected sequence is derived, not hardcoded: `grep -n 'build_stages' tests/unit/engine/test_pipeline.py` shows the new test calling it, and the test contains no literal equal to the full arrow-joined production sequence.
    - The guidebook is read with an explicit encoding: `grep -n 'encoding="utf-8"' tests/unit/engine/test_pipeline.py` returns a line inside the new test.
    - Temporarily editing the guidebook's production flow line (for example swapping two stage names) makes the test FAIL. Restore the file and record the observed failure message in the SUMMARY. A guard that stays green on a mutated guidebook is vacuous and is a task failure.
    - Temporarily removing one entry from the display-name mapping makes the test fail with the explicit unmapped-class assertion, not with a `KeyError`. Record the observed message in the SUMMARY.
    - The new test reuses the existing monkeypatch-and-`load_config` construction: `git diff tests/unit/engine/test_pipeline.py` shows no second config-construction fixture added.
    - The new test is not marked `slow`, `e2e`, `skip` or `xfail`, and completes in under a second.
    - `git diff --stat docs/` is empty — this task touches the internal planning guidebook only, not the published docs.
    - `hatch run lint` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A planning-document line plus a test; no runtime behavior changes and a revert is a single-commit git revert.</reversibility>
  <done>The guidebook's production stage-flow line matches `build_stages`, and a test derives that sequence from a real `build_stages` call and asserts it appears in the guidebook — proven non-vacuous by mutating the doc to red.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the correction across section 6 — per-stage subsections, the synthetic variant, and the data-flow table (D-14, D-15)</name>
  <files>.planning/GUIDEBOOK.md, tests/unit/engine/test_pipeline.py</files>
  <read_first>
    - `src/aquapose/engine/pipeline.py` lines 44-53 — `_STAGE_OUTPUT_FIELDS`, which is what proves the Pose stage writes no dedicated `PipelineContext` field (its tuple is empty) and which context field each other stage writes.
    - `src/aquapose/engine/pipeline.py` lines 390-410 — the synthetic-mode branch, its 4-item stage list, and the `SyntheticDataStage` constructor arguments.
    - The docstring and stage class of each module you describe: `src/aquapose/core/detection/`, `src/aquapose/core/pose/`, `src/aquapose/core/tracking/`, `src/aquapose/core/association/stage.py`, `src/aquapose/core/reconstruction/`. Ground each subsection's In/Out claims in the stage's own code, not in the guidebook's existing prose.
    - `.planning/GUIDEBOOK.md` lines 105-200 — the rest of section 6: the five per-stage subsections (111-176), the PipelineContext Data Flow table (177-185), the Identity Model block and the CarryForward block.
    - `.planning/phases/113-concepts-tutorial/deferred-items.md` (the Plan 04 section) — the detailed drift record Phase 113 filed, including that the segmentation midline backend was removed entirely in v3.7.
    - `tests/unit/engine/test_pipeline.py` as it stands after Task 1 — the test you are parametrizing.
  </read_first>
  <action>
Expand out from Task 1's proven slice. Every claim you write must be one you
verified against `src/aquapose/engine/pipeline.py` and the stage modules
themselves; do not consult `docs/getting-started/concepts.md`.

Restructure the per-stage subsections to match the corrected order: keep
`### Stage 1 — Detection` as it is apart from any claim you find to be wrong;
insert a new `### Stage 2 — Pose` subsection; renumber the tracking subsection to
Stage 3, the association subsection to Stage 4, and the reconstruction subsection
to Stage 5. Fold the content of the current Midline subsection into the new Pose
subsection, dropping the parts that describe the removed segmentation backend and
the swappable-backend framing that no longer exists, and keeping the parts that
are still true of the pose path (the six anatomical keypoints, the fixed
per-keypoint `t` values in [0,1], the spline fitted on observed keypoints only,
evaluation restricted to the observed t-range with NaN and zero confidence
outside it, and per-point confidence flowing through to reconstruction). Verify
each of those against the pose stage's own code before keeping it.

The Pose subsection must state the two structural facts that make this reorder
matter: it runs BEFORE tracking and association, and it enriches `Detection`
objects in place rather than writing a dedicated `PipelineContext` field — the
latter is what `_STAGE_OUTPUT_FIELDS` proves by giving `PoseStage` an empty
tuple. Correct the Stage 4 association subsection's In/Out line if it still names
inputs the reordered pipeline does not supply at that point.

Add a `### Synthetic Mode` subsection immediately after Stage 5, documenting the
4-stage variant returned when `config.mode == "synthetic"`, with its own fenced
flow line containing exactly:

  Synthetic Data -> 2D Tracking -> Cross-Camera Association -> Reconstruction

again with the U+2192 arrow. State plainly that `SyntheticDataStage` replaces
`DetectionStage` and that there is no Pose stage in this variant, and name the
context fields it populates per `_STAGE_OUTPUT_FIELDS`.

Update the PipelineContext Data Flow table: add a Pose row in its correct
position, renumber the rows, and set each row's Reads/Writes from
`_STAGE_OUTPUT_FIELDS` and the stage constructors rather than from the table's
current contents. The Pose row's Writes cell must reflect the empty output tuple.

Then parametrize `test_build_stages_sequence_matches_guidebook` over both modes,
adding the `mode="synthetic"` case. Vary only `mode` in the config; everything
else stays as Task 1 built it.

Finally, run a bounded drift scan over the REST of the guidebook — sections 3, 4,
7, 8 and 14 only — checking each against the source tree for the same class of
staleness. Do not fix what you find. If you find at most two drifted sections,
record them in the SUMMARY. If you find more than two, file a single new todo in
`.planning/todos/pending/` naming each drifted section and the source that
contradicts it, and record the todo path in the SUMMARY. This is a bug phase, not
a docs phase.
  </action>
  <verify>
    <automated>hatch run test tests/unit/engine/test_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run test tests/unit/engine/test_pipeline.py` exits 0 with BOTH parametrizations of the guard collected and passing.
    - The synthetic flow line exists verbatim: `grep -Fq 'Synthetic Data → 2D Tracking → Cross-Camera Association → Reconstruction' .planning/GUIDEBOOK.md` succeeds.
    - Section 6 has a Pose stage subsection: `grep -q '^### Stage 2 — Pose' .planning/GUIDEBOOK.md` succeeds, and `grep -q '^### Stage 5 — Reconstruction' .planning/GUIDEBOOK.md` still succeeds.
    - A synthetic-mode subsection exists: `grep -q '^### Synthetic Mode' .planning/GUIDEBOOK.md` succeeds.
    - The Pose subsection states the in-place enrichment and the absence of a dedicated context field. Quote that sentence verbatim in the SUMMARY next to the `_STAGE_OUTPUT_FIELDS` line that proves it.
    - The data-flow table has a Pose row and its stage numbering runs 1 through 5 in order. Paste the whole rebuilt table into the SUMMARY.
    - The SUMMARY lists, per corrected claim, the source file and line range it was checked against. A claim with no source citation is a defect.
    - The SUMMARY states the result of the bounded scan of sections 3, 4, 7, 8 and 14: either the at-most-two drifted sections, or the path of the todo filed for more than two.
    - `git diff --stat docs/` is empty.
    - `hatch run lint` exits 0.
  </acceptance_criteria>
  <done>Section 6 states the real production order and the synthetic variant end to end — flow lines, per-stage subsections, and data-flow table — every claim traces to a source file and line range recorded in the summary, and the guard covers both modes.</done>
</task>

<task type="auto">
  <name>Task 3: File the two stale todos to done/ with re-verified evidence, and record the two findings this phase surfaced (D-03, D-02, D-05)</name>
  <read_first>
    - `.planning/todos/pending/2026-02-28-regenerate-golden-regression-test-data-for-v2-1.md` in full.
    - `.planning/todos/pending/2026-02-28-move-lut-generation-to-pre-pipeline.md` in full.
    - `src/aquapose/core/association/stage.py` lines 40-90 — the LUT loading and the `FileNotFoundError` raised when they are absent. This is the evidence for the second todo; read it rather than trusting CONTEXT.md's summary of it.
    - `.planning/todos/done/` — list it and open one existing entry to match the house note style.
    - `.planning/phases/113.1-pre-release-bug-fixes/113.1-CONTEXT.md` D-02, D-03 and D-05 — the exact scope fence on which todos may move and which must not.
    - `src/aquapose/training/prep.py` config writer (as changed by `113.1-01`, or as it stands if that plan has not landed yet) — the non-atomic read-modify-write being recorded.
    - `pyproject.toml` `dependencies` list, and `src/aquapose/training/coco_interchange.py` line 10 plus `src/aquapose/training/labelstudio_export.py` line 10 — the `pillow` finding: PIL is imported directly by `training/` modules but does not appear in `dependencies`; it currently resolves transitively through `ultralytics` and `torchvision`.
  </read_first>
  <files>.planning/todos/pending/, .planning/todos/done/</files>
  <action>
Re-verify each closing rationale against the working tree BEFORE moving anything.
For the golden-regression todo, confirm that `tests/golden/` and
`scripts/generate_golden_data.py` are genuinely absent — run the check, do not
copy CONTEXT.md's assertion. For the LUT todo, confirm by reading
`core/association/stage.py` that pre-generated LUTs are loaded and that a
`FileNotFoundError` is raised when they are missing, and confirm that the
`prep generate-luts` subcommand exists in the CLI. If either rationale does not
hold up, STOP and report it rather than filing the todo — a wrong closure is
worse than an open todo.

Move both files from `.planning/todos/pending/` to `.planning/todos/done/` using
`git mv` so history follows the file. Append a `## Filed` section to each,
recording: the date, which of the two reasons applies (obsolete, or already
implemented), the specific evidence with file paths and line numbers as verified
just now, and the phase and plan that filed it. State the reason explicitly in
words — a reader must not have to infer it from the evidence.

Then create two new pending todos recording findings this phase surfaced but is
not fixing:

1. The `calibrate-keypoints` config update is a non-atomic read-modify-write:
   `yaml.safe_load` from the file, then reopen for write. An interrupted or
   concurrent write truncates the user's `config.yaml`. Name the file and the
   function, describe the temp-file-plus-atomic-rename shape a fix would take,
   and note that this is the `concurrency` edge recorded as a backstop in
   `113.1-01-PLAN.md` rather than an acceptance criterion.
2. `pillow` is imported directly at module level by `training/coco_interchange.py`
   and `training/labelstudio_export.py` (and, after `113.1-01`, by
   `training/prep.py`) but is not listed in `pyproject.toml` `dependencies`; it
   resolves only transitively. Name the three importing modules and state that
   the fix is a one-line dependency declaration, deliberately not made here
   because this phase's fences forbid recreating the hatch environment.

Do not touch any other pending todo (D-02). In particular the Zenodo/DOI todo
stays exactly where it is, unedited (D-05), and the basedpyright backlog todo
stays pending for Phase 113.2 (D-01).
  </action>
  <verify>
    <automated>test -f .planning/todos/done/2026-02-28-move-lut-generation-to-pre-pipeline.md && test ! -f .planning/todos/pending/2026-02-28-move-lut-generation-to-pre-pipeline.md</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist under `.planning/todos/done/` and neither remains under `.planning/todos/pending/`.
    - `git log --follow --oneline -- .planning/todos/done/2026-02-28-move-lut-generation-to-pre-pipeline.md` shows the pre-move history, proving `git mv` was used rather than a delete-and-create.
    - Each filed file ends with a `## Filed` section naming the reason in words and citing evidence with file paths: `grep -q 'core/association/stage.py' .planning/todos/done/2026-02-28-move-lut-generation-to-pre-pipeline.md` succeeds, and `grep -q 'scripts/generate_golden_data.py' .planning/todos/done/2026-02-28-regenerate-golden-regression-test-data-for-v2-1.md` succeeds.
    - The evidence was re-verified, not copied: paste into the SUMMARY the actual command output showing `tests/golden/` and `scripts/generate_golden_data.py` absent, and the `core/association/stage.py` line range containing the `FileNotFoundError`.
    - The two new pending todos exist and each names the specific file and function it describes.
    - `test -f .planning/todos/pending/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md` succeeds and `git diff .planning/todos/pending/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md` is empty (D-05).
    - `grep -q 'doi\.org' docs/getting-started/tutorial.md` FAILS (D-05 fence still holds).
    - `test -f .planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md` succeeds (D-01: Phase 113.2's work stays queued).
    - `git status --porcelain .planning/todos/pending/` shows changes only for the two removed files and the newly added ones — no other pending todo was modified (D-02).
  </acceptance_criteria>
  <done>Both stale todos are in done/ with a stated reason and freshly verified evidence, two new findings are recorded as pending todos, and no other todo — least of all the Zenodo one — was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/GUIDEBOOK.md` → planning agents | `CLAUDE.md` instructs `discuss-phase` to read this file as authoritative project context, so its contents cross into every future phase discussion. |
| `.planning/todos/` → project backlog | Moving a file between `pending/` and `done/` is the project's only record that an item was resolved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113.1-10 | Tampering | `.planning/GUIDEBOOK.md` as planning input | high | mitigate | This is the defect: a stale section 6 has already corrupted one phase's acceptance criteria. Task 1 rewrites it against `build_stages` with per-claim source citations recorded in the summary, and Task 2 adds a test that derives the sequence from the code so future drift goes red instead of silently propagating into planning. |
| T-113.1-11 | Repudiation | todo closure without evidence | medium | mitigate | Each filed todo carries a `## Filed` note stating the reason in words and citing verified file paths and line numbers, and the evidence is re-checked against the working tree in this plan rather than copied from CONTEXT.md. |
| T-113.1-12 | Tampering | scope creep into other pending todos | medium | mitigate | Acceptance criteria assert that `git status` on `.planning/todos/pending/` shows changes only for the two files D-03 names plus the newly added ones, and that the Zenodo todo's diff is empty. |
| T-113.1-13 | Spoofing | a guard test that pins itself | medium | mitigate | The expected sequence must be derived from a real `build_stages` call; the acceptance criteria require mutating the guidebook to red and recording the observed failure, which a literal-versus-literal comparison could not produce. |
| T-113.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. The undeclared-`pillow` finding is recorded as a todo, explicitly not fixed, because declaring it would invite an environment rebuild that this phase's fences forbid. |

ASVS level 1; blocking severity: high. The one `high` row, T-113.1-10, is mitigated within this plan by Tasks 1 and 2 together.
</threat_model>

## Edge Coverage (spec-less probe fallback)

This phase has no SPEC, so the deterministic edge probe ran instead. It surfaced
5 rows, all for QA-05, and **all 5 are owned by `113.1-01`** — QA-05's
edge-bearing surface is the `calibrate-keypoints` command. This plan owns 0 rows
and authors none, which is the correct outcome rather than a gap: the
phase-wide no-silent-drop check lives in `113.1-01-PLAN.md`.

<verification>
1. Both corrected flow lines appear verbatim in `.planning/GUIDEBOOK.md`; `grep -c '→ Midline →'` returns 0.
2. Section 6 carries a Stage 2 Pose subsection, a Synthetic Mode subsection, and a data-flow table with a Pose row numbered 1 through 5 in order.
3. Every corrected claim is traced in the SUMMARY to a source file and line range.
4. `hatch run test tests/unit/engine/test_pipeline.py` exits 0 with both parametrizations of the guard passing.
5. The guard is proven non-vacuous: a mutated guidebook turns it red, and an unmapped stage class produces a readable assertion rather than a KeyError.
6. Both stale todos are in `done/` with a stated reason and freshly verified evidence; `git log --follow` shows the move preserved history.
7. The Zenodo todo is untouched and `grep -q 'doi\.org' docs/getting-started/tutorial.md` still FAILS.
8. `hatch run lint` exits 0; `git diff --stat docs/` is empty.
</verification>

<success_criteria>
- QA-05 criterion 4 is true: GUIDEBOOK section 6 states the real stage order plus the 4-stage synthetic variant, verified against `build_stages` rather than against other prose.
- QA-05 criterion 6 is true: the two stale todos are filed to `done/` with a note recording why.
- The correction is durable: a future pipeline reorder that does not update the guidebook turns the suite red.
</success_criteria>

<output>
Create `.planning/phases/113.1-pre-release-bug-fixes/113.1-03-SUMMARY.md` when done.
Record in it: a per-claim table mapping each corrected guidebook statement to the
source file and line range it was verified against; the verbatim Pose-stage
in-place-enrichment sentence next to the `_STAGE_OUTPUT_FIELDS` line that proves
it; the observed failure message from mutating the guidebook to red and from
removing a mapping entry; the result of the bounded scan of sections 3, 4, 7, 8
and 14; the actual command output re-verifying both todo closure rationales; and
the paths of every todo added, moved, or left deliberately untouched.
</output>
