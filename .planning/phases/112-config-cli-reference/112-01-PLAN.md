---
phase: 112-config-cli-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docs/conf.py
autonomous: true
requirements: [DOCS-05]
must_haves:
  truths:
    - "The docs env can import and register the sphinx-click extension"
    - "sphinx-build imports aquapose.cli:cli cleanly under mocked imports (no unmocked-import warning under -W)"
  artifacts:
    - path: "pyproject.toml"
      provides: "sphinx-click in the detached docs env dependency list"
      contains: "sphinx-click"
    - path: "docs/conf.py"
      provides: "sphinx_click.ext registered in the extensions list"
      contains: "sphinx_click.ext"
  key_links:
    - from: "docs/conf.py"
      to: "aquapose.cli:cli"
      via: "autodoc_mock_imports covers the CLI import graph"
      pattern: "autodoc_mock_imports"
---

<objective>
Wire the `sphinx-click` extension into the detached docs build so the CLI reference page (Plan 02) can auto-render every Click command group. This is the foundation wave: the extension must be installed and registered, and the mock-import list must cover everything importing `aquapose.cli:cli` pulls in, before any `.. click::` directive is rendered.

Purpose: `sphinx-click` is a new docs-env dependency (D-01). Registering it and confirming `autodoc_mock_imports` covers the CLI import chain is the primary build-integrity risk to clear before the CLI page exists — under `-W`, any unmocked transitive import fails the build.
Output: `pyproject.toml` and `docs/conf.py` modified; `hatch run docs:build` still green (no new page yet, but the extension loads and the env resolves).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-config-cli-reference/112-CONTEXT.md
@.planning/phases/112-config-cli-reference/112-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sphinx-click to the detached docs env and register the extension</name>
  <read_first>
    - pyproject.toml (the `[tool.hatch.envs.docs]` block, lines ~82-99 — `detached = true`, the `dependencies` list, and the `build` script)
    - docs/conf.py (the `extensions` list, lines 45-56)
    - .planning/phases/112-config-cli-reference/112-PATTERNS.md (the `pyproject.toml` and `docs/conf.py` pattern sections)
  </read_first>
  <action>In pyproject.toml, add the PyPI package `sphinx-click` (hyphenated) to the `dependencies` list under `[tool.hatch.envs.docs]` (currently ending with `sphinxcontrib-mermaid` and `numpy`). Keep the list ordering consistent with the existing sphinx-* entries. Do NOT change `detached = true` or the `build` script. In docs/conf.py, append the string `"sphinx_click.ext"` (underscore, `.ext` submodule — the import/extension name, distinct from the hyphenated PyPI name) to the `extensions` list after `"sphinxcontrib.mermaid"`. Do not reorder or remove existing extensions. Recreate the docs env so the new dependency installs: run `hatch env prune` on the docs env or `hatch env create docs` as needed before building.</action>
  <verify>
    <automated>hatch run docs:build; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml `[tool.hatch.envs.docs].dependencies` contains the exact string `sphinx-click`.
    - docs/conf.py `extensions` list contains the exact string `"sphinx_click.ext"`.
    - `hatch run docs:build` exits 0 (the extension loads without error; no unresolved-extension warning promoted by `-W`).
    - No existing extension, dependency, `detached`, or `build`-script line was removed or reordered destructively.
  </acceptance_criteria>
  <done>The docs env resolves `sphinx-click`, `docs/conf.py` registers `sphinx_click.ext`, and `hatch run docs:build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Verify autodoc_mock_imports covers the aquapose.cli:cli import graph</name>
  <read_first>
    - docs/conf.py (the `autodoc_mock_imports` list, lines 108-131)
    - src/aquapose/cli.py (top-of-file imports, lines 1-20 — `click`, `yaml`, `aquapose.cli_utils`, `aquapose.core.reid.cli`, `aquapose.engine`, `aquapose.engine.orchestrator`, `aquapose.training.cli`, `aquapose.training.data_cli`, `aquapose.training.prep`, `aquapose.training.pseudo_label_cli`)
    - src/aquapose/core/reid/cli.py (top imports — reid group pulls in torch/timm/pytorch_metric_learning transitively)
    - src/aquapose/training/cli.py, src/aquapose/training/data_cli.py, src/aquapose/training/prep.py, src/aquapose/training/pseudo_label_cli.py (top imports only)
  </read_first>
  <action>`sphinx-click` imports `aquapose.cli:cli`, which transitively imports the five subcommand groups (`reid_group`, `train_group`, `data_group`, `prep_group`, `pseudo_label_group`) and the engine. Trace the third-party modules these import chains pull in (e.g. `click`, `yaml`, `torch`, `timm`, `pytorch_metric_learning`, `ultralytics`, `boxmot`, `cv2`, `h5py`, `shapely`, `sklearn`, `scipy`, `skimage`, `matplotlib`, `plotly`, `PIL`, `igraph`, `leidenalg`, `loguru`, `aquacal`, `pycocotools`). Confirm each already appears in `autodoc_mock_imports` (the list currently spans lines 108-131). For any third-party top-level import surfaced by the CLI graph that is NOT in the list, add its top-level import name (the name you would `import`, e.g. `sklearn` not `scikit-learn`). Do NOT add first-party `aquapose.*` modules (those are importable from src via the sys.path insert). Do NOT remove existing mock entries.</action>
  <verify>
    <automated>hatch run docs:build 2>&1 | grep -v '^#' | grep -ci "WARNING.*import\|no module named" ; echo "EXIT: ${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run docs:build` exits 0 with zero warnings (the `-W` gate stays clean).
    - The build log contains no `WARNING` mentioning `import` and no `No module named` message attributable to the CLI import graph.
    - Every third-party top-level import reachable from `aquapose.cli:cli` is present in `docs/conf.py` `autodoc_mock_imports` (verified by cross-reading the CLI + subcommand-group import statements against the list).
  </acceptance_criteria>
  <done>`autodoc_mock_imports` demonstrably covers the `aquapose.cli:cli` import graph; `hatch run docs:build` is green with no import warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → docs build env | A new third-party package (`sphinx-click`) is installed into the detached docs environment. |

This is a documentation-only phase. No new runtime attack surface, no user input parsing, no network endpoints. The only boundary crossed is the supply-chain one: adding a build-time dependency.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-SC | Tampering | `sphinx-click` PyPI package added to docs env | mitigate | `sphinx-click` is an established, widely-used Sphinx extension (see Package Legitimacy note below); pinned only to a floor via the docs env resolver; runs at docs-build time only, never in the runtime pipeline. Verify on pypi.org/project/sphinx-click before install. |
| T-112-INT | Tampering | docs build integrity under `-W` | mitigate | The `sphinx-build -W --keep-going` gate promotes any warning (including a compromised/broken extension emitting errors) to a build failure, so silent corruption of the rendered docs is caught at build/CI time. |

Package legitimacy: `sphinx-click` is a mature, first-party-adjacent Sphinx extension maintained by the Click ecosystem, published on PyPI with a long release history. Treated as `[TRUSTED]` — no blocking human-verify checkpoint required, but Task 1 should confirm the package page on pypi.org/project/sphinx-click before install if any doubt arises.
</threat_model>

<verification>
- `hatch run docs:build` exits 0 with zero warnings.
- `sphinx-click` present in the docs env dependency list; `sphinx_click.ext` present in `extensions`.
- No import warnings from the CLI graph in the build log.
</verification>

<success_criteria>
The `sphinx-click` extension is installed and registered, `autodoc_mock_imports` covers the `aquapose.cli:cli` import chain, and `hatch run docs:build` remains green — clearing the primary build-integrity risk before the CLI reference page is authored.
</success_criteria>

<output>
Create `.planning/phases/112-config-cli-reference/112-01-SUMMARY.md` when done.
</output>
