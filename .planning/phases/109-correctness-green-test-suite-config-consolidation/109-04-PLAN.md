---
phase: 109-correctness-green-test-suite-config-consolidation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/training/test_reid_training.py
  - src/aquapose/training/reid_training.py
autonomous: true
requirements: [QA-02]
must_haves:
  truths:
    - "The 2 @slow re-ID end-to-end tests pass: the input crop size fed to the Swin backbone matches the backbone's expected input (D-10)"
    - "The fix addresses the real cause (32px vs a 224 fixed-input Swin), classified per D-02, not a skip/xfail or tolerance dodge"
    - "No @pytest.mark.skip or xfail was added to any test in this plan"
  artifacts:
    - path: "tests/unit/training/test_reid_training.py"
      provides: "End-to-end re-ID smoke + checkpoint tests using a backbone-compatible crop size"
      contains: "def test_smoke_end_to_end_training"
  key_links:
    - from: "tests/unit/training/test_reid_training.py end-to-end fixtures"
      to: "reid_training backbone input contract (MegaDescriptor-T-224)"
      via: "crop_size matching the Swin fixed input (or dynamic_img_size backbone)"
      pattern: "crop_size"
---

<objective>
Resolve the re-ID Swin input-size mismatch that fails the 2 `@pytest.mark.slow`
end-to-end training tests (`TestTrainReidEndToEnd::test_smoke_end_to_end_training`
and `::test_checkpoint_has_backbone_state`): both pass `crop_size=32` into an
end-to-end run through the 224-input MegaDescriptor-T Swin backbone, tripping timm's
`_assert(H == img_size)`. Per D-10 (and D-02 discipline), classify the real cause and
fix it. This gates the slow-tests suite green.

Purpose: `hatch run test-all` (slow-tests.yml) must be green; these two tests are the
only slow-suite failures. Exclusive files → runs in parallel (Wave 1) with the other
plans.

Output: The two end-to-end fixtures feed a backbone-compatible input (matching the
sibling `ImageCropDataset` unit tests, which already use `crop_size=224`), with a
D-02/D-10 root-cause note; both slow tests pass under `test-all`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify and fix the re-ID Swin input-size mismatch (D-10)</name>
  <files>tests/unit/training/test_reid_training.py, src/aquapose/training/reid_training.py</files>
  <read_first>
    - tests/unit/training/test_reid_training.py — `test_smoke_end_to_end_training` (line ~472) and `test_checkpoint_has_backbone_state` (line ~504); both build `ReidTrainingConfig(..., crop_size=32, unfreeze_blocks=2, ...)` and call `train_reid_end_to_end`. Note the sibling `TestImageCropDataset` tests (line ~371) correctly use `crop_size=224`, and `_make_crops_dir` writes 32×32 source jpgs (fine — they get resized to crop_size before the backbone)
    - src/aquapose/training/reid_training.py — `ReidTrainingConfig` defaults (`model_name="hf-hub:BVRA/MegaDescriptor-T-224"` line ~84, `crop_size: int = 224` line ~86); `ImageCropDataset` resizes to `(crop_size, crop_size)` (line ~658); `train_reid_end_to_end` builds the backbone via `timm.create_model(config.model_name, num_classes=0, pretrained=True)` (line ~799) with no img_size override → a fixed 224-input Swin that asserts H==224
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md — D-10 (classify: fixture 32px wrong / needs dynamic_img_size / timm-version change, then fix the real cause) and D-02 (root-cause note; flag genuine regressions)
  </read_first>
  <action>
    Classify per D-02/D-10: the production default `crop_size=224` matches the
    MegaDescriptor-T-224 Swin contract, and the sibling dataset unit tests use 224 — the
    two end-to-end fixtures' `crop_size=32` is the stale/incorrect input, feeding 32px to a
    fixed-224 Swin. This is a TEST fixture error, not a production regression. Fix by
    changing `crop_size=32` to `crop_size=224` in both `ReidTrainingConfig(...)`
    constructions in the two slow tests, so the resized crops match the backbone's expected
    input. Do NOT weaken the backbone or add `dynamic_img_size`/`img_size` overrides to
    production `timm.create_model` unless changing the fixture proves insufficient — the
    backbone's fixed 224 input is the intended contract and end-to-end training must
    exercise the real input size. If, after the fixture fix, timm still asserts (e.g. a
    timm-version behavior change), then and only then classify it as a production issue and
    pass the appropriate `img_size`/`dynamic_img_size` argument to `timm.create_model`,
    recording that as the true root cause. Keep the source 32×32 jpgs in `_make_crops_dir`
    (they are upscaled by the dataset resize; only the post-resize `crop_size` must match
    the backbone). Record the one-line root-cause note.
  </action>
  <verify>
    <automated>hatch run test-all tests/unit/training/test_reid_training.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_smoke_end_to_end_training` and `test_checkpoint_has_backbone_state` pass under `test-all` (slow marker included)
    - The crop size fed to the backbone matches its expected input (no timm `_assert(H == img_size)` failure)
    - The fix is a real cause fix (fixture input corrected, or a justified production img_size override), NOT a skip/xfail
    - `hatch run test-all tests/unit/training/test_reid_training.py -q` exits 0 with no skip/xfail added
    - A one-line D-02/D-10 root-cause note is recorded in the SUMMARY
  </acceptance_criteria>
  <done>Both slow re-ID end-to-end tests pass with a backbone-compatible input size; the real cause is classified and fixed; root-cause note recorded.</done>
</task>

</tasks>

<threat_model>
Internal test-correctness work. No external attack surface; no dependency installs
(timm/torch already present). N/A beyond confirming the fix does not weaken the
backbone contract.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04-01 | Tampering | backbone input contract | accept | Fix restores the intended 224 input; does not relax model validation. No new inputs cross a trust boundary. |
</threat_model>

<verification>
- `hatch run test-all tests/unit/training/test_reid_training.py -q` exits 0
- No skip/xfail added
</verification>

<success_criteria>
The 2 slow re-ID end-to-end tests pass by feeding a backbone-compatible input size;
the real cause is classified per D-02/D-10; no skip/xfail introduced.
</success_criteria>

<output>
Create `.planning/phases/109-correctness-green-test-suite-config-consolidation/109-04-SUMMARY.md` when done, with the D-02/D-10 root-cause note.
</output>
