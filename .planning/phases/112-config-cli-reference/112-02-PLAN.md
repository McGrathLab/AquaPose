---
phase: 112-config-cli-reference
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - docs/reference/cli.rst
autonomous: true
requirements: [DOCS-05]
must_haves:
  truths:
    - "A user can look up every CLI command group (run/init/eval/eval-compare/tune/viz/stitch/smooth-z plus data/train/prep/pseudo-label/reid) with its purpose and arguments without reading source"
    - "Each documented command group carries a hand-authored worked example using generic placeholder paths"
    - "The top-level --project/-p option is rendered as a global option before the subcommand"
  artifacts:
    - path: "docs/reference/cli.rst"
      provides: "Auto-rendered CLI reference for all command groups plus hand-authored worked examples"
      contains: ".. click:: aquapose.cli:cli"
      min_lines: 40
  key_links:
    - from: "docs/reference/cli.rst"
      to: "aquapose.cli:cli"
      via: "sphinx-click directive :nested: full"
      pattern: "click:: aquapose\\.cli:cli"
    - from: "docs/reference/cli.rst"
      to: "docs/api/cli.rst"
      via: ":doc: cross-reference to the module API page"
      pattern: ":doc:`.*api/cli"
---

<objective>
Author the user-facing CLI Reference page: a single `.rst` page whose body is the `sphinx-click` directive over the root `cli` group (auto-rendering purpose + arguments for every command and subcommand), interleaved with hand-authored worked examples in the CLAUDE.md generic placeholder style. Satisfies DOCS-05.

Purpose: DOCS-05 requires every CLI command group documented with purpose, arguments, and a worked example. `sphinx-click` supplies purpose+arguments straight from the Click definitions (which cannot drift — D-01); the worked examples are hand-authored and deliberately decoupled from the Phase 111 tutorial dataset (D-07, D-09).
Output: `docs/reference/cli.rst` created. (Toctree wiring and the build-green gate are Plan 04.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-config-cli-reference/112-CONTEXT.md
@.planning/phases/112-config-cli-reference/112-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/reference/cli.rst with the sphinx-click directive and worked examples</name>
  <read_first>
    - docs/api/cli.rst (Phase 110 analog — the RST directive-page shape: title + underline + directive block; KEEP this page as-is per D-13)
    - src/aquapose/cli.py (lines 26-39 — the `cli` group and global `--project/-p` option; lines 41-56 — `run` command with `--mode`/`--set` options; lines 155-219 — `init_cmd`; and the `cli.add_command(...)` registrations at lines 880-884 for `data`/`train`/`prep`/`pseudo-label`/`reid` groups)
    - .planning/phases/112-config-cli-reference/112-PATTERNS.md (the `docs/reference/cli.rst` pattern section — exact directive form and the CLI source-of-truth inventory table)
    - CLAUDE.md (Pipeline CLI section — the `-p` top-level-arg convention and example style `aquapose -p YH run --max-chunks 6`)
  </read_first>
  <action>Create `docs/reference/cli.rst` (RST, not MyST — directive pages use `.rst` per D-12). Give it an RST title `CLI Reference` with a `=` underline. The body's core is a single directive: `.. click:: aquapose.cli:cli` with the two option lines `:prog: aquapose` and `:nested: full`. The directive targets the Click **group object** `cli` (defined at cli.py:26), NOT the `main` wrapper — `main()` merely calls `cli()` and sphinx-click needs the group. `:prog: aquapose` sets the rendered command name to match the console script; `:nested: full` recurses into all five registered subgroups (`data`, `train`, `prep`, `pseudo-label`, `reid` — registered at cli.py:880-884) so every subcommand renders under D-08 flat, group-driven organization. Add a short intro paragraph above the directive noting that `--project/-p` is a top-level option placed BEFORE the subcommand. Interleave hand-authored worked examples using RST `.. code-block:: bash` literal blocks, in the CLAUDE.md generic placeholder style with `aquapose -p myproject ...` — cover at minimum: the root `run` command (e.g. `aquapose -p myproject run --max-chunks 6`), `init` (project scaffold), and one example per subgroup (`data`, `train`, `prep`, `reid`) showing correct syntax and a common flag. Examples MUST use generic placeholder project/paths — do NOT reference Phase 111 tutorial dataset paths (D-09). Add a one-line `:doc:` cross-reference to the module API page: `:doc:`API module reference <../api/cli>``. Do NOT edit `docs/api/cli.rst` in this plan (Plan 04 owns the reciprocal cross-link). Do NOT add or edit any Click docstrings or `help=` text — coverage is already sufficient (scope fence). Do NOT wire this page into any toctree here (Plan 04 owns toctree wiring).</action>
  <verify>
    <automated>test -f docs/reference/cli.rst && grep -q "click:: aquapose.cli:cli" docs/reference/cli.rst && grep -q ":nested: full" docs/reference/cli.rst && grep -q "aquapose -p myproject" docs/reference/cli.rst && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/cli.rst` exists with an RST title `CLI Reference` and `=` underline.
    - The file contains the exact directive line `.. click:: aquapose.cli:cli` with option lines `:prog: aquapose` and `:nested: full`.
    - At least one worked example per: root `run`, `init`, and each of the `data`/`train`/`prep`/`reid` subgroups, all in `.. code-block:: bash` blocks using the `aquapose -p myproject ...` generic placeholder style.
    - No example references Phase 111 tutorial dataset paths (grep for tutorial-specific paths returns nothing).
    - The file contains a `:doc:` cross-reference to `../api/cli`.
    - `docs/api/cli.rst` is unmodified by this plan.
  </acceptance_criteria>
  <done>`docs/reference/cli.rst` renders every command group via `sphinx-click` (`:nested: full`), carries a worked example per group in generic placeholder style, and cross-links to the module API page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source Click definitions → rendered docs | `sphinx-click` introspects `aquapose.cli:cli` at build time and renders it into HTML. |

Documentation-only page. No new runtime surface, no user input, no network. The only "input" is the project's own Click command tree, introspected at build time under mocked imports.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02-I | Information disclosure | worked examples in cli.rst | mitigate | Examples use generic placeholder project/paths (`aquapose -p myproject`), never real credentials, tokens, or dataset-specific absolute paths (D-09) — nothing sensitive is embedded in the rendered docs. |
| T-112-02-D | Denial of service | sphinx-click introspecting the CLI graph | accept | Introspection runs only at docs-build/CI time on trusted first-party code under mocked imports; a build failure is caught by the `-W` gate (Plan 04), not shipped. Low value, no runtime exposure. |
| T-112-SC | Tampering | `sphinx-click` extension (inherited) | mitigate | Covered by Plan 01's supply-chain mitigation; this plan only consumes the already-registered extension. |
</threat_model>

<verification>
- `docs/reference/cli.rst` exists and contains `.. click:: aquapose.cli:cli` with `:prog: aquapose` and `:nested: full`.
- Worked examples present for root + each subgroup, in generic placeholder style.
- Cross-reference to `../api/cli` present. (Full-build green is verified in Plan 04 once the page is in the toctree.)
</verification>

<success_criteria>
A reader can look up any AquaPose CLI command group — its purpose, arguments, and a worked example — from `docs/reference/cli.rst` without reading source, satisfying DOCS-05.
</success_criteria>

<output>
Create `.planning/phases/112-config-cli-reference/112-02-SUMMARY.md` when done.
</output>
