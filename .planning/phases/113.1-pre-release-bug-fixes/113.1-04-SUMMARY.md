---
phase: 113.1-pre-release-bug-fixes
plan: 04
subsystem: infra
tags: [ast, import-boundary, sphinx, pytest, refactor]

requires: []
provides:
  - "`discover_camera_videos` relocated to `aquapose.core.types.video_discovery`, re-exported unchanged from `aquapose.io`"
  - "AST-based `core/` import-boundary guard (`tests/unit/core/test_import_boundary.py`) covering module-level, function-local, and TYPE_CHECKING-guarded imports across the whole forbidden-package set"
  - "Annotated, stale-checked allowlist for the five pre-existing `core/reid/` and `core/synthetic.py` exemptions, plus a filed todo tracking the `core/reid/` fix"
affects: [113.2-typecheck-backlog]

actuals:
  tokens: 4348
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "AST-walk-based architectural guard tests (ast.parse + ast.walk over every .py under a subtree) instead of grep, so nested/TYPE_CHECKING imports can't evade the check"
    - "Annotated allowlist with a symmetric stale-entry test — an allowlist can only shrink, never silently persist past its subject's removal"

key-files:
  created:
    - src/aquapose/core/types/video_discovery.py
    - tests/unit/core/test_import_boundary.py
    - .planning/todos/pending/2026-09-02-core-reid-import-boundary-exemptions.md
  modified:
    - src/aquapose/core/types/frame_source.py
    - src/aquapose/core/types/__init__.py
    - src/aquapose/io/__init__.py
    - docs/api/io.rst
    - docs/api/core/types.rst
    - scripts/viz_swap_events.py

key-decisions:
  - "Took Option 3 (relocate discover_camera_videos into core/) over the todo's own Option 1 (caller passes resolved paths in) after re-verifying the 10-site/3-in-core claim against source: VideoFrameSource has exactly 10 construction sites, 3 of them inside core/reid/ (miner.py:658, runner.py:225, swap_detector.py:375). Option 1 would have forced those three to import aquapose.io themselves, relocating the boundary violation rather than removing it."
  - "AST scan of core/ (with aquapose.engine/io/training/evaluation/visualization/cli/cli_utils as the forbidden set) found 6 non-core-internal import sites beyond the fixed frame_source.py one: 5 in core/reid/ (new, undocumented by CONTEXT.md) and 1 in core/synthetic.py (pre-existing, already tracked by tools/import_boundary_checker.py's own allowlist). Allowlisted all 6 with inline reasons; filed a new todo for the 5 core/reid/ ones since CONTEXT.md's claim about them was incomplete."
  - "Fixed scripts/viz_swap_events.py's `from aquapose.io.discovery import ...` import (Rule 1 — a bug this task's own module deletion would otherwise introduce) even though the file isn't in files_modified, since it's a direct consequence of deleting src/aquapose/io/discovery.py."

patterns-established:
  - "AST-based boundary guards over grep-based ones for architectural rules — the codebase already had one precedent (tools/import_boundary_checker.py, engine/cli scope only); this plan adds a pytest-native equivalent scoped to io/training/evaluation/visualization plus engine/cli, and both now co-exist without conflict."

requirements-completed: [QA-05]

coverage:
  - id: D1
    description: "core/ no longer imports aquapose.io anywhere (module-level, function-local, or TYPE_CHECKING) — the phase's QA-05 criterion 5 deliverable"
    requirement: "QA-05"
    verification:
      - kind: unit
        ref: "tests/unit/core/test_import_boundary.py#test_core_does_not_import_io"
        status: pass
    human_judgment: false
  - id: D2
    description: "Full core/ import boundary (engine, io, training, evaluation, visualization, cli, cli_utils) enforced by a durable AST guard with an annotated, non-stale-able allowlist"
    requirement: "QA-05"
    verification:
      - kind: unit
        ref: "tests/unit/core/test_import_boundary.py#test_core_does_not_import_upper_layers"
        status: pass
      - kind: unit
        ref: "tests/unit/core/test_import_boundary.py#test_import_boundary_allowlist_has_no_stale_entries"
        status: pass
    human_judgment: false
  - id: D3
    description: "discover_camera_videos keeps its exact signature/behavior and both aquapose.io.discover_camera_videos and aquapose.core.types.discover_camera_videos resolve to the same object; VideoFrameSource's constructor signature is unchanged"
    requirement: "QA-05"
    verification:
      - kind: unit
        ref: "tests/unit/core/types/test_frame_source.py (unmodified — git diff --stat is empty)"
        status: pass
      - kind: other
        ref: "python -c identity check: aquapose.io.discover_camera_videos is aquapose.core.types.discover_camera_videos"
        status: pass
    human_judgment: false
  - id: D4
    description: "docs build clean with the relocated module documented in its new home"
    requirement: "QA-05"
    verification:
      - kind: other
        ref: "hatch run docs:build (sphinx-build -W --keep-going) exit 0; docs/_build/html/api/core/types.html contains video_discovery"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-02
status: complete
---

# Phase 113.1 Plan 04: Close the core/ import boundary Summary

**Relocated `discover_camera_videos` from `aquapose.io.discovery` into `aquapose.core.types.video_discovery` (git-mv, byte-identical function body) and replaced the ad-hoc grep-style boundary check with an AST-walking pytest guard covering the full forbidden-package set, with an annotated allowlist for the six pre-existing exemptions the guard's own scan surfaced.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-09-02T21:03 UTC (approx, first task commit)
- **Completed:** 2026-09-02T21:07 UTC
- **Tasks:** 2
- **Files modified:** 9 (8 in Task 1, 2 in Task 2, `tests/unit/core/test_import_boundary.py` touched by both)

## Accomplishments

- `discover_camera_videos` lives in `src/aquapose/core/types/video_discovery.py` (Layer 1), moved via `git mv` with only a docstring addition — the function body is byte-identical to the pre-move version (confirmed by diff inspection).
- `aquapose.io.discover_camera_videos` and `aquapose.core.types.discover_camera_videos` are the same object (`is` identity confirmed) — zero call-site churn across all 10 `VideoFrameSource` construction sites.
- `VideoFrameSource.__init__`'s `(video_dir, calibration_path, max_frames)` signature is untouched; `tests/unit/core/types/test_frame_source.py` has an empty `git diff --stat`.
- `docs/api/io.rst`'s stale `aquapose.io.discovery` automodule block removed; `docs/api/core/types.rst` gained an `aquapose.core.types.video_discovery` block. `hatch run docs:build` (sphinx `-W --keep-going`) exits 0.
- New `tests/unit/core/test_import_boundary.py` walks every `.py` under `src/aquapose/core/` with `ast`, catching module-level, function-local, and `TYPE_CHECKING`-guarded imports alike — a grep on import lines cannot see the latter two, and the plan's own prohibition explicitly forbids using either as an escape hatch.
- `test_core_does_not_import_io` passes with zero allowlist entries for `aquapose.io` (non-negotiable — exempting it would be self-defeating).
- `test_core_does_not_import_upper_layers` covers `aquapose.engine`, `aquapose.io`, `aquapose.training`, `aquapose.evaluation`, `aquapose.visualization`, `aquapose.cli`, `aquapose.cli_utils`, consulting an annotated `_KNOWN_EXCEPTIONS` allowlist for the 6 pre-existing violations the scan found.
- `test_import_boundary_allowlist_has_no_stale_entries` proves the allowlist can't quietly outlive its subject — every entry must still correspond to a live import or the test fails naming the stale entry.
- Filed `.planning/todos/pending/2026-09-02-core-reid-import-boundary-exemptions.md` recording the 5 `core/reid/` exemptions the plan's flagged assumption called out, since `113.1-CONTEXT.md`'s "only module-level violation" claim was found incomplete.
- Fixed `scripts/viz_swap_events.py`'s now-broken `from aquapose.io.discovery import ...` import (Rule 1 — a bug introduced directly by this task's own module deletion, not pre-existing).

## Complete AST Scan — Non-Core `aquapose.*` Imports Under `src/aquapose/core/`

Full scan against the forbidden-package set (`aquapose.engine`, `aquapose.io`, `aquapose.training`, `aquapose.evaluation`, `aquapose.visualization`, `aquapose.cli`, `aquapose.cli_utils`), run against source after Task 1's fix landed (so `aquapose.io` is already clean):

| File | Line | Classification | Import |
|---|---|---|---|
| `core/reid/cli.py` | 9 | module-level | `aquapose.cli_utils` (`get_project_dir`, `resolve_run`) |
| `core/reid/cli.py` | 16 | TYPE_CHECKING | `aquapose.training.reid_training` (`ReidTrainingConfig`) |
| `core/reid/cli.py` | 267 | function-local | `aquapose.training.reid_training` |
| `core/reid/cli.py` | 434 | function-local | `aquapose.training.reid_training` |
| `core/reid/swap_detector.py` | 323 | function-local | `aquapose.training.reid_training` |
| `core/synthetic.py` | 25 | TYPE_CHECKING | `aquapose.engine.config` (`SyntheticConfig`) |

Zero hits for `aquapose.io` anywhere under `core/` — the phase deliverable. The `core/synthetic.py` entry is a **pre-existing, already-documented** design decision independently tracked by `tools/import_boundary_checker.py`'s own `_IB003_ALLOWLIST` (same file, same imported module); the 5 `core/reid/` entries are the ones this plan's flagged assumption carried forward and are newly tracked by the todo filed in Task 2.

## `_KNOWN_EXCEPTIONS` — Verbatim

```python
_KNOWN_EXCEPTIONS: dict[str, frozenset[str]] = {
    # core/reid/cli.py:9 (module-level) imports aquapose.cli_utils for
    # get_project_dir/resolve_run; core/reid/cli.py:16 (TYPE_CHECKING),
    # :267, and :434 (function-local) import aquapose.training.reid_training.
    # 113.1-CONTEXT.md's "only module-level violation" claim (verified
    # 2026-09-02) was found incomplete during Phase 113.1 planning -- both
    # of these were live on the same date. Carried under D-02's scope
    # fence rather than fixed here. Tracked:
    # .planning/todos/pending/2026-09-02-core-reid-import-boundary-exemptions.md
    "reid/cli.py": frozenset({"aquapose.cli_utils", "aquapose.training"}),
    # core/reid/swap_detector.py:323 (function-local) imports
    # aquapose.training.reid_training. Same exemption class and the same
    # tracking todo as reid/cli.py above.
    "reid/swap_detector.py": frozenset({"aquapose.training"}),
    # core/synthetic.py:25 (TYPE_CHECKING) imports aquapose.engine.config
    # for the SyntheticConfig annotation. Pre-existing, already-documented
    # design decision (see tools/import_boundary_checker.py's own
    # _IB003_ALLOWLIST, which independently allowlists the same pair):
    # SyntheticDataStage receives SyntheticConfig from the engine layer as
    # a constructor argument, and config flows strictly downward
    # (engine -> core), never upward. Annotation-only, guarded by
    # TYPE_CHECKING so it never executes at runtime.
    "synthetic.py": frozenset({"aquapose.engine"}),
}
```

## Non-Vacuity Demonstrations — Observed Failure Messages

All three were run against the live tree during execution, then reverted before commit (confirmed via `git diff --stat` returning empty for each touched file).

**1. Injected `aquapose.io` import (Task 1, `test_core_does_not_import_io`)** — a function-local `from aquapose.io import discover_camera_videos` was inserted into `src/aquapose/core/synthetic.py`'s `run()` method body:

```
AssertionError: core/ must not import aquapose.io:
src/aquapose/core/synthetic.py:142 imports 'aquapose.io'
assert not ["src/aquapose/core/synthetic.py:142 imports 'aquapose.io'"]
```

**2. Injected `aquapose.engine.config` import (Task 2, `test_core_does_not_import_upper_layers`)** — a module-level `from aquapose.engine.config import PipelineConfig` was inserted into `src/aquapose/core/context.py` (a non-allowlisted module) immediately after its module docstring:

```
AssertionError: core/ must not import upper-layer packages (allowlist exceptions notwithstanding):
context.py:14 imports 'aquapose.engine.config' (forbidden: aquapose.engine)
assert not ["context.py:14 imports 'aquapose.engine.config' (forbidden: aquapose.engine)"]
```

**3. Removed `aquapose.cli_utils` import (Task 2, `test_import_boundary_allowlist_has_no_stale_entries`)** — the `from aquapose.cli_utils import get_project_dir, resolve_run` line was deleted from `src/aquapose/core/reid/cli.py`:

```
AssertionError: Stale _KNOWN_EXCEPTIONS entries found:
reid/cli.py: allowlisted for 'aquapose.cli_utils' but no matching import was found -- remove this exemption
assert not ["reid/cli.py: allowlisted for 'aquapose.cli_utils' but no matching import was found -- remove this exemption"]
```

## Byte-Identity Confirmation

`git diff src/aquapose/io/discovery.py src/aquapose/core/types/video_discovery.py` (compared against the pre-move blob via the `git mv`-preserved rename) shows exactly one hunk: 5 added lines extending the module docstring. The `discover_camera_videos` function body (signature, glob logic, docstring text of the function itself, return statement) is untouched — zero lines changed inside the function. `git log --follow --oneline -- src/aquapose/core/types/video_discovery.py` shows the pre-move history (`a851c22`, `19ea21e`, `a3ebff5`) preceding this plan's commit, confirming `git mv` (rename-preserving) was used rather than a copy-and-delete.

## Todo Filed

`.planning/todos/pending/2026-09-02-core-reid-import-boundary-exemptions.md` — records all 5 `core/reid/` exemptions (file, line, module-level/function-local/TYPE_CHECKING classification, and the specific imported package), states that `113.1-CONTEXT.md`'s "only module-level violation" claim was found incomplete during this plan's execution, and notes they were deliberately not fixed here per D-02's scope fence. Cross-referenced from the `_KNOWN_EXCEPTIONS` inline comments in `tests/unit/core/test_import_boundary.py`.

## Final Full-Suite Pytest Summary

```
FAILED tests/unit/training/test_pseudo_label_cli.py::TestGenerateCommand::test_generates_merged_obb_and_separate_pose
1 failed, 1399 passed, 3 skipped, 17 deselected, 20 warnings in 11.17s
```

Exactly one failure — the documented pre-existing `test_generates_merged_obb_and_separate_pose`, to be fixed by plan `113.1-06`. Pass count is 3 higher than the sibling-reported baseline (1396 passed), matching the 3 new tests this plan added (`test_core_does_not_import_io`, `test_core_does_not_import_upper_layers`, `test_import_boundary_allowlist_has_no_stale_entries`).

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end "core no longer reaches into io"** — `9383120` (feat)
2. **Task 2: Widen the guard to the whole boundary, with an annotated allowlist that cannot go stale** — `eb74f4f` (test)

_Note: Task 2 carried `tdd="true"` but its own deliverable is the test suite itself (no separate implementation file to RED/GREEN against) — a single `test(...)` commit is the correct shape here, matching the plan's own commit-type guidance for "Test-only changes."_

## Files Created/Modified

- `src/aquapose/core/types/video_discovery.py` - `discover_camera_videos`, relocated verbatim (git mv) from `aquapose/io/discovery.py`, module docstring extended to explain the Layer-1 placement
- `src/aquapose/core/types/frame_source.py` - import source changed from `aquapose.io.discovery` to `aquapose.core.types.video_discovery`; nothing else touched
- `src/aquapose/core/types/__init__.py` - `discover_camera_videos` added to imports and `__all__` (ASCII-sort position after `VideoFrameSource`)
- `src/aquapose/io/__init__.py` - `discover_camera_videos` now imported from `aquapose.core.types.video_discovery` instead of the deleted `.discovery` submodule; `__all__` unchanged
- `docs/api/io.rst` - removed the `aquapose.io.discovery` automodule block
- `docs/api/core/types.rst` - added an `aquapose.core.types.video_discovery` automodule block (alphabetical position, after `reconstruction`)
- `scripts/viz_swap_events.py` - fixed a now-broken `from aquapose.io.discovery import ...` to `from aquapose.io import ...` (Rule 1, not in original `files_modified`)
- `tests/unit/core/test_import_boundary.py` (new) - `test_core_does_not_import_io` (Task 1), `test_core_does_not_import_upper_layers` and `test_import_boundary_allowlist_has_no_stale_entries` (Task 2), plus `_FORBIDDEN_PACKAGES` and `_KNOWN_EXCEPTIONS` module-level constants
- `.planning/todos/pending/2026-09-02-core-reid-import-boundary-exemptions.md` (new) - tracks the 5 `core/reid/` exemptions carried out of this plan

## Decisions Made

- **Option 3 over Option 1 (re-verified, not assumed):** Confirmed via `grep -rln "VideoFrameSource("` that there are exactly 10 construction sites, 3 inside `core/reid/` (`miner.py`, `runner.py`, `swap_detector.py`). Option 1 (caller resolves paths) would have forced those three `core/` modules to import `aquapose.io` themselves — moving the violation rather than removing it. Option 3 (relocate the pure-`pathlib` function into `core/`) achieves zero call-site churn.
- **`aquapose.calibration` excluded from `_FORBIDDEN_PACKAGES`** per GUIDEBOOK section 3's Layer-1 placement, with the reasoning written as an inline comment above the tuple so it reads as a decision, not an oversight.
- **`core/synthetic.py`'s pre-existing TYPE_CHECKING exemption allowlisted alongside the new `core/reid/` ones**, since the plan's forbidden set (`aquapose.engine` included) makes it a hit under the new test even though it was already a recognized, separately-tracked design decision in `tools/import_boundary_checker.py`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed `scripts/viz_swap_events.py`'s broken import**
- **Found during:** Task 1, after deleting `src/aquapose/io/discovery.py`
- **Issue:** `scripts/viz_swap_events.py:279` had `from aquapose.io.discovery import discover_camera_videos`, which would `ModuleNotFoundError` once the submodule was deleted. Not in the plan's `files_modified` list, but a direct consequence of this task's own change.
- **Fix:** Changed to `from aquapose.io import discover_camera_videos` (the public re-export, which is exactly what the plan's `must_haves` guarantee stays resolvable).
- **Files modified:** `scripts/viz_swap_events.py`
- **Verification:** No test suite covers this script (`grep -rn "viz_swap_events" tests/` returns nothing), so verification was manual read-through of the one-line change; the import path itself is proven live by the `test_core_does_not_import_io` identity/re-export checks elsewhere.
- **Committed in:** `9383120` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix, Rule 1)
**Impact on plan:** Necessary to avoid leaving a broken import in the tree as a direct side effect of the module deletion. No scope creep — did not touch `core/reid/` or anything outside the plan's stated blast radius.

## Issues Encountered

- Initial non-vacuity injection attempts landed import statements inside docstrings or function-signature parens by mis-locating insertion points via `sed` line-offset arithmetic; caught immediately by re-parsing with `ast.parse` before running the test, corrected, and re-verified. No incorrect state was ever committed.
- Injecting a module-level `aquapose.engine.config` import into `src/aquapose/core/context.py` (a widely-imported module) triggered a circular-import collection failure across ~66 unrelated test files when run via the full `hatch run test` suite. Worked around by running only the target test file directly through the hatch-managed interpreter (`hatch run python -m pytest tests/unit/core/test_import_boundary.py -k ...`) for that one demonstration step, then reverting before any real verification run. The real (non-demonstration) verification runs all used `hatch run test` as required.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- QA-05 criterion 5 is closed: `core/` has zero `aquapose.io` imports, and the wider boundary is now enforced by a durable, self-verifying test rather than convention.
- The `core/reid/` exemptions are visible, annotated, and tracked via the newly filed todo — ready to be picked up as its own follow-up work whenever prioritized; not blocking this phase or 113.2.
- `tools/import_boundary_checker.py` (the pre-existing pre-commit hook, scoped to `engine`/`cli` only) was left untouched — it is a distinct, narrower mechanism from this plan's pytest-based guard and the two do not conflict. Worth noting for anyone maintaining both going forward.

## Self-Check: PASSED

All 9 claimed files verified present on disk; both task commit hashes (`9383120`, `eb74f4f`) verified present in `git log --oneline --all`.

---
*Phase: 113.1-pre-release-bug-fixes*
*Completed: 2026-09-02*
