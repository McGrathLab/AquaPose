---
phase: 108-branch-reconciliation-repo-hygiene
plan: 05
type: execute
wave: 5
depends_on: ["108-04"]
files_modified:
  - docs/conf.py
  - docs/api/index.rst
  - src/aquapose/
autonomous: false
requirements: [FOUND-01, FOUND-02]

must_haves:
  truths:
    - "FOUND-01/D-14: `hatch run docs:build` — `sphinx-build -W --keep-going -b html docs docs/_build/html` — exits 0 from a clean detached env on `dev`."
    - "D-15: `autodoc_mock_imports` was re-derived against dev's actual third-party imports, not copied blind from main — `timm`, `pytorch_metric_learning`, `shapely`, and `sklearn` are mocked."
    - "D-15: the docs env installs no CUDA torch — it is detached and `numpy` is its only scientific dependency."
    - "D-13: every `automodule` target in `docs/api/*.rst` resolves against dev's tree."
    - "D-14: the `.rst` tree is green but admittedly incomplete — `cli`, `cli_utils`, and `logging` are left uncovered for Phase 110."
    - "108-01's four a66287a docstring repairs are preserved — this plan does NOT re-derive them and must not revert them."
    - "108-02's AGPL copyright line and 108-01's `napoleon_use_ivar` / `_resolve_release()` all survive in `docs/conf.py`."
    - "FOUND-02: the Documentation workflow ran on a push to `dev` and concluded `success`."
    - "The `CHANGELOG.md` license note survived python-semantic-release's post-push regeneration."
    - "No forced update reached `origin/dev`; `main` was never pushed to."
  artifacts:
    - path: "docs/conf.py"
      provides: "mock list covering dev's third-party imports"
      contains: "pytorch_metric_learning"
    - path: "docs/_build/html/index.html"
      provides: "proof the -W build produced output"
  key_links:
    - from: "pyproject.toml [tool.hatch.envs.docs] detached = true"
      to: "docs/conf.py autodoc_mock_imports"
      via: "autodoc imports from src/ via sys.path with heavy deps mocked"
      pattern: "autodoc_mock_imports"
    - from: ".github/workflows/docs.yml"
      to: "hatch run docs:build"
      via: "push to dev triggers the build"
      pattern: "hatch run docs:build"
---

<objective>
Drive `sphinx-build -W --keep-going` to exit 0 on `dev` (FOUND-01) by re-deriving
`autodoc_mock_imports` against dev's real imports (D-15), verifying every `automodule` target
resolves (D-13), and repairing whatever docstring drift accumulated over 1496 commits — then push
`dev` once and confirm the Documentation workflow goes green (FOUND-02).

Purpose: the docs build has been RED on `dev` since the docs env grew a CUDA torch dependency.
`a66287a` repaired it on `main`; plan 108-01 merged that repair in and separately re-applied the
four `src/` docstring hunks the merge dropped. What remains is the delta between main's tree at that
commit and dev's tree 1496 commits later.

Output: a green `hatch run docs:build`, a pushed `dev`, and a green Documentation Actions run.

**Scope fence (D-14):** the bar is a green build, nothing more. Do **not** add `.rst` files for
uncovered modules (`cli`, `cli_utils`, `logging`, and the ~52 submodules) — full coverage and the
tier-one/tier-two IA are **Phase 110's** job under DOCS-01/DOCS-02, and two phases must not both be
editing the same `.rst` files. Do **not** touch the failing test suite — it is red with 8 known
failures and stays red; Phase 109 owns that. Do **not** edit `pyproject.toml` — plan 108-02 owns it.

This is the only plan in the phase that pushes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**No worktree isolation.** This plan runs sequentially on the main working tree with HEAD on `dev`,
and it publishes that branch. Do not run it in a detached worktree.
</execution_context>

<context>
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md
@.planning/GUIDEBOOK.md

**Verified facts — do not re-derive.**

Tracked top-level members of `src/aquapose/`: packages `calibration`, `core`, `engine`,
`evaluation`, `io`, `synthetic`, `training`; modules `cli.py`, `cli_utils.py`, `logging.py`.
Main's 7 API `.rst` files target exactly those 7 packages, one `.. automodule:: aquapose.<pkg>`
each, so **every automodule target already resolves**. `cli`, `cli_utils`, and `logging` are
uncovered — that is intentional and is Phase 110's scope (D-14).

`docs/api/index.rst` is a `maxdepth: 2` toctree listing exactly `calibration`, `core`, `engine`,
`evaluation`, `io`, `synthetic`, `training`. `docs/api/engine.rst` carries
`:exclude-members: PipelineContext, Stage, ChunkHandoff, load_chunk_cache` to suppress duplicate
objects from the `core` re-exports. Preserve that line verbatim.

`autodoc_mock_imports` as delivered by the 108-01 merge (`docs/conf.py` lines 101–120) is exactly:
`aquacal, boxmot, click, cv2, h5py, igraph, leidenalg, loguru, matplotlib, plotly, PIL,
pycocotools, scipy, skimage, torch, torchvision, ultralytics, yaml`.

Scanning top-level third-party import names across `git ls-files 'src/aquapose/**/*.py'` on `dev`
yields four modules dev imports that this list does **not** mock and which therefore **must be
added**: `timm`, `pytorch_metric_learning`, `shapely`, `sklearn`. Four entries in the list are no
longer imported by dev (`boxmot`, `loguru`, `skimage`, `torchvision`) — harmless, keep them.
`numpy` is deliberately a **real** dependency of the docs env, not a mock; do not add it to the
mock list.

**Already done by earlier plans in this phase — do not repeat and do not revert:**
- 108-01 merged `main`'s repaired `docs/conf.py`, `docs/index.md`, `docs/api/*.rst` (8 files), and
  `.github/workflows/docs.yml`, and deleted dev's dead `mesh/optimization/segmentation/utils` rst.
- 108-01 re-applied `a66287a`'s four `src/` docstring hunks: the escaped cache glob in
  `src/aquapose/core/context.py`, the escaped curvature expression in
  `src/aquapose/synthetic/fish.py`, and the inserted blank lines in
  `src/aquapose/engine/observers.py` and `src/aquapose/synthetic/scenarios.py`. **This plan owns no
  part of a66287a's docstring work.**
- 108-01 purged 29 superseded `src/`/`tests/`/`scripts/` paths the merge added silently; none may
  reappear.
- 108-02 set `docs/conf.py`'s `copyright` to the D-08 institution string and relicensed
  `pyproject.toml` to AGPL-3.0-or-later.
- 108-03 removed `src/aquapose/{reconstruction,segmentation,tracking,visualization}` (untracked
  `__pycache__`-only leftovers) and cleared the working tree.

**Warning classes `a66287a` had to fix, likely to recur on dev's newer code:**
- Google-style `Attributes:` sections colliding with `:undoc-members:` dataclass fields — handled
  globally by `napoleon_use_ivar`.
- Malformed docstrings: a missing blank line before a list; a `|token|` pair parsed as an RST
  substitution; a `*glob*` pattern parsed as emphasis.
- A markdown document not included in any toctree.

**Post-push CI behavior — expect all of this:**
- `docs.yml` runs on push to `dev` and **must** conclude `success`. This is the FOUND-02 gate.
- `test.yml` runs on push to `dev` and **will fail** — 8 known failures, Phase 109's scope. Do not
  attempt to fix them.
- `release.yml` runs on push to `dev` with `contents: write` and executes `semantic-release version`
  on the `dev` prerelease channel. It will bump `project.version` from `1.2.0-dev.0`, regenerate
  `CHANGELOG.md`, commit `chore(release): <version>`, and create a prerelease tag. This is expected
  and correct. `publish.yml` only fires on full-release tags, so nothing reaches PyPI.
- Because the relicense (108-02) landed before this push, no release is ever cut against an
  MIT-declared tree.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive autodoc_mock_imports and verify every automodule target resolves</name>
  <files>docs/conf.py, docs/api/index.rst</files>
  <read_first>
    - `docs/conf.py` in full (post-merge, post-108-02) — the `autodoc_mock_imports` list at lines
      101–120, the `copyright` line 108-02 changed, `napoleon_use_ivar`, and `_resolve_release()`.
    - `git show a66287a` — its message enumerates every warning class that was fatal under `-W`.
    - All eight files in `docs/api/`, including `index.rst`'s toctree.
    - `pyproject.toml` `[project] dependencies` and `[tool.hatch.envs.docs]` — the declared runtime
      deps versus what the detached docs env actually installs. **Read only; 108-02 owns this file.**
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-01-SUMMARY.md` — confirms which
      a66287a hunks are already applied so you do not redo them.
  </read_first>
  <action>
    Derive the mock list empirically rather than trusting the pre-verified list blind: scan
    top-level third-party import names across `git ls-files 'src/aquapose/**/*.py'` by matching
    lines beginning with `import` or `from` and taking the first dotted segment, then subtracting
    stdlib names, `aquapose` itself, and false positives from prose inside docstrings.

    Add every third-party root module that appears in dev's sources and is missing from
    `autodoc_mock_imports`. At minimum this adds `timm`, `pytorch_metric_learning`, `shapely`, and
    `sklearn`. Keep the existing entries even where dev no longer imports them. Do **not** add
    `numpy` — it is a real dependency of the detached docs env. Keep the list in the existing
    roughly-alphabetical style and preserve the explanatory comment above it.

    Then verify the `.rst` tree (D-13): for each `.. automodule:: aquapose.<X>` directive in
    `docs/api/*.rst`, confirm that `git ls-files` returns a path for `src/aquapose/<X>/__init__.py`
    or `src/aquapose/<X>.py`. Repair any target that drifted. Confirm `docs/api/index.rst`'s toctree
    lists exactly the seven sibling files and that no orphan `.rst` exists in `docs/api/`.

    Do **not** add new `.rst` files or new automodule targets for `cli`, `cli_utils`, `logging`, or
    any submodule — D-14 fences this plan at a green build; Phase 110 owns coverage. Preserve
    `engine.rst`'s `:exclude-members:` line verbatim. Do not revert 108-01's docstring repairs or
    108-02's copyright line.

    Commit as `fix(108-05): re-derive autodoc mock imports against dev's import surface`.
    Do not push yet.
  </action>
  <verify>
    <automated>grep -q 'pytorch_metric_learning' docs/conf.py &amp;&amp; grep -q '"timm"' docs/conf.py &amp;&amp; grep -q '"shapely"' docs/conf.py &amp;&amp; grep -q '"sklearn"' docs/conf.py &amp;&amp; test "$(sed -n '/autodoc_mock_imports = \[/,/^\]/p' docs/conf.py | grep -c '"numpy"')" -eq 0 &amp;&amp; test "$(ls docs/api/*.rst | wc -l)" -eq 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pytorch_metric_learning' docs/conf.py` returns `1`.
    - `grep -c '"timm"' docs/conf.py`, `grep -c '"shapely"' docs/conf.py`, and `grep -c '"sklearn"' docs/conf.py` each return `1`.
    - `grep -c '"ultralytics"' docs/conf.py` returns `1` and `grep -c '"leidenalg"' docs/conf.py` returns `1`.
    - `sed -n '/autodoc_mock_imports = \[/,/^\]/p' docs/conf.py | grep -c '"numpy"'` returns `0`.
    - `grep -c 'napoleon_use_ivar' docs/conf.py` returns `1` and `grep -q 'def _resolve_release' docs/conf.py` exits 0 (D-16 preserved).
    - `grep -c 'McGrath Lab' docs/conf.py` returns `1` — 108-02's copyright preserved.
    - Every name produced by `grep -h '.. automodule::' docs/api/*.rst | sed 's/.*aquapose\.//' | sort -u` satisfies `git ls-files src/aquapose/<name>` returning at least one path.
    - `ls docs/api/*.rst | wc -l` returns `8` — no new `.rst` added (D-14).
    - `grep -c ':exclude-members: PipelineContext, Stage, ChunkHandoff, load_chunk_cache' docs/api/engine.rst` returns `1`.
    - `grep -c '``\*_cache.pkl``' src/aquapose/core/context.py` returns `1` — 108-01's repair intact.
    - `git diff --name-only HEAD~1 HEAD | tr '\n' ' '` returns exactly `docs/conf.py ` (or `docs/api/index.rst docs/conf.py ` if a toctree repair was genuinely required).
    - `git ls-files src/aquapose/core/midline tests/unit/segmentation scripts/perf_validate.py | wc -l` returns `0` — 108-01's purge still holds.
  </acceptance_criteria>
  <done>`autodoc_mock_imports` covers every third-party root module dev imports, `numpy` stays real, every automodule target resolves to a tracked package, the `.rst` file count is unchanged at 8, and no earlier plan's work was reverted.</done>
</task>

<task type="auto">
  <name>Task 2: Drive `hatch run docs:build` to exit 0</name>
  <files>docs/conf.py, src/aquapose/</files>
  <read_first>
    - The full `hatch run docs:build` output from a first run — this is the authoritative list of
      what remains broken; read it before editing anything.
    - `git show a66287a` — the malformed-docstring classes it fixed on main are the patterns most
      likely to recur in dev's newer docstrings.
    - `.claude/rules/code-style.md` — docstrings are Google style; any repair must stay Google style
      and keep type hints on public functions.
    - Each source file named in a warning, before editing it.
    - `pyproject.toml` `[tool.hatch.envs.docs.scripts] build` — the exact `-W --keep-going` command
      string that must remain intact. **Read only.**
  </read_first>
  <action>
    Provision a clean detached docs env and run the gate. `hatch env prune` is too broad — instead
    run `hatch env remove docs` (ignore failure if absent), then `hatch run docs:build`. Confirm
    from the install output that the env does **not** pull torch or CUDA wheels. If it does,
    `detached = true` did not survive; stop and report rather than editing `pyproject.toml`, which
    plan 108-02 owns.

    Iterate until exit code 0. Permitted repairs, in preference order:
    1. Fix the offending docstring in `src/aquapose/**` — add the missing blank line before a list,
       escape or reformat text RST misreads (pipe-delimited substitution references, `*glob*`
       emphasis), correct indentation. Preserve meaning; do not rewrite prose.
    2. Extend `autodoc_mock_imports` if an import error surfaces a module task 1's scan missed.
    3. Add a targeted `:exclude-members:` to an existing `.rst` file, matching the pattern
       `engine.rst` already uses, only when a genuine duplicate-object collision cannot be fixed at
       the docstring.
    4. Add a document to a toctree if a "not included in any toctree" warning appears.

    Forbidden: removing `-W` or adding `--no-fail-on-warning` to the `build` script; adding
    `suppress_warnings` to silence a class of warning wholesale; adding `nitpicky = False` style
    blanket escapes; editing `pyproject.toml`; adding new `.rst` files or new automodule targets
    (D-14); changing any runtime behavior — docstring text only inside `src/`; reverting 108-01's
    four docstring repairs.

    Record the count and category of warnings fixed for the SUMMARY.

    Verify the build is reproducible from clean: `rm -rf docs/_build && hatch run docs:build` exits
    0 a second time. Run `hatch run lint` to confirm the docstring edits did not break ruff.
    Confirm `docs/_build/` is not staged.

    Commit as `fix(108-05): repair Sphinx -W build on dev`. Do not push yet.
  </action>
  <verify>
    <automated>rm -rf docs/_build &amp;&amp; hatch run docs:build &amp;&amp; test -f docs/_build/html/index.html &amp;&amp; test -f docs/_build/html/api/index.html &amp;&amp; hatch run lint</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run docs:build` exits 0.
    - Running `rm -rf docs/_build && hatch run docs:build` a second time also exits 0.
    - `test -f docs/_build/html/index.html` exits 0 and `test -f docs/_build/html/api/index.html` exits 0.
    - `grep -c 'sphinx-build -W --keep-going -b html docs docs/_build/html' pyproject.toml` returns `1` — the `-W` gate was not weakened.
    - `grep -c 'suppress_warnings' docs/conf.py` returns `0` and `grep -c 'nitpicky' docs/conf.py` returns `0`.
    - `git diff --exit-code HEAD -- pyproject.toml` exits 0 — 108-02 owns that file.
    - `hatch run lint` exits 0.
    - `ls docs/api/*.rst | wc -l` returns `8`.
    - `git diff HEAD~1 HEAD -- src | grep -cE '^[+-][[:space:]]*(def |class |import |from |return |@)'` returns `0` — only docstring text changed under `src/`.
    - `grep -c '``\*_cache.pkl``' src/aquapose/core/context.py` returns `1` and `grep -c '``radius=1/|curvature|``' src/aquapose/synthetic/fish.py` returns `1` — 108-01's repairs intact.
    - `git status --porcelain docs/_build` outputs zero lines.
    - `hatch run python -c "import aquapose"` exits 0.
  </acceptance_criteria>
  <done>`sphinx-build -W --keep-going` exits 0 twice from a clean detached env that installed no torch, with the `-W` gate intact, no warnings suppressed, and only docstring text changed under `src/`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Push dev and confirm the Documentation workflow is green</name>
  <files>.github/workflows/docs.yml, CHANGELOG.md</files>
  <read_first>
    - `git log --oneline main..dev` — the exact commit set about to be published, including the
      108-01 merge commit.
    - `.github/workflows/release.yml` lines 1–20 — the `on: push: branches: [main, dev]` trigger,
      the `chore(release):` head-commit guard, and the `contents: write` / `RELEASE_TOKEN`
      permissions. Understand what this will do before pushing.
    - `.github/workflows/publish.yml` lines 1–8 — confirm its tag filter excludes prerelease tags,
      so nothing reaches PyPI.
    - `CHANGELOG.md` lines 1–8 — the licensing note 108-02 placed above `<!-- version list -->`,
      which must survive semantic-release's regeneration.
    - `pyproject.toml` `[tool.semantic_release.branches.dev]` — `prerelease = true`,
      `prerelease_token = "dev"`. **Read only.**
  </read_first>
  <action>
    Automate everything first, then pause. In order:

    1. Re-confirm the gate is green on the exact tree about to be published:
       `rm -rf docs/_build && hatch run docs:build` exits 0, and `git status --porcelain` is empty.
    2. Review what is about to be published: `git log --oneline main..dev | head -30`. Confirm no
       commit subject carries a `!` breaking-change marker and no body carries a
       `BREAKING CHANGE:` footer — either would make python-semantic-release cut a `2.0.0-dev.N`
       instead of the intended `1.2.0-dev.N` (D-03). If one exists, stop and report.
    3. Re-confirm the merge invariants one last time before publishing: `git log --oneline dev..main`
       is empty, `git ls-files | grep -c 'core/midline\|perf_validate'` returns 0, and
       `git ls-files docs/api | wc -l` returns 8.
    4. `git push origin dev`. Use no flags. **`--force` and `--force-with-lease` are forbidden**, and
       do not push, tag, or otherwise write to `main` (D-04, D-05).
    5. Watch the triggered runs: `gh run list --branch dev --limit 10`, then
       `gh run watch <docs-run-id>` for the Documentation run. Record each workflow's conclusion.
    6. After `release.yml` finishes, run `git fetch origin && git pull --ff-only origin dev` to take
       the bot's `chore(release): <version>` commit. Then verify: `project.version` matches
       `1.2.*-dev.*`; the new tag matches `v1.2.*-dev.*`; the `AGPL-3.0-or-later` line in
       `CHANGELOG.md` is still above the `<!-- version list -->` marker; and `hatch run docs:build`
       still exits 0 on the pulled tree.
    7. Do not attempt to fix the failing `test.yml` run — 8 known failures are Phase 109's scope.

    Then pause for the user to confirm the Actions state.
  </action>
  <what-built>
    Everything automatable has been done:

    1. Re-verified the `-W` docs build green from clean and the working tree empty on the exact tree
       being published, and re-checked the merge invariants: `dev` still fully contains `main`, none
       of the 29 superseded paths reappeared, and `docs/api/` still holds exactly 8 `.rst` files.
    2. `git push origin dev` — a normal fast-forward push. `--force` and `--force-with-lease` were
       not used and are forbidden; nothing was pushed to `main`.
    3. Watched the resulting runs with `gh run list --branch dev --limit 10` and
       `gh run watch <docs-run-id>`, recording each workflow's conclusion.
    4. After `release.yml` finished, ran `git pull --ff-only origin dev` to take the bot's
       `chore(release): <version>` commit, then re-checked that the licensing note is still above
       the `<!-- version list -->` marker in `CHANGELOG.md`, that `project.version` is a
       `1.2.x-dev.N` prerelease, and that the docs build is still green on the pulled tree.

    The task output above states the resulting version, the prerelease tag name, and each workflow's
    conclusion.
  </what-built>
  <how-to-verify>
    1. Open the repository's Actions tab, or run `gh run list --branch dev --limit 10`.
    2. Confirm the **Documentation** run for the pushed commit shows a green check. This is the
       FOUND-02 acceptance gate and phase success criterion 2.
    3. Confirm the **Tests** run is red. That is expected and correct: 8 known failures are Phase
       109's scope and were deliberately not touched. Do not ask for them to be fixed now.
    4. Confirm the **Release** run created a prerelease tag of the form `v1.2.0-dev.N` — not a
       `v2.x` tag — and that **Publish to PyPI** did not run.
    5. Confirm nothing was published under the old license: the tag was cut after the AGPL
       relicense commit, so `git show <tag>:pyproject.toml | grep AGPL` should match.
    6. Confirm `main` is untouched on GitHub: it should still show `1.1.1` and still be the default
       branch. That staleness is deliberate until Phase 114 (D-04, D-05, D-06).

    Reply "approved" if Documentation is green and the tag is a `1.2.x` prerelease, or describe what
    you see.
  </how-to-verify>
  <acceptance_criteria>
    - `git log --oneline origin/dev..dev` outputs zero lines — the push succeeded and local is not ahead.
    - `git log --oneline dev..main` outputs zero lines — `main` is still fully contained and was not pushed to.
    - `git rev-parse origin/main` equals the `MAIN_PRE` value recorded in the 108-01 SUMMARY.
    - `git reflog origin/dev 2>/dev/null | grep -ci 'forced-update'` returns `0`.
    - `gh run list --workflow=docs.yml --branch=dev --limit=1 --json conclusion --jq '.[0].conclusion'` returns `success`.
    - `gh run list --workflow=publish.yml --limit=5 --json headBranch,createdAt` shows no run created by this push.
    - `git describe --tags --abbrev=0 dev` matches `v1.2.*-dev.*`.
    - `git show $(git describe --tags --abbrev=0 dev):pyproject.toml | grep -c 'AGPL-3.0-or-later'` returns at least `1`.
    - `git show $(git describe --tags --abbrev=0 dev):pyproject.toml | grep -cw MIT` returns `0`.
    - After the pull, the first `AGPL-3.0-or-later` line number in `CHANGELOG.md` is still strictly less than the `<!-- version list -->` line number.
    - `rm -rf docs/_build && hatch run docs:build` still exits 0 on the post-pull tree.
    - `git status --porcelain` outputs zero lines.
    - `git ls-files docs/api | wc -l` returns `8` and `git ls-files src/aquapose/core/midline scripts/perf_validate.py | wc -l` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git log --oneline origin/dev..dev)" &amp;&amp; test -z "$(git log --oneline dev..main)" &amp;&amp; test "$(gh run list --workflow=docs.yml --branch=dev --limit=1 --json conclusion --jq '.[0].conclusion')" = "success" &amp;&amp; git describe --tags --abbrev=0 dev | grep -q '^v1\.2\..*-dev\.'</automated>
  </verify>
  <resume-signal>Type "approved" or describe the issues you see in the Actions runs.</resume-signal>
  <done>`dev` is pushed without any forced update, the Documentation workflow concluded `success` on a push to `dev`, the release bot cut a `1.2.x-dev` prerelease tag against an AGPL-declared tree, the CHANGELOG licensing note survived regeneration, `main` is untouched, and the user has confirmed the Actions state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `dev` -> `origin/dev` | first publication of this phase's merge, relicense, and 671 newly tracked project-record files |
| push -> `release.yml` | runs with `contents: write` via `RELEASE_TOKEN`; creates commits and tags autonomously |
| tag -> `publish.yml` -> PyPI | a full-release tag would publish the package under whatever license the tree declares |
| `src/` docstrings -> rendered public documentation | docstring edits made to satisfy `-W` change what users read |
| `-W` gate in `pyproject.toml` | the only automated control proving the docs are buildable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-22 | Tampering | published `origin/dev` history | mitigate | Task 3 uses a plain `git push origin dev`; `--force` and `--force-with-lease` are explicitly forbidden and acceptance asserts `git reflog origin/dev` records no forced update |
| T-108-23 | Elevation of Privilege | `release.yml` (`contents: write`, `RELEASE_TOKEN`) auto-tagging on push | mitigate | The push is deliberately sequenced last in the phase (wave 5, depending on every other plan) so the tag is cut against an already-relicensed tree; acceptance asserts the tagged `pyproject.toml` contains `AGPL-3.0-or-later` and no whole-word `MIT` |
| T-108-24 | Elevation of Privilege | `publish.yml` reaching PyPI | mitigate | Its trigger excludes prerelease tags; acceptance asserts no publish run was created and that the new tag matches `v1.2.*-dev.*` rather than a full release |
| T-108-25 | Tampering | the `-W` docs gate being weakened to force a pass | mitigate | Removing `-W`, adding `--no-fail-on-warning`, adding `suppress_warnings`, and adding `nitpicky` escapes are all explicitly forbidden; acceptance asserts the exact build-script string is intact and both escape-hatch greps return 0 |
| T-108-26 | Tampering | runtime behavior changed while "fixing docstrings" | mitigate | Task 2 restricts `src/` edits to docstring text and asserts the diff contains no changed `def`/`class`/`import`/`from`/`return`/decorator lines; `hatch run lint` must pass |
| T-108-27 | Information Disclosure | the detached docs env silently reinstalling the project and pulling ~5.8 GB CUDA torch, reintroducing the RTD failure | mitigate | Task 2 removes and re-provisions the `docs` env and requires inspecting the install output for torch/CUDA wheels, stopping rather than patching `pyproject.toml` |
| T-108-28 | Denial of Service | masking or entangling with the 8 known test failures | mitigate | Task 3's checkpoint explicitly states a red Tests run is expected and instructs the reviewer not to request fixes; no task touches `tests/` |
| T-108-36 | Tampering | publishing a tree in which 108-01's merge invariants have silently regressed | mitigate | Task 3 step 3 re-asserts, immediately before pushing, that `dev..main` is empty, that none of the purged paths reappeared, and that `docs/api/` still holds exactly 8 files; every earlier task in this plan carries the same purge assertion |
| T-108-SC | Tampering | pip installs into the docs env (sphinx, furo, myst-parser, sphinx-copybutton, sphinx-design, nbsphinx, sphinxcontrib-mermaid, numpy) | mitigate | The dependency set is unchanged from main's already-shipped, human-reviewed `[tool.hatch.envs.docs]` block — no new package is introduced by this plan, and the plan forbids editing `pyproject.toml`. If a new package ever becomes necessary it is `[ASSUMED]` and requires a blocking human legitimacy check before install |
</threat_model>

<verification>
- `hatch run docs:build` exits 0 from a clean detached env, twice (FOUND-01, D-14).
- The docs env installed no torch/CUDA wheels (D-15).
- `autodoc_mock_imports` includes `timm`, `pytorch_metric_learning`, `shapely`, `sklearn`; `numpy` is not mocked (D-15).
- `napoleon_use_ivar`, `_resolve_release()`, and 108-02's copyright line are intact (D-16, D-08).
- 108-01's four a66287a docstring repairs are still present.
- `docs/api/` still holds exactly 8 `.rst` files; no coverage was added (D-14).
- `-W` is still in the build script and no warning class is suppressed.
- Documentation workflow conclusion on the pushed `dev` commit is `success` (FOUND-02).
- The release tag is a `1.2.x-dev` prerelease cut against an AGPL-declared tree; no PyPI publish ran.
- `git log --oneline dev..main` is still empty, `origin/main` is unchanged, and no forced update occurred.
</verification>

<success_criteria>
- The Sphinx repair is fully forward-ported and the `-W` gate passes on `dev` from clean.
- Docs breakage is now caught at push, not at release.
- The `.rst` tree is green and honestly incomplete, handed to Phase 110 untouched.
- Nothing was force-pushed, nothing published to PyPI, `main` untouched, tests left red for Phase 109.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-05-SUMMARY.md` when done.
Record: the final `autodoc_mock_imports` list, the count and category of warnings fixed, the docs
env size, the resulting package version and prerelease tag, and each workflow's conclusion.
</output>
