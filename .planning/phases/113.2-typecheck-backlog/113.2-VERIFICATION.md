---
phase: 113.2-typecheck-backlog
verified: 2026-09-09T17:31:38Z
status: passed
score: 12/12 must-haves verified (4 ROADMAP criteria + 8 plan-06 truths; T-5 verified on intent with its letter-deviation escalated)
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Accept or reject the disclosed `pyproject.toml` 16-line diff against D-07's zero-line gate (version bump + `basedpyright==1.38.4` + `ultralytics>=8.0,<=8.4.28`)."
    expected: "Accepted. `[tool.basedpyright]` is byte-identical, the hatch script is byte-identical, and the two dependency edits tighten the gate (they are what made CI reproducible) rather than loosen it. The version bump is semantic-release automation, not authored work."
    why_human: "A gate said zero lines and the answer is sixteen. Whether a gate's intent may outrank its letter is a judgment call reserved to the developer, not a fact recoverable from the tree."
  - test: "Accept or reject the disclosed modification of `tests/unit/training/test_pseudo_label_cli.py` against a gate that permitted only the addition of `tests/unit/core/test_h5.py`."
    expected: "Accepted with a caveat. The change is unrelated to typing, the underlying platform-variance defect was filed (`todos/pending/2026-09-03-gap-fish-pseudo-label-emission-varies-by-platform.md`) rather than repaired, and it was required to obtain any CI signal at all. The caveat: it relaxes `assert len(lines) == 2` to `>= 1`, which un-pins the consensus+gap merge that Phase 113.1 plan `113.1-06` delivered and that `113.1-VERIFICATION.md` verified as passed."
    why_human: "This is a cross-phase interaction: 113.2 partially reverted a previously verified 113.1 deliverable. Whether that is acceptable, and whether 113.1 should be re-opened, is the developer's call."
  - test: "Correct Note B of `113.2-06-SUMMARY.md`. Verify independently: run the plan's own escape-hatch regex against `d3e6073` and against `HEAD`."
    expected: "Both yield 155, not 193. The plan's baseline of 155 was CORRECT; the summary's claim that it was 'a bad transcription' is itself the error. 193 is what the broader bare-`Any` regex yields (also unchanged, 193 -> 193). The summary's conclusion — no error closed by widening — holds under both metrics, but its account of which number is real is wrong, and it contradicts the durable todo `2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md`, which correctly records 155."
    why_human: "A factual correction to a completed SUMMARY is a documentation edit outside a verifier's remit; and the terminal audit's credibility is the developer's to weigh."
---

# Phase 113.2: Typecheck Backlog Verification Report

**Phase Goal:** `hatch run typecheck` exits 0, so the CI `typecheck` job passes and Phase 114's badge row can be honest
**Verified:** 2026-09-09T17:31:38Z
**Status:** passed (all three escalations resolved by the developer 2026-09-09)
**Re-verification:** No — initial verification

## Summary

**The phase achieved its goal, and achieved it the way it claimed to.** Every
gate that protects the honesty of the zero was re-measured independently
against `d3e6073` on this tree — not read out of a SUMMARY — and every one of
them holds byte-exact:

- `hatch run typecheck` -> `0 errors, 0 warnings, 0 notes`, **exit 0**, under
  the pinned `basedpyright 1.38.4` (version confirmed live, so this is not a
  stale-env green).
- `[tool.basedpyright]` is exactly `pythonVersion = "3.11"` +
  `typeCheckingMode = "basic"` and **nothing else** — no rule override, no
  `exclude`, no `ignore`.
- The hatch script is still `basedpyright src/ {args}`.
- `git diff d3e6073 -- .github/workflows/test.yml` is **0 lines**.
- Suppression directives in `src/aquapose/`: **71 -> 73**, and the `+2` is
  accounted for line-for-line by the two `# pyright: ignore[reportArgumentType]`
  directives in `training/reid_training.py`, each rule-scoped and each carrying
  a five-line inline attribution to `pytorch_metric_learning`'s unannotated
  `MultiSimilarityLoss.__init__`. No blanket `# type: ignore` was added anywhere.
- Escape-hatch `Any` type positions: **155 -> 155** (plan regex) and **193 ->
  193** (bare-`Any` regex). Unchanged under both metrics — nothing was closed
  by widening.
- `cast(` calls: **49 -> 49**.
- `hatch run test`: **1410 passed, 2 skipped, 17 deselected**, exit 0,
  re-executed live.
- CI run `33703999565` on `2c30f5c` (`dev`): job `typecheck` (`100489163999`)
  **success**, all 8 jobs green — confirmed via `gh run view`, not via the
  SUMMARY. `git diff 2c30f5c..HEAD -- src/ tests/ pyproject.toml .github/` is
  empty, so the CI-tested code is byte-identical to the tree verified here.

**The two deviations the SUMMARY discloses are real, are correctly
characterized, and neither undermines the goal.** They are escalated for
acceptance rather than waved through.

**One SUMMARY claim is wrong and I could not confirm it.** Note B asserts the
plan's `Any` baseline of 155 was a transcription error and the true figure is
193. Re-running the plan's own regex against `d3e6073` yields **155** — the
plan was right. The 193 figure comes from a different, broader regex. The
audit's *conclusion* survives, because both metrics are unchanged; its
*account* does not. This is the one place where the terminal audit is less
accurate than the plan it audits.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | `hatch run typecheck` exits 0 (from 98 errors) | VERIFIED | Live run: `0 errors, 0 warnings, 0 notes`, `EXIT=0`, under `basedpyright 1.38.4` (the pinned version, confirmed by `hatch run basedpyright --version`) |
| SC-2 | CI `typecheck` job passes on `dev` | VERIFIED | `gh run view 33703999565` -> job `typecheck` id `100489163999` **success**; all 8 jobs success on `2c30f5c` (dev). Tree parity confirmed: `git diff 2c30f5c..HEAD -- src/ tests/ pyproject.toml .github/` empty |
| SC-3 | Fixes at root cause, grouped by cluster — not blanket `# type: ignore` | VERIFIED | `git diff --stat d3e6073 -- src/` = 10 files, 180 insertions. The h5py cluster is closed structurally by a new shared narrowing helper (`core/h5.py`, 79 lines) imported at 4 call sites; the reid cluster by read-only protocol properties; the tail by a `TypedDict`, a redeclaration removal, and a runtime `click.BadParameter` narrow. Exactly 2 suppressions repo-wide, both rule-scoped |
| SC-4 | A decision is recorded on whether `typeCheckingMode` stays `basic` | VERIFIED | `.planning/PROJECT.md:336` carries the D-07 row in Decision/Rationale/Outcome shape; deferred evaluation filed as `todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md` (substantive — names the four rule families that would resurface and prescribes re-measurement first) |
| T-1 | typecheck reports `0 errors, 0 warnings, 0 notes` | VERIFIED | as SC-1 |
| T-2 | Zero reached by fixing code, not weakening the checker | VERIFIED | `[tool.basedpyright]` = `pythonVersion="3.11"` + `typeCheckingMode="basic"`, nothing else; script still `basedpyright src/ {args}`; workflow diff 0 lines |
| T-3 | Exactly 2 suppressions added (71 -> 73), both in `reid_training.py`, rule-scoped, attributed | VERIFIED | Counted 73 current / 71 at `d3e6073` (baseline extracted via `git archive d3e6073`, measured with the identical command). `git diff d3e6073 -- src/aquapose/training/reid_training.py` shows exactly the two `# pyright: ignore[reportArgumentType]` additions at lines 480 and 821, each with a 5-line rationale comment. The other 4 directives in that file are pre-existing |
| T-4 | `Any` type positions unchanged | VERIFIED (the truth as written is right; the SUMMARY's number is wrong) | Plan regex: **155 -> 155**. Broader bare-`Any`: **193 -> 193**. Unchanged either way. See Warning W-1 |
| T-5 | Suite unchanged; the only `tests/` change is the addition of `test_h5.py` | VERIFIED ON INTENT — LETTER DEVIATES | Suite: `1410 passed, 2 skipped, 17 deselected` (not `1403/3`; the +7 are `test_h5.py`, the -1 skip is a now-passing test). `tests/` diff = `A test_h5.py` **plus** `M test_pseudo_label_cli.py`. The intent clause — "no existing test edited, weakened, skipped or deleted **to accommodate a type change**" — holds: the edit is `00e670e`, unrelated to typing. Escalated |
| T-6 | CI `typecheck` job unchanged and passing on `dev` | VERIFIED | 0-line workflow diff + green job, as SC-2 |
| T-7 | D-07 recorded, not acted on | VERIFIED | PROJECT.md row + pending todo; `typeCheckingMode` untouched in the tree |
| T-8 | Every plan 01-05 finding recorded in a durable artifact | VERIFIED | `todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md` exists and is absent from `pending/`; `todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md` and `.../2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md` exist; `.../2026-09-03-gap-fish-pseudo-label-emission-varies-by-platform.md` exists and is a genuinely substantive defect report (CI matrix table, three named investigation entry points, an explicit "do not pin the test to the local count" instruction, and a terminal gate) |

**Score:** 12/12 truths verified (T-5 on intent, with its letter-deviation escalated).

### Prohibitions

| # | Prohibition | Status | Evidence |
|---|-------------|--------|----------|
| P-1 | No reaching zero by suppressing/weakening the checker | UPHELD | Four independent byte-exact checks: config, script, CI job, suppression delta. Widening also ruled out (`Any` and `cast(` both flat). This is the load-bearing prohibition and it is clean |
| P-2 | No runtime behavior change under cover of a typing fix; no test weakened to accommodate one | UPHELD | Read every `src/` diff hunk. All are annotation-, narrowing- or protocol-level. The one apparent runtime addition — `click.BadParameter` for `--mode` in `core/reid/cli.py:579` — is **unreachable in practice**: the option is already `type=click.Choice(["seeded", "scan"])` at line 536, so the guard is defensive narrowing, not a behavior change. `swap_detector.py` reads go from `grp["x"][()]` to `require_dataset(grp, "x")[()]`, semantically identical on the success path. The one test edit is not a typing accommodation (see T-5) |
| P-3 | No silently repairing a latent defect that correct typing reveals | UPHELD, and exercised twice | (a) The unreachable `if points is None` guard at `stitching.py:563` was folded into the h5py-idiom todo, not fixed under cover. (b) The platform-varying gap-fish emission was filed as its own todo with the explicit instruction not to pin the test to a local count — exactly the behavior this prohibition demands. (c) `swap_detector.py` line-310 was adjudicated a narrowing artifact with the verdict written down |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/aquapose/core/h5.py` | Narrowing helper raising a real exception naming key + file, no bare `assert` (D-03) | VERIFIED | 79 lines, two functions. `require_dataset`/`require_group` do a runtime `isinstance` check and raise `TypeError(f"Expected {key!r} to be an h5py.X in {parent.file.filename!r}, found {type(obj).__name__} instead.")` — key AND file both named, actual type reported. **No `assert` anywhere in the file.** Module docstring states the `python -O` reasoning for rejecting `assert` and justifies the `core/` placement against the import-boundary guard |
| — wiring | Genuinely imported and used at the claimed call sites | VERIFIED | `reid/runner.py` (import + 5 refs), `reid/swap_detector.py` (import + 3), `core/stitching.py` (import + 6), `evaluation/stages/smoothing.py` (2 function-local imports + 12 refs). Not orphaned |
| `tests/unit/core/test_h5.py` | Real coverage of the helper | VERIFIED | 102 lines, 7 tests: identity on both helpers, `TypeError` in both mismatch directions *with assertions that the message contains the key and the filename*, `KeyError` propagation, slash-path resolution on both. Not a smoke test |
| `todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md` | Closed with accurate note | VERIFIED | present in `done/`, absent from `pending/` |
| `todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md` | D-07 deferred decision | VERIFIED | present, substantive |
| `todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md` | Two-idiom finding | VERIFIED | present |
| `.planning/PROJECT.md` D-07 row | Decision on record | VERIFIED | line 336 |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `pyproject.toml` `typecheck` script | `.github/workflows/test.yml` `typecheck` job | Job runs `hatch run typecheck` verbatim | WIRED | Both sides byte-identical to `d3e6073`. Local green and CI green are now the same claim — and are additionally made reproducible by the `basedpyright==1.38.4` exact pin, which is the fix for the divergence that broke this link once |
| `hatch run typecheck` exit 0 | Phase 114 README-02 badge row | QA-06 completion | WIRED | `REQUIREMENTS.md:31` `[x] QA-06`; traceability row at line 120 cites CI run `33703999565`. Phase 114 is unblocked on an earned green |
| plans 01-05 findings | `.planning/todos/` | Durable filing | WIRED | 4 todos verified on disk; the one finding with no todo (swap_detector line-310) is correctly so — its verdict was "narrowing artifact, not a live defect" |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Typecheck gate is green | `hatch run typecheck` | `0 errors, 0 warnings, 0 notes`, `EXIT=0` | PASS |
| The green is under the pinned checker, not a stale env | `hatch run basedpyright --version` | `basedpyright 1.38.4` (matches the `==1.38.4` pin) | PASS |
| Suite is green and unweakened | `hatch run test` | `1410 passed, 2 skipped, 17 deselected` in 55s, `EXIT=0` | PASS |
| Suppression delta is exactly +2 | suppression regex on tree vs `git archive d3e6073` extraction | 73 vs 71 | PASS |
| No widening | same technique, `Any` regexes | 155 vs 155 (and 193 vs 193) | PASS |
| No cast inflation | same technique, `cast(` | 49 vs 49 | PASS |
| CI job green on `dev` | `gh run view 33703999565` | job `typecheck` `100489163999` success; 8/8 jobs success | PASS |
| CI-tested code == verified tree | `git diff 2c30f5c..HEAD -- src/ tests/ pyproject.toml .github/` | empty | PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist in this repo and neither the PLANs nor the
SUMMARYs declare a probe. The phase's runnable gates are `hatch run typecheck`
and `hatch run test`, both executed above under Behavioral Spot-Checks. Not
skipped silently — there is nothing to run.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| QA-06 | 113.2-01..06 | `hatch run typecheck` exits 0 and the CI `typecheck` job passes, so the README-02 badge row can honestly claim green CI | SATISFIED | Both halves independently re-executed: local exit 0 under the pinned checker; CI job `100489163999` green on `dev`. Marked `[x]` at `REQUIREMENTS.md:31` with the run id recorded at line 120 |

No orphaned requirements: `REQUIREMENTS.md` maps only QA-06 to phase 113.2.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` scan across all 11 files touched by the phase | — | **None found.** Debt-marker gate clean |
| `src/aquapose/core/reid/cli.py` | 579-582 | Unreachable defensive branch (`click.Choice` already rejects the value) | Info | Costs three lines and can never fire from the CLI. Chosen over `cast` deliberately — the safer of the two idioms, and it survives if the `Choice` is ever loosened |

### Deviations Audited (not rubber-stamped)

**D-A. `pyproject.toml` 16-line diff against D-07's zero-line gate — DISCLOSURE HONEST, GOAL NOT UNDERMINED.**
I read the full diff. It is exactly three things and nothing else: the
semantic-release version bump `1.2.0-dev.3 -> 1.2.0-dev.6`, `basedpyright` ->
`basedpyright==1.38.4`, and `ultralytics>=8.0` -> `ultralytics>=8.0,<=8.4.28`.
Both dependency edits carry inline comments giving the reason and the condition
for raising them. **`[tool.basedpyright]` is untouched** and `typeCheckingMode`
is still `basic` — the thing D-07 was actually protecting is intact, which I
verified by reading the section, not by trusting the claim. The direction of the
change is toward a *stricter, reproducible* gate: an exactly pinned checker is
the reason the local `0 errors` I just reproduced means the same thing as the CI
`0 errors`. The SUMMARY's framing ("the gate's letter is broken, the gate's
intent is intact") is accurate. Escalated for acceptance because a gate that
says zero lines and gets sixteen deserves a human signature, not a verifier's.

**D-B. `tests/unit/training/test_pseudo_label_cli.py` modified against a one-file gate — DISCLOSURE HONEST, WITH ONE OMISSION.**
The diff relaxes `assert len(lines) == 2` to `assert len(lines) >= 1` and
replaces the old comment with an eleven-line explanation naming the tracking
todo. Judgment on each axis:

- *Is it a typing accommodation?* No. Commit `00e670e`, no `src/` change, and
  the failure it responds to is a platform/interpreter variance in gap-fish
  label emission with every input mocked.
- *Was the underlying defect silently repaired?* No — and this is the strongest
  evidence the phase respected its own prohibitions. The todo
  `2026-09-03-gap-fish-pseudo-label-emission-varies-by-platform.md` carries the
  six-job CI matrix, the failing assertion text, three named investigation entry
  points in `pseudo_label_cli.py`, an explicit "diagnose first; do not pin the
  test to whichever count the local machine produces," and a terminal gate
  restoring an exact count. That is the surface-don't-absorb behavior P-2 and
  P-3 demand.
- *Was it necessary?* Yes. Criterion 2 requires a green CI run on `dev`; the
  `test` jobs gate the workflow, so no `typecheck` signal was obtainable while
  four of six matrix jobs failed on this assertion.
- **The omission:** the SUMMARY does not say that the `len(lines) == 2` pin was
  a *Phase 113.1 deliverable* — `113.1-06-PLAN.md` is described in ROADMAP as
  the "per-line OBB label assertion that also pins the consensus+gap merge,"
  and `113.1-VERIFICATION.md` (status: passed) recorded that pin as part of a
  closed gap. So 113.2 partially reverted a previously verified sibling-phase
  deliverable. The todo *does* state this lineage plainly and even credits the
  over-strict assertion for exposing the variance — so the fact is on the
  record; it is the SUMMARY's own deviation note that under-reports the
  cross-phase consequence. Escalated.

### Warnings

**W-1 (documentation accuracy, terminal audit). `113.2-06-SUMMARY.md` Note B is wrong, and it is wrong in the one artifact whose job is measurement accuracy.**
It claims: "Running the plan's own regex against `d3e6073` yields **193**... The
155 figure appears nowhere in a measurement and should be read as an authoring
error in `113.2-06-PLAN.md`." I extracted `src/aquapose` at `d3e6073` via
`git archive` and ran the plan's regex verbatim against both trees:

| Regex | `d3e6073` | HEAD |
|---|---|---|
| the plan's four-alternative type-position regex | **155** | **155** |
| broader bare-`Any` word match | 193 | 193 |

The plan's 155 was **correct**, not a transcription error; 193 belongs to a
different regex. Corroborating: the durable D-07 todo written by this same plan
records "The 155 type-position `Any` escape-hatch annotations... measured
2026-09-02, unchanged by Phase 113.2" — so the phase's own permanent artifact
contradicts its SUMMARY. Materially this changes nothing: the count is unchanged
under both metrics, so no typecheck error was closed by widening, and truth T-4
passes. But a terminal audit that accuses its own plan of a transcription error,
on the basis of an unlabeled regex substitution, weakens the audit's authority.
Recommend correcting Note B rather than leaving it.

**W-2 (stale cross-reference).** `.planning/phases/113-concepts-tutorial/deferred-items.md:28`
still states the test "pins `len(lines) == 2` so the merge itself is covered."
That has not been true since `00e670e`. One-line doc drift, no functional impact.

**W-3 (process, self-disclosed).** Plan 06 Task 1's audit was performed on
2026-09-03 but its reconciliation table was not written to disk until
2026-09-09 — six days in which the phase's terminal gate existed only as
executed actions. The SUMMARY discloses this itself. Noted because it is exactly
the class of gap this verification exists to catch, and it was caught by the
author first.

### Human Verification Required

Three items, all decisions rather than tests — see frontmatter for the full
statements:

1. **Accept the `pyproject.toml` 16-line deviation** (D-A). My recommendation:
   accept. Intent verified byte-exact; direction is tightening, not loosening.
2. **Accept the `test_pseudo_label_cli.py` deviation** (D-B), and decide whether
   the partial reversion of a verified 113.1 deliverable warrants a note against
   Phase 113.1. My recommendation: accept the test change; add the cross-phase
   note.
3. **Correct Note B of `113.2-06-SUMMARY.md`** (W-1). My recommendation:
   correct it — restore 155 as the real baseline and label 193 as the broader
   regex. The audit's conclusion stands either way.

None of the three blocks Phase 114.

### Gaps Summary

**No gaps.** The phase goal is achieved and, more importantly, achieved
honestly: 98 -> 0 errors with an untouched `typeCheckingMode`, an untouched
hatch script, a 0-line CI-workflow diff, a flat `Any` count, a flat `cast(`
count, and a suppression budget of exactly two — both rule-scoped, both
attributed inline to a specific third-party inference defect, both accounted for
line-for-line in the diff. The CI `typecheck` job is green on `dev` for the
first time, on code byte-identical to the tree verified here, and the green is
reproducible because the gate itself is now pinned.

The adversarial hypothesis for this phase — that zero was bought by silencing
the checker — is falsified on every axis I could measure.

What remains is not a gap but a signature: two disclosed gate-letter deviations
that need a human accept/reject, and one incorrect measurement claim in the
terminal audit that should be corrected.  `status: human_needed` reflects those
decisions, not an unverified behavior.

---

---

## Escalation Resolutions (developer, 2026-09-09)

All three items under `human_verification` are resolved. Status moves from
`human_needed` to `passed`.

1. **`pyproject.toml` 16-line diff vs D-07's zero-line gate — ACCEPTED.**
   The developer accepts the gate's intent over its letter. `[tool.basedpyright]`
   and the hatch script are byte-identical, and the two dependency edits
   (`basedpyright==1.38.4` exact, `ultralytics>=8.0,<=8.4.28` bounded) are what
   made the CI result reproducible rather than incidental. The version bump is
   semantic-release automation.

2. **`test_pseudo_label_cli.py` modified vs the one-file `tests/` gate — ACCEPTED.**
   The developer accepts the change. The relaxation un-pins a Phase 113.1
   deliverable (`113.1-06`, verified in `113.1-VERIFICATION.md`), and that
   cross-phase lineage is now recorded in `113.2-06-SUMMARY.md` note C and in
   `.planning/phases/113-concepts-tutorial/deferred-items.md`. **113.1 is NOT
   re-opened:** the pin encoded an expectation the code does not actually meet
   across platforms, so the defect is in the code, not in 113.1's work, and it
   is filed as
   `.planning/todos/pending/2026-09-03-gap-fish-pseudo-label-emission-varies-by-platform.md`.

3. **Note B of `113.2-06-SUMMARY.md` — CORRECTED** (commit `ca508a9`), before
   this resolution was recorded. 155 is restored as the real baseline under the
   plan's own regex (155 -> 155); 193 is labelled as the broader bare-`Any`
   match (193 -> 193). The error is left visible in the summary rather than
   overwritten.

---

*Verified: 2026-09-09T17:31:38Z*
*Verifier: Claude (gsd-verifier)*
*Escalations resolved: 2026-09-09 (developer)*
