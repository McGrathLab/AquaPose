---
phase: 108-branch-reconciliation-repo-hygiene
plan: 04
type: execute
wave: 3
depends_on: ["108-03"]
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements: [REC-01]

must_haves:
  truths:
    - "REC-01/D-21: `.planning/MILESTONES.md` contains a v3.11 Appearance-Based ReID entry reconstructed from ROADMAP.md and `phases/102-107/`."
    - "D-21: `.planning/MILESTONES.md` also contains a v2.2 Backends entry — the same class of gap, reconstructed the same way."
    - "D-21: the file's entries read newest-first by ship date with no out-of-order tail (v1.0 no longer sits between v3.0 and v2.0)."
    - "D-22: both new entries use the existing heading and field schema — no richer schema was invented."
    - "D-21: no `(none recorded)` accomplishment list on an older entry was filled in."
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "complete, chronologically ordered milestone record"
      contains: "## v3.11 Appearance-Based ReID (Shipped:"
  key_links:
    - from: ".planning/MILESTONES.md"
      to: ".planning/ROADMAP.md"
      via: "phase ranges and ship dates agree between the two files"
      pattern: "Phases 102-107"
---

<objective>
Backfill the two missing MILESTONES.md entries — **v3.11 Appearance-Based ReID** (REC-01) and
**v2.2 Backends** (D-21, same class of gap) — reconstructed from ROADMAP.md and the phase
directories, and re-sort the broken tail into chronological order.

Purpose: satisfies REC-01. v3.11 was in progress when `.planning/` was untracked on `dev`, so it
never got an entry; v2.2 has the same gap. The record is fully reconstructable from
`.planning/ROADMAP.md` and `.planning/phases/`.

Output: `.planning/MILESTONES.md` with 16 entries in strict newest-first ship-date order.

Scope fence: this plan edits exactly one file. Do not fill in `(none recorded)` accomplishment
lists on older entries (D-21 — archaeology across 14 milestones, explicitly deferred). Do not
invent a richer entry schema (D-22). **Do not push** — the phase's single push happens in
plan 108-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md

**Pre-verified facts — do not re-derive:**

Present entries and their current file order (top to bottom): v3.10, v3.9, v3.8, v3.7, v3.6,
v3.5, v3.4, v3.3, v3.2, v3.1, v3.0, **v1.0, v2.0, v2.1** — the last three are out of order.
Missing: **v2.2 Backends** and **v3.11 Appearance-Based ReID**.

Ship dates (from `.planning/ROADMAP.md` lines 8 and 20, and the existing headings):
- v3.11 Appearance-Based ReID — Phases **102-107** — shipped **2026-03-26**
- v3.10 — 2026-03-15 · v3.1 — 2026-03-03 · v3.0 — 2026-03-02
- v2.2 Backends — Phases **29-33.1** — shipped **2026-03-01**
- v2.1 Identity — 2026-02-28 · v2.0 Alpha — 2026-02-27 · v1.0 MVP — 2026-02-25

Target newest-first order: v3.11, v3.10, v3.9, v3.8, v3.7, v3.6, v3.5, v3.4, v3.3, v3.2, v3.1,
v3.0, **v2.2, v2.1, v2.0, v1.0**.

Source material:
- `.planning/ROADMAP.md` line 267 — `<summary>✅ v3.11 Appearance-Based ReID (Phases 102-107) — SHIPPED 2026-03-26</summary>` and the collapsed block beneath it (phase list, goals, success criteria, plan counts).
- `.planning/ROADMAP.md` line 82 — `<summary>✅ v2.2 Backends (Phases 29-33.1) — SHIPPED 2026-03-01</summary>` and its block.
- Phase directories on disk: `.planning/phases/102-embedding-infrastructure`,
  `103-training-data-mining`, `104-backbone-fine-tuning`, `105-swap-detection-and-repair`,
  `106-cli-integration`, `107-unfrozen-backbone-fine-tuning` — each holds `*-PLAN.md`,
  `*-SUMMARY.md`, and some hold `*-VERIFICATION.md` / `*-CONTEXT.md` / `*-RESEARCH.md`.
- `.planning/phases/29-*` … `33*` for v2.2.

**Entry schema to match exactly (D-22)** — taken from the existing v3.10 and v3.9 entries:

- `## <version> <Name> (Shipped: YYYY-MM-DD)` heading
- `**Phases completed:** N phases (RANGE), M plans, K tasks`
- `**Timeline:** N days (YYYY-MM-DD → YYYY-MM-DD)` *(include only if derivable)*
- `**Codebase:** N LOC source` *(include only if derivable)*
- `**Git range:** N commits, M files changed (+A / -B)` *(include only if derivable)*
- `**Key accomplishments:**` followed by a numbered list
- `**Delivered:**` one-paragraph summary
- `**Known gaps (accepted as tech debt):**` bulleted list *(optional)*
- `---` separator

Omit a field rather than fabricate a number for it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill the v3.11 Appearance-Based ReID entry</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` lines 1–50 — the `# Milestones` heading and the v3.10 entry,
      which is the **format source of truth** for the new entry (D-22).
    - `.planning/ROADMAP.md` starting at line 267 — the `v3.11 Appearance-Based ReID (Phases 102-107)`
      collapsed block: phase list, per-phase goals, success criteria, and plan counts.
    - Every `*-SUMMARY.md` under `.planning/phases/10{2,3,4,5,6,7}-*` — the accomplishment source.
    - Every `*-VERIFICATION.md` under those directories — the source for the `Known gaps` list.
  </read_first>
  <action>
    Insert a new entry immediately after the `# Milestones` heading and before the existing
    `## v3.10 Publication Metrics` entry — v3.11 shipped 2026-03-26, later than v3.10's 2026-03-15,
    so it is the newest and belongs at the top.

    Heading, verbatim: `## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)`

    Populate the fields using the schema above:
    - `**Phases completed:**` — count the six directories `102`–`107` and count the actual
      `*-PLAN.md` files across them for the plan count. Count tasks by counting `<task` elements
      across those PLAN files, or omit the task count if the PLAN files are not all present
      (103 has SUMMARYs but no PLANs on disk).
    - `**Timeline:**` — derive the span from the first and last commit dates touching
      `.planning/phases/10[2-7]-*` (`git log --format=%ad --date=short -- <paths> | tail -1`
      and `| head -1`). Omit if the range is not derivable.
    - `**Codebase:**` and `**Git range:**` — derive if cheap, otherwise omit. Do not fabricate.
    - `**Key accomplishments:**` — 5–7 numbered items drawn from the phase SUMMARYs: embedding
      infrastructure, training-data mining, backbone fine-tuning (frozen and unfrozen),
      swap detection and repair, and CLI integration. Each item must be a concrete, specific
      claim traceable to a SUMMARY — not a restatement of the phase title.
    - `**Delivered:**` — one paragraph describing what the milestone shipped overall.
    - `**Known gaps (accepted as tech debt):**` — only if the VERIFICATION files record real
      gaps; omit the section otherwise.
    - Close with a `---` separator line.

    Do not modify any existing entry in this task. Do not fill `(none recorded)` lists (D-21).

    Commit as `docs(108-04): backfill v3.11 Appearance-Based ReID milestone entry`. Do not push.
  </action>
  <verify>
    <automated>grep -q '^## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)$' .planning/MILESTONES.md &amp;&amp; test "$(grep -n '^## v3.11' .planning/MILESTONES.md | cut -d: -f1)" -lt "$(grep -n '^## v3.10' .planning/MILESTONES.md | cut -d: -f1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)$' .planning/MILESTONES.md` returns `1`.
    - The line number of `^## v3.11` is greater than 1 and strictly less than the line number of `^## v3.10`.
    - Within the v3.11 section, `grep -c '^\*\*Phases completed:\*\*'` returns `1` and the value contains `102-107`.
    - Within the v3.11 section, `grep -c '^\*\*Key accomplishments:\*\*'` returns `1`, followed by at least 5 numbered list items.
    - Within the v3.11 section, `grep -c '^\*\*Delivered:\*\*'` returns `1`.
    - The v3.11 section ends with a line that is exactly `---`.
    - `grep -c '(none recorded)' .planning/MILESTONES.md` is unchanged from its pre-task value.
    - `git diff HEAD~1 --stat -- .planning/MILESTONES.md` shows insertions only (`grep -c '^-[^-]'` on the diff returns `0`).
  </acceptance_criteria>
  <done>`.planning/MILESTONES.md` opens with a v3.11 Appearance-Based ReID entry in the existing schema, positioned above v3.10, with 5+ specific accomplishments traceable to `phases/102-107/` SUMMARYs.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill the v2.2 Backends entry</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` — the v3.11 entry you just wrote and the existing v3.0 and v2.1
      entries (v2.1 is the nearest-in-era format analog and is a sparser entry).
    - `.planning/ROADMAP.md` starting at line 82 — the `v2.2 Backends (Phases 29-33.1)` collapsed
      block.
    - `ls .planning/phases/` filtered to the `29`–`33` range, and every `*-SUMMARY.md` inside
      those directories.
  </read_first>
  <action>
    Insert a v2.2 entry **between** the existing `## v3.0 Ultralytics Unification (Shipped: 2026-03-02)`
    entry and the next entry below it. v2.2 shipped 2026-03-01, so newest-first places it directly
    after v3.0.

    Heading, verbatim: `## v2.2 Backends (Shipped: 2026-03-01)`

    Same schema and same omit-rather-than-fabricate rule as task 1. `**Phases completed:**` must
    reference the range `29-33.1`. Accomplishments come from the ROADMAP block and the phase
    SUMMARYs in that range; if the phase directories are sparse, a shorter accomplishment list is
    acceptable — match what v2.1's entry does rather than padding.

    Do not modify any existing entry. Do not fill `(none recorded)` lists (D-21).

    Commit as `docs(108-04): backfill v2.2 Backends milestone entry`. Do not push.
  </action>
  <verify>
    <automated>grep -q '^## v2.2 Backends (Shipped: 2026-03-01)$' .planning/MILESTONES.md &amp;&amp; test "$(grep -n '^## v3.0' .planning/MILESTONES.md | cut -d: -f1)" -lt "$(grep -n '^## v2.2' .planning/MILESTONES.md | cut -d: -f1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## v2.2 Backends (Shipped: 2026-03-01)$' .planning/MILESTONES.md` returns `1`.
    - The line number of `^## v3.0` is strictly less than the line number of `^## v2.2`.
    - Within the v2.2 section, `grep -c '^\*\*Phases completed:\*\*'` returns `1` and the value contains `29-33.1`.
    - Within the v2.2 section, `grep -c '^\*\*Key accomplishments:\*\*'` returns `1` with at least 3 numbered items.
    - The v2.2 section ends with a line that is exactly `---`.
    - `grep -c '^## v' .planning/MILESTONES.md` returns `16`.
    - `git diff HEAD~1 -- .planning/MILESTONES.md | grep -c '^-[^-]'` returns `0`.
  </acceptance_criteria>
  <done>A v2.2 Backends entry in the existing schema sits directly below v3.0, referencing phases 29-33.1, bringing the file to 16 milestone entries.</done>
</task>

<task type="auto">
  <name>Task 3: Re-sort the tail into chronological order</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` in full — specifically every `^## v` heading and its
      `(Shipped: YYYY-MM-DD)` date, which is the sort key.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` § D-21 — the
      re-sort mandate and its explicit exclusion of `(none recorded)` archaeology.
  </read_first>
  <action>
    Move whole entry blocks (heading through the trailing `---`) so that the file reads strictly
    newest-first by the `(Shipped: …)` date. Only the tail is currently wrong: the block order
    after v3.0 must become **v2.2, v2.1, v2.0, v1.0** (currently v1.0, v2.0, v2.1 with v2.2 newly
    inserted above them).

    Target full order, top to bottom:
    v3.11, v3.10, v3.9, v3.8, v3.7, v3.6, v3.5, v3.4, v3.3, v3.2, v3.1, v3.0, v2.2, v2.1, v2.0, v1.0.

    This is a pure block move. Do not reword, reformat, add to, or trim the content of any entry
    you are moving — in particular do not fill v2.1's `(none recorded)` accomplishment list
    (D-21). Verify by diffing: the set of non-heading lines before and after must be identical,
    only their order changes.

    Normalize separators so every entry is followed by exactly one `---` line and one blank line,
    matching the existing style, and confirm the file still opens with `# Milestones`.

    Commit as `docs(108-04): re-sort MILESTONES.md into chronological order`. Do not push.
  </action>
  <verify>
    <automated>grep -oP '^## v\S+ .*\(Shipped: \K[0-9-]+' .planning/MILESTONES.md > /tmp/ms_dates.txt &amp;&amp; sort -r /tmp/ms_dates.txt | diff -q - /tmp/ms_dates.txt</automated>
  </verify>
  <acceptance_criteria>
    - Extracting the ship dates in file order (`grep -oP '^## v\S+ .*\(Shipped: \K[0-9-]+' .planning/MILESTONES.md`) yields a list that is already in descending order — `sort -r` of it `diff`s clean against itself.
    - `grep -c '^## v' .planning/MILESTONES.md` returns `16`.
    - `grep -oP '^## \Kv[0-9.]+' .planning/MILESTONES.md | tail -4 | tr '\n' ' '` returns `v2.2 v2.1 v2.0 v1.0 `.
    - `head -1 .planning/MILESTONES.md` is exactly `# Milestones`.
    - `sort .planning/MILESTONES.md` is byte-identical to `sort` of the previous commit's version (`git show HEAD~1:.planning/MILESTONES.md | sort`) — proving the re-sort moved lines without editing any.
    - `grep -c '(none recorded)' .planning/MILESTONES.md` is unchanged from its value at the start of this plan.
    - `git status --porcelain .planning/MILESTONES.md` outputs zero lines.
  </acceptance_criteria>
  <done>All 16 milestone entries appear in strict descending ship-date order, with entry content byte-preserved and no `(none recorded)` list touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconstructed history → the permanent project record | MILESTONES.md is the durable narrative other phases and the eventual publication cite; a fabricated number is indistinguishable from a measured one |
| bulk block re-ordering → existing entry content | a whole-file rewrite can silently drop or alter entries a reader will never re-verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-18 | Spoofing | fabricated metrics in a reconstructed milestone entry | mitigate | The schema instruction is explicit: derive `Timeline`/`Codebase`/`Git range` from `git log`/`git diff --stat`, and **omit the field rather than fabricate**; accomplishments must be traceable to a named `*-SUMMARY.md` |
| T-108-19 | Tampering | accidental loss of existing entries during the block re-sort | mitigate | Task 3's acceptance criterion diffs `sort` of the file against `sort` of `HEAD~1`'s version, which fails on any added, dropped, or edited line; tasks 1 and 2 assert insertion-only diffs |
| T-108-20 | Repudiation | scope creep into `(none recorded)` archaeology | mitigate | `grep -c '(none recorded)'` is asserted unchanged in tasks 1 and 3 |
| T-108-21 | Information Disclosure | milestone prose leaking private paths or credentials | accept | Content is reconstructed from `.planning/` files that plan 108-03 already scanned with detect-secrets before tracking them; no new external source is introduced |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages and runs no build; it edits one markdown file |
</threat_model>

<verification>
- `.planning/MILESTONES.md` contains `## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)` (REC-01).
- It contains `## v2.2 Backends (Shipped: 2026-03-01)` (D-21).
- Ship dates in file order are strictly descending (D-21).
- Entry count is 16 and both new entries use the existing schema (D-22).
- `sort` of the file matches `sort` of the pre-re-sort version (no content altered).
- `(none recorded)` count unchanged (D-21 exclusion respected).
- Nothing pushed; no file other than `.planning/MILESTONES.md` modified.
</verification>

<success_criteria>
- The milestone record has no missing milestone and no out-of-order entry.
- Every number in the two new entries is either measured or absent.
- The diff touches exactly one file.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-04-SUMMARY.md` when done.
Record which schema fields were omitted from each new entry and why.
</output>
