---
phase: 108-branch-reconciliation-repo-hygiene
plan: 03
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - .gitignore
  - tests/integration/segmentation/__init__.py
  - .planning/
autonomous: false
requirements: [FOUND-04]

must_haves:
  truths:
    - "D-18: `.planning/` is tracked normally — `git check-ignore .planning/STATE.md` exits non-zero and GSD commits no longer need `git add -f`."
    - "D-18: the full project record (622 markdown files) is in git history, not just the 33 previously force-added files."
    - "D-20: the dead `tests/integration/segmentation/` package is gone, and no other module was swept."
    - "D-19/FOUND-04: `git status --porcelain` on `dev` is empty — no `11.0`, no `~/` directory."
    - "OI-02: the SAM2 clone under `~/PycharmProjects/sam2` — including its `checkpoints/` model weights — was moved aside with explicit user confirmation, not destroyed silently."
    - "D-19: no defensive `.gitignore` entries were added for `~/` or pip-redirect logs."
    - "FOUND-04/ROADMAP criterion 4: the leftover empty `src/aquapose/{reconstruction,segmentation,tracking,visualization}` directories — which hold only `__pycache__` and are what criterion 4 actually refers to — are gone from the working tree, while the live `core/` modules of the same names remain."
    - "`.planning/` contains no credentials — detect-secrets passes over the newly tracked files."
  artifacts:
    - path: ".gitignore"
      provides: "build-artifact coverage without the .planning/ contradiction"
      contains: "runs/"
    - path: ".planning/MILESTONES.md"
      provides: "a tracked, non-force-added project record"
  key_links:
    - from: ".gitignore"
      to: ".planning/"
      via: "absence of an ignore rule"
      pattern: "git check-ignore .planning/STATE.md -> exit 1"
    - from: "pyproject.toml [tool.hatch.build.targets.sdist]"
      to: ".planning/"
      via: "sdist exclude — what makes un-ignoring safe"
      pattern: "exclude = \\[\".planning/\""
---

<objective>
Resolve the `.gitignore`/tracked-files contradiction by un-ignoring `.planning/` (D-18), delete
the two genuinely dead artifacts (D-20), and clear the working tree of `11.0` and the stray `~/`
directory (D-19), with an explicit user gate before touching the SAM2 model weights (OI-02).

Purpose: satisfies FOUND-04. **Note that ROADMAP.md criterion 4 is factually wrong** — `git
ls-files` already tracks no `runs/`, no `tmp/`, no `yolo26n*.pt`, and no vendored SAM2, and
`.gitignore` already covers all of them (`tmp/` :94, `*.pt` :97, `runs/` :99). A fresh clone of
`dev` is already clean. The same criterion's "dead `reconstruction`/`segmentation`/`tracking`
directories" resolve to four *untracked, `__pycache__`-only* leftovers at the **top level** of
`src/aquapose/` — those are removed here. The same-named packages under
`src/aquapose/core/` are **live production code** and must not be touched.

Output: `.gitignore` without the `.planning/` rule, the full `.planning/` tree committed, the
empty `tests/integration/segmentation/` package deleted, the four stale top-level `src/aquapose/`
directories gone, and a clean `git status`.

**Do not edit `pyproject.toml`** — plan 108-02 owns it this wave. **Do not push** — the phase's
single push happens in plan 108-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md

**Pre-verified facts — do not re-derive:**

- `.gitignore` line 100 is `.planning/`. Lines 94/97/99 are `tmp/`, `*.pt`, `runs/` and stay.
- `git ls-files .planning | wc -l` = **33**. `find .planning -type f | wc -l` = **622**.
  Un-ignoring surfaces ~589 new files.
- `.planning/` is 7.0 MB and contains only `.md` (602), `.gitkeep` (17), `.txt` (1), `.svg` (1),
  and `.json` (1). Nothing over 200 KB. No binaries.
- `pyproject.toml:57` already has `exclude = [".planning/", ".claude/", ".hooks/"]` under
  `[tool.hatch.build.targets.sdist]` — this is what makes D-18 safe. No packaging change needed.
- `.pre-commit-config.yaml` runs `detect-secrets` with `--baseline .secrets.baseline` on **all**
  staged files, with **no** `.planning/` exclude. The `trailing-whitespace`, `end-of-file-fixer`,
  `check-yaml`, and `check-added-large-files` hooks all *do* exclude `^\.planning/`.
- `git ls-files tests/integration` returns exactly three paths: `tests/integration/__init__.py`,
  `tests/integration/segmentation/__init__.py`, `tests/integration/test_calibration_cross_validation.py`.
  Nothing in the repo imports or references `integration.segmentation`.
- **What ROADMAP criterion 4's "dead `reconstruction`/`segmentation`/`tracking` directories"
  actually are:** four *top-level* leftovers at `src/aquapose/reconstruction`,
  `src/aquapose/segmentation`, `src/aquapose/tracking`, and `src/aquapose/visualization`. Each
  contains **only `__pycache__/*.pyc`** — verified: 0 non-`.pyc` files in each, and
  `git ls-files` returns 0 paths for each. They are stale build residue from packages that moved
  under `core/` or were removed in v3.7. The live production modules are
  `src/aquapose/core/reconstruction/` and `src/aquapose/core/tracking/` — **different paths, do
  not touch them.** Tracked top-level members of `src/aquapose/` are exactly: `calibration`,
  `core`, `engine`, `evaluation`, `io`, `synthetic`, `training`, `cli.py`, `cli_utils.py`,
  `logging.py`, `__init__.py`, `py.typed`.
- Untracked-and-unignored working-tree paths: `11.0` (a pip install log from `pip install boxmot`)
  and `~/` (a literal tilde directory containing a full SAM2 clone at `~/PycharmProjects/sam2`
  **with a `checkpoints/` directory of downloaded model weights**).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-ignore .planning/ and commit the full project record</name>
  <files>.gitignore, .planning/</files>
  <read_first>
    - `.gitignore` lines 88–102 — confirm line 100 is exactly `.planning/` and that lines 94, 97,
      99 (`tmp/`, `*.pt`, `runs/`) are the build-artifact rules that must stay.
    - `pyproject.toml` lines 55–60 — the `[tool.hatch.build.targets.sdist]` exclude list, the
      source of truth for why un-ignoring is safe. **Read only; do not edit this file.**
    - `.pre-commit-config.yaml` — note that `detect-secrets` has no `^\.planning/` exclude while
      the four `pre-commit-hooks` entries do.
    - `.secrets.baseline` — the existing allowlist.
    - `.planning/STATE.md` § Blockers/Concerns — records this contradiction as a known blocker.
  </read_first>
  <action>
    Delete the single line `.planning/` from `.gitignore` (line 100). Change nothing else in that
    file. Per D-19, do **not** add any new ignore rules — in particular do not add `~/` or a
    pip-log pattern.

    Before staging, audit the ~589 files that are about to become visible:
    1. `git add -A .planning/` then `git diff --cached --stat | tail -1` to confirm the count.
    2. Run `pre-commit run detect-secrets --all-files`. **If it reports any finding, stop and
       report to the user — do not update `.secrets.baseline` to silence it.**
    3. Run `grep -rniE '(api[_-]?key|client[_-]?secret|access[_-]?token|password|BEGIN [A-Z ]*PRIVATE KEY)[[:space:]]*[:=]' .planning/` and, separately,
       `grep -rlc 'ghp_\|github_pat_\|sk-' .planning/`. Any hit is a hard stop for user review.
    4. Informational only (not a blocker): count absolute local paths with
       `grep -rc 'C:\\\\Users' .planning/ | grep -vc ':0$'` and record the number in the SUMMARY.
       Local Windows paths naming the already-public author are not a leak; do not scrub them.

    Verify no file is unexpectedly large: `find .planning -type f -size +1M` must print nothing.

    Commit as `chore(108-03): un-ignore .planning/ and track the full project record`. This
    commit must **not** require `git add -f` — that friction ending is the point of D-18.
    Do not push.
  </action>
  <verify>
    <automated>! git check-ignore -q .planning/STATE.md &amp;&amp; test "$(git ls-files .planning | wc -l)" -gt 600 &amp;&amp; git diff --quiet HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore .planning/STATE.md` exits non-zero.
    - `grep -c '^\.planning/$' .gitignore` returns `0`.
    - `grep -c '^runs/$' .gitignore` returns `1`, `grep -c '^tmp/$' .gitignore` returns `1`, `grep -c '^\*\.pt$' .gitignore` returns `1`.
    - `git diff HEAD~1 -- .gitignore | grep -c '^+[^+]'` returns `0` (deletion only — no rule was added, satisfying D-19).
    - `git ls-files .planning | wc -l` returns at least `600`.
    - `git status --porcelain .planning/` outputs zero lines.
    - `pre-commit run detect-secrets --all-files` exits 0.
    - `git diff --exit-code HEAD -- .secrets.baseline` exits 0 (the baseline was not widened).
    - `find .planning -type f -size +1M` outputs zero lines.
    - `git diff --exit-code HEAD -- pyproject.toml` exits 0 (108-02 owns that file).
  </acceptance_criteria>
  <done>`.planning/` is no longer ignored, all 600+ project-record files are tracked in a normal commit, detect-secrets passes over them, and the `.secrets.baseline` was not widened.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead tests/integration/segmentation package</name>
  <files>tests/integration/segmentation/__init__.py</files>
  <read_first>
    - `tests/integration/segmentation/__init__.py` — confirm it is empty or contains only a
      docstring, and that the directory holds no test modules.
    - `tests/integration/__init__.py` and `tests/integration/test_calibration_cross_validation.py`
      — the sibling files that stay.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` § D-20 — the
      scope fence: exactly two dead artifacts, no broader sweep.
  </read_first>
  <action>
    Confirm nothing references the package: `grep -rn "integration.segmentation\|integration/segmentation" --include='*.py' --include='*.toml' --include='*.cfg' --include='*.ini' .`
    must return no hits outside `.planning/`. If it returns a hit, stop and report.

    `git rm -r tests/integration/segmentation` and remove the now-empty directory from disk.

    **Do not remove anything else.** In particular, `src/aquapose/core/reconstruction/` and
    `src/aquapose/core/tracking/` are live production modules despite ROADMAP criterion 4 calling
    them dead — leave them untouched. `docs/api/{mesh,optimization,segmentation,utils}.rst` are
    the other dead artifact and were already deleted by the 108-01 merge; verify they are gone
    rather than deleting them again. D-20 caps dead-code removal at exactly these two things —
    do not start a broader sweep.

    Do not attempt to fix any failing test. The suite is red with 8 known failures and stays red
    through this phase; Phase 109 owns that.

    Confirm collection still works: `hatch run test --collect-only -q tests/integration` exits 0.

    Commit as `chore(108-03): remove dead tests/integration/segmentation package`. Do not push.
  </action>
  <verify>
    <automated>test ! -e tests/integration/segmentation &amp;&amp; hatch run test --collect-only -q tests/integration > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `test -e tests/integration/segmentation` exits non-zero.
    - `git ls-files tests/integration/segmentation` outputs zero lines.
    - `git ls-files tests/integration | wc -l` returns `2`.
    - `hatch run test --collect-only -q tests/integration` exits 0.
    - `git ls-files docs/api/mesh.rst docs/api/optimization.rst docs/api/segmentation.rst docs/api/utils.rst` outputs zero lines.
    - `git ls-files src/aquapose/core/reconstruction | wc -l` is greater than 0 and `git ls-files src/aquapose/core/tracking | wc -l` is greater than 0 (live code untouched).
    - `git diff --stat HEAD~1` shows exactly one deleted file.
  </acceptance_criteria>
  <done>The empty `tests/integration/segmentation` package is deleted, integration test collection still succeeds, and no live module or additional file was removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Clear the working tree — confirm before touching the SAM2 model weights</name>
  <files>11.0, ~/PycharmProjects/sam2, src/aquapose/reconstruction, src/aquapose/segmentation, src/aquapose/tracking, src/aquapose/visualization</files>
  <read_first>
    - `git status --porcelain` — the authoritative list of what is untracked and unignored.
    - `find src/aquapose/reconstruction src/aquapose/segmentation src/aquapose/tracking src/aquapose/visualization -type f ! -name '*.pyc'` — must print nothing before you delete these
      directories, and `git ls-files` on each must print nothing. If either prints anything, stop.
    - `ls src/aquapose/core` — confirm `reconstruction/` and `tracking/` live here and are the
      production modules that must survive.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` § `<open_items>`
      OI-02 and § D-19 — OI-02 explicitly qualifies D-19 and requires a user confirmation here.
    - The contents of the tilde directory: `find '~' -maxdepth 4 -type d -name checkpoints` and
      `du -sh '~'` — you must be able to tell the user how much data and which weight files are
      at stake before anything moves.
  </read_first>
  <action>
    Automate everything first, then pause. In order:

    1. Confirm the four top-level leftovers are pure residue:
       `find src/aquapose/reconstruction src/aquapose/segmentation src/aquapose/tracking src/aquapose/visualization -type f ! -name '*.pyc'`
       must print nothing, and `git ls-files` on each must print nothing. **If either prints
       anything, stop and report — do not delete.** Then `rm -rf` those four paths. Do not touch
       `src/aquapose/core/reconstruction` or `src/aquapose/core/tracking`.
    2. `rm -f ./11.0` — a never-tracked pip install log, safe to delete unconditionally (D-19).
    3. Measure what is at stake before moving anything: `du -sh './~'` and
       `find './~' -maxdepth 5 -type d -name checkpoints -exec du -sh {} \;`.
    4. **Move, do not delete** (OI-02): `mkdir -p ../aquapose-quarantine-108` then
       `mv './~' ../aquapose-quarantine-108/tilde`. Never run `rm -rf` against the tilde
       directory or anything under it at this step — it holds SAM2 model weights whose removal
       is required by no success criterion.
    5. Confirm the move preserved the clone: `test -d ../aquapose-quarantine-108/tilde/PycharmProjects/sam2`
       and list its `checkpoints/` contents.
    6. Confirm the live modules still import:
       `hatch run python -c "import aquapose.core.reconstruction, aquapose.core.tracking"`.
    7. Per D-19, add no `.gitignore` entries for `~/` or pip logs.

    Then pause and ask the user what should happen to the quarantined copy. Only execute a
    `rm -rf ../aquapose-quarantine-108/` if the user explicitly replies "delete". If they reply
    "relocate <path>", `mv` it there. If "keep", leave it in place.

    If nothing here required a commit (all four paths were untracked), say so explicitly rather
    than creating an empty commit.
  </action>
  <what-built>
    Everything automatable has already been done. The four stale top-level directories
    `src/aquapose/{reconstruction,segmentation,tracking,visualization}` were removed after
    confirming each holds zero non-`.pyc` files and zero tracked paths — this is what ROADMAP
    criterion 4 actually meant, and it cost nothing because they were pure `__pycache__` residue.
    `src/aquapose/core/reconstruction/` and `src/aquapose/core/tracking/` were left untouched.

    Then: `11.0` (a throwaway pip install log from
    `pip install boxmot`, never tracked) has been deleted with `rm -f ./11.0`, and the tilde
    directory has been **moved, not deleted**, to a location outside the repository —
    `../aquapose-quarantine-108/tilde/` — preserving `~/PycharmProjects/sam2` and its
    `checkpoints/` directory of downloaded SAM2 model weights intact.

    Nothing has been destroyed. `rm -rf` was deliberately not used: OI-02 records that these
    weights may be multi-gigabyte and not trivially re-obtainable offline, and that their removal
    is required by **no** success criterion (the directory was never tracked by git, so a fresh
    clone of `dev` is already clean without it).
  </what-built>
  <how-to-verify>
    1. Confirm the repo is clean: run `git status --porcelain` in the project root. It must print
       nothing.
    2. Confirm nothing was lost: `ls ../aquapose-quarantine-108/tilde/PycharmProjects/sam2/checkpoints`
       should list the SAM2 weight files that were previously under the tilde directory. The
       reported size is in the task output above.
    3. Decide what happens to the quarantined copy and tell me which:
       - **keep** — leave it at `../aquapose-quarantine-108/` indefinitely (default; costs only disk)
       - **relocate** — give me a path to move it to (e.g. your real `~/PycharmProjects/sam2`)
       - **delete** — I will `rm -rf ../aquapose-quarantine-108/` and the weights are gone permanently

    Do not answer "delete" unless you are sure you can re-download those checkpoints.
  </how-to-verify>
  <acceptance_criteria>
    - `git status --porcelain` outputs zero lines.
    - `test -e ./11.0` exits non-zero.
    - `test -e './~'` exits non-zero (no literal tilde directory remains in the repo root).
    - Before the user's decision: `test -d ../aquapose-quarantine-108/tilde/PycharmProjects/sam2` exits 0 — the clone survived the move.
    - `git diff --exit-code HEAD -- .gitignore` exits 0 for this task (D-19: no defensive ignore rules were added for `~/` or pip logs).
    - `git ls-files | grep -c '^11\.0$'` returns `0` and `git ls-files | grep -c 'sam2'` returns `0` (confirming neither was ever tracked).
    - `test -e src/aquapose/reconstruction`, `test -e src/aquapose/segmentation`, `test -e src/aquapose/tracking`, and `test -e src/aquapose/visualization` each exit non-zero.
    - `test -d src/aquapose/core/reconstruction` and `test -d src/aquapose/core/tracking` each exit 0 (live modules preserved).
    - `hatch run python -c "import aquapose.core.reconstruction, aquapose.core.tracking"` exits 0.
    - The user's disposition choice (keep / relocate / delete) is recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain)" &amp;&amp; test ! -e ./11.0 &amp;&amp; test ! -e './~' &amp;&amp; test ! -e src/aquapose/reconstruction &amp;&amp; test ! -e src/aquapose/segmentation &amp;&amp; test ! -e src/aquapose/tracking &amp;&amp; test ! -e src/aquapose/visualization &amp;&amp; test -d src/aquapose/core/reconstruction &amp;&amp; test -d src/aquapose/core/tracking &amp;&amp; hatch run python -c "import aquapose.core.reconstruction, aquapose.core.tracking"</automated>
  </verify>
  <resume-signal>Reply "keep", "relocate &lt;path&gt;", or "delete" — or describe issues.</resume-signal>
  <done>The repository working tree is clean, `11.0` is gone, the tilde directory no longer sits in the repo, the SAM2 checkpoints are preserved at a quarantine path, and the user has explicitly chosen their fate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untracked local filesystem → destructive shell commands | `~/PycharmProjects/sam2/checkpoints` holds irreplaceable-offline model weights that no success criterion requires deleting |
| previously-ignored `.planning/` → public git history | 589 files never before scanned by the pre-commit secret hooks become permanently published |
| `.secrets.baseline` → detect-secrets gate | widening the baseline to make a commit pass would silently disable the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-12 | Denial of Service | local SAM2 model weights under `~/PycharmProjects/sam2/checkpoints` | mitigate | Task 3 is a blocking human checkpoint; the action moves the directory to `../aquapose-quarantine-108/` instead of `rm -rf`, and acceptance asserts the clone survives the move before the user is asked; deletion only ever happens on an explicit "delete" reply |
| T-108-13 | Information Disclosure | credentials or tokens inside the 589 newly tracked `.planning/` files | mitigate | Task 1 runs `pre-commit run detect-secrets --all-files` plus explicit greps for `api_key`/`client_secret`/`access_token`/`password`/PEM headers/`ghp_`/`github_pat_`/`sk-`, with a hard stop on any hit |
| T-108-14 | Tampering | `.secrets.baseline` allowlist | mitigate | The action forbids baselining a finding to make the commit pass; acceptance asserts `git diff --exit-code HEAD -- .secrets.baseline` exits 0 |
| T-108-15 | Information Disclosure | absolute local paths in `.planning/` naming the developer's machine | accept | The paths name the already-public repository author and a local Windows home directory; they expose no credential, host, or private endpoint. Counted and recorded in the SUMMARY for visibility, not scrubbed |
| T-108-16 | Tampering | live production modules misidentified as dead by ROADMAP criterion 4 | mitigate | Task 2 scopes deletion to one file, greps for references first, and acceptance asserts `src/aquapose/core/{reconstruction,tracking}` still have tracked files and that exactly one file was deleted. Task 3's removal of the same-named *top-level* leftovers is gated on each holding zero non-`.pyc` files and zero tracked paths, and acceptance re-imports `aquapose.core.reconstruction` and `aquapose.core.tracking` to prove the live modules survived |
| T-108-17 | Denial of Service | repository size / large files entering history | mitigate | `.planning/` is 7.0 MB of markdown with no file over 200 KB; task 1 asserts `find .planning -type f -size +1M` prints nothing before committing |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. `hatch run test --collect-only` uses the already-provisioned default env against the existing, unchanged dependency set |
</threat_model>

<verification>
- `git check-ignore .planning/STATE.md` exits non-zero (D-18).
- `git ls-files .planning | wc -l` >= 600, and the `.gitignore` diff is deletion-only (D-19).
- `pre-commit run detect-secrets --all-files` exits 0 with an unmodified `.secrets.baseline`.
- `tests/integration/segmentation` no longer exists; `src/aquapose/core/{reconstruction,tracking}` do (D-20).
- `git status --porcelain` is empty (FOUND-04).
- The four stale top-level `src/aquapose/` directories are gone; `aquapose.core.reconstruction`
  and `aquapose.core.tracking` still import (FOUND-04, ROADMAP criterion 4 read correctly).
- The SAM2 clone survives at the quarantine path unless the user explicitly says otherwise (OI-02).
- Nothing pushed; `pyproject.toml` unmodified.
</verification>

<success_criteria>
- GSD planning commits no longer require `git add -f`.
- The complete project record is in git history.
- The working tree is clean without any irreversible loss of local model assets.
- Dead-artifact removal stayed inside the D-20 fence.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-03-SUMMARY.md` when done.
Record: the newly tracked `.planning/` file count, the absolute-path grep count, the measured size
of the quarantined tilde directory, and the user's verbatim disposition choice for it.
</output>
