---
phase: 113.2-typecheck-backlog
plan: 04
type: execute
wave: 2
depends_on: ["113.2-01"]
files_modified:
  - src/aquapose/core/reid/runner.py
  - src/aquapose/core/stitching.py
autonomous: true
requirements: [QA-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`hatch run typecheck` reports 0 errors for `src/aquapose/core/reid/runner.py`, down from 27."
    - "`hatch run typecheck` reports 0 errors for `src/aquapose/core/stitching.py`, down from 12."
    - "The helper from `aquapose.core.h5` is reached by a core-internal import in both files, so `tests/unit/core/test_import_boundary.py` passes with no new `_KNOWN_EXCEPTIONS` entry."
    - "The 39 errors here are closed by narrowing at exactly 9 read expressions — the downstream `.shape`, `len()`, `searchsorted` and arithmetic errors resolve as a consequence, not by individual annotation."
    - "`hatch run test` still reports `1403 passed, 3 skipped, 17 deselected`, with no test file edited."
  artifacts:
    - src/aquapose/core/reid/runner.py
    - src/aquapose/core/stitching.py
  key_links:
    - "`core/reid/runner.py` and `core/stitching.py` -> `aquapose.core.h5` — core-internal imports, the case that forced the helper under `core/` in the first place (D-04); if the helper had gone in `io/` or `evaluation/`, `core/stitching.py` could not reach it."
    - "`core/stitching.py:557-559` -> `_compute_body_lengths(points, fish_id, frame_index)` at :568 — three narrowed reads feeding one typed call; narrowing at the read closes both the read errors and the three argument errors at the call."
    - "`core/reid/runner.py:120-123` -> the `.shape`, `<`, `-` and `np.linalg.norm` uses at :126-150 — one narrowing per read closes 23 downstream errors."
---

<objective>
Apply the h5py narrowing helper proven in plan 01 to the two remaining `core/` files that
carry the cluster: `core/reid/runner.py` (27 errors) and `core/stitching.py` (12).

This is the expansion step of the tracer. The pattern is already validated end-to-end across
a layer boundary; here it is exercised on the case that actually justified the D-04 placement
constraint — `core/` importing the helper without crossing a forbidden edge.

Purpose: close 39 of the phase's 98 errors, and confirm that the helper's signature holds
for the slash-separated path form (`pre["midlines/fish_id"]`) as well as the two-step
group-then-dataset form.
Output: both files at 0 typecheck errors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113.2-typecheck-backlog/113.2-CONTEXT.md
@.planning/phases/113.2-typecheck-backlog/113.2-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/code-style.md
</context>

<measured_baseline>
Re-measured at commit `d3e6073`. Both files' errors trace to a small number of unnarrowed
read expressions; everything else is downstream fallout.

**`core/reid/runner.py` — 27 errors, all from 4 reads.** The file has two `h5py.File` blocks.
The one at lines 72-75 already uses `cast(...)` and is clean. The one at lines 119-123 does
not, and its four reads produce all 27:

| Read | Downstream errors it causes |
|---|---|
| `pre["midlines/fish_id"]` (:120) | `.shape` at :126, :130; `int(fid_pre[...])` at :131 |
| `post["midlines/fish_id"]` (:121) | `.shape` at :141; `<` at :142; `int(...)` at :150 |
| `pre["midlines/points"]` (:122) | `.shape` at :127; `np.isnan` call/arg at :136 |
| `post["midlines/points"]` (:123) | `np.isnan` call/arg at :145; `-` at :147 |

Note these use the **slash-path form**, resolving through the group in one subscript. The
helper must handle it (plan 01 specifies that it does, since h5py resolves paths natively).

**`core/stitching.py` — 12 errors, all from 4 reads across two blocks.** Three of the file's
five `h5py.File` blocks already use `cast(...)` and are clean (lines 77-84, 360-363, 670-673).

| Block | Unnarrowed reads | Errors |
|---|---|---|
| :555-559 | `grp = f["midlines"]` then `points`, `fish_id`, `frame_index` | 6 at :557-559, plus 3 at :568 where all three flow into `_compute_body_lengths` typed `NDArray[float32] / NDArray[int32] / NDArray[int64]` |
| :704-708 | `grp["frame_index"]` at :708 (the `grp` and `fish_id_ds` on :705-706 are already cast) | 1 at :708, plus 2 at :712 `np.searchsorted` |
</measured_baseline>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the four ID-mapping reads in core/reid/runner.py</name>
  <files>src/aquapose/core/reid/runner.py</files>
  <read_first>
    - `src/aquapose/core/reid/runner.py` lines 105-155 — the `with h5py.File(pre_path, "r") as pre, h5py.File(post_path, "r") as post:` block and the entire mapping loop that consumes its four arrays. This is the whole change surface.
    - `src/aquapose/core/reid/runner.py` lines 68-80 — the *other* h5py block in this file, which already uses `cast(...)` and typechecks. Leave it alone; it is an existing-`cast()` site and plan 01's `<design_decisions>` puts those out of scope.
    - `src/aquapose/core/h5.py` — the helper as written in plan 01, for exact names and whether it accepts slash-separated paths.
    - `.planning/phases/113.2-typecheck-backlog/113.2-01-SUMMARY.md` — the helper's final signature and any deviation from the plan.
    - `tests/unit/core/test_import_boundary.py` — confirm a `core/` to `core/` import needs no allowlist entry.
  </read_first>
  <action>
    Replace the four subscript reads at lines 120-123 with `require_dataset` calls against the
    open file handles, keeping each existing `[:]` subscript and each existing local variable
    name. Use the slash-separated path form directly (`require_dataset(pre, "midlines/fish_id")`)
    rather than splitting into a `require_group` plus a second lookup, so the diff stays at
    four expressions.

    Add the import from `aquapose.core.h5` in the third-party/local grouping the file already
    uses. Change nothing in the mapping loop at lines 125-152 — the `.shape`, comparison,
    subtraction and `np.isnan` errors there are consequences of the four unnarrowed reads and
    must resolve on their own. If any error at those lines survives the narrowing, that
    indicates a second root cause and is a finding to report, not to annotate around (D-06).

    Do not touch the `cast(...)` block at lines 72-75.
  </action>
  <verify>
    <automated>test "$(hatch run typecheck 2>&1 | grep -F 'core/reid/runner.py:' | grep -c ' - error: ')" = "0"</automated>
    <automated>hatch run test -k "import_boundary or reid"</automated>
    <automated>hatch run test 2>&1 | tail -1 | grep -q '1403 passed, 3 skipped, 17 deselected'</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(hatch run typecheck 2>&1 | grep -F 'core/reid/runner.py:' | grep -c ' - error: ')" = "0"` succeeds — 27 -> 0.
    - `test "$(grep -c 'require_dataset' src/aquapose/core/reid/runner.py)" = "4"` succeeds — exactly four narrowing calls, one per read; no per-error annotation crept into the loop below.
    - `test "$(grep -c 'cast(h5py' src/aquapose/core/reid/runner.py)" = "3"` succeeds — the three pre-existing `cast()` calls at lines 73-75 are untouched, per plan 01's scope decision.
    - `grep -n 'from aquapose.core.h5 import' src/aquapose/core/reid/runner.py` matches — the core-internal import is present.
    - `hatch run test -k "import_boundary or reid"` passes, and `_KNOWN_EXCEPTIONS` in `tests/unit/core/test_import_boundary.py` is unchanged: `git diff --name-only d3e6073 -- tests/` lists at most `tests/unit/core/test_h5.py`.
    - `hatch run test 2>&1 | tail -1` contains `1403 passed, 3 skipped, 17 deselected`.
    - `hatch run typecheck 2>&1 | grep -oP 'src/aquapose/\S+?\.py(?=:\d+:\d+ - error)' | sort -u` lists only files from the measured 9-file baseline.
  </acceptance_criteria>
  <done>`core/reid/runner.py` is at 0 typecheck errors from exactly four narrowing calls, the pre-existing `cast()` block untouched, and the import-boundary guard still passing with no new exemption.</done>
  <reversibility rating="reversible">Four call-site expressions plus one import; reverting restores the file exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Narrow the swap-detection and swap-application reads in core/stitching.py</name>
  <files>src/aquapose/core/stitching.py</files>
  <read_first>
    - `src/aquapose/core/stitching.py` lines 550-575 — the first unnarrowed block: `grp = f["midlines"]`, the three reads at :557-559, the `if points is None:` guard at :563, and the `_compute_body_lengths(points, fish_id, frame_index)` call at :568 whose three parameters are annotated `NDArray[float32]`, `NDArray[int32]` and `NDArray[int64]`.
    - `src/aquapose/core/stitching.py` lines 700-716 — the second block, where `grp` and `fish_id_ds` are already `cast(...)` at :705-706 but `grp["frame_index"]` at :708 is not, feeding `np.searchsorted` at :712.
    - `src/aquapose/core/stitching.py` lines 75-90 and 355-370 — the three blocks that already use `cast(...)` and typecheck cleanly. These are out of scope (plan 01 `<design_decisions>`); read them only to match local naming style.
    - `src/aquapose/core/h5.py` and `.planning/phases/113.2-typecheck-backlog/113.2-01-SUMMARY.md` — the helper's exact signature.
    - `src/aquapose/core/reid/runner.py` as converted in Task 1 — the in-phase precedent to match.
  </read_first>
  <action>
    In the first block, replace `grp = f["midlines"]` with a `require_group` call and each of
    the three `grp["<name>"][:]` reads with `require_dataset`, keeping the existing subscripts
    and variable names. The three `reportArgumentType` errors at line 568 are downstream of
    these reads and must resolve without touching the `_compute_body_lengths` signature —
    that signature states the real contract and is not the defect.

    While reading this block, note that the `if points is None:` guard at line 563 is
    unreachable once `points` is a narrowed dataset read. Do not remove it — deleting a guard
    is a behavior change (D-06). Record it in the plan SUMMARY as a finding for a follow-up
    todo.

    In the second block, replace only the `grp["frame_index"]` read at line 708 with
    `require_dataset`, which also clears the two `np.searchsorted` errors at :712. Line 705-706
    are pre-existing `cast()` calls in the same `with` block; plan 01 permits converting those
    for block coherence but does not require it. If you convert them, list the conversion in
    the SUMMARY as behavior-adjacent (it adds a runtime raise path that did not exist) and
    ensure the `cast(h5py` count criterion below is adjusted accordingly in the SUMMARY
    narrative. The lower-risk default is to leave them.

    Reuse the single `aquapose.core.h5` import for both blocks.
  </action>
  <verify>
    <automated>test "$(hatch run typecheck 2>&1 | grep -F 'core/stitching.py:' | grep -c ' - error: ')" = "0"</automated>
    <automated>hatch run test -k "import_boundary or stitching or swap"</automated>
    <automated>hatch run test 2>&1 | tail -1 | grep -q '1403 passed, 3 skipped, 17 deselected'</automated>
    <automated>hatch run lint && hatch run format --check</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(hatch run typecheck 2>&1 | grep -F 'core/stitching.py:' | grep -c ' - error: ')" = "0"` succeeds — 12 -> 0.
    - `test "$(grep -c 'def _compute_body_lengths' src/aquapose/core/stitching.py)" = "1"` and `git diff d3e6073 -- src/aquapose/core/stitching.py | grep -c '^-.*def _compute_body_lengths'` returns `0` — the typed signature at the call site is unchanged; the fix was at the reads, not by loosening the callee.
    - `grep -n 'if points is None' src/aquapose/core/stitching.py` still matches — the now-unreachable guard was reported, not deleted (D-06).
    - `test "$(grep -c 'require_dataset\|require_group' src/aquapose/core/stitching.py)" -ge "5"` succeeds — one group narrowing plus at least four dataset narrowings.
    - `hatch run test -k "import_boundary or stitching or swap"` passes.
    - `hatch run test 2>&1 | tail -1` contains `1403 passed, 3 skipped, 17 deselected`, and `git diff --name-only d3e6073 -- tests/` lists at most `tests/unit/core/test_h5.py`.
    - `hatch run typecheck 2>&1 | grep -oP 'src/aquapose/\S+?\.py(?=:\d+:\d+ - error)' | sort -u` lists only `core/reid/swap_detector.py` (owned by plan 05) and any file not yet processed by a completed plan — no new file appears.
    - `hatch run lint` and `hatch run format --check` exit 0.
    - The plan SUMMARY records the unreachable `points is None` guard as a follow-up finding, and states whether any `cast()` was converted.
  </acceptance_criteria>
  <done>`core/stitching.py` is at 0 typecheck errors, `_compute_body_lengths`' typed signature is unchanged, the unreachable guard is reported rather than removed, and the suite is unchanged.</done>
  <reversibility rating="reversible">Read-site expressions plus one import; no signature, guard or logic change to revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk `midlines.h5` / `midlines_stitched.h5` -> `core/reid/runner.py` | Two separate pipeline artifacts are opened together and their datasets cross-referenced by index; a shape or kind mismatch between them was previously unchecked at the read. |
| on-disk `.h5` opened `r+` -> `core/stitching.py:704` | A read-write handle: this block mutates `fish_id` in place to apply swap corrections. A wrong narrowing here writes bad data, not just a bad read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113.2-10 | Tampering | `core/stitching.py:704-716` opens the H5 `r+` and writes corrected `fish_id` values; a narrowing mistake in this block corrupts a pipeline artifact | high | mitigate | Only the `frame_index` read is changed; the `fish_id_ds` write handle at :706 is left exactly as-is. Full suite (which covers stitching and swap application) must stay at `1403 passed`, and an acceptance criterion pins the untouched `_compute_body_lengths` signature. |
| T-113.2-11 | Tampering | Loosening `_compute_body_lengths`' `NDArray[float32]` / `NDArray[int32]` / `NDArray[int64]` parameter annotations to silence the three errors at :568, discarding a real dtype contract | medium | mitigate | Explicitly forbidden in the action; acceptance criterion greps the phase diff for a removed `def _compute_body_lengths` line and requires 0. |
| T-113.2-12 | Denial of Service | The new runtime kind-check raises on a read path that previously proceeded, aborting a pipeline run mid-stitch | low | mitigate | Applied only at error sites, never to existing `cast()` sites; the suite exercises these paths on real fixtures and must stay green. |
| T-113.2-13 | Elevation of Privilege | A new import in `core/` crossing a forbidden layer boundary | low | mitigate | `aquapose.core.h5` is core-internal by construction; `tests/unit/core/test_import_boundary.py` is run as an explicit gate in both tasks and must pass with no new `_KNOWN_EXCEPTIONS` entry. |
| T-113.2-SC | Tampering | Supply chain — package-manager installs | high | accept | No `pip`/`npm`/`cargo` install tasks. Both files already import `h5py`; nothing new is added. |

ASVS level 1, blocking threshold `high`. T-113.2-10 is `high` and dispositioned `mitigate`, not accepted.
</threat_model>

<artifacts_this_phase_produces>
This plan produces **no new symbols and no new files**. It adds one `aquapose.core.h5` import
to each of two modules and rewrites nine read expressions:

| File | Change |
|---|---|
| `src/aquapose/core/reid/runner.py` | import + 4 `require_dataset` calls at the ID-mapping block |
| `src/aquapose/core/stitching.py` | import + 1 `require_group` and 4 `require_dataset` calls across two blocks |

No symbol is added, removed or renamed; no signature changes. Nothing is added to `tests/`.
The phase-wide artifact list is consolidated in `113.2-06-PLAN.md`.
</artifacts_this_phase_produces>

<verification>
- `core/reid/runner.py` and `core/stitching.py` both report 0 typecheck errors.
- `_compute_body_lengths`' typed signature is unchanged in the phase diff.
- The pre-existing `cast()` blocks in both files are untouched, or any conversion is recorded as behavior-adjacent.
- `hatch run test -k import_boundary` passes with no new `_KNOWN_EXCEPTIONS` entry.
- `hatch run test` reports `1403 passed, 3 skipped, 17 deselected` with no test edited.
- `hatch run lint` and `hatch run format --check` exit 0.
</verification>

<success_criteria>
The h5py narrowing pattern is applied across `core/` at nine read expressions, closing 39
errors without changing a single downstream annotation, signature or guard — demonstrating
that the cluster really was one root cause rather than 39 independent defects.
</success_criteria>

<output>
Create `.planning/phases/113.2-typecheck-backlog/113.2-04-SUMMARY.md` when done.
Record: the unreachable `if points is None` guard at `stitching.py:563` as a follow-up
finding, whether any `cast()` was converted for block coherence, and whether every downstream
error resolved from the read narrowing alone.
</output>
