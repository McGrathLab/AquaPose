---
phase: 113.2-typecheck-backlog
plan: 05
type: execute
wave: 2
depends_on: ["113.2-01"]
files_modified:
  - src/aquapose/core/reid/swap_detector.py
autonomous: true
requirements: [QA-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`hatch run typecheck` reports 0 errors for `src/aquapose/core/reid/swap_detector.py`, down from 7."
    - "The `reportOptionalMemberAccess` at line 310 is resolved by a written diagnosis that states whether the value can be `None` at runtime, not by an annotation that assumes it cannot (D-06)."
    - "If the diagnosis concludes the optional access is a live defect, it is surfaced as a todo and the phase does not silently repair it — per CONTEXT's Deferred Ideas, a real bug uncovered by correct typing is a finding, not this phase's work."
    - "The pre-existing `_KNOWN_EXCEPTIONS` entry for `reid/swap_detector.py` in the import-boundary guard is untouched — those exemptions are explicitly out of scope for this phase."
    - "`hatch run test` still reports `1403 passed, 3 skipped, 17 deselected`, with no test file edited."
  artifacts:
    - src/aquapose/core/reid/swap_detector.py
  key_links:
    - "`swap_detector.py:296` (`grp[\"swap_events\"]`) -> `:453` (`len(...)`) and `:460` (`for row in ...`) — one unnarrowed read producing three errors two hundred lines away, via the `self._swap_events_raw` instance attribute."
    - "`swap_detector.py:305` (`npz[\"embeddings\"].astype(...)`) -> `:310` (`.shape[0]`) — the assignment whose inferred type determines whether the optional access at 310 is a typing artifact or a live defect."
    - "`swap_detector.py:802` (`grp[\"frame_index\"]`) -> `:805` (`np.searchsorted`) — a read on an `r+` handle whose result indexes an in-place repair write."
---

<objective>
Close the last h5py cluster file, and handle the one error in this phase that CONTEXT flags
as most likely to be a real latent defect rather than a typing gap.

`core/reid/swap_detector.py` carries 7 errors: 6 are the familiar union-narrowing shape, and
one — `reportOptionalMemberAccess` at line 310, `"shape" is not a known attribute of "None"` —
is singled out by D-06 and by CONTEXT's short-tail analysis as the one to treat as a bug
until proven otherwise.

Purpose: the cheapest way to make that error disappear is an annotation that asserts the
value is never `None`. If that assertion is wrong, this phase would have converted a
detectable crash into an undetectable one while reporting progress. The plan forces the
diagnosis to happen and be written down before any edit.
Output: the file at 0 errors, and a recorded verdict on line 310.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113.2-typecheck-backlog/113.2-CONTEXT.md
@.planning/phases/113.2-typecheck-backlog/113.2-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/code-style.md
</context>

<measured_baseline>
Re-measured at commit `d3e6073`. Seven errors, six of them one root cause:

| Site | Rule | Cause |
|---|---|---|
| `:296:41` | `reportIndexIssue` | `grp["swap_events"][()]` — the only read in the constructor block not already `cast(...)` |
| `:453:49` | `reportArgumentType` | `len(self._swap_events_raw)` — downstream of :296 |
| `:460:20` | `reportIndexIssue` | `for row in self._swap_events_raw` — downstream of :296 |
| `:802:31` | `reportIndexIssue` | `grp["frame_index"][:]` on the `r+` repair handle |
| `:805:37` | `reportCallIssue` | `np.searchsorted` overload — downstream of :802 |
| `:805:53` | `reportArgumentType` | `np.searchsorted` argument — downstream of :802 |
| `:310:34` | `reportOptionalMemberAccess` | `self._embeddings.shape[0]` — **the D-06 candidate, unrelated to h5py** |

Structural facts verified on this tree, which the diagnosis in Task 2 must start from rather
than re-derive:

- `self._embeddings` is declared `NDArray[np.float32] | None = None` at line 299.
- Lines 303-310 are one block: `if npz_path.exists():` guards `npz = np.load(...)` at 304, the
  assignment `self._embeddings = npz["embeddings"].astype(np.float32)` at 305, and the
  `logger.info(...)` whose argument at line 310 is `self._embeddings.shape[0]`.
- The constructor block at lines 288-292 already uses `cast(h5py.Group, ...)` /
  `cast(h5py.Dataset, ...)` for `frame_index`, `fish_id` and `points`; only `swap_events` at
  :296 was left unnarrowed. The `r+` block at :799-800 is likewise already cast except for
  `frame_index` at :802.
- `tests/unit/core/test_import_boundary.py` carries a `_KNOWN_EXCEPTIONS` entry for
  `reid/swap_detector.py` (`aquapose.training`, a function-local import at line 323). That
  exemption is out of scope; the guard's stale-entry test will fail if it is disturbed.
</measured_baseline>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the two unnarrowed h5py reads and their four downstream errors</name>
  <files>src/aquapose/core/reid/swap_detector.py</files>
  <read_first>
    - `src/aquapose/core/reid/swap_detector.py` lines 285-300 — the constructor's `with h5py.File(...)` block: the three already-`cast` reads, the `if "swap_events" in grp:` membership test, and the unnarrowed read at :296 that feeds `self._swap_events_raw`.
    - `src/aquapose/core/reid/swap_detector.py` lines 445-470 — `_run_seeded`, where `self._swap_events_raw` is length-checked at :453 and iterated at :460. Note the `is None` guard at :453 — the attribute is genuinely optional here, so narrowing must preserve the `| None` arm.
    - `src/aquapose/core/reid/swap_detector.py` lines 795-812 — the `r+` repair block, the unnarrowed `grp["frame_index"]` at :802, and the `np.searchsorted` at :805 whose result drives an in-place `fish_id` rewrite.
    - `src/aquapose/core/h5.py` and `.planning/phases/113.2-typecheck-backlog/113.2-01-SUMMARY.md` — the helper's exact signature.
    - `src/aquapose/core/reid/runner.py` and `src/aquapose/core/stitching.py` as converted in plan 04 — the in-phase precedent to match.
  </read_first>
  <action>
    Replace the `grp["swap_events"][()]` read at line 296 and the `grp["frame_index"][:]` read
    at line 802 with `require_dataset` calls, keeping the existing subscripts and variable
    names, and add the `aquapose.core.h5` import.

    `self._swap_events_raw` must keep an optional type: line 294 sets it to `None` and line
    453 tests `is None`, so the attribute is legitimately `NDArray | None`. Give it an explicit
    optional annotation at the line 294 declaration so the narrowing at :296 flows to :453 and
    :460 without removing the `None` arm. Do not delete the `is None` guard, and do not
    annotate `:453` or `:460` individually — they must resolve from the declaration plus the
    narrowed read.

    Leave the `cast(...)` calls at :289-292 and :799-800 exactly as they are, per plan 01's
    scope decision on pre-existing casts. Add no import that would extend the
    `_KNOWN_EXCEPTIONS` entry for this file.
  </action>
  <verify>
    <automated>test "$(hatch run typecheck 2>&1 | grep -F 'core/reid/swap_detector.py:' | grep -c ' - error: ')" = "1"</automated>
    <automated>hatch run test -k "import_boundary or swap or reid"</automated>
    <automated>hatch run test 2>&1 | tail -1 | grep -q '1403 passed, 3 skipped, 17 deselected'</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(hatch run typecheck 2>&1 | grep -F 'core/reid/swap_detector.py:' | grep -c ' - error: ')" = "1"` succeeds — 7 -> 1, with only the line-310 `reportOptionalMemberAccess` remaining for Task 2.
    - `hatch run typecheck 2>&1 | grep -F 'core/reid/swap_detector.py:' | grep -c 'reportOptionalMemberAccess'` returns `1` — the surviving error is exactly the D-06 candidate and nothing else.
    - `test "$(grep -c 'require_dataset' src/aquapose/core/reid/swap_detector.py)" = "2"` succeeds — two narrowing calls, one per unnarrowed read; the downstream errors resolved on their own.
    - `test "$(grep -c 'cast(h5py' src/aquapose/core/reid/swap_detector.py)" = "6"` succeeds — the six pre-existing casts at :289-292 and :799-800 are untouched.
    - `grep -n 'self._swap_events_raw is None' src/aquapose/core/reid/swap_detector.py` still matches — the optional guard survives; the `None` arm was preserved, not annotated away.
    - `hatch run test -k "import_boundary or swap or reid"` passes, including the guard's stale-entry test, confirming the `_KNOWN_EXCEPTIONS` entry for this file is still accurate and untouched.
    - `hatch run test 2>&1 | tail -1` contains `1403 passed, 3 skipped, 17 deselected`, and `git diff --name-only d3e6073 -- tests/` lists at most `tests/unit/core/test_h5.py`.
  </acceptance_criteria>
  <done>`core/reid/swap_detector.py` is down to exactly one error — the line-310 `reportOptionalMemberAccess` — with the optional `swap_events` guard intact and the import-boundary exemptions undisturbed.</done>
  <reversibility rating="reversible">Two read-site expressions, one attribute annotation and one import.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Diagnose the line-310 optional access before touching it, then act on the verdict</name>
  <files>src/aquapose/core/reid/swap_detector.py</files>
  <read_first>
    - `src/aquapose/core/reid/swap_detector.py` lines 296-315 — the full block: the `self._embeddings: NDArray[np.float32] | None = None` declaration at :299, the `if npz_path.exists():` guard at :303, `np.load(npz_path, allow_pickle=True)` at :304, the assignment at :305, and the `logger.info` whose argument at :310 is `self._embeddings.shape[0]`.
    - Every other use of `self._embeddings` in the file (search the whole module) — the diagnosis needs the full set of readers to say whether any of them can run with the attribute still `None`.
    - `.planning/phases/113.2-typecheck-backlog/113.2-CONTEXT.md` — decision D-06 and the Deferred Ideas entry stating that a real bug uncovered by correct typing is surfaced, not fixed here.
  </read_first>
  <action>
    First determine, from the source and without editing anything, whether `self._embeddings`
    can actually be `None` at line 310. Establish two things: whether line 310 is inside the
    same `if npz_path.exists():` block as the assignment at line 305, and what type
    basedpyright infers for `np.load(...)["embeddings"].astype(np.float32)`. If that expression
    infers as the dynamic escape-hatch type, then assigning it to a declared optional leaves
    the declared union in force and the error is a narrowing artifact rather than a live
    `None`.

    Then act on the verdict, and write the verdict into the plan SUMMARY either way:

    - **If it is a narrowing artifact** (the expected outcome): bind the loaded array to a
      local with an explicit concrete array annotation, then assign that local to
      `self._embeddings`. This gives the assignment a real type so the narrowing holds at
      line 310. Do not add a suppression directive, do not add an `is not None` guard that
      can never be false, and do not change the declared optional type of the attribute — it
      is genuinely optional when the `.npz` is absent.
    - **If it is a live defect** — some reader of `self._embeddings` really can run with it
      `None` — then stop. Do not repair it. Write
      `.planning/todos/pending/2026-09-02-swap-detector-embeddings-optional-access.md`
      describing the reachable path, and add the minimal correct typing that keeps the
      checker honest about the risk rather than hiding it. Report the situation prominently
      in the plan SUMMARY so the phase's terminal gate in `113.2-06-PLAN.md` records it.

    Apply the same treatment, if needed, to `self._emb_frames` and `self._emb_fish_ids`, which
    are declared and assigned in the same pattern immediately alongside.
  </action>
  <verify>
    <automated>test "$(hatch run typecheck 2>&1 | grep -F 'core/reid/swap_detector.py:' | grep -c ' - error: ')" = "0"</automated>
    <automated>hatch run test -k "swap or reid or import_boundary"</automated>
    <automated>hatch run test 2>&1 | tail -1 | grep -q '1403 passed, 3 skipped, 17 deselected'</automated>
    <automated>hatch run lint && hatch run format --check</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(hatch run typecheck 2>&1 | grep -F 'core/reid/swap_detector.py:' | grep -c ' - error: ')" = "0"` succeeds — the file is fully clean, 7 -> 0.
    - `test "$(grep -cE '(type|pyright|basedpyright):[[:space:]]*ignore' src/aquapose/core/reid/swap_detector.py)" = "0"` succeeds — the D-06 candidate was resolved by typing, not silenced.
    - `grep -n '_embeddings: NDArray\[np.float32\] | None' src/aquapose/core/reid/swap_detector.py` still matches — the attribute's genuine optionality is preserved; the fix was at the assignment, not by asserting the attribute is never `None`.
    - The plan SUMMARY contains an explicit verdict section for line 310 stating (a) whether line 310 is inside the assignment's guard block, (b) what type the loaded expression infers as, and (c) the conclusion: narrowing artifact or live defect.
    - If the verdict is "live defect", `.planning/todos/pending/2026-09-02-swap-detector-embeddings-optional-access.md` exists and names the reachable path — and the SUMMARY says the defect was **not** repaired in this phase.
    - `hatch run test 2>&1 | tail -1` contains `1403 passed, 3 skipped, 17 deselected`, with `git diff --name-only d3e6073 -- tests/` listing at most `tests/unit/core/test_h5.py`.
    - `hatch run lint` and `hatch run format --check` exit 0.
  </acceptance_criteria>
  <done>`core/reid/swap_detector.py` is at 0 typecheck errors, with a written verdict on line 310 and no suppression directive anywhere in the file.</done>
  <reversibility rating="reversible">A local binding with an explicit annotation; no behavior change and nothing persisted. If the verdict is "live defect", the defect itself is deliberately left in place for a later decision, so nothing irreversible happens here either.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk `reid/embeddings.npz` -> `SwapDetector.__init__` | Loaded with `allow_pickle=True`, which is an arbitrary-code-execution surface if the file is attacker-controlled. Pre-existing; this plan reads that line but does not change it. |
| on-disk `.h5` opened `r+` -> `swap_detector.py:799-812` | Read-write handle driving an in-place `fish_id` rewrite; the `frame_index` read narrowed in Task 1 supplies the row offset for that write. |
| optional instance state -> its readers | `self._embeddings`, `self._emb_frames`, `self._emb_fish_ids` and `self._swap_events_raw` are all declared optional and read elsewhere; a wrong non-optional assertion converts a loud `AttributeError` into silent wrong behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113.2-14 | Tampering | Asserting `self._embeddings` is never `None` to clear line 310 when a reader can in fact run with it unset — converting a detectable crash into silent wrong output | high | mitigate | Task 2 requires a written verdict before any edit, forbids both a suppression directive and an always-true `is not None` guard, and pins the attribute's declared optional type with a grep criterion. A "live defect" verdict routes to a todo instead of a repair, per D-06. |
| T-113.2-15 | Tampering | Narrowing `self._swap_events_raw` in a way that removes its `None` arm, making the `is None` guard at :453 dead and swap detection run on absent data | medium | mitigate | The action requires an explicit optional annotation; an acceptance criterion greps that the `is None` guard still exists. |
| T-113.2-16 | Tampering | A narrowing error at :802 feeding the wrong row offset into the in-place `fish_id` rewrite at :805-812, corrupting a stitched artifact | high | mitigate | Only the read expression changes; the write handle at :800 is left as-is. The full suite exercises swap repair and must stay at `1403 passed`. |
| T-113.2-17 | Elevation of Privilege | `np.load(..., allow_pickle=True)` at :304 is an arbitrary-code-execution surface on an untrusted `.npz` | medium | accept | Pre-existing and out of scope: this is a typing phase (D-06 forbids behavior changes), the file is produced by this project's own pipeline in a run directory, and changing the load policy would alter runtime behavior. Recorded here so it is visible rather than implied; `/gsd-secure-phase` owns deserialization canon. |
| T-113.2-SC | Tampering | Supply chain — package-manager installs | high | accept | No `pip`/`npm`/`cargo` install tasks. `h5py` and `numpy` are already imported by this module. |

ASVS level 1, blocking threshold `high`. Both `high` threats on live surfaces (T-113.2-14, T-113.2-16) are dispositioned `mitigate`. The accepted `high` row is supply chain, which has no surface in this phase.
</threat_model>

<artifacts_this_phase_produces>
This plan produces **no new symbols and no new source files**. It adds one
`aquapose.core.h5` import, two narrowing calls, and up to four explicit local/attribute
annotations:

| File | Change |
|---|---|
| `src/aquapose/core/reid/swap_detector.py` | import + 2 `require_dataset` calls; explicit optional annotation on `self._swap_events_raw`; typed local bindings for the three `.npz`-loaded arrays |

Conditionally produced, only if Task 2's verdict is "live defect":

| Artifact | Kind |
|---|---|
| `.planning/todos/pending/2026-09-02-swap-detector-embeddings-optional-access.md` | new todo recording an unrepaired latent defect, per D-06 |

Nothing is added to `tests/`. The phase-wide artifact list is consolidated in `113.2-06-PLAN.md`.
</artifacts_this_phase_produces>

<verification>
- `core/reid/swap_detector.py` reports 0 typecheck errors.
- The file contains zero suppression directives.
- `self._embeddings` and `self._swap_events_raw` retain their declared optional types, and the `is None` guard at :453 still exists.
- The six pre-existing `cast()` calls are untouched.
- `hatch run test -k import_boundary` passes, including the stale-allowlist test, with the `reid/swap_detector.py` exemption unchanged.
- `hatch run test` reports `1403 passed, 3 skipped, 17 deselected` with no test edited.
- `hatch run lint` and `hatch run format --check` exit 0.
</verification>

<success_criteria>
The last h5py cluster file is clean, and the phase's single most likely latent defect has a
written verdict backed by source rather than an annotation that assumed the answer.
</success_criteria>

<output>
Create `.planning/phases/113.2-typecheck-backlog/113.2-05-SUMMARY.md` when done.
Record: the full line-310 verdict (guard-block containment, inferred type of the loaded
expression, and the artifact-or-defect conclusion), and whether a todo was filed.
</output>
