---
phase: 108-branch-reconciliation-repo-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docs/conf.py
  - docs/index.md
  - docs/api/index.rst
  - docs/api/engine.rst
  - docs/api/evaluation.rst
  - docs/api/synthetic.rst
  - docs/api/training.rst
  - docs/api/mesh.rst
  - docs/api/optimization.rst
  - docs/api/segmentation.rst
  - docs/api/utils.rst
  - .github/workflows/docs.yml
autonomous: true
requirements: [FOUND-02, FOUND-03]

must_haves:
  truths:
    - "D-01: `main`'s 4 commits are ancestors of `dev` via a real merge commit — `git log --oneline dev..main` is empty."
    - "D-01: `dev`'s 1496 published commits are untouched — no rebase, no force-push, no history rewrite."
    - "D-04/D-05: the `main` branch ref and GitHub's default-branch setting are unchanged by this plan."
    - "D-06 (accepted consequence): a GitHub visitor lands on a 1496-commit-stale, MIT-declaring `main` for Phases 108-113. This is knowingly accepted because the repository is not announced until Phase 114, which owns the `main` sync, the release tag, and the Read the Docs source-branch decision. Do not silently 'improve' this by flipping the default branch."
    - "D-03: `dev`'s package version is `1.2.0-dev.0` — ahead of the shipped `1.1.1`, still on the prerelease channel."
    - "D-17/FOUND-02: `.github/workflows/docs.yml` on `dev` triggers on push to `dev`."
    - "D-13: the four dead `.rst` files are gone from `dev` and `main`'s 7-file API set is present."
    - "D-15/D-16: `docs/conf.py` on `dev` carries `napoleon_use_ivar`, `autodoc_mock_imports`, and `_resolve_release()`; the docs hatch env is `detached = true`."
  artifacts:
    - path: "pyproject.toml"
      provides: "version 1.2.0-dev.0 + detached docs env with numpy"
      contains: "detached = true"
    - path: ".github/workflows/docs.yml"
      provides: "push trigger on main and dev"
      contains: "branches: [main, dev]"
    - path: "docs/conf.py"
      provides: "forward-ported Sphinx repair from a66287a"
      contains: "napoleon_use_ivar"
    - path: "docs/api/engine.rst"
      provides: "live automodule target replacing dead mesh.rst"
  key_links:
    - from: "dev"
      to: "main"
      via: "merge commit"
      pattern: "git log --oneline dev..main -> empty"
    - from: "pyproject.toml [tool.hatch.envs.docs]"
      to: "docs/conf.py autodoc_mock_imports"
      via: "detached env relies on mocks instead of installed deps"
      pattern: "detached = true"
---

<objective>
Bring `main`'s 4 release/docs commits onto `dev` with a real merge (D-01), resolving the single
expected conflict site — `pyproject.toml` — in favor of the phase decisions (D-02, D-03), and
verify that the Sphinx repair (`a66287a`) and the docs CI push trigger landed intact.

Purpose: `dev` becomes the single source of truth with true ancestry to `main`, so every future
`main`→`dev` sync is conflict-free. FOUND-03 is satisfied here; FOUND-02's workflow file arrives
as a side effect of the merge (D-17) and is verified rather than re-authored.

Output: a merge commit on `dev`; `pyproject.toml` at `1.2.0-dev.0` with a detached docs env;
`docs/` matching `main`'s repaired tree; `docs.yml` triggering on push to `dev`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md

**Pre-verified merge facts — do not re-derive:**

- Merge base is `cdd9a74`. `dev` is 1496 commits ahead of `main`; `main` has exactly 4 commits
  not in `dev`: `19ea21e`, `9aa1fd6` (chore(release): 1.1.0), `a66287a` (fix(docs): repair Sphinx
  build and gate it on push), `2aba732` (chore(release): 1.1.1).
- Files changed on the **main side** since the merge base: `.github/workflows/docs.yml`,
  `docs/conf.py`, `docs/index.md`, `docs/api/{index,engine,evaluation,synthetic,training}.rst`
  (added), `docs/api/{mesh,optimization,segmentation,utils}.rst` (deleted),
  `docs/reports/*` (added), `pyproject.toml`.
- Files changed on the **dev side** since the merge base: `docs/reports/*` (identical blobs to
  main's — merges without conflict), `pyproject.toml`.
- Therefore **`pyproject.toml` is the only expected conflict**. All of `docs/` and `.github/`
  should merge clean, taking main's side, because dev never touched those paths.

**`pyproject.toml` resolution targets:**

| Region | dev side | main side | Resolution |
|---|---|---|---|
| `project.version` (line 7) | `1.1.0-dev.7` | `1.1.1` | **`1.2.0-dev.0`** (D-03) |
| `project.license` (line 11) | `{text = "MIT"}` | `{text = "MIT"}` | Leave MIT here — plan 108-02 owns the relicense |
| `project.dependencies` | grown (timm, shapely, pytorch-metric-learning, igraph, leidenalg, …) | older | **Keep dev's** |
| `[tool.hatch.envs.docs]` | no `detached`, no `numpy` | `detached = true` + explanatory comment + `numpy` in deps | **Take main's** (D-15) |
| `[tool.hatch.envs.docs.scripts] build` | identical on both | identical | either |
| everything else | newer | 1496 commits stale | **Keep dev's** |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge main into dev and resolve pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - `pyproject.toml` (current `dev` state) — note line 7 `version`, line 11 `license`, the
      `dependencies` array, and `[tool.hatch.envs.docs]` at line 83.
    - `git show main:pyproject.toml` — the source-of-truth analog for the docs env block
      (`detached = true` plus its explanatory comment, and `numpy` in the dependency list).
    - `git log -1 --format=%B a66287a` — the specification for the Sphinx repair being ported.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` §§ D-01, D-02, D-03,
      D-04, D-05.
  </read_first>
  <action>
    Confirm the current branch is `dev` and that `git status --porcelain` shows only the two
    untracked paths `11.0` and `~/` (those are plan 108-03's problem; leave them alone).

    Run `git merge main -m "merge: reconcile main into dev (1.1.1 release history + a66287a Sphinx repair)"`.

    Do NOT use `git rebase`, `git cherry-pick`, `--force`, or `push --force` at any point.
    Do NOT check out, commit to, or push `main`.

    Resolve the `pyproject.toml` conflict by hand (not with `-X ours` / `-X theirs`):
    - Set `version = "1.2.0-dev.0"` on line 7 of the `[project]` table.
    - Leave `license = {text = "MIT"}` and the MIT OSI classifier untouched — plan 108-02 owns
      the relicense and owns this file after this plan.
    - Keep `dev`'s full `dependencies` array verbatim (it must still contain `timm`,
      `shapely`, `pytorch-metric-learning`, `igraph`, `leidenalg`, `ultralytics`).
    - Replace `dev`'s `[tool.hatch.envs.docs]` block with `main`'s: the `detached = true` key,
      main's two-sentence comment above it explaining the CUDA-torch / Read the Docs limit, and
      main's dependency list ending in `"numpy",`. Keep the existing
      `[tool.hatch.envs.docs.scripts] build` line (identical on both sides).
    - Keep `dev`'s `[tool.semantic_release]` tables unchanged.

    If any file outside `pyproject.toml` conflicts, resolve `docs/**` and `.github/**` in favor
    of `main` (dev never modified them since the merge base) and stop to report anything else.

    Then `git add pyproject.toml` and complete the merge with `git commit --no-edit`.

    Do NOT push. The push is deliberately deferred to plan 108-05 so that `release.yml`
    (which runs `semantic-release version` on every push to `dev`) cannot cut a prerelease tag
    while the repository still declares MIT.
  </action>
  <verify>
    <automated>git log --oneline dev..main | wc -l | grep -qx '0' &amp;&amp; git rev-parse -q --verify MERGE_HEAD; test $? -ne 0 &amp;&amp; grep -q 'version = "1.2.0-dev.0"' pyproject.toml &amp;&amp; grep -q 'detached = true' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline dev..main` outputs zero lines.
    - `git rev-parse -q --verify MERGE_HEAD` exits non-zero (the merge is committed, not in progress).
    - `git status --porcelain` lists only `11.0` and `~/` as untracked, with no modified files.
    - `grep -c 'version = "1.2.0-dev.0"' pyproject.toml` returns `1`.
    - `grep -c 'detached = true' pyproject.toml` returns `1`.
    - `grep -c '"numpy",' pyproject.toml` returns at least `1`.
    - `grep -q 'timm' pyproject.toml` exits 0 and `grep -q 'pytorch-metric-learning' pyproject.toml` exits 0.
    - `git rev-parse main` equals its pre-merge value (recorded before the merge) — `main` is untouched.
    - `git log --oneline -1 --merges` names the new merge commit.
    - `git reflog dev | grep -qi 'rebase'` exits non-zero.
  </acceptance_criteria>
  <done>A merge commit exists on `dev`, `main` is fully contained in `dev`, `pyproject.toml` is at `1.2.0-dev.0` with a detached docs env and dev's dependency list, and nothing has been pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the merged docs and CI artifacts landed intact</name>
  <files>docs/conf.py, docs/api/index.rst, .github/workflows/docs.yml</files>
  <read_first>
    - `docs/conf.py` (post-merge working tree) and its source-of-truth analog `git show main:docs/conf.py`.
    - `.github/workflows/docs.yml` (post-merge) and `git show main:.github/workflows/docs.yml`.
    - `docs/api/index.rst` (post-merge) and `git show main:docs/api/index.rst`.
    - `docs/index.md` (post-merge) — main's version restores the `reports/z_uncertainty_report`
      toctree entry, which `a66287a` added specifically to silence a `-W` fatal orphan-document warning.
  </read_first>
  <action>
    This is a verification task, not an authoring task (D-13, D-17: the merge delivers these; the
    job is to confirm they survived). Assert, and repair only by restoring main's blob
    (`git checkout main -- <path>`) if a file drifted:

    1. `docs/conf.py` byte-identical to `git show main:docs/conf.py`. It must contain
       `napoleon_use_ivar`, `autodoc_mock_imports`, `def _resolve_release()`, the
       `from importlib.metadata import version as _package_version` alias, and
       `version = ".".join(release.split(".")[:2])`. (Plan 108-05 will extend
       `autodoc_mock_imports`; do not extend it here.)
    2. `docs/api/` contains exactly: `calibration.rst`, `core.rst`, `engine.rst`,
       `evaluation.rst`, `index.rst`, `io.rst`, `synthetic.rst`, `training.rst` — and none of
       `mesh.rst`, `optimization.rst`, `segmentation.rst`, `utils.rst`.
    3. `.github/workflows/docs.yml` contains `branches: [main, dev]` under `on: push:` and
       retains the `pull_request: branches: [main]` block. This is what satisfies FOUND-02.
    4. `docs/index.md` toctree lists `reports/z_uncertainty_report`.

    Do NOT modify `.github/workflows/{release,publish,test,slow-tests}.yml`. Do NOT add the docs
    build to `.pre-commit-config.yaml` (D-17 explicitly rejects it).

    If any repair was needed, commit it as `fix(108-01): restore merged docs artifact`.
    Do not push.
  </action>
  <verify>
    <automated>git diff --quiet main -- docs/conf.py .github/workflows/docs.yml docs/api/index.rst</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code main -- docs/conf.py` exits 0.
    - `git diff --exit-code main -- .github/workflows/docs.yml` exits 0.
    - `grep -c 'napoleon_use_ivar' docs/conf.py` returns `1`.
    - `grep -q 'def _resolve_release' docs/conf.py` exits 0.
    - `grep -q 'autodoc_mock_imports' docs/conf.py` exits 0.
    - `grep -q 'branches: \[main, dev\]' .github/workflows/docs.yml` exits 0.
    - `git ls-files docs/api | wc -l` returns `8`.
    - `git ls-files docs/api/mesh.rst docs/api/optimization.rst docs/api/segmentation.rst docs/api/utils.rst` outputs zero lines.
    - `grep -q 'reports/z_uncertainty_report' docs/index.md` exits 0.
    - `git log --oneline dev --not main | grep -c .` is greater than 0 while `git log --oneline dev..main | grep -c .` is 0.
  </acceptance_criteria>
  <done>All four merged artifacts are present on `dev` and identical to `main`'s verified-green versions; the four dead `.rst` files are deleted; `docs.yml` triggers on push to `dev`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `dev` → `origin/dev` | 1496 published commits; any history rewrite here is destructive to a shared remote |
| local repo → `main` / `origin/main` | a frozen release branch (D-04) and GitHub's default branch (D-05); no write is authorized |
| repo push → GitHub Actions | `release.yml`, `test.yml`, `docs.yml` all fire on push to `dev`; `release.yml` holds `contents: write` via `RELEASE_TOKEN` and creates commits and tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | published `origin/dev` history | mitigate | Plan mandates `git merge main` only; `git rebase`, `git cherry-pick` of the 4 commits, and any `--force` push are explicitly forbidden in the task action; acceptance asserts `git reflog dev` contains no rebase entry |
| T-108-02 | Tampering | frozen `main` branch ref | mitigate | No task checks out, commits to, or pushes `main`; acceptance records and re-asserts `git rev-parse main` is unchanged |
| T-108-03 | Elevation of Privilege | `release.yml` (`contents: write`, `RELEASE_TOKEN`) | mitigate | Push is deferred out of this plan entirely to 108-05, after the relicense lands, so no prerelease tag can be cut against an MIT-declared tree |
| T-108-04 | Tampering | CI workflow trigger surface | mitigate | Only `docs.yml` changes, and only by taking main's already-reviewed blob; task 2 acceptance pins it byte-identical to `main` and forbids edits to the other four workflows |
| T-108-05 | Repudiation | merge provenance | accept | A real merge commit records true ancestry between the branches; this is the intended audit trail, no extra control needed |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages — the merge only edits `pyproject.toml` declarations; `hatch` env creation happens in plan 108-05 against an already-reviewed dependency set |
</threat_model>

<verification>
- `git log --oneline dev..main` returns zero lines (FOUND-03).
- `git rev-parse main` unchanged from the pre-merge value (D-04, D-05).
- `pyproject.toml` version is `1.2.0-dev.0` (D-03).
- `.github/workflows/docs.yml` has `branches: [main, dev]` under `on: push` (FOUND-02, D-17).
- `docs/api/` holds exactly main's 8-file set; the four dead `.rst` files are gone (D-13).
- `docs/conf.py` is identical to `main`'s (D-15, D-16 groundwork).
- Nothing has been pushed to any remote.
</verification>

<success_criteria>
- `dev` fully contains `main`; a merge commit exists; no history was rewritten.
- `main` and the GitHub default-branch setting are untouched.
- `pyproject.toml`: `version = "1.2.0-dev.0"`, `detached = true` docs env with `numpy`, dev's
  dependency list intact, license fields still MIT (108-02's job).
- The Sphinx repair and docs push trigger are present on `dev` and byte-identical to `main`'s.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-01-SUMMARY.md` when done.
Record the pre-merge `git rev-parse main` value and the merge commit SHA.
</output>
