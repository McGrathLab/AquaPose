---
phase: 110-api-reference-docs-tiering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api/evaluation.rst
  - docs/api/evaluation/core.rst
  - docs/api/evaluation/stages.rst
  - docs/api/evaluation/viz.rst
  - docs/api/training.rst
  - docs/api/training/core.rst
  - docs/api/synthetic.rst
  - docs/api/core/reid.rst
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "Every tier-two submodule (evaluation + stages + viz, all of training, synthetic, core/reid) has an automodule directive in a per-package rst page"
    - "Each tier-two section index (evaluation, training, synthetic, core/reid) carries the verbatim note 'Research utility — not part of the supported pipeline API.' placed immediately after the page heading (D-05)"
    - "Private viz modules (_frames, _loader) are NOT automoduled"
  artifacts:
    - path: "docs/api/evaluation.rst"
      provides: "tier-two section index with status note + toctree to evaluation/core, stages, viz"
      contains: "Research utility — not part of the supported pipeline API."
    - path: "docs/api/evaluation/core.rst"
      provides: "automodule pages for evaluation compare/metrics/output/runner/tuning"
      contains: "automodule:: aquapose.evaluation.runner"
    - path: "docs/api/evaluation/stages.rst"
      provides: "automodule pages for all 8 evaluation.stages submodules"
      contains: "automodule:: aquapose.evaluation.stages.reconstruction"
    - path: "docs/api/evaluation/viz.rst"
      provides: "automodule pages for evaluation.viz animation/detections/overlay/trails"
      contains: "automodule:: aquapose.evaluation.viz.animation"
    - path: "docs/api/training.rst"
      provides: "tier-two section index with status note + toctree to training/core"
      contains: "Research utility — not part of the supported pipeline API."
    - path: "docs/api/training/core.rst"
      provides: "automodule pages for all 21 training submodules"
      contains: "automodule:: aquapose.training.store"
    - path: "docs/api/synthetic.rst"
      provides: "expanded tier-two page with status note + synthetic submodules + core.synthetic"
      contains: "Research utility — not part of the supported pipeline API."
    - path: "docs/api/core/reid.rst"
      provides: "tier-two page with status note + all core.reid submodules"
      contains: "Research utility — not part of the supported pipeline API."
  key_links:
    - from: "docs/api/evaluation.rst"
      to: "docs/api/evaluation/*.rst"
      via: "toctree"
      pattern: "evaluation/(core|stages|viz)"
---

<objective>
Author the tier-two "Research Utilities" API reference pages: section indexes that
carry the verbatim honest status note (D-05) plus per-package automodule pages that
surface every research submodule (training, evaluation + stages + viz, synthetic,
core/reid) per D-02 / D-08.

Purpose: Delivers the tier-two half of DOCS-02 (every research module rendered) and
the tier-two content + honest labeling for DOCS-01. Status note is section-level
(on the index / top of each tier-two page), NOT per-module banners (D-05).
Output: 3 section-index pages with notes, 4 automodule content pages, plus expanded
synthetic.rst and new core/reid.rst.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-api-reference-docs-tiering/110-CONTEXT.md
@.planning/phases/110-api-reference-docs-tiering/110-PATTERNS.md
@docs/api/evaluation.rst
@docs/api/training.rst
@docs/api/synthetic.rst
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evaluation section index + core/stages/viz content pages</name>
  <read_first>
    - docs/api/evaluation.rst (current flat stub — becomes a section index with note + toctree)
    - docs/api/index.rst (toctree pattern reference: :maxdepth: 2, no :hidden:)
    - docs/api/calibration.rst (automodule content-page pattern for the sub-pages)
    - src/aquapose/evaluation/__init__.py, src/aquapose/evaluation/stages/__init__.py, src/aquapose/evaluation/viz/__init__.py (confirm submodule names; note _frames.py and _loader.py are private)
    - .planning/phases/110-api-reference-docs-tiering/110-PATTERNS.md (blueprints for evaluation.rst, evaluation/core, evaluation/stages, evaluation/viz; verbatim note wording in Shared Patterns)
  </read_first>
  <action>
    Create subdirectory docs/api/evaluation/ and author one section index + three
    content pages.

    - docs/api/evaluation.rst (rewrite in place, heading "Evaluation"): a `.. note::`
      admonition containing the verbatim line "Research utility — not part of the
      supported pipeline API." placed immediately after the heading, then a
      `.. toctree:: :maxdepth: 2` listing evaluation/core, evaluation/stages,
      evaluation/viz. Replace the old flat automodule.
    - docs/api/evaluation/core.rst (heading "Evaluation Core"): automodule
      aquapose.evaluation, then .compare, .metrics, .output, .runner, .tuning.
    - docs/api/evaluation/stages.rst (heading "Evaluation Stages"): automodule
      aquapose.evaluation.stages, then .detection, .tracking, .association,
      .fragmentation, .midline, .reconstruction, .smoothing, .stitching.
    - docs/api/evaluation/viz.rst (heading "Evaluation Visualisation"): automodule
      aquapose.evaluation.viz, then .animation, .detections, .overlay, .trails.
      Do NOT automodule aquapose.evaluation.viz._frames or ._loader (private).

    All content-page automodules use :members: :undoc-members: :show-inheritance:.
  </action>
  <verify>
    <automated>python -c "import pathlib; NOTE='Research utility — not part of the supported pipeline API.'; idx=pathlib.Path('docs/api/evaluation.rst').read_text(encoding='utf-8'); assert NOTE in idx and '.. note::' in idx and 'evaluation/stages' in idx, 'index bad'; s=pathlib.Path('docs/api/evaluation/stages.rst').read_text(); assert 'aquapose.evaluation.stages.reconstruction' in s and 'aquapose.evaluation.stages.stitching' in s; v=pathlib.Path('docs/api/evaluation/viz.rst').read_text(); assert 'aquapose.evaluation.viz.animation' in v and '_frames' not in v and '_loader' not in v; c=pathlib.Path('docs/api/evaluation/core.rst').read_text(); assert 'aquapose.evaluation.runner' in c; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/api/evaluation.rst contains a ".. note::" whose body is exactly "Research utility — not part of the supported pipeline API." and a toctree listing evaluation/core, evaluation/stages, evaluation/viz
    - docs/api/evaluation.rst no longer contains a bare top-level ".. automodule:: aquapose.evaluation" (moved to evaluation/core.rst)
    - docs/api/evaluation/stages.rst contains automodule entries for all 8 stages including .reconstruction and .stitching
    - docs/api/evaluation/viz.rst contains .animation/.detections/.overlay/.trails and does NOT contain "_frames" or "_loader"
    - docs/api/evaluation/core.rst contains ".. automodule:: aquapose.evaluation.runner"
  </acceptance_criteria>
  <done>Evaluation is a tier-two section index carrying the verbatim note, with three content pages covering evaluation core, all 8 stages, and the 4 public viz modules.</done>
</task>

<task type="auto">
  <name>Task 2: Training section, synthetic, and core/reid tier-two pages</name>
  <read_first>
    - docs/api/training.rst (current flat stub — becomes a section index with note + toctree to training/core)
    - docs/api/synthetic.rst (extend in place — add note + submodules)
    - docs/api/calibration.rst (automodule content-page pattern)
    - src/aquapose/training/__init__.py, src/aquapose/synthetic/__init__.py, src/aquapose/core/reid/__init__.py (confirm submodule names)
    - .planning/phases/110-api-reference-docs-tiering/110-PATTERNS.md (blueprints for training.rst, training/core, synthetic, core/reid; verbatim note wording)
  </read_first>
  <action>
    - docs/api/training.rst (rewrite in place, heading "Training"): `.. note::` with the
      verbatim "Research utility — not part of the supported pipeline API." after the
      heading, then a toctree listing training/core. Create docs/api/training/ subdir.
    - docs/api/training/core.rst (heading "Training"): automodule aquapose.training, then
      one automodule per training submodule — cli, coco_convert, coco_interchange, common,
      compare, data_cli, datasets, elastic_deform, geometry, hard_mining,
      labelstudio_export, labelstudio_import, prep, pseudo_label_cli, pseudo_labels,
      reid_training, run_manager, select_diverse_subset, store, store_schema,
      yolo_training. (All 21 non-__init__ modules; verify against training/__init__.py so
      none is dropped.)
    - docs/api/synthetic.rst (extend in place, heading "Synthetic"): add `.. note::` with
      the verbatim label after the heading, then keep aquapose.synthetic and add
      .fish, .rig, .scenarios, .trajectory, .detection, .stubs, plus aquapose.core.synthetic
      (D-02 groups core/synthetic.py with the synthetic package on this tier-two page).
    - docs/api/core/reid.rst (new, heading "Re-identification"): `.. note::` verbatim label
      after heading, then automodule aquapose.core.reid, .embedder, .eval, .miner, .runner,
      .swap_detector, .cli. NOTE: this file lives in docs/api/core/ which plan 01 also
      populates — plan 01 owns the tier-one core/*.rst files; this plan owns ONLY
      core/reid.rst (disjoint file, no overlap).

    All automodules use :members: :undoc-members: :show-inheritance:.
  </action>
  <verify>
    <automated>python -c "import pathlib,re; NOTE='Research utility — not part of the supported pipeline API.'; ti=pathlib.Path('docs/api/training.rst').read_text(encoding='utf-8'); assert NOTE in ti and 'training/core' in ti; tc=pathlib.Path('docs/api/training/core.rst').read_text(); [__import__('sys').exit('MISSING '+m) for m in ['store','store_schema','yolo_training','pseudo_labels','run_manager','select_diverse_subset','labelstudio_import'] if f'aquapose.training.{m}' not in tc]; sy=pathlib.Path('docs/api/synthetic.rst').read_text(encoding='utf-8'); assert NOTE in sy and 'aquapose.core.synthetic' in sy and 'aquapose.synthetic.trajectory' in sy; rd=pathlib.Path('docs/api/core/reid.rst').read_text(encoding='utf-8'); assert NOTE in rd and 'aquapose.core.reid.swap_detector' in rd; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/api/training.rst contains a ".. note::" with exactly "Research utility — not part of the supported pipeline API." and a toctree to training/core
    - docs/api/training/core.rst contains an automodule for all 21 training submodules (including store, store_schema, yolo_training, run_manager, pseudo_labels, select_diverse_subset, labelstudio_import) — cross-checked against training/__init__.py
    - docs/api/synthetic.rst contains the verbatim note, ".. automodule:: aquapose.synthetic.trajectory", and ".. automodule:: aquapose.core.synthetic"
    - docs/api/core/reid.rst contains the verbatim note and ".. automodule:: aquapose.core.reid.swap_detector"
    - Each of evaluation.rst, training.rst, synthetic.rst, core/reid.rst carries the note exactly once, positioned after the page heading
  </acceptance_criteria>
  <done>Training section index + content page cover all 21 training modules; synthetic expanded with note and core.synthetic; core/reid.rst created with note and all reid modules.</done>
</task>

</tasks>

<verification>
- All 8 tier-two files exist with expected automodule targets and verbatim notes (task verify commands).
- Private viz modules excluded.
- Note wording matches D-05 verbatim (em-dash preserved; files read/written as utf-8 per project cp1252 pitfall).
- Full -W build integrity is validated in plan 03 after the index wires these into the Research Utilities toctree — do NOT run the build here.
</verification>

<success_criteria>
Every tier-two submodule from the CONTEXT inventory has a per-package automodule
entry, and every tier-two section carries the honest status note verbatim, matching
D-02 membership and D-05 labeling.
</success_criteria>

<output>
Create `.planning/phases/110-api-reference-docs-tiering/110-02-SUMMARY.md` when done.
List every tier-two rst created/modified, its automodule targets, and confirm the
verbatim note placement — so plan 03's index-wiring and coverage cross-check can
consume it.
</output>
