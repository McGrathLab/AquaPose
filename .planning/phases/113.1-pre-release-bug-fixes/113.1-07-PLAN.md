---
phase: 113.1-pre-release-bug-fixes
plan: 07
type: execute
wave: 2
depends_on: [113.1-01, 113.1-02, 113.1-03, 113.1-04]
files_modified:
  - pyproject.toml
  - CLAUDE.md
  - .planning/todos/pending/2026-09-02-pillow-undeclared-dependency.md
  - .planning/todos/done/2026-09-02-pillow-undeclared-dependency.md
  - .planning/todos/pending/2026-09-02-guidebook-sections-3-4-7-8-14-drift-beyond-section-6.md
autonomous: true
requirements: [QA-05]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`pillow` is a declared runtime dependency in `pyproject.toml`. Before this plan it was imported at module level by three `src/` modules (`training/prep.py`, `training/coco_interchange.py`, `training/labelstudio_export.py`) and resolved only transitively through `ultralytics` (`pillow>=7.1.2`) and `torchvision` (`pillow!=8.3.*,>=5.3.0`) — so `pip install aquapose` worked by accident, not by declaration."
    - "The declared floor is compatible with both transitive constraints and with the API the code actually uses: `prep.py` imports `UnidentifiedImageError`, which Pillow added in 7.0.0."
    - "`CLAUDE.md` no longer instructs any agent to read `.planning/GUIDEBOOK.md` as authoritative context. The `## Agent-Specific Instructions` block that told `discuss-phase` to ingest it is removed, replaced by a short note recording that the guidebook is a human reference and is known to have drifted, so a future maintainer does not reinstate the hook."
    - "The GUIDEBOOK drift itself is NOT fixed here — sections 3, 4, 7, 8 and 14 stay as they are and their todo stays pending. Only the ingestion path is closed (user decision, 2026-09-02). Section 6 remains correct and remains pinned by `test_build_stages_sequence_matches_guidebook` from 113.1-03; that test must keep passing."
    - "`hatch run test` shows exactly one failure, the documented pre-existing `test_generates_merged_obb_and_separate_pose`. This plan runs in the same wave as `113.1-06`, which fixes that failure, and does not depend on its outcome — if the suite is already fully green because 06 ran first, that is also acceptable. Any OTHER failure is a regression and must be investigated, not waived."
    - "`hatch run docs:build` exits 0 and `hatch run lint` exits 0 — `pyproject.toml` is the packaging manifest and a malformed edit breaks both."
  artifacts:
    - path: "pyproject.toml"
      provides: "An explicit pillow runtime dependency"
      contains: "pillow"
    - path: "CLAUDE.md"
      provides: "Agent instructions with the GUIDEBOOK ingestion hook removed"
      contains: "GUIDEBOOK"
  key_links:
    - from: "pyproject.toml"
      to: "src/aquapose/training/prep.py"
      via: "the declared pillow dependency now covers the module-level `from PIL import Image, UnidentifiedImageError` that 113.1-01 added a third call site for"
      pattern: "pillow"
  prohibitions:
    - requirement_id: QA-05
      category: correctness
      statement: "MUST NOT remove, replace, or vendor any PIL usage in `src/` to avoid declaring the dependency. The imports are legitimate; the manifest is what is wrong."
    - requirement_id: QA-05
      category: safety
      statement: "MUST NOT run `hatch env create`, `env prune`, or `env remove` to 'verify' the new dependency resolves. The current environment has working CUDA (torch 2.5.1+cu121) and recreating it may silently yield CPU-only torch — a far worse outcome than an unverified version floor. Verify by reading the installed version and the transitive constraints, not by rebuilding."
    - requirement_id: QA-05
      category: transparency
      statement: "MUST NOT fix the GUIDEBOOK sections 3/4/7/8/14 drift, and MUST NOT close its todo. The user's decision was explicitly to leave the content and close only the ingestion path; silently doing the larger job would substitute the executor's judgment for a stated choice."
---

<objective>
Close two pre-release defects that both share a failure mode: something that
looks fine because nothing has forced it to be wrong yet.

`pillow` is imported by three `src/` modules and declared by none — it resolves
only because `ultralytics` and `torchvision` happen to pull it in. `pip install
aquapose` is exactly what Phase 114 is about to advertise, so an accidental
dependency is a release defect.

`CLAUDE.md` instructs `discuss-phase` to read `.planning/GUIDEBOOK.md` as
authoritative project context. `113.1-03` corrected the guidebook's section 6
but found sections 3, 4, 7, 8 and 14 have drifted too. Per the user's decision
(2026-09-02) the content stays as-is and the **ingestion path** is closed
instead — stale prose that nothing treats as authoritative is inert.

Purpose: the packaging half and the agent-context half of the pre-release
backlog.
Output: a declared pillow dependency, and a `CLAUDE.md` that no longer feeds a
known-drifted document into planning.
</objective>

## Artifacts this phase produces

Created by **this plan**: no new symbols. One dependency line added to
`pyproject.toml`; one instruction block removed from `CLAUDE.md` and replaced
with a short explanatory note.

<context>
@.planning/ROADMAP.md
@.planning/phases/113.1-pre-release-bug-fixes/113.1-CONTEXT.md
@.planning/todos/pending/2026-09-02-pillow-undeclared-dependency.md
@CLAUDE.md
</context>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<tasks>

<task type="tracer">
  <name>Task 1: Declare pillow as a runtime dependency</name>

  <read_first>
    - `pyproject.toml` — the `[project] dependencies` list (approximately lines
      26-45), to match the existing style. Neighbours are pinned as
      `numpy>=1.24`, `opencv-python>=4.8`, `scipy>=1.11`, `h5py>=3.9` — a bare
      lower-bound `>=` with no upper cap.
    - `.planning/todos/pending/2026-09-02-pillow-undeclared-dependency.md` — the
      filed analysis, including the three importing modules.
    - `src/aquapose/training/prep.py` (import block, approximately line 19) —
      confirm it imports `UnidentifiedImageError`, which sets the true API floor
      at Pillow 7.0.0.
  </read_first>

  <action>
    Add `pillow>=10.0` to the `dependencies` array in `pyproject.toml`, placed to
    match the list's existing ordering convention.

    `10.0` is chosen as a modern floor that satisfies every real constraint
    rather than the bare minimum: the code's own API floor is 7.0.0
    (`UnidentifiedImageError`), `ultralytics` requires `pillow>=7.1.2`, and
    `torchvision` requires `pillow!=8.3.*,>=5.3.0`. A `>=10.0` floor is above all
    three, so it cannot conflict, and it matches how the project pins its other
    scientific dependencies. The installed version in the working environment is
    12.1.1.

    Do not add an upper cap — no neighbouring dependency has one.
    Do not modify any `src/` file.
  </action>

  <acceptance_criteria>
    - `pyproject.toml` `[project] dependencies` contains a `pillow` entry with a
      `>=` lower bound and no upper cap.
    - The file still parses: `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0.
    - The declared floor is at or above 7.1.2, verified against the two
      transitive constraints by reading them from installed metadata
      (`importlib.metadata.requires('ultralytics')` and `...('torchvision')`) and
      recording both in the SUMMARY. Do NOT recreate the hatch environment to
      check this.
    - `hatch run lint` exits 0 and `hatch run docs:build` exits 0.
    - `git diff --stat src/` is empty for this task's commit.
    - The todo `.planning/todos/pending/2026-09-02-pillow-undeclared-dependency.md`
      is moved to `.planning/todos/done/` with a `## Fixed` note naming this
      phase and plan and the declared floor.
  </acceptance_criteria>

  <verify>
    python3 -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); deps=d['project']['dependencies']; print([x for x in deps if 'pillow' in x.lower()]); sys.exit(0 if any('pillow' in x.lower() for x in deps) else 1)"
    hatch run lint
    hatch run docs:build
  </verify>
</task>

<task>
  <name>Task 2: Stop CLAUDE.md feeding GUIDEBOOK.md into planning as authoritative</name>

  <read_first>
    - `CLAUDE.md` — the `## Agent-Specific Instructions` section (approximately
      lines 65-71). It is the ONLY place in the repo that instructs an agent to
      ingest the guidebook; verified 2026-09-02 by grep across `*.md`, `*.py`,
      `*.toml`, `*.json`.
    - `.planning/todos/pending/2026-09-02-guidebook-sections-3-4-7-8-14-drift-beyond-section-6.md`
      — the drift this closes the blast radius of, filed by 113.1-03.
    - `tests/unit/engine/test_pipeline.py` (approximately lines 317-395) — the
      section 6 drift guard added by 113.1-03. It must keep passing; this task
      does not touch section 6 or the guard.
  </read_first>

  <action>
    Remove the `## Agent-Specific Instructions` section from `CLAUDE.md` —
    including its `### discuss-phase` block and all three of its numbered steps,
    the second of which is the instruction to read `.planning/GUIDEBOOK.md` as
    discussion context. The section contains nothing else, so it goes entirely.

    In its place, add a short note (2-3 sentences, no heading hierarchy deeper
    than the surrounding document) recording that `.planning/GUIDEBOOK.md` is a
    human-oriented reference, that it is known to have drifted from the code in
    sections 3, 4, 7, 8 and 14, and that agents should ground pipeline and
    architecture claims in source rather than in the guidebook. Name the drift
    todo by filename so the reason is traceable. The point of the note is to stop
    a future maintainer from reinstating the hook without knowing why it was
    removed.

    Do not edit `.planning/GUIDEBOOK.md`. Do not close or modify the drift todo.
  </action>

  <acceptance_criteria>
    - `CLAUDE.md` no longer contains the string `.planning/GUIDEBOOK.md` inside
      any instruction telling an agent to read it. A mention inside the new
      explanatory note is expected and correct.
    - `grep -n "Agent-Specific Instructions" CLAUDE.md` returns nothing.
    - `grep -rn "GUIDEBOOK" CLAUDE.md` returns only the explanatory note's lines.
    - `.planning/GUIDEBOOK.md` is unmodified: `git diff --stat .planning/GUIDEBOOK.md`
      is empty for this task's commit.
    - The drift todo is still in `.planning/todos/pending/` and unmodified.
    - `hatch run test -k test_build_stages_sequence_matches_guidebook` passes —
      the section 6 guard from 113.1-03 is unaffected.
    - The SUMMARY states that the guidebook content was deliberately left
      drifted per the user's 2026-09-02 decision, and that only the ingestion
      path was closed.
  </acceptance_criteria>

  <verify>
    grep -n "Agent-Specific Instructions" CLAUDE.md || echo "OK: section removed"
    grep -n "GUIDEBOOK" CLAUDE.md
    git diff --stat .planning/GUIDEBOOK.md
    hatch run test -k test_build_stages_sequence_matches_guidebook
  </verify>
</task>

</tasks>

<threat_model>
ASVS L1, block on high.

| ID | STRIDE | Threat | Severity | Mitigation | Disposition |
|---|---|---|---|---|---|
| T-113.1-07-SC | Supply chain | A newly declared dependency pins a floor that conflicts with a transitive constraint, or silently widens the install surface | high | Floor is verified against `ultralytics` (`pillow>=7.1.2`) and `torchvision` (`pillow!=8.3.*,>=5.3.0`) read from installed metadata, and recorded in the SUMMARY. `pillow` is already present transitively in every current install, so declaring it adds no new package to the resolved set — it only makes an existing one explicit. No new third-party package is introduced. | mitigate |
| T-113.1-07-E | Elevation | Rebuilding the hatch env to "verify" resolution silently downgrades torch to CPU-only | high | Prohibition forbids `hatch env create`/`prune`/`remove`; acceptance criteria verify by reading metadata instead | mitigate |
| T-113.1-07-T | Tampering | The GUIDEBOOK drift is silently fixed instead of left, substituting executor judgment for a stated user decision | medium | Prohibition plus an acceptance criterion asserting `git diff --stat .planning/GUIDEBOOK.md` is empty and the todo is still pending | mitigate |
| T-113.1-07-I | Info disclosure | None in scope | n/a | n/a | accept |
</threat_model>

<edge_coverage>
This plan owns 0 rows of the phase's 5 probe-surfaced edges. All 5 are owned by
`113.1-01`. No edge is dropped, dismissed, or duplicated here.
</edge_coverage>

<success_criteria>
- [ ] `pillow>=10.0` declared in `pyproject.toml`, floor verified against both transitive constraints
- [ ] `pyproject.toml` still parses; `hatch run lint` and `hatch run docs:build` exit 0
- [ ] No hatch environment was created, pruned, or removed
- [ ] `CLAUDE.md`'s `## Agent-Specific Instructions` section removed, explanatory note added in its place
- [ ] `.planning/GUIDEBOOK.md` unmodified and its drift todo still pending
- [ ] The section 6 drift guard still passes
- [ ] No `src/` file modified
</success_criteria>
