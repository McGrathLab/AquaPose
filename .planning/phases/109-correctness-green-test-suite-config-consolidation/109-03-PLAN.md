---
phase: 109-correctness-green-test-suite-config-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aquapose/cli.py
  - pyproject.toml
  - tests/unit/training/test_training_cli.py
  - tests/unit/evaluation/test_viz.py
autonomous: true
requirements: [QA-04, QA-02]
must_haves:
  truths:
    - "The init tutorial config uses only relative, platform-neutral paths (video_dir, calibration_path, output_dir, weights_path all relative) and is written with encoding='utf-8' (QA-04, D-08)"
    - "test_viz.py::test_prefers_stitched_h5 reads generated animation output with encoding='utf-8' (never OS default cp1252) (D-08)"
    - "The 3 train {obb,seg,pose} --help flag assertions match the real CLI: --val-split is not asserted because the split lives in `data assemble`, not the train commands (D-11)"
    - "pyproject.toml [project.urls] contains no tlancaster6 references (D-06)"
    - "No @pytest.mark.skip or xfail was added to any test in this plan"
  artifacts:
    - path: "src/aquapose/cli.py"
      provides: "init_cmd config written utf-8; audited user-facing read/write_text for encoding"
      contains: "encoding=\"utf-8\""
    - path: "pyproject.toml"
      provides: "Corrected Homepage/Repository/Issues URLs"
      contains: "[project.urls]"
    - path: "tests/unit/training/test_training_cli.py"
      provides: "Corrected train help flag assertions"
      contains: "def test_train_obb_help_shows_expected_flags"
  key_links:
    - from: "tests/unit/training/test_training_cli.py expected flag list"
      to: "src/aquapose/training/cli.py train obb/seg/pose @click.option set"
      via: "assertion matches real command definition"
      pattern: "--val-split"
    - from: "tests/unit/evaluation/test_viz.py out_path.read_text"
      to: "generated animation file"
      via: "encoding='utf-8'"
      pattern: "read_text\\(encoding"
---

<objective>
Close the platform-independent and Windows-cell miscellany: the 3 stale
`--val-split` train-help assertions (D-11), the cp1252 encoding failure in
`test_viz` (D-08), QA-04 tutorial-config platform-neutrality plus the sibling
`write_text` encoding audit in `init_cmd` (D-05/D-08), and the 3 stale
`tlancaster6/AquaPose` URLs in `pyproject.toml` (D-06). All small, mostly
independent; exclusive files so this runs in parallel with the config and store plans.

Purpose: Get the 3 platform-independent CLI-help failures (red on every CI cell) and
the 1 Windows encoding failure green, and make the tutorial config runnable unmodified
across Linux/macOS/Windows (QA-04), while fixing shippable PyPI metadata URLs.

Output: Corrected train-help assertions; `test_viz` reading utf-8; `init_cmd` writing
its config with `encoding="utf-8"` and only relative paths; corrected pyproject URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md
@.planning/GUIDEBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 3 stale --val-split train-help assertions (D-11) and the cp1252 encoding read (D-08)</name>
  <files>tests/unit/training/test_training_cli.py, tests/unit/evaluation/test_viz.py</files>
  <read_first>
    - tests/unit/training/test_training_cli.py — `test_train_obb_help_shows_expected_flags` (line ~29), `test_train_seg_help_shows_expected_flags` (line ~108), `test_train_pose_help_shows_expected_flags` (line ~132); each asserts `--val-split` is in the help output
    - src/aquapose/training/cli.py — the `train obb` (line ~114), `seg` (line ~245), `pose` (line ~314) command @click.option blocks: NONE defines `--val-split`; dataset splitting is a `data assemble` concern (val_fraction), not a train-command flag
    - tests/unit/evaluation/test_viz.py — `test_prefers_stitched_h5` (line ~324); `content = out_path.read_text()` at line ~336 uses the OS default encoding (cp1252 on Windows) on the generated animation file
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md — D-11 (classify renamed/removed vs dropped; one-line root-cause note per test) and D-08 (pass encoding="utf-8")
  </read_first>
  <action>
    D-11 classification (record as the one-line root-cause note per test): `--val-split`
    is NOT a train-command flag — the train obb/seg/pose commands never define it; the
    train/val split is performed earlier by `data assemble` via `val_fraction`. This is a
    stale assertion, not a dropped feature, so REMOVE `--val-split` from the three
    `expected_flags` lists to match the real CLI help. Do not add a `--val-split` option to
    the train commands (that would misplace the split responsibility). Leave the other
    asserted flags (`--data-dir`, `--tag`, `--epochs`, `--device`, `--batch-size`,
    `--imgsz`, `--model`, `--weights`) intact — verify each still appears in the real help
    for its command. For D-08, change the `test_viz` `out_path.read_text()` to
    `out_path.read_text(encoding="utf-8")` so it does not depend on the Windows default
    cp1252; if the file under test is genuinely binary, read bytes and decode utf-8 instead
    — determine which by inspecting what `generate_animation` writes (it is text/SVG/HTML
    containing "Fish 99").
  </action>
  <verify>
    <automated>hatch run test tests/unit/training/test_training_cli.py tests/unit/evaluation/test_viz.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The three train-help tests pass; none asserts `--val-split`; each still asserts the retained flag set present in the real command help
    - `test_prefers_stitched_h5` reads with `encoding="utf-8"` and passes on the current platform
    - `hatch run test tests/unit/training/test_training_cli.py tests/unit/evaluation/test_viz.py -q` exits 0 with no skip/xfail added
    - A one-line D-02/D-11 root-cause note per fixed test is recorded in the SUMMARY
  </acceptance_criteria>
  <done>The 3 CLI-help tests and the viz encoding test pass with the fixes matching real CLI behavior and utf-8 reads; root-cause notes recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Make init_cmd config platform-neutral + utf-8 (QA-04/D-05/D-08) and fix pyproject URLs (D-06)</name>
  <files>src/aquapose/cli.py, pyproject.toml</files>
  <read_first>
    - src/aquapose/cli.py — `init_cmd` (line ~155): the config dict already uses relative `video_dir="videos"`, `calibration_path="geometry/calibration.json"`, `output_dir="runs"`, and relative `weights_path` under detection/pose; the final `(project_dir / "config.yaml").write_text(header + yaml_content)` at line ~208 has NO encoding= argument
    - .planning/GUIDEBOOK.md §11 — the project_dir-relative resolution convention the tutorial config must round-trip through (paths relative in the file, resolved at load)
    - pyproject.toml — `[project.urls]` (lines 46-50): Homepage/Repository/Issues use `github.com/tlancaster6/aquapose`
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md — D-05 (fix existing tutorial config: relative + platform-neutral), D-08 (audit sibling read_text/write_text on user-facing paths for missing encoding), D-06 (fix the 3 tlancaster6 URLs; leave the rest for Phase 114)
  </read_first>
  <action>
    In `init_cmd`, add `encoding="utf-8"` to the `config.yaml` `write_text` call (D-08).
    Audit the surrounding user-facing read_text/write_text in cli.py for the same missing
    encoding and add `encoding="utf-8"` where a user-facing text file is written/read;
    record which sites were touched. Confirm the scaffolded config's non-`project_dir`
    paths are all relative and forward-slash / os-neutral (`video_dir`,
    `calibration_path`, `output_dir`, `detection.weights_path`, `pose.weights_path`) so the
    tutorial config runs unmodified on Linux/macOS/Windows via the GUIDEBOOK §11
    project_dir-relative resolution (QA-04/D-05); the sole absolute value is `project_dir`
    itself, which is correct and machine-specific by design. Do NOT hardcode any absolute
    or backslash paths into the scaffold. In `pyproject.toml`, replace the 3
    `tlancaster6/AquaPose` (case-insensitive) URLs — Homepage, Repository, Issues — with
    the correct owner/repo per the project's canonical GitHub location (D-06); leave the
    Documentation URL and all other stale URLs (CODE_OF_CONDUCT.md, docs/contributing.md,
    CHANGELOG.md) for Phase 114.
  </action>
  <verify>
    <automated>hatch run test tests/unit -q -k "init or scaffold or project"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'encoding="utf-8"' src/aquapose/cli.py` increases (init_cmd config write now specifies utf-8)
    - The scaffolded config's `video_dir`/`calibration_path`/`output_dir`/`detection.weights_path`/`pose.weights_path` are all relative (only `project_dir` is absolute)
    - `grep -ci "tlancaster6" pyproject.toml` returns 0
    - `hatch run check` (lint + typecheck) exits 0 for the edited files
  </acceptance_criteria>
  <done>init_cmd writes a utf-8, relative-path tutorial config; user-facing text I/O in cli.py specifies utf-8; the 3 pyproject URLs are corrected.</done>
</task>

</tasks>

<threat_model>
Internal correctness / repo-hygiene work. No new external attack surface; the URL
edits are static metadata, the encoding edits are local file I/O, the test edits
change assertions only.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-03-01 | Information Disclosure | scaffolded config paths | accept | Only `project_dir` is absolute (machine-specific by design); no secrets written. |
</threat_model>

<verification>
- `hatch run test tests/unit/training/test_training_cli.py tests/unit/evaluation/test_viz.py -q` exits 0
- `grep -ci tlancaster6 pyproject.toml` returns 0
- init_cmd config write specifies encoding="utf-8"
</verification>

<success_criteria>
The 3 stale CLI-help tests + the encoding test pass; the tutorial config is relative,
platform-neutral, and utf-8; the 3 pyproject URLs are corrected; no skip/xfail added.
</success_criteria>

<output>
Create `.planning/phases/109-correctness-green-test-suite-config-consolidation/109-03-SUMMARY.md` when done, with the D-11/D-02 root-cause notes for each fixed CLI-help test.
</output>
