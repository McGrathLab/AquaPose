---
phase: 108-branch-reconciliation-repo-hygiene
plan: 04
type: execute
wave: 4
depends_on: ["108-03"]
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements: [REC-01]

must_haves:
  truths:
    - "REC-01/D-21: `.planning/MILESTONES.md` contains a v3.11 Appearance-Based ReID entry reconstructed from ROADMAP.md and `.planning/phases/102-107/`."
    - "D-21: it also contains a v2.2 Backends entry — the same class of gap — reconstructed from `.planning/milestones/v2.2-phases/29-*` through `33.1-*`, which the 108-01 merge made available on `dev`."
    - "D-21: entries read newest-first by ship date, with no out-of-order tail — v1.0 no longer sits between v3.0 and v2.0."
    - "D-22: both new entries use the existing heading and field schema; no richer schema was invented."
    - "D-21: no `(none recorded)` accomplishment list on an older entry was filled in."
    - "T-108-18: every numeric field in the two new entries is either measured from git/disk or omitted — never estimated."
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "complete, chronologically ordered milestone record"
      contains: "## v3.11 Appearance-Based ReID (Shipped:"
  key_links:
    - from: ".planning/MILESTONES.md"
      to: ".planning/ROADMAP.md"
      via: "phase ranges and ship dates agree between the two files"
      pattern: "102-107"
    - from: ".planning/MILESTONES.md"
      to: ".planning/milestones/v2.2-phases/"
      via: "the v2.2 accomplishments trace to those SUMMARY files"
      pattern: "29-33.1"
---

<objective>
Backfill the two missing MILESTONES.md entries — **v3.11 Appearance-Based ReID** (REC-01) and
**v2.2 Backends** (D-21, the same class of gap) — reconstructed from ROADMAP.md and the phase
record, then re-sort the broken tail into chronological order.

Purpose: satisfies REC-01. v3.11 was in progress while `.planning/` was untracked on `dev`, so it
never got an entry; v2.2 has the same gap. Both are now fully reconstructable: v3.11 from
`.planning/phases/102-107/`, and v2.2 from `.planning/milestones/v2.2-phases/`, which the 108-01
merge brought onto `dev` from `main`'s archive (those directories did not exist on `dev` before).

Output: `.planning/MILESTONES.md` with 16 entries in strict newest-first ship-date order.

Scope fence: this plan edits exactly one file. Do not fill `(none recorded)` accomplishment lists
on older entries (D-21 — archaeology across 14 milestones, explicitly deferred). Do not invent a
richer entry schema (D-22). **Do not push** — the phase's single push happens in plan 108-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**No worktree isolation.** This plan runs sequentially on the main working tree with HEAD on `dev`.
`.planning/` is now un-ignored (108-03), so `git add` works without `-f`.
</execution_context>

<context>
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md

**Verified facts — do not re-derive.**

`.planning/MILESTONES.md` opens with `# Milestones` on line 1 and currently holds **14** entries in
this file order: v3.10, v3.9, v3.8, v3.7, v3.6, v3.5, v3.4, v3.3, v3.2, v3.1, v3.0, **v1.0, v2.0,
v2.1** — the last three are out of order. Missing: **v2.2 Backends** and **v3.11 Appearance-Based
ReID**. The file contains exactly **1** occurrence of `(none recorded)`.

The 108-01 merge resolved this file to dev's side, which was correct: dev's 14 entries are a strict
superset of main's 9.

Ship dates (from `.planning/ROADMAP.md` lines 8 and 20 and the existing headings):

| Milestone | Phases | Shipped |
|---|---|---|
| v3.11 Appearance-Based ReID | 102-107 | 2026-03-26 |
| v3.10 Publication Metrics | 97-101 | 2026-03-15 |
| v3.1 Reconstruction | — | 2026-03-03 |
| v3.0 Ultralytics Unification | — | 2026-03-02 |
| v2.2 Backends | 29-33.1 | 2026-03-01 |
| v2.1 Identity | — | 2026-02-28 |
| v2.0 Alpha | — | 2026-02-27 |
| v1.0 MVP | — | 2026-02-25 |

Target newest-first order: v3.11, v3.10, v3.9, v3.8, v3.7, v3.6, v3.5, v3.4, v3.3, v3.2, v3.1,
v3.0, **v2.2, v2.1, v2.0, v1.0**.

**Source material:**

- `.planning/ROADMAP.md` line 267 — `✅ v3.11 Appearance-Based ReID (Phases 102-107) — SHIPPED 2026-03-26`
  and the collapsed block beneath it (phase list, goals, success criteria, plan counts).
- `.planning/ROADMAP.md` line 82 — `✅ v2.2 Backends (Phases 29-33.1) — SHIPPED 2026-03-01` and its block.
- **v3.11:** `.planning/phases/102-embedding-infrastructure`, `103-training-data-mining`,
  `104-backbone-fine-tuning`, `105-swap-detection-and-repair`, `106-cli-integration`,
  `107-unfrozen-backbone-fine-tuning`. Tracked contents are uneven — 103 has SUMMARYs but no PLANs;
  104 and 106 and 107 have SUMMARYs and CONTEXTs; 105 has PLANs, CONTEXT, and RESEARCH but no
  SUMMARY; VERIFICATIONs exist for 102, 103, and 107. Derive counts from what is actually on disk.
- **v2.2:** `.planning/milestones/v2.2-phases/` — six phase directories
  (`29-guidebook-audit`, `30-config-and-contracts`, `31-training-infrastructure`,
  `32-yolo-obb-detection-backend`, `33-keypoint-midline-backend`,
  `33.1-keypoint-training-data-augmentation`) holding **12 SUMMARY files**, 11 PLANs, 6 CONTEXTs,
  3 RESEARCHes, and 6 VERIFICATIONs. This is a complete record — the v2.2 entry can be as
  substantive as v3.10's.

**Entry schema to match exactly (D-22)** — taken verbatim from the existing v3.10 entry:

- `## <version> <Name> (Shipped: YYYY-MM-DD)` heading
- `**Phases completed:** N phases (RANGE), M plans, K tasks`
- `**Timeline:** N days (YYYY-MM-DD → YYYY-MM-DD)` *(include only if derivable)*
- `**Codebase:** N LOC source` *(include only if derivable)*
- `**Git range:** N commits, M files changed (+A / -B)` *(include only if derivable)*
- `**Key accomplishments:**` followed by a numbered list
- `**Delivered:**` one-paragraph summary
- `**Known gaps (accepted as tech debt):**` bulleted list *(optional)*
- `---` separator, then a blank line

Omit a field rather than fabricate a number for it (T-108-18).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill the v3.11 Appearance-Based ReID entry</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` lines 1–26 — the `# Milestones` heading and the complete v3.10
      entry, which is the **format source of truth** for the new entry (D-22).
    - `.planning/ROADMAP.md` starting at line 267 — the v3.11 collapsed block: phase list, per-phase
      goals, success criteria, and plan counts.
    - Every `*-SUMMARY.md` under `.planning/phases/102-*` through `107-*` — the accomplishment source.
    - Every `*-VERIFICATION.md` under those directories — the source for the `Known gaps` list.
  </read_first>
  <action>
    Insert a new entry immediately after the `# Milestones` heading and before the existing
    `## v3.10 Publication Metrics` entry. v3.11 shipped 2026-03-26, later than v3.10's 2026-03-15,
    so it is the newest and belongs at the top.

    Heading, verbatim: `## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)`

    Populate the fields using the schema in `<context>`:
    - `**Phases completed:**` — count the six directories `102` through `107`; count actual
      `*-PLAN.md` files across them for the plan count; count tasks by counting `<task` elements
      across those PLAN files. If PLAN files are missing for some phases (103 has none), state the
      plan and task counts as measured over the PLANs that exist and note the incompleteness in the
      SUMMARY, or omit the task count. Do not estimate.
    - `**Timeline:**` — derive from the first and last commit dates touching
      `.planning/phases/10[2-7]-*` using `git log --format=%ad --date=short --` on those paths,
      taking `tail -1` and `head -1`. Omit the field if the range is not derivable.
    - `**Codebase:**` and `**Git range:**` — derive with `git diff --shortstat` over the milestone's
      commit range if that range is cheaply identifiable; otherwise omit. Do not fabricate.
    - `**Key accomplishments:**` — 5 to 7 numbered items drawn from the phase SUMMARYs: embedding
      infrastructure, training-data mining, frozen backbone fine-tuning, unfrozen backbone
      fine-tuning, swap detection and repair, and CLI integration. Each item must be a concrete,
      specific, measurable claim traceable to a named SUMMARY — not a restatement of the phase
      title.
    - `**Delivered:**` — one paragraph describing what the milestone shipped overall.
    - `**Known gaps (accepted as tech debt):**` — only if the VERIFICATION files record real gaps;
      omit the section otherwise. Phase 105 having no SUMMARY on disk is itself a candidate gap.
    - Close with a `---` separator line followed by a blank line.

    Do not modify any existing entry in this task. Do not fill `(none recorded)` lists (D-21).

    Commit as `docs(108-04): backfill v3.11 Appearance-Based ReID milestone entry`. Do not push.
  </action>
  <verify>
    <automated>grep -qx '## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)' .planning/MILESTONES.md &amp;&amp; test "$(grep -n '^## v3.11' .planning/MILESTONES.md | cut -d: -f1)" -lt "$(grep -n '^## v3.10' .planning/MILESTONES.md | cut -d: -f1)" &amp;&amp; test "$(git diff HEAD~1 HEAD -- .planning/MILESTONES.md | grep -c '^-[^-]')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cx '## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)' .planning/MILESTONES.md` returns `1`.
    - The line number of `^## v3.11` is greater than `1` and strictly less than the line number of `^## v3.10`.
    - Within the v3.11 section, `grep -c '^\*\*Phases completed:\*\*'` returns `1` and that line contains `102-107`.
    - Within the v3.11 section, `grep -c '^\*\*Key accomplishments:\*\*'` returns `1`, followed by at least 5 lines matching `^[0-9]\+\. `.
    - Within the v3.11 section, `grep -c '^\*\*Delivered:\*\*'` returns `1`.
    - The v3.11 section ends with a line that is exactly `---`.
    - `grep -c '(none recorded)' .planning/MILESTONES.md` returns `1` — unchanged.
    - `grep -c '^## v' .planning/MILESTONES.md` returns `15`.
    - `git diff HEAD~1 HEAD -- .planning/MILESTONES.md | grep -c '^-[^-]'` returns `0` — insertion only.
    - `git diff --name-only HEAD~1 HEAD | tr '\n' ' '` returns exactly `.planning/MILESTONES.md `.
    - `git status --porcelain` outputs zero lines.
  </acceptance_criteria>
  <done>`.planning/MILESTONES.md` opens with a v3.11 Appearance-Based ReID entry in the existing schema, positioned above v3.10, with 5 or more specific accomplishments traceable to `phases/102-107/` SUMMARYs and no fabricated metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill the v2.2 Backends entry from the merged archive</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` — the v3.11 entry you just wrote and the existing v3.0 and v2.1
      entries; v2.1 is the nearest-in-era format analog and is a sparser entry.
    - `.planning/ROADMAP.md` starting at line 82 — the `v2.2 Backends (Phases 29-33.1)` collapsed
      block.
    - All **12** `*-SUMMARY.md` files under `.planning/milestones/v2.2-phases/` across the six phase
      directories `29-guidebook-audit`, `30-config-and-contracts`, `31-training-infrastructure`,
      `32-yolo-obb-detection-backend`, `33-keypoint-midline-backend`, and
      `33.1-keypoint-training-data-augmentation`.
    - The six `*-VERIFICATION.md` files in those directories — the source for `Known gaps`.
    - `.planning/milestones/v2.1-MILESTONE-AUDIT.md` — the adjacent milestone's audit, for era
      context on what v2.2 inherited.
  </read_first>
  <action>
    These source directories were **not on `dev` before the 108-01 merge**; they came from `main`'s
    archive and are the reason the merge kept the full `.planning/` file set. Confirm they are
    present with `git ls-files .planning/milestones/v2.2-phases | wc -l` before starting. If that
    returns 0, stop and report — the merge did not land as planned and this task cannot be done
    honestly.

    Insert a v2.2 entry **between** the existing `## v3.0 Ultralytics Unification (Shipped: 2026-03-02)`
    entry and the entry that follows it. v2.2 shipped 2026-03-01, so newest-first places it directly
    after v3.0.

    Heading, verbatim: `## v2.2 Backends (Shipped: 2026-03-01)`

    Same schema and the same omit-rather-than-fabricate rule as task 1. `**Phases completed:**` must
    reference the range `29-33.1` and count the 6 directories, the 11 `*-PLAN.md` files, and the
    task count derived from `<task` elements across those PLANs. Accomplishments come from the 12
    SUMMARY files — the record is complete for this milestone, so aim for 5 to 7 concrete items
    covering the guidebook audit, the config-and-contracts rework, the training infrastructure, the
    YOLO-OBB detection backend, the keypoint midline backend, and the keypoint training-data
    augmentation. Each item must be traceable to a named SUMMARY.

    Do not modify any existing entry. Do not fill `(none recorded)` lists (D-21).

    Commit as `docs(108-04): backfill v2.2 Backends milestone entry`. Do not push.
  </action>
  <verify>
    <automated>grep -qx '## v2.2 Backends (Shipped: 2026-03-01)' .planning/MILESTONES.md &amp;&amp; test "$(grep -n '^## v3.0' .planning/MILESTONES.md | cut -d: -f1)" -lt "$(grep -n '^## v2.2' .planning/MILESTONES.md | cut -d: -f1)" &amp;&amp; test "$(grep -c '^## v' .planning/MILESTONES.md)" -eq 16</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cx '## v2.2 Backends (Shipped: 2026-03-01)' .planning/MILESTONES.md` returns `1`.
    - The line number of `^## v3.0` is strictly less than the line number of `^## v2.2`.
    - Within the v2.2 section, `grep -c '^\*\*Phases completed:\*\*'` returns `1` and that line contains `29-33.1`.
    - Within the v2.2 section, `grep -c '^\*\*Key accomplishments:\*\*'` returns `1` with at least 5 lines matching `^[0-9]\+\. `.
    - Within the v2.2 section, `grep -c '^\*\*Delivered:\*\*'` returns `1`.
    - The v2.2 section ends with a line that is exactly `---`.
    - `grep -c '^## v' .planning/MILESTONES.md` returns `16`.
    - `grep -c '(none recorded)' .planning/MILESTONES.md` returns `1` — unchanged.
    - `git diff HEAD~1 HEAD -- .planning/MILESTONES.md | grep -c '^-[^-]'` returns `0` — insertion only.
    - `git diff --name-only HEAD~1 HEAD | tr '\n' ' '` returns exactly `.planning/MILESTONES.md `.
    - `git ls-files .planning/milestones/v2.2-phases | wc -l` is greater than `35` — the source archive is still tracked.
  </acceptance_criteria>
  <done>A v2.2 Backends entry in the existing schema sits directly below v3.0, references phases 29-33.1, draws at least 5 accomplishments from the 12 merged SUMMARY files, and brings the file to 16 milestone entries.</done>
</task>

<task type="auto">
  <name>Task 3: Re-sort the tail into chronological order</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` in full — specifically every `^## v` heading and its
      `(Shipped: YYYY-MM-DD)` date, which is the sort key.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` section D-21 — the
      re-sort mandate and its explicit exclusion of `(none recorded)` archaeology.
  </read_first>
  <action>
    Move whole entry blocks — heading through the trailing `---` — so the file reads strictly
    newest-first by the `(Shipped: ...)` date. Only the tail is wrong: the block order after v3.0
    must become **v2.2, v2.1, v2.0, v1.0** (currently v1.0, v2.0, v2.1 with v2.2 newly inserted
    above them).

    Target full order, top to bottom: v3.11, v3.10, v3.9, v3.8, v3.7, v3.6, v3.5, v3.4, v3.3, v3.2,
    v3.1, v3.0, v2.2, v2.1, v2.0, v1.0.

    This is a pure block move. Do not reword, reformat, add to, or trim the content of any entry you
    move — in particular do not fill v2.1's `(none recorded)` accomplishment list (D-21). Verify by
    sorting: the multiset of lines before and after must be identical; only their order changes.

    Normalize separators so every entry is followed by exactly one `---` line and one blank line,
    matching the existing style, and confirm the file still opens with `# Milestones`. If separator
    normalization changes any line, redo it — normalization must be whitespace-and-`---` only, and
    must not alter the sorted line multiset beyond blank lines.

    Commit as `docs(108-04): re-sort MILESTONES.md into chronological order`. Do not push.
  </action>
  <verify>
    <automated>grep -oP '^## v\S+ .*\(Shipped: \K[0-9-]+' .planning/MILESTONES.md &gt; /tmp/ms_dates.txt &amp;&amp; sort -r /tmp/ms_dates.txt | diff -q - /tmp/ms_dates.txt &amp;&amp; test "$(grep -c '^## v' .planning/MILESTONES.md)" -eq 16</automated>
  </verify>
  <acceptance_criteria>
    - Extracting ship dates in file order with `grep -oP '^## v\S+ .*\(Shipped: \K[0-9-]+' .planning/MILESTONES.md` yields a list that `sort -r` reproduces byte-identically.
    - `grep -c '^## v' .planning/MILESTONES.md` returns `16`.
    - `grep -oP '^## \Kv[0-9.]+' .planning/MILESTONES.md | tail -4 | tr '\n' ' '` returns `v2.2 v2.1 v2.0 v1.0 `.
    - `grep -oP '^## \Kv[0-9.]+' .planning/MILESTONES.md | head -1` returns `v3.11`.
    - `head -1 .planning/MILESTONES.md` is exactly `# Milestones`.
    - `grep -v '^$' .planning/MILESTONES.md | sort` is byte-identical to `git show HEAD~1:.planning/MILESTONES.md | grep -v '^$' | sort` — proving the re-sort moved lines without editing any.
    - `grep -c '(none recorded)' .planning/MILESTONES.md` returns `1` — unchanged.
    - `git diff --name-only HEAD~1 HEAD | tr '\n' ' '` returns exactly `.planning/MILESTONES.md `.
    - `git status --porcelain` outputs zero lines.
    - `git log --oneline origin/dev..dev | wc -l` is greater than `0` — nothing was pushed.
  </acceptance_criteria>
  <done>All 16 milestone entries appear in strict descending ship-date order, entry content is byte-preserved apart from blank-line normalization, and no `(none recorded)` list was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconstructed history -> the permanent project record | MILESTONES.md is the durable narrative other phases and the eventual publication cite; a fabricated number is indistinguishable from a measured one |
| bulk block re-ordering -> existing entry content | a whole-file rewrite can silently drop or alter entries a reader will never re-verify |
| merged `.planning/milestones/` archive -> the v2.2 entry | the entry's factual claims rest entirely on files that arrived on `dev` two plans ago |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-18 | Spoofing | fabricated metrics in a reconstructed milestone entry | mitigate | The schema instruction is explicit: derive `Timeline`, `Codebase`, and `Git range` from `git log` and `git diff --shortstat`, and **omit the field rather than fabricate**; every accomplishment must be traceable to a named `*-SUMMARY.md` |
| T-108-19 | Tampering | accidental loss of existing entries during the block re-sort | mitigate | Task 3's acceptance criterion diffs the blank-stripped sorted file against the blank-stripped sorted `HEAD~1` version, which fails on any added, dropped, or edited line; tasks 1 and 2 assert insertion-only diffs |
| T-108-20 | Repudiation | scope creep into `(none recorded)` archaeology | mitigate | `grep -c '(none recorded)'` is asserted to return exactly `1` in all three tasks |
| T-108-35 | Spoofing | writing a v2.2 entry with no source material if the merge archive is absent | mitigate | Task 2 opens with a hard gate: `git ls-files .planning/milestones/v2.2-phases` must be non-empty or the task stops and reports rather than inventing content |
| T-108-21 | Information Disclosure | milestone prose leaking private paths or credentials | accept | Content is reconstructed from `.planning/` files that plans 108-01 and 108-03 already scanned with detect-secrets before tracking them; no new external source is introduced |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages and runs no build; it edits one markdown file |
</threat_model>

<verification>
- `.planning/MILESTONES.md` contains `## v3.11 Appearance-Based ReID (Shipped: 2026-03-26)` (REC-01).
- It contains `## v2.2 Backends (Shipped: 2026-03-01)` (D-21).
- Ship dates in file order are strictly descending (D-21).
- Entry count is 16 and both new entries use the existing schema (D-22).
- The blank-stripped sorted file matches the blank-stripped sorted pre-re-sort version — no content altered.
- `(none recorded)` count is still 1 (D-21 exclusion respected).
- The v2.2 source archive under `.planning/milestones/v2.2-phases/` is still tracked.
- Nothing pushed; no file other than `.planning/MILESTONES.md` modified.
</verification>

<success_criteria>
- The milestone record has no missing milestone and no out-of-order entry.
- Every number in the two new entries is either measured or absent.
- Every accomplishment traces to a named SUMMARY file.
- The diff touches exactly one file.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-04-SUMMARY.md` when done.
Record which schema fields were omitted from each new entry and why, and the measured plan and task
counts for each milestone.
</output>
