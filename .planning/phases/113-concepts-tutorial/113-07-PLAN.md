---
phase: 113-concepts-tutorial
plan: 07
type: execute
wave: 4
depends_on: [113-04, 113-06]
files_modified:
  - docs/getting-started/tutorial.md
  - docs/getting-started/index.md
  - .planning/todos/pending/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md
autonomous: false
requirements: [DOCS-07, DATA-03]

user_setup:
  - service: zenodo
    why: "Publishing the reserved draft is an authenticated account action under the maintainer's identity; a published DOI is permanent (D-04)."
    dashboard_config:
      - task: "Attach the 22 files listed in 113-06-SUMMARY.md to the reserved draft and publish it, then report the record URL and the resolved DOI"
        location: "The Zenodo draft URL recorded in 113-06-SUMMARY.md"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new user can go from the install guide to a complete pipeline run on the published dataset and an interpreted 3D output, following the tutorial alone, with an expectation to check at each step (DOCS-07)."
    - "The tutorial covers the one-time `aquapose prep generate-luts` step before the pipeline run, and states that skipping it makes the pipeline fail fast with a LUTs-not-found error rather than producing a degraded result (D-13)."
    - "Every command in the tutorial is one that was executed end to end during the Plan 05 verification run, in the same order, and the tutorial reflects the corrections that run produced (D-14)."
    - "Expected results are stated as approximate values with tolerance ranges rather than exact figures, because GPU nondeterminism means a reader's run will not match bit for bit (D-12)."
    - "The tutorial quotes stage timings as a range spanning two real machines — the deposit's GTX 1660 SUPER reference and the GPU used for the Plan 05 verification run — so a reader can locate their own hardware between them (D-15)."
    - "The tutorial walks the real `midlines` group schema in `outputs.h5`, including the quality fields `n_cameras`, `mean_residual`, `max_residual` and `is_low_confidence`, and shows loading and plotting one fish (D-13)."
    - "The tutorial states that a value landing exactly on a quoted tolerance boundary is a pass, not a failure — for example a median reprojection residual of exactly the quoted figure, or exactly the minimum quoted number of fish visible in a frame, is within expectation."
    - "The tutorial documents the empty and degenerate cases a reader will actually encounter and names them as expected rather than as errors: frames where fewer than all nine fish are reconstructed, fish-frames flagged `is_low_confidence`, fish-frames that were not reconstructed at all, and cameras that see no fish in a given frame."
    - "The tutorial states that identity comes from the `fish_id` dataset and that a fish's slot position within a frame is not a stable identity, so a reader does not infer identity from row order in `outputs.h5`."
    - "The tutorial cites the published Zenodo record by its resolved DOI, not a placeholder, and links the record (D-04, DATA-03)."
    - "The tutorial restates the hard constraint that video must never be spatially downscaled, because calibration intrinsics and refractive ray-casting are bound to the 1600x1200 capture resolution."
    - "The deposit README remains the terse recipe and the docs tutorial is the long form; neither is trimmed to a link and neither is a mirrored duplicate of the other (D-11)."
    - "The tutorial cross-links to docs/reference/cli.rst for command options and docs/reference/config.md for field meanings rather than restating them, and back to docs/getting-started/concepts.md for what the stages do."
    - "hatch run docs:build (sphinx-build -W --keep-going) exits 0 with all three Getting Started pages reachable."
  artifacts:
    - path: "docs/getting-started/tutorial.md"
      provides: "End-to-end tutorial: download, LUT generation, pipeline run, visualization, and output interpretation with expected results per step"
      min_lines: 150
      contains: "generate-luts"
    - path: "docs/getting-started/index.md"
      provides: "Getting Started section index completed with a Tutorial card and toctree entry"
      contains: "tutorial"
  key_links:
    - from: "docs/getting-started/index.md"
      to: "docs/getting-started/tutorial.md"
      via: "grid-item-card :link: tutorial plus a hidden toctree entry, third after installation and concepts"
      pattern: "tutorial"
    - from: "docs/getting-started/tutorial.md"
      to: "the published Zenodo record"
      via: "the resolved DOI link that supplies the dataset the tutorial runs on"
      pattern: "doi.org"
    - from: "docs/getting-started/tutorial.md"
      to: "docs/reference/config.md"
      via: "cross-link for the meaning of the config fields the tutorial's config.yaml sets"
      pattern: "reference/config"
    - from: "docs/getting-started/tutorial.md"
      to: "docs/getting-started/concepts.md"
      via: "cross-link back to the stage explanations rather than restating them"
      pattern: "concepts"
  prohibitions:
    - requirement_id: DOCS-07
      category: transparency
      statement: "MUST NOT quote an expected result, timing, or reproducibility claim that was not actually measured — every number on the page must trace to either the deposit's reference outputs or the Plan 05 verification run, and the page must not imply bit-for-bit reproducibility that GPU nondeterminism makes false."
    - requirement_id: DOCS-07
      category: safety
      statement: "MUST NOT suggest, or leave room for a reader to infer, that spatially downscaling input video is an acceptable way to reduce runtime or memory — doing so silently invalidates every ray cast and produces confidently wrong 3D output with no error."
    - requirement_id: DOCS-07
      category: transparency
      statement: "MUST NOT present a normal partial-observability outcome as a failure state, or a genuine failure as normal — a reader must be able to tell from the page which is which."
---

<objective>
Publish the reserved Zenodo deposit (D-04, DATA-03) and write the end-to-end
tutorial against the published record (DOCS-07): install, download, the one-time
LUT generation, the pipeline run, the visualization, and interpreting the 3D
output — with an expectation to check at each step, drawn from the measurements
Plan 05 produced rather than from assumption.

This is the last expansion of the Getting Started slice, and the page that makes
ROADMAP success criterion 3 true.

Purpose: DOCS-07 (tutorial with expected results at each step) and DATA-03 (the
dataset is deposited with a citable DOI).
Output: a published Zenodo record, `docs/getting-started/tutorial.md`, a completed
three-card Getting Started section, and the folded upload todo closed.
</objective>

## Artifacts this phase produces

New symbol created by this plan: `docs/getting-started/tutorial.md`, plus a
Tutorial `grid-item-card` and a `tutorial` toctree entry inside the existing
`docs/getting-started/index.md`, and the resolved Zenodo DOI as a published
external identifier. No CLI flags, functions, config keys, dependency groups or
test names are created. The phase-wide artifact list lives in `113-01-PLAN.md`
under "Artifacts this phase produces".

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-concepts-tutorial/113-CONTEXT.md
@.planning/phases/113-concepts-tutorial/113-PATTERNS.md
@.planning/phases/113-concepts-tutorial/113-05-SUMMARY.md
@.planning/phases/113-concepts-tutorial/113-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Upload the 22 verified files to the reserved draft and publish the record</name>
  <precondition>`113-06-SUMMARY.md` records a reserved DOI, a draft URL, and the mechanically produced 22-path upload list, and running `sha256sum -c checksums.sha256` from inside `aquapose-tutorial-data/` still reports 22 OK.</precondition>
  <read_first>
    - `.planning/phases/113-concepts-tutorial/113-06-SUMMARY.md` — the draft URL, the reserved DOI, and the exact 22 relative paths to attach. Attach these and nothing else.
    - `aquapose-tutorial-data/checksums.sha256` — the manifest the uploaded files must match; the digests are what a downloader will verify against.
    - `aquapose-tutorial-data/README.md` — the record's own README, which now carries the reserved DOI in its citation block.
  </read_first>
  <what-built>
Plan 06 created the Zenodo draft with the metadata from `zenodo-metadata.json`,
reserved the DOI, wrote that DOI into the deposit README and its generating
template, re-emitted the checksum manifest, and produced the exact upload list.
The tree is verified clean. Only the authenticated publish action remains, and it
must be performed by the maintainer in their own Zenodo session.

This task walks through a one-way door: publishing mints a permanently resolvable
DOI, and correcting a mistake afterwards requires publishing a new version while
the original DOI keeps resolving to the flawed files (D-04). The go/no-go gate for
that door was already taken by the human in Plan 06 Task 1, and the rating is
recorded there on the DOI-reservation task; this task does not re-ask it.
  </what-built>
  <action>
Attach exactly the 22 files listed in `113-06-SUMMARY.md` to the reserved Zenodo
draft and publish it, then report the record URL and the resolved DOI so the
tutorial can cite the published dataset rather than a placeholder (D-04,
DATA-03).
  </action>
  <instructions>
1. Open the draft URL recorded in `113-06-SUMMARY.md`.
2. Attach exactly the 22 files listed there, preserving the directory structure
   (`videos/`, `geometry/`, `models/`, `reference_outputs/`, and the four
   root-level files). Do **not** attach `geometry/luts/`, any `runs/` directory,
   or anything else not on the list — those are transient machine-specific
   artifacts.
3. Confirm the metadata still matches `zenodo-metadata.json` and that the DOI
   shown on the draft is the same string recorded in `113-06-SUMMARY.md`.
4. Publish.
5. Report back the record URL, the resolved DOI (the version DOI), and the
   concept DOI if Zenodo shows one.
  </instructions>
  <verification>
- `https://doi.org/<the resolved DOI>` resolves to the published Zenodo record.
- The Zenodo record API (`https://zenodo.org/api/records/<record_id>`) reports exactly 22 files.
- Every file name in that API response appears in `aquapose-tutorial-data/checksums.sha256`, and every manifest entry appears in the API response — the two sets are equal.
- The DOI shown on the published record is byte-identical to the reserved DOI already written into `aquapose-tutorial-data/README.md` and into `scripts/package_tutorial_dataset.py`.
- The record's license reads CC-BY-4.0 and its description matches `zenodo-metadata.json`.
  </verification>
  <acceptance_criteria>
    - The record URL and the resolved DOI are recorded in the SUMMARY.
    - The resolved DOI matches the DOI already written into `aquapose-tutorial-data/README.md` and into the README template in `scripts/package_tutorial_dataset.py` — verify with `grep` against both files and record the result.
    - `https://doi.org/<the resolved DOI>` resolves to the Zenodo record — verify by fetching it and recording the resulting record URL.
    - The published record lists exactly 22 files. Verify mechanically against the Zenodo record API (`https://zenodo.org/api/records/<record_id>`) and record the returned file count.
    - Each published file's name appears in `aquapose-tutorial-data/checksums.sha256`, and no published file is absent from the manifest. Record the comparison outcome.
    - If any check fails, STOP and surface it before writing the tutorial — the tutorial's citation and download step depend on this record being correct.
  </acceptance_criteria>
  <resume-signal>Reply with the record URL, the resolved version DOI, and the concept DOI if shown</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the end-to-end tutorial against the published dataset (D-11, D-12, D-13, D-15)</name>
  <read_first>
    - `.planning/phases/113-concepts-tutorial/113-05-SUMMARY.md` in full — the verbatim command sequence with exit statuses, the three wall-clock timings, the two-point timing range, the statistics comparison table, the deviation log with verdicts, and the run directory's file listing. **Every number on the tutorial page comes from here or from the deposit's reference outputs; nothing is estimated.**
    - `.planning/phases/113-concepts-tutorial/113-06-SUMMARY.md` — the DOI and record URL to cite.
    - `aquapose-tutorial-data/README.md` — the terse "How to Reproduce" recipe this page expands into long form. D-11: the deposit README stays the short form and is not trimmed to a link; this page is not a mirrored duplicate of it.
    - `aquapose-tutorial-data/reference_outputs/outputs.h5` — the schema to walk: the `midlines` group with its spline degree and knot-vector attributes, and the datasets `points`, `control_points`, `half_widths`, `z_offsets`, `arc_length`, `centroid_z`, `fish_id`, `frame_index`, `n_cameras`, `mean_residual`, `max_residual`, `is_low_confidence`.
    - `docs/getting-started/installation.md` and `docs/getting-started/concepts.md` — the two pages this one follows; match their voice, and cross-link rather than repeat their content.
    - `docs/reference/cli.rst` and `docs/reference/config.md` — the Phase 112 reference pages to cross-link for command options and field meanings. Phase 112's D-09 deferred dataset-anchored runnable examples to this phase, so this page is where they land.
    - `src/aquapose/cli_utils.py` `resolve_project` — the CWD-upward `config.yaml` search that makes the `cd` plus bare `aquapose run` form work, and `resolve_run` for how the visualization step's run reference is resolved.
    - `src/aquapose/engine/pipeline.py` around line 372 — the fail-fast LUTs-not-found guard the tutorial must warn about.
    - `src/aquapose/cli.py` `smooth_z_cmd` (the `smooth-z` command) and `src/aquapose/core/reconstruction/temporal_smoothing.py` `smooth_centroid_z` — the post-processing step added by D-20. Read the command's options (`--sigma-frames` default 3, `--dry-run`), its stitched-vs-raw input preference, and — importantly — its **write path**: it does NOT edit in place, it `shutil.copy2`s the input to `{stem}_smoothed.h5` and modifies the copy. The docstring's "shifts all z-coordinates" wording reads like an in-place edit and is misleading; trust the code. Do not describe behavior the source does not have.
    - `src/aquapose/evaluation/viz/_loader.py` `resolve_h5_path` (line 151) — the exact file-preference order `viz` uses: `midlines_stitched_smoothed.h5` → `midlines_stitched.h5`, then `midlines_smoothed.h5` → `midlines.h5`, with `--unstitched` skipping only the two stitched entries. This is what makes a bare `aquapose viz` pick up smoothed data automatically, and also why there is no way back to an unsmoothed render once the smoothed file exists.
    - `src/aquapose/cli.py` `viz` options — in particular `-o/--output-dir` (default `{run_dir}/viz/`), which is what a reader needs in order to keep pre- and post-smoothing renders side by side instead of clobbering the first.
    - `scripts/package_tutorial_dataset.py` `regenerate_reference_outputs` — confirms the shipped `reference_outputs/` are produced by `aquapose run` then `aquapose viz` only, with **no** smoothing pass. This is why the D-20 step is placed after the results comparison.
    - `.planning/GUIDEBOOK.md` section 6, "Identity Model" — global fish IDs are assigned at cross-camera association and are what `fish_id` carries.
    - `.planning/phases/113-concepts-tutorial/113-CONTEXT.md` — D-11 through D-15, and the `<specifics>` block's measured reference statistics and the never-downscale hard constraint.
  </read_first>
  <files>docs/getting-started/tutorial.md</files>
  <action>
Write `docs/getting-started/tutorial.md` as MyST `.md` — the long-form walkthrough
(D-11). Each step gets: the command, what is happening, roughly how long it takes,
and what the reader should see. Structure:

1. **Before you start** — cross-link to the installation guide as a prerequisite
   and to the concepts page for what the stages do. State the hardware
   expectation using the two real data points (D-15): the deposit's reference run
   took 786.45 s for the pipeline and 150.85 s for visualization on a GTX 1660
   SUPER (6.4 GB); the Plan 05 verification run produced a second pair on a newer
   GPU. Quote both, named by GPU, so a reader can locate their own hardware
   between them. State the disk requirement (215 MB dataset plus roughly 600 MB
   of generated lookup tables).

2. **Get the dataset** — link the published Zenodo record by its resolved DOI
   (`https://doi.org/<DOI>`), give the download and extraction steps, and show
   verifying the download with `sha256sum -c checksums.sha256` from inside the
   extracted directory, stating that all 22 files should report OK. Include the
   citation, using the bibtex entry from the deposit README with the real DOI.

3. **Generate the refractive lookup tables** (D-13) — the one-time
   `aquapose prep generate-luts` step, run from inside the extracted directory.
   Explain what it computes (referring back to the concepts page rather than
   re-explaining refraction), how long it takes, how much disk it consumes, and
   state plainly that the tables are deterministic from the calibration file,
   are not shipped with the dataset, and that skipping this step makes the
   pipeline **fail fast** with a LUTs-not-found error rather than degrade
   silently.

4. **Run the pipeline** — the bare `aquapose run` form. Explain why no config
   flag is needed: the CLI walks up from the current directory looking for
   `config.yaml`, which is why the `cd` step matters. Do not invent a
   config-path flag — the `run` command has none. Describe what the run writes
   and where, using the run-directory listing recorded in Plan 05's summary, and
   name the HDF5 artifact by whatever the verification run actually produced.
   Cross-link to `../reference/cli.rst` for the full option surface and to
   `../reference/config.md` for what the config fields mean; do not restate them.
   Mention the useful options a first-time reader will want by name only
   (verbose output, and limiting the number of chunks for a quick smoke run),
   linking to the reference for details.

5. **Visualize** — the visualization command in the form the Plan 05 run proved
   correct, what it produces (the interactive 3D animation and the reprojection
   overlay mosaic), and roughly how long it takes on each of the two reference
   GPUs. Note the ffmpeg dependency for MP4 export, cross-linking to the install
   guide's prerequisites.

6. **Check your results** (D-12) — the heart of the page. Present expected
   results as approximate values with tolerance ranges, never as exact figures,
   and say why: GPU nondeterminism means two runs of the same data do not match
   bit for bit. Draw the values from Plan 05's comparison table and the deposit
   reference: roughly 95 percent of fish-frames reconstructed; a median
   reprojection residual around 3 px, with the mean higher and the 95th
   percentile substantially higher; a median of about 4 contributing cameras per
   reconstructed fish out of 12; a few percent of fish-frames flagged as low
   confidence; and 6 to 9 fish visible in any given frame with 9 typical. Choose
   the tolerance wording and which subset of statistics to quote (Claude's
   Discretion per CONTEXT.md) — but state explicitly that a value landing exactly
   on a quoted boundary is within expectation, not a failure. Give the reader a
   short, runnable snippet that computes these same statistics from their own
   `outputs.h5` so the check is mechanical rather than impressionistic.

7. **Read the output** (D-13) — walk the real `midlines` group schema: the spline
   degree and knot vector attributes, and each dataset with its shape and
   meaning, including the four quality fields (`n_cameras`, `mean_residual`,
   `max_residual`, `is_low_confidence`) and what a reader should do with them.
   Then show loading the file and plotting one fish's 3D midline for one frame.
   State clearly that identity is carried by the `fish_id` dataset and that a
   fish's slot position within a frame is not a stable identity, so the reader
   must not read identity from row order.

8. **Reduce z jitter with `smooth-z`** (D-20) — the recommended post-processing
   step. Place it *after* the results check and schema walk, not before: the
   deposit's `reference_outputs/` were generated by `regenerate_reference_outputs`
   running only `aquapose run` then `aquapose viz`, so they are **pre-smoothing**,
   and steps 6-7 must compare against them on equal terms. State that explicitly
   here so a reader who smooths first and then compares does not think their run
   diverged.

   Cover: what the command does (temporally smooths per-fish centroid z with a
   Gaussian filter within continuous track segments, then shifts `control_points`
   and `points` by the smoothing delta, leaving the unused dataset NaN); that it
   **writes a new file and never modifies the input** — `{stem}_smoothed.h5`
   alongside it, via `shutil.copy2` then edit, so the original is always preserved
   and the before/after comparison is free; that it reads
   `midlines_stitched.h5` when present and falls back to `midlines.h5` (so the
   output is `midlines_stitched_smoothed.h5` or `midlines_smoothed.h5`
   accordingly); the `--sigma-frames` knob with its default of 3; and `--dry-run`
   as the way to see the jitter numbers before producing the file.

   **Then show how to visualize the smoothed data — this is the part a reader
   cannot guess.** `aquapose viz` already prefers the smoothed file
   automatically: `resolve_h5_path` in `src/aquapose/evaluation/viz/_loader.py`
   resolves `midlines_stitched_smoothed.h5` → `midlines_stitched.h5`, then
   `midlines_smoothed.h5` → `midlines.h5`. So after `smooth-z`, a bare
   `aquapose viz` renders the smoothed reconstruction with no extra flag. Say this
   explicitly; nothing in the command output reveals it.

   Give the reader the side-by-side recipe, because the default clobbers: `viz`
   writes to `{run_dir}/viz/`, so re-running it overwrites the pre-smoothing
   renders. Use `-o/--output-dir` to keep both, e.g. render the smoothed pass into
   a separate directory and open the two animations together. That comparison is
   the honest visual confirmation the step worked.

   Warn about the one-way edge: once `midlines_smoothed.h5` exists there is **no
   flag** that forces `viz` back to the unsmoothed file — `--unstitched` only
   skips the *stitched* variants and still prefers `midlines_smoothed.h5` over
   `midlines.h5`. A reader who wants the unsmoothed render after smoothing must
   move or rename the smoothed file, or have kept the earlier `viz` output via
   `-o`. State this plainly rather than letting them discover it.

   Quote the measured effect from the Plan 05 verification run rather than an
   estimate — mean frame-to-frame centroid z jitter fell from **0.500 cm to
   0.082 cm** at the default sigma across 13 fish and 7769 fish-frames. Present
   it as an approximate improvement with the same tolerance discipline step 6
   uses, and show the `--dry-run` output as the way a reader confirms the
   improvement on their own run before committing to the rewrite.

   Explain *why* the jitter exists, cross-linking to the concepts page rather than
   re-deriving it: depth along the camera ray is the weakest-constrained axis in a
   refractive multi-view reconstruction, so per-frame z estimates scatter more than
   x and y. This is the step that makes 3D trajectories usable for downstream
   kinematics. Cross-link to `../reference/cli.rst` for the full option surface.

   Every claim in this step must be checked against
   `src/aquapose/evaluation/viz/_loader.py` `resolve_h5_path` and
   `src/aquapose/cli.py` `smooth_z_cmd` before it is written down — the resolution
   order and the copy-not-in-place behavior are both easy to get backwards from
   the docstrings alone.

9. **What is normal and what is not** — name the outcomes that look like failures
   but are not: cameras that see no fish in a frame, frames where fewer than all
   nine fish are reconstructed, fish-frames flagged low confidence, and
   fish-frames dropped entirely because too few cameras observed them. Give the
   approximate rate for each from the measured statistics, and contrast them with
   things that genuinely indicate a problem (a LUTs-not-found failure, a residual
   distribution far above the quoted range, zero fish reconstructed).

10. **Using your own footage** — a short closing section restating the hard
   constraint that video must **never** be spatially downscaled, because the
   calibration intrinsics and refractive ray-casting are bound to the 1600x1200
   capture resolution and halving it silently invalidates every ray cast.
   Temporal trimming and bitrate reduction are safe. Then cross-link to the CLI
   and config references for setting up a new project.

Use fenced `bash` blocks with inline `#` comments for every runnable step and
fenced `python` blocks for the two analysis snippets, matching the idiom already
used in `docs/contributing.md` and the deposit README. Narration goes in prose
between fences.

Constraints:
- Every quoted number traces to `113-05-SUMMARY.md` or to the deposit's reference
  outputs. Do not estimate, round to a "nicer" figure without saying it is
  approximate, or carry over a figure from a source artifact that the
  verification run contradicted.
- Do not add image or binary assets; if a diagram is needed, use a Mermaid fence
  as the concepts page does (D-03a).
- Do not modify `docs/getting-started/index.md` in this task — Task 3 wires the
  page in.
  </action>
  <verify>
    <automated>test -f docs/getting-started/tutorial.md && grep -q 'generate-luts' docs/getting-started/tutorial.md && grep -q 'doi.org' docs/getting-started/tutorial.md && grep -q 'smooth-z' docs/getting-started/tutorial.md && grep -q 'dry-run' docs/getting-started/tutorial.md && grep -q '_smoothed.h5' docs/getting-started/tutorial.md && grep -qE '\-o |--output-dir' docs/getting-started/tutorial.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/getting-started/tutorial.md` exists and `wc -l` reports at least 150 lines.
    - `grep -q 'aquapose prep generate-luts' docs/getting-started/tutorial.md` succeeds and the surrounding text states the pipeline fails fast without it (D-13).
    - `grep -q 'doi.org' docs/getting-started/tutorial.md` succeeds, and the DOI it contains is byte-identical to the resolved DOI recorded in the SUMMARY of Task 1.
    - `grep -q 'sha256sum -c' docs/getting-started/tutorial.md` succeeds (download verification step).
    - `grep -q 'is_low_confidence' docs/getting-started/tutorial.md`, `grep -q 'mean_residual' docs/getting-started/tutorial.md`, `grep -q 'n_cameras' docs/getting-started/tutorial.md` and `grep -q 'fish_id' docs/getting-started/tutorial.md` all succeed (D-13 schema walk).
    - `grep -q 'control_points' docs/getting-started/tutorial.md` succeeds.
    - `grep -q 'GTX 1660 SUPER' docs/getting-started/tutorial.md` succeeds and a second, differently named GPU also appears, with a timing attached to each (D-15 two-point range).
    - `grep -q '1600' docs/getting-started/tutorial.md` succeeds and the surrounding text forbids spatial downscaling.
    - `grep -q 'reference/config' docs/getting-started/tutorial.md` and `grep -q 'reference/cli' docs/getting-started/tutorial.md` both succeed (cross-linked, not restated).
    - `grep -q 'concepts' docs/getting-started/tutorial.md` succeeds (links back to the concepts page).
    - Every numeric expectation on the page is traceable: list each quoted figure in the SUMMARY next to its source (Plan 05 measurement or deposit reference). Any figure without a source is a defect.
    - The page states that a value exactly on a quoted tolerance boundary passes — quote that sentence verbatim in the SUMMARY.
    - The page states that identity comes from `fish_id` and not from slot order — quote that sentence verbatim in the SUMMARY.
    - The page names the expected partial-observability outcomes as normal, with approximate rates — quote that section's headings in the SUMMARY.
  </acceptance_criteria>
  <done>A reader who has finished the installation guide can follow this page from downloading the published dataset to interpreting one fish's 3D midline, checking their own run against a measured expectation at every step, and can tell a normal partial result from a real failure.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the Getting Started section and close the folded upload todo</name>
  <read_first>
    - `docs/getting-started/index.md` — the two-card grid and two-entry toctree left by Plans 01 and 04. The new card and entry must match its syntax exactly.
    - `docs/getting-started/tutorial.md` (from Task 2) — to write an accurate one-line card description.
    - `docs/index.md` — to confirm the root Getting Started card's description still describes what the section now contains (install, concepts, tutorial); adjust only that one description line if it has become inaccurate.
    - `.planning/todos/pending/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md` — the folded todo, whose steps 2 through 5 are now complete. Its step 1 (regenerate from the Windows staging directory) was never needed.
    - `.planning/phases/113-concepts-tutorial/113-06-SUMMARY.md` — the DOI and record URL to record when closing the todo.
  </read_first>
  <files>docs/getting-started/index.md, .planning/todos/pending/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md</files>
  <action>
Add a third `grid-item-card` to `docs/getting-started/index.md` titled
`Tutorial`, with `:link: tutorial` and `:link-type: doc`, placed after the
`Concepts` card, with a one-line description naming the end-to-end run on the
published dataset. Add `tutorial` to the hidden `{toctree}` as the third entry.
Change nothing else in the file.

Check that the root `Getting Started` card description in `docs/index.md` still
accurately describes a section containing installation, concepts and tutorial;
adjust only that description line if it does not.

Then close the folded todo. Move
`.planning/todos/pending/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md` into
`.planning/todos/done/`, appending a short completion note recording: the
resolved DOI, the record URL, that steps 2 through 5 were completed in Plans 06
and 07, and that step 1 was not needed because the finalized tree was downloaded
and verified rather than regenerated. Do not touch
`.planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md` — that
one is D-19 and stays open by design.

Run the docs build one final time. All three Getting Started pages must be
reachable and the build clean under `-W`.
  </action>
  <verify>
    <automated>hatch run docs:build && test -f docs/_build/html/getting-started/tutorial.html && test -f docs/_build/html/getting-started/concepts.html && test -f docs/_build/html/getting-started/installation.html</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run docs:build` exits 0.
    - `docs/_build/html/getting-started/{installation,concepts,tutorial}.html` all exist.
    - `grep -c 'grid-item-card' docs/getting-started/index.md` is exactly 3.
    - `grep -c 'tutorial' docs/getting-started/index.md` is at least 2 (card `:link:` and toctree entry).
    - `test -f .planning/todos/done/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md` succeeds and `test ! -f .planning/todos/pending/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md` succeeds.
    - `test -f .planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md` still succeeds (D-19 stays open).
    - `grep -q '10\.' .planning/todos/done/2026-09-01-upload-yh-tutorial-dataset-to-zenodo.md` succeeds (the completion note records the DOI).
    - `hatch run test` exits 0.
  </acceptance_criteria>
  <done>The Getting Started section is complete with three cards and three pages, the whole docs tree builds clean under `sphinx-build -W --keep-going`, and the deferred Zenodo upload todo is closed with the DOI recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local deposit tree → permanent public record | The publish action makes 22 files immutable and citable under a resolvable DOI. |
| tutorial claims → reader's scientific conclusions | The tutorial tells a researcher what a correct run looks like. An unmeasured or overstated expectation causes a reader to accept a bad run or reject a good one, with no software gate to catch it. |
| tutorial instructions → reader's data pipeline | The never-downscale constraint is the difference between valid and silently invalid 3D reconstruction on a reader's own footage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-24 | Elevation of privilege | Zenodo publish action | high | mitigate | Publishing is a blocking `checkpoint:human-action` performed by the maintainer in their own authenticated session; no API token is issued to an agent or stored in the repository. |
| T-113-25 | Tampering | published file set | high | mitigate | Only the mechanically generated 22-path list from `113-06-SUMMARY.md` may be attached, and the acceptance criteria verify the published record's file count and names against `checksums.sha256` through the Zenodo record API after publication. |
| T-113-26 | Spoofing | dataset download link in the tutorial | high | mitigate | The tutorial links the dataset by its resolved DOI through `doi.org` rather than by a mutable mirror URL, and instructs the reader to verify the download against the deposit's own `checksums.sha256` before use. |
| T-113-27 | Repudiation | quoted expected results | medium | mitigate | Every figure on the page is traced in the SUMMARY to either a Plan 05 measurement or the deposit's reference outputs, and the page states expectations as ranges with an explicit statement that runs are not bit-for-bit reproducible. |
| T-113-28 | Tampering | reader's own footage pipeline | medium | mitigate | The never-spatially-downscale hard constraint is restated in the tutorial with the reason (calibration intrinsics and ray-casting are bound to 1600x1200), and is covered by a prohibition in this plan's `must_haves`. |
| T-113-29 | Information disclosure | tutorial command output examples | low | accept | Output examples are transcribed from the Plan 05 run with local paths generalized to the deposit-relative form; no host paths or usernames are reproduced. |
| T-113-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. `pip install aquapose` appears only as documented prose, cross-referenced to the install guide's PyPI disclosure (D-09). |

ASVS level 1; blocking severity: high. T-113-24, T-113-25 and T-113-26 are all mitigated within this plan.
</threat_model>

## Edge Coverage (spec-less probe fallback)

This phase has no SPEC.md, so the deterministic edge probe ran instead. This plan
owns 3 of the 5 probe-surfaced rows, all for DOCS-07. All three were resolvable
from CONTEXT.md — D-12's tolerance ranges, D-13's schema walk, and the GUIDEBOOK
Identity Model — and are authored as plain `must_haves.truths` strings above.

| Requirement | Category | Probe question | Disposition here |
|---|---|---|---|
| DOCS-07 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | **AUTHORED** — truth: a value landing exactly on a quoted tolerance boundary is a pass, not a failure. |
| DOCS-07 | empty | What is the result for empty, single-element, or null input? | **AUTHORED** — truth: the tutorial documents the empty/degenerate cases (frames with fewer than all nine fish, low-confidence flags, unreconstructed fish-frames, cameras with no detections) and names them expected rather than errors. |
| DOCS-07 | ordering | When elements compare equal, is output order specified and stable? | **AUTHORED** — truth: identity comes from the `fish_id` dataset; a fish's slot position within a frame is not a stable identity. |

### Phase-wide no-silent-drop check

5 probe-surfaced edges = 3 authored into `must_haves.truths` (this plan) + 2
surfaced as flagged assumptions (DOCS-03 in `113-01-PLAN.md`, DOCS-04 in
`113-04-PLAN.md`). No row was dismissed or dropped.

<verification>
1. The Zenodo record is published, resolves through `https://doi.org/<DOI>`, and lists exactly 22 files matching `checksums.sha256`.
2. The DOI in `docs/getting-started/tutorial.md` matches the DOI in `aquapose-tutorial-data/README.md` and in `scripts/package_tutorial_dataset.py`.
3. `hatch run docs:build` exits 0 and all three Getting Started HTML pages exist.
4. `grep -c 'grid-item-card' docs/getting-started/index.md` is exactly 3.
5. Every numeric expectation on the tutorial page is traced to a source in the SUMMARY.
6. `hatch run test` exits 0.
7. The folded upload todo is in `.planning/todos/done/` with the DOI recorded; the basedpyright backlog todo is still pending (D-19).
</verification>

<success_criteria>
- DOCS-07 is satisfiable by reading one page: install through a complete run on the published dataset to an interpreted 3D output, with a checkable expectation at every step.
- DATA-03 is complete: the dataset is deposited with a citable, resolving DOI, videos and calibration under CC-BY-4.0 and model weights labelled AGPL-derived.
- The deposit README remains the terse recipe and the tutorial is the long form — neither duplicates nor replaces the other (D-11).
- Every number on the page was measured, not assumed.
</success_criteria>

<output>
Create `.planning/phases/113-concepts-tutorial/113-07-SUMMARY.md` when done.
Record in it: the published record URL and resolved DOI, the Zenodo API file-count
and name-comparison results, a table tracing every numeric expectation on the
tutorial page to its source, the verbatim boundary-value sentence, the verbatim
`fish_id` identity sentence, the tutorial's section heading list, and the
tolerance wording chosen (Claude's Discretion per CONTEXT.md).
</output>
