---
phase: 113.2-typecheck-backlog
plan: 06
type: execute
wave: 3
depends_on: ["113.2-01", "113.2-02", "113.2-03", "113.2-04", "113.2-05"]
files_modified:
  - .planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md
  - .planning/todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md
  - .planning/todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md
  - .planning/todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md
  - .planning/PROJECT.md
autonomous: false
requirements: [QA-06]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`hatch run typecheck` exits 0 and reports `0 errors, 0 warnings, 0 notes`, down from the re-measured baseline of 98 errors (ROADMAP criterion 1)."
    - "Zero was reached by fixing code, not by weakening the checker: `pyproject.toml` `[tool.basedpyright]` still reads `typeCheckingMode = \"basic\"` with no added rule overrides, no `exclude`/`ignore` entry, and the hatch script is still `basedpyright src/`."
    - "Exactly 2 suppression directives were added across the entire phase (repo-wide count 73 against a measured baseline of 71), both in `training/reid_training.py`, both rule-scoped, both attributed inline to `pytorch_metric_learning`'s untyped constructor."
    - "The type-position count of the dynamic escape-hatch annotation across `src/aquapose/` is unchanged at 155 — no error was closed by widening."
    - "`hatch run test` reports `1403 passed, 3 skipped, 17 deselected` and the only change under `tests/` in the whole phase is the addition of `tests/unit/core/test_h5.py` — no existing test was edited, weakened, skipped or deleted to accommodate a type change."
    - "The `typecheck` job in `.github/workflows/test.yml` is unchanged and passes on `dev` (ROADMAP criterion 2)."
    - "A decision on whether `typeCheckingMode` stays `basic` or is tightened is recorded (ROADMAP criterion 4, per D-07): it stays `basic` for this phase, and the tightening evaluation is filed as its own todo rather than acted on."
    - "Every finding surfaced by plans 01-05 (the unreachable guard, the line-310 verdict, the two-idiom h5py inconsistency) is recorded in a durable artifact, not left in a plan SUMMARY alone."
  prohibitions:
    - "The phase must not reach zero errors by suppressing or weakening the type checker rather than fixing the code — no blanket suppression directives, no widening annotations to the dynamic escape-hatch type, no change to `typeCheckingMode`, no per-rule disabling, no file or path exclusion, and no weakening of the `typecheck` hatch script or the CI job that runs it. Reaching zero this way would make the README-02 badge row claim a green it did not earn, which is the exact dishonesty QA-06 exists to prevent."
    - "The phase must not change runtime behavior under cover of a typing fix, and must not edit, weaken, skip or delete any existing test in order to keep the suite green. A test that needs editing is evidence the change was behavioral, and the correct response is to stop and surface it, not to absorb it."
    - "The phase must not silently repair a latent defect that correct typing reveals. Such a finding belongs to its own decision with its own visibility; folding it into a typing diff hides a real bug inside a change everyone reads as cosmetic."
  artifacts:
    - .planning/todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md
    - .planning/todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md
    - .planning/todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md
  key_links:
    - "`pyproject.toml [tool.hatch.envs.default.scripts] typecheck` -> `.github/workflows/test.yml` `typecheck` job — the CI job runs the identical command, so a locally green `hatch run typecheck` is the strongest available local predictor of the CI result. This link is why weakening the local script would silently weaken CI too."
    - "`hatch run typecheck` exit status -> Phase 114 README-02 badge row — the consumer that makes this phase's honesty requirement load-bearing rather than aesthetic."
    - "plans 01-05 SUMMARY findings -> `.planning/todos/pending/` — the durable record; a finding that lives only in a SUMMARY is effectively lost once the phase is archived."
---

<objective>
Prove the phase reached zero the right way, and record the decisions and findings it produced.

Reaching `0 errors` is trivially achievable and nearly worthless if reached wrong. This plan
is the gate that makes the wrong path fail: it audits the finished phase against the measured
pre-phase baselines for suppression directives, escape-hatch annotations, checker
configuration and test-tree changes, then confirms the CI job that has never passed on `dev`
now does.

Purpose: turn "the count is 0" into "the count is 0 and the signal that made the count
meaningful is intact."
Output: a verified-green typecheck, the D-07 decision on record, and three findings filed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113.2-typecheck-backlog/113.2-CONTEXT.md
@.planning/phases/113.2-typecheck-backlog/113.2-01-SUMMARY.md
@.planning/phases/113.2-typecheck-backlog/113.2-02-SUMMARY.md
@.planning/phases/113.2-typecheck-backlog/113.2-03-SUMMARY.md
@.planning/phases/113.2-typecheck-backlog/113.2-04-SUMMARY.md
@.planning/phases/113.2-typecheck-backlog/113.2-05-SUMMARY.md
@.planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md
</context>

<pre_phase_baselines>
All measured on this working tree at commit `d3e6073`, before any phase work. The audit in
Task 1 compares against these exact numbers.

| Quantity | Command | Baseline |
|---|---|---|
| typecheck errors | `hatch run typecheck 2>&1 \| grep -c ' - error: '` | **98** |
| suite result | `hatch run test 2>&1 \| tail -1` | **`1403 passed, 3 skipped, 17 deselected`** |
| suppression directives in `src/aquapose/` | `grep -rEho '(type\|pyright\|basedpyright):[[:space:]]*ignore' --include=*.py src/aquapose/ \| wc -l` | **71** |
| type-position escape-hatch annotations | `grep -rEho '(:[[:space:]]*Any\b\|->[[:space:]]*Any\b\|\[Any\b\|\bAny\])' --include=*.py src/aquapose/ \| wc -l` | **155** |
| `cast(` calls in `src/aquapose/` | `grep -rEho '\bcast\(' --include=*.py src/aquapose/ \| wc -l` | **49** |
| `tests/` tree object | `git rev-parse HEAD:tests` | **`129c0386389ccdf8deb9c9fa8cd4129879cfdc09`** |
| checker config | `pyproject.toml [tool.basedpyright]` | `pythonVersion = "3.11"`, `typeCheckingMode = "basic"`, and nothing else |
| typecheck script | `pyproject.toml [tool.hatch.envs.default.scripts]` | `typecheck = "basedpyright src/ {args}"` |
| CI job | `.github/workflows/test.yml` lines 46-61 | `typecheck` job, `run: hatch run typecheck`, no `continue-on-error` |

Expected post-phase values: errors **0**; suite **unchanged**; suppression directives **73**
(71 + the two `pytorch_metric_learning` directives from plan 02); escape-hatch annotations
**155** (unchanged); `cast(` **49 or fewer** (plan 04 permits converting a small number in
blocks it already touches); `tests/` changed only by the addition of
`tests/unit/core/test_h5.py`; checker config, script and CI job **byte-identical**.
</pre_phase_baselines>

<probe_accounting>
No SPEC exists for this phase, so the spec-less probe fallback fired for both the edge and
prohibition sections. Accounting is recorded here so nothing was dropped silently.

**Edges — deterministic probe over QA-06.** The probe returned 1 item:
`{applicable: 1, resolved: 0, unresolved: 1}`, category `unclassified`, status `unresolved`.
Per the `--auto` rules an `unclassified` row is never auto-resolved with a backstop and is
never auto-dismissed, so it is surfaced below as a flagged assumption.
**Equality: 1 surfaced == 0 authored into `must_haves.truths` + 1 flagged assumption.**

**Prohibitions — two-stage recall/precision pass over QA-06.** Stage 1 over-produced ~12
must-NOT candidates. Stage 2 dropped the routine-engineering ones (unused imports, formatting
drift, import-ordering hygiene — owned by `hatch run lint` and code review) and kept 3
values-axis items, all authored descriptor-less into `must_haves.prohibitions` above.
**Equality: 3 kept == 3 authored + 0 flagged.**

Canon-referral drop (breadcrumb, not minted): arbitrary-code-execution via
`np.load(..., allow_pickle=True)` at `swap_detector.py:304` is canon deserialization
security — covered by `/gsd-secure-phase`; not minted as a prohibition here. It is recorded
as T-113.2-17 in `113.2-05-PLAN.md`'s threat register so it stays visible.
</probe_accounting>

<flagged_assumptions>
**FA-01 (from the edge probe, `unresolved`/`unclassified`).** QA-06 reads "`hatch run typecheck`
exits 0 and the CI `typecheck` job passes, so the README-02 badge row can honestly claim green
CI." The deterministic edge taxonomy classifies data and behavior shapes — boundaries,
adjacency, encoding, ordering — and QA-06 asserts a tool exit status instead, so it falls
outside every category and stays `unresolved` rather than being force-fit.

The assumption this plan set makes, which is **not** stated in QA-06's text: *"exits 0" means
zero errors under the committed `basic` configuration with no file excluded, no rule disabled
and no blanket suppression* — not merely "the process returned status 0." That reading is
imported from ROADMAP criterion 3 and CONTEXT D-05/D-07, and the entire anti-suppression
apparatus in this phase rests on it. If the developer intended the weaker literal reading,
this phase is substantially over-built and Task 1's audit gates should be relaxed.

Surfaced, not resolved. It is not blocking: the strict reading is strictly safer, and the
worst case of being wrong is wasted rigor rather than a wrong outcome.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Audit the finished phase against the pre-phase baselines</name>
  <files>(read-only audit — this task modifies no file)</files>
  <read_first>
    - `.planning/phases/113.2-typecheck-backlog/113.2-01-SUMMARY.md` through `113.2-05-SUMMARY.md` — every finding, deviation and behavior-adjacent change the five plans recorded, so the audit reconciles claims against measurements.
    - `pyproject.toml` `[tool.basedpyright]` (around line 126) and `[tool.hatch.envs.default.scripts]` (around line 72) — the two places a weakened gate would hide.
    - `.github/workflows/test.yml` lines 44-62 — the `typecheck` job definition.
    - The `<pre_phase_baselines>` table above — the numbers every gate below compares against.
  </read_first>
  <action>
    Run each command in the `<pre_phase_baselines>` table against the finished working tree and
    compare to the expected post-phase value. Produce a reconciliation table in the plan
    SUMMARY with baseline, expected, actual and verdict for every row.

    Where a count differs from expectation, do not adjust the expectation. Identify which plan
    caused the difference and whether the SUMMARY for that plan disclosed it. An undisclosed
    difference in the suppression, escape-hatch or checker-configuration rows is a phase
    failure regardless of the error count, and must be reported as such rather than
    rationalized.

    Also reconcile every finding the five plans surfaced — the unreachable `points is None`
    guard from plan 04, the line-310 verdict from plan 05, any `cast()` conversions, and the
    `_compute_reproj_stats` whole-number check from plan 01 — and confirm each has a
    destination in Task 2 or Task 3.
  </action>
  <verify>
    <automated>hatch run typecheck; test "$?" = "0"</automated>
    <automated>hatch run typecheck 2>&1 | tail -1 | grep -q '0 errors, 0 warnings, 0 notes'</automated>
    <automated>hatch run test 2>&1 | tail -1 | grep -q '1403 passed, 3 skipped, 17 deselected'</automated>
    <automated>test "$(git diff --name-status d3e6073 -- tests/ | wc -l)" = "1"</automated>
    <automated>hatch run lint && hatch run format --check</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run typecheck` exits 0 and its last line is `0 errors, 0 warnings, 0 notes` (ROADMAP criterion 1).
    - `test "$(grep -rEho '(type|pyright|basedpyright):[[:space:]]*ignore' --include=*.py src/aquapose/ | wc -l)" = "73"` succeeds — baseline 71 plus exactly the two disclosed directives.
      <!-- planner-discipline-allow: ignore -->
    - `test "$(grep -rEho '(:[[:space:]]*Any\b|->[[:space:]]*Any\b|\[Any\b|\bAny\])' --include=*.py src/aquapose/ | wc -l)" = "155"` succeeds — unchanged; no error was closed by widening to the dynamic escape-hatch type.
      <!-- planner-discipline-allow: Any -->
    - `test "$(grep -rEho '\bcast\(' --include=*.py src/aquapose/ | wc -l)" -le "49"` succeeds — the unchecked-narrowing count did not grow.
    - `git diff d3e6073 -- pyproject.toml | grep -c '^[+-]'` returns `0` — `typeCheckingMode`, `pythonVersion` and the `typecheck` script are all byte-identical, so zero was not reached by loosening the checker.
    - `git diff d3e6073 -- .github/workflows/test.yml | grep -c '^[+-]'` returns `0` — the CI job was not weakened (ROADMAP criterion 3 in spirit as well as letter).
    - `git diff --name-status d3e6073 -- tests/` outputs exactly one line, an `A` for `tests/unit/core/test_h5.py` — no existing test was edited, weakened, skipped or deleted.
    - `hatch run test 2>&1 | tail -1` contains `1403 passed, 3 skipped, 17 deselected`.
    - `git diff --stat d3e6073 -- src/aquapose/ | tail -1` shows changes confined to the nine baseline error files plus the new `src/aquapose/core/h5.py` and `src/aquapose/core/reid/embedder.py` — no unrelated source file was touched.
    - `hatch run lint` and `hatch run format --check` exit 0.
    - The plan SUMMARY contains the full reconciliation table with a verdict per row, and an explicit statement of whether any row failed.
  </acceptance_criteria>
  <done>Every baseline row reconciles to its expected post-phase value, the typecheck is honestly green, and the reconciliation table is written into the SUMMARY.</done>
  <reversibility rating="reversible">A read-only audit; it changes nothing.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Record the D-07 decision and file the phase's three findings</name>
  <files>.planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md, .planning/todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md, .planning/todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md, .planning/todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md` — the originating todo, including its "Solution" step 3 which asks the `typeCheckingMode` question this task answers, and its incomplete file list (it omits `core/stitching.py`, 12 errors) which the closure note should correct for the record.
    - An existing file in `.planning/todos/done/` — for the frontmatter and closure-note convention used when a todo is completed.
    - `.planning/todos/pending/2026-09-02-core-reid-import-boundary-exemptions.md` — the format for a deliberately-deferred finding todo.
    - `.planning/PROJECT.md` lines 228-245 — the Key Decisions table format (Decision / Rationale / Outcome).
    - `113.2-01-SUMMARY.md` (the `cast()`-vs-helper scope decision and the `_compute_reproj_stats` finding), `113.2-04-SUMMARY.md` (the unreachable `points is None` guard) and `113.2-05-SUMMARY.md` (the line-310 verdict) — the findings this task files.
  </read_first>
  <action>
    Record the D-07 decision explicitly, satisfying ROADMAP criterion 4: `typeCheckingMode`
    stays `basic` for this phase, and whether to tighten it now that the tree is green is a
    separate decision that this phase deliberately does not make. Add a row to the
    `.planning/PROJECT.md` Key Decisions table capturing it, and create
    `.planning/todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md`
    describing what tightening would involve and what would have to be re-measured first.
    Recording the decision is the deliverable; do not change `pyproject.toml`.

    Create `.planning/todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md`
    for the two-idiom finding: `core/stitching.py`, `core/reid/runner.py` and
    `core/reid/swap_detector.py` now mix pre-existing unchecked `cast(h5py.X, ...)` narrowing
    with the new runtime-checked `aquapose.core.h5` helper, and the clean-looking idiom is the
    unsafe one. List the exact remaining `cast()` sites and note that converting them adds a
    runtime raise path, which is why this phase deliberately left them (D-06).

    Fold the smaller findings into that same todo or into the tightening todo, whichever fits:
    the unreachable `if points is None` guard at `core/stitching.py:563`, and — only if plan 05
    concluded "live defect" — a pointer to the separate
    `2026-09-02-swap-detector-embeddings-optional-access.md` todo it filed.

    Close the originating backlog todo by moving it from `pending/` to `done/` with a closure
    note stating the outcome (98 -> 0), the two-directive suppression budget and its
    justification, and the correction that its own file list was incomplete — it omitted
    `core/stitching.py`, which carried 12 of the 98 errors. That correction is the fourth
    instance in three phases of a planning document disagreeing with the code, and saying so
    on the record is the point.
  </action>
  <verify>
    <automated>test -f .planning/todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md && test ! -f .planning/todos/pending/2026-09-02-fix-basedpyright-typecheck-backlog.md</automated>
    <automated>test -f .planning/todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md</automated>
    <automated>test -f .planning/todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md</automated>
    <automated>test "$(git diff d3e6073 -- pyproject.toml | grep -c '^[+-]')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md` exists, `.planning/todos/pending/` no longer contains it, and its closure note states the `98 -> 0` outcome, the two-directive budget with its `pytorch_metric_learning` justification, and the correction that the todo's own file list omitted `core/stitching.py` (12 errors).
    - `.planning/todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md` exists and states that the decision is deferred, not made, per D-07.
    - `.planning/todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md` exists and lists the specific surviving `cast(h5py` sites by file and line, and the reachable-count check `grep -rn 'cast(h5py' src/aquapose/ | wc -l` in the todo matches the actual tree.
    - `grep -c 'typeCheckingMode' .planning/PROJECT.md` returns at least `1` — the D-07 decision is on the Key Decisions table, in the table's Decision / Rationale / Outcome shape.
    - `test "$(git diff d3e6073 -- pyproject.toml | grep -c '^[+-]')" = "0"` succeeds — the decision was recorded, not acted on (D-07).
    - `git diff --name-only d3e6073 -- src/` output is unchanged by this task — it touches no source file.
    - Every finding listed in the five plan SUMMARYs has a named destination, and the plan SUMMARY says where each one went.
  </acceptance_criteria>
  <done>D-07 is recorded in PROJECT.md and as a deferred todo, the two findings todos exist with concrete site lists, the originating backlog todo is closed with an accurate note, and `pyproject.toml` is untouched.</done>
  <reversibility rating="reversible">Planning-directory documents and one PROJECT.md table row; no code, no configuration.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the CI typecheck job passes on dev for the first time</name>
  <what-built>
    All 98 basedpyright errors are closed. `hatch run typecheck` now exits 0 with
    `0 errors, 0 warnings, 0 notes`, reached by one new runtime-checked h5py narrowing helper
    (`src/aquapose/core/h5.py`) applied at 15 read sites across five files, one protocol
    variance fix, one `TypedDict`, three small annotation corrections, and exactly two
    rule-scoped suppression directives for an untyped third-party constructor.

    Task 1 verified the tree against pre-phase baselines: the checker configuration, the
    `typecheck` hatch script and the CI job are byte-identical; the escape-hatch annotation
    count is unchanged at 155; the suppression count moved 71 -> 73 and no further; and the
    only change under `tests/` in the whole phase is the addition of `tests/unit/core/test_h5.py`.

    ROADMAP criterion 2 is the one thing that cannot be confirmed locally: the `typecheck` job
    has never passed on `dev`, and confirming it now does requires a push, which is your call
    rather than the executor's.
  </what-built>
  <how-to-verify>
    1. Review the reconciliation table in `113.2-06-SUMMARY.md` — especially the suppression
       (expect 73), escape-hatch (expect 155) and `pyproject.toml` diff (expect 0 lines) rows.
    2. Spot-check the two suppression directives:
       `grep -n -B2 'reportArgumentType' src/aquapose/training/reid_training.py`
       Each should carry an inline reason naming `pytorch_metric_learning` and its untyped
       `MultiSimilarityLoss.__init__`. If either reads as a bare silencer, reject.
    3. Skim the new helper: `cat src/aquapose/core/h5.py`. It should raise a real exception
       naming the key and file, with no assertion statement anywhere (D-03).
    4. Read the line-310 verdict in `113.2-05-SUMMARY.md`. If it concluded "live defect",
       confirm you are satisfied that leaving it unrepaired and filed as a todo is the
       outcome you want (D-06 says surface, do not silently fix — but this is your call).
    5. Push `dev` and watch the Tests workflow. The `typecheck` job should pass for the first
       time. If it fails while local `hatch run typecheck` is green, the difference is almost
       certainly resolved dependency versions in CI (h5py ships no `py.typed`, so basedpyright
       infers from library source and a different installed version can infer differently) —
       report the CI error text rather than adding a suppression to make it go away.
  </how-to-verify>
  <resume-signal>Type "approved" once the CI `typecheck` job is green on `dev`, or paste the CI failure output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository state -> CI quality signal -> README badge row | The `typecheck` job's result becomes a public claim about the project in Phase 114. Anything that weakens the job weakens the claim without weakening its appearance. |
| local `hatch run typecheck` -> CI `hatch run typecheck` | The same command in two dependency-resolution environments. `h5py` ships no `py.typed`, so inference comes from library source and a version skew can produce different results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113.2-18 | Spoofing | A green badge asserting a quality state the codebase does not have, because zero was reached by suppression, widening, rule-disabling or file exclusion | high | mitigate | Task 1 gates on counted deltas against measured pre-phase baselines (suppressions 71->73, escape-hatch 155 unchanged, `cast(` <= 49) plus zero-line diffs on `pyproject.toml` and `.github/workflows/test.yml`. The count alone cannot satisfy the gate. |
| T-113.2-19 | Tampering | A test edited, weakened, skipped or deleted so the suite stays green across a behavior change disguised as a typing fix | high | mitigate | `git diff --name-status d3e6073 -- tests/` must output exactly one line, an `A` for the new helper test. Any modification to an existing test file fails the gate outright. |
| T-113.2-20 | Repudiation | Findings (the unreachable guard, the line-310 verdict, the two-idiom `cast()` inconsistency) living only in plan SUMMARYs and vanishing at archive time | medium | mitigate | Task 2 files each into `.planning/todos/pending/` with concrete site lists, and an acceptance criterion requires every SUMMARY finding to have a named destination. |
| T-113.2-21 | Tampering | CI green diverging from local green due to dependency-version skew, then being "fixed" in CI with a suppression | medium | mitigate | The checkpoint explicitly instructs reporting the CI error text rather than suppressing, and names version skew as the likely cause so it is diagnosed rather than papered over. |
| T-113.2-SC | Tampering | Supply chain — package-manager installs | high | accept | No `pip`/`npm`/`cargo` install tasks anywhere in this phase; no dependency added, removed or version-changed. `pyproject.toml` is gated to a zero-line diff, which proves it. |

ASVS level 1, blocking threshold `high`. Both `high`-severity threats on live surfaces (T-113.2-18, T-113.2-19) are dispositioned `mitigate` with hard counted gates. The accepted `high` row is supply chain, which has no surface here and is proven absent by the `pyproject.toml` zero-diff gate.
</threat_model>

<artifacts_this_phase_produces>
Consolidated list for the **whole phase**, across all six plans.

**New source symbols (all in one new module):**

| Symbol | Module path | Kind |
|---|---|---|
| `require_group(parent, key)` | `aquapose.core.h5` | new public function — narrows `Group \| Dataset \| Datatype` to `h5py.Group`, raises `TypeError` naming key, found type and filename |
| `require_dataset(parent, key)` | `aquapose.core.h5` | new public function — narrows to `h5py.Dataset`, same raise contract |
| `__all__` | `aquapose.core.h5` | new module export list |
| `CameraVisibility` (name at executor's discretion) | `aquapose.evaluation.stages.reconstruction` | new `TypedDict` — per-key shape for the `camera_visibility` metrics field |

**Changed existing symbols:**

| Symbol | Module path | Change |
|---|---|---|
| `ReidConfigLike.{model_name,batch_size,crop_size,device,embedding_dim}` | `aquapose.core.reid.embedder` | writable attributes -> read-only properties |
| `ReconstructionMetrics.camera_visibility` | `aquapose.evaluation.stages.reconstruction` | field retyped to the new `TypedDict` |
| `SwapDetector._swap_events_raw` | `aquapose.core.reid.swap_detector` | gains an explicit optional annotation |

**New files:**

| Path | Kind |
|---|---|
| `src/aquapose/core/h5.py` | new module — the only new source file in the phase |
| `tests/unit/core/test_h5.py` | new test module — the only change under `tests/` in the phase |
| `.planning/todos/pending/2026-09-02-evaluate-tightening-basedpyright-typecheckingmode.md` | new todo (D-07 deferred decision) |
| `.planning/todos/pending/2026-09-02-h5py-read-idiom-inconsistency-cast-vs-checked-helper.md` | new todo (two-idiom finding) |
| `.planning/todos/done/2026-09-02-fix-basedpyright-typecheck-backlog.md` | closed originating todo (moved from `pending/`) |
| `.planning/todos/pending/2026-09-02-swap-detector-embeddings-optional-access.md` | **conditional** — only if plan 05's line-310 verdict is "live defect" |

**Nothing is removed or renamed.** No dependency is added. No configuration file changes.
</artifacts_this_phase_produces>

<verification>
- `hatch run typecheck` exits 0 with `0 errors, 0 warnings, 0 notes`.
- Suppression directives across `src/aquapose/`: exactly 73 (baseline 71 + 2 disclosed).
- Type-position escape-hatch annotations: exactly 155 (unchanged).
- `cast(` calls: 49 or fewer.
- `git diff d3e6073 -- pyproject.toml .github/workflows/test.yml` is empty.
- `git diff --name-status d3e6073 -- tests/` is exactly one `A` line for `tests/unit/core/test_h5.py`.
- `hatch run test` reports `1403 passed, 3 skipped, 17 deselected`.
- `hatch run lint` and `hatch run format --check` exit 0.
- The three todos exist and the originating todo is closed with an accurate note.
- The CI `typecheck` job passes on `dev` (human-confirmed at the checkpoint).
</verification>

<success_criteria>
QA-06 is satisfied honestly: `hatch run typecheck` exits 0, the CI job passes on `dev` for the
first time, zero was demonstrably reached by fixing code rather than weakening the checker,
no existing test was touched, and the D-07 decision plus every finding the phase produced is
on the record.
</success_criteria>

<output>
Create `.planning/phases/113.2-typecheck-backlog/113.2-06-SUMMARY.md` when done.
Record: the full baseline reconciliation table with a verdict per row, where each of the five
plans' findings was filed, the D-07 decision text as written into PROJECT.md, and the CI run
URL confirming the `typecheck` job passed on `dev`.
</output>
