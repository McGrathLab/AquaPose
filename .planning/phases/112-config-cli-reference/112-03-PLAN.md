---
phase: 112-config-cli-reference
plan: 03
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - docs/reference/config.md
autonomous: true
requirements: [DOCS-06]
must_haves:
  truths:
    - "A user can look up any config field — its type, default, and effect — without reading source"
    - "The reference is tiered: an Essential flat table (the fields aquapose init scaffolds) and an Advanced per-stage section, so a tutorial user is not confronted with every field at once"
    - "Every leaf config field across all 10 dataclasses (PipelineConfig + the 9 stage/reid configs) is covered, not the roadmap's undercounted 71"
    - "The config-loading precedence (defaults → YAML → CLI --set → freeze) is surfaced in the page intro"
  artifacts:
    - path: "docs/reference/config.md"
      provides: "Tiered config-field reference — Essential table plus Advanced per-stage subsections"
      contains: "Essential"
      min_lines: 80
  key_links:
    - from: "docs/reference/config.md"
      to: "src/aquapose/engine/config.py"
      via: "field type/default/effect adapted from the Attributes: docstrings"
      pattern: "n_animals"
    - from: "docs/reference/config.md"
      to: "docs/api/engine.rst"
      via: "cross-reference to the autodoc module page"
      pattern: "api/engine"
---

<objective>
Author the user-facing Config Reference page: a hand-authored, tiered MyST `.md` page adapting the existing `Attributes:` docstrings in `src/aquapose/engine/config.py` into an Essential flat table (Tier 1) and Advanced per-stage subsections (Tier 2). Satisfies DOCS-06.

Purpose: DOCS-06 requires every config field documented with type, default, and effect, tiered so a tutorial user isn't hit with all fields at once (D-02, D-04, D-05). Flat autodoc (already present via `api/engine.rst`) cannot deliver tiering, so this page is hand-authored. Coverage is EVERY leaf field that exists — reconcile against source, do NOT treat the roadmap's "71" as the target (D-06; PATTERNS.md reconciled ~86 leaf fields across 10 dataclasses).
Output: `docs/reference/config.md` created. (Toctree wiring and build-green gate are Plan 04.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-config-cli-reference/112-CONTEXT.md
@.planning/phases/112-config-cli-reference/112-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the complete config-field inventory and the Essential (Tier 1) table</name>
  <read_first>
    - src/aquapose/engine/config.py (ALL 10 dataclasses and their `Attributes:` docstrings: `DetectionConfig` line 29, `PoseConfig` 83, `AssociationConfig` 122, `TrackingConfig` 206, `LutConfig` 280, `SyntheticConfig` 301, `ZDenoisingConfig` 320, `ReconstructionConfig` 337, `ReidConfig` 379, `PipelineConfig` 429 — plus the module docstring at lines 1-8 for the loading precedence)
    - src/aquapose/cli.py (lines 178-208 — the `init_cmd` config dict: `project_dir`, `video_dir`, `calibration_path`, `output_dir`, `n_animals`, `detection.detector_kind`, `detection.weights_path`, `pose.weights_path`; these define the Essential set per D-05)
    - aquapose-tutorial-data/config.yaml (real minimal working config — cross-check the essential field set)
    - .planning/phases/112-config-cli-reference/112-PATTERNS.md (the config source-of-truth inventory table and the Tier-1/Tier-2 layout guidance)
  </read_first>
  <action>Create `docs/reference/config.md` (MyST `.md` — the page is authored tables/prose with no directives required, D-12; `z_uncertainty_report.md` is the MyST-authored-page precedent). Start with a `# Config Reference` title and a short intro paragraph that states the config-loading precedence — defaults then YAML file then CLI `--set` overrides then freeze (from config.py:1-8 and GUIDEBOOK §11) — and notes the CLI override syntax `--set key=val` (e.g. `--set detection.detector_kind=mog2`, from cli.py:51-56). First reconcile the EXACT leaf-field inventory by reading every `Attributes:` block in config.py; the count is ~86 leaf fields across 10 dataclasses (PATTERNS.md gives per-dataclass counts: PipelineConfig 18, Detection 7, Pose 9, Association 21, Tracking 13, Lut 5, Synthetic 4, ZDenoising 1, Reconstruction 8, Reid 5) — treat these as guides and confirm against source; do NOT anchor on the roadmap's "71". Then author the Essential (Tier 1) section: a single flat Markdown table with columns `Field | Type | Default | What to set it to`, covering exactly the fields `aquapose init` scaffolds plus `mode` (and `device` if must-know): `n_animals`, `video_dir`, `calibration_path`, `output_dir`, `project_dir`, `detection.detector_kind`, `detection.weights_path`, `pose.weights_path`, `mode`. Use dotted paths for nested fields (e.g. `detection.weights_path`). Adapt each row's type/default/effect from the corresponding `Attributes:` docstring — do NOT autodoc (D-02). Do NOT edit config.py or any docstring (scope fence). Do NOT wire into a toctree here (Plan 04).</action>
  <verify>
    <automated>test -f docs/reference/config.md && grep -q "Config Reference" docs/reference/config.md && grep -qi "Essential" docs/reference/config.md && grep -q "n_animals" docs/reference/config.md && grep -qi "defaults" docs/reference/config.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/config.md` exists with a `# Config Reference` title.
    - The intro states the loading precedence (defaults, then YAML, then CLI --set, then freeze) and mentions `--set key=val` override syntax.
    - An "Essential" section contains a single flat table with columns Field / Type / Default / What to set it to.
    - The Essential table includes rows for `n_animals`, `video_dir`, `calibration_path`, `output_dir`, `project_dir`, `detection.detector_kind`, `detection.weights_path`, `pose.weights_path`, and `mode`.
    - config.py and its docstrings are unmodified.
  </acceptance_criteria>
  <done>The Essential tier is a single flat table covering exactly the `init`-scaffolded fields (plus `mode`), with type/default/effect adapted from the source docstrings; the loading-precedence intro is present.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Advanced (Tier 2) per-stage subsections covering every remaining field</name>
  <read_first>
    - docs/reference/config.md (the file created in Task 1 — append to it)
    - src/aquapose/engine/config.py (all `Attributes:` blocks again, per dataclass — this task tables the full inventory)
    - docs/api/engine.rst (Phase 110 analog for section ordering; KEEP as-is per D-13 — do NOT edit it here)
    - .planning/GUIDEBOOK.md (§6 pipeline narrative order — Detection, Pose, Tracking, Association, Reconstruction; and §8 backends vs configurable models for `detector_kind`/`backend` fields)
    - .planning/phases/112-config-cli-reference/112-PATTERNS.md (Tier-2 per-stage subsection ordering guidance)
  </read_first>
  <action>Append an Advanced section to `docs/reference/config.md`, organized into per-stage `##`/`###` subsections ordered by the pipeline narrative (D-discretion default from GUIDEBOOK §6): Detection, Pose, Tracking, Association, Reconstruction (including the nested `z_denoising` / `ZDenoisingConfig.enabled`), LUT, Synthetic, Reid, plus a subsection for the top-level `PipelineConfig` fields not already in the Essential table (e.g. `run_id`, `device`, `n_sample_points`, `chunk_size`, `stop_after`, and the nested stage-config references named as containers). Each subsection is a Markdown table with columns `Field | Type | Default | Effect`, one row per leaf field of that dataclass, adapted from its `Attributes:` docstring. The union of the Essential table and all Advanced subsections MUST cover every leaf field across all 10 dataclasses (~86) — a field may appear in Essential OR Advanced but every field appears at least once. Note explicitly (in prose or a note admonition) that `z_denoising` is nested under `ReconstructionConfig`. Add a one-line cross-reference near the top or bottom linking to the autodoc module page via a MyST `{doc}` role or link to `../api/engine` (D-13). Do NOT edit `docs/api/engine.rst` (Plan 04 owns the reciprocal cross-link). Do NOT wire into a toctree (Plan 04). Do NOT add a drift-guard test (D-03 explicitly declined).</action>
  <verify>
    <automated>hatch run python -c "from aquapose.engine import config as c; import dataclasses as d; names=set(); [names.update(f.name for f in d.fields(getattr(c,n))) for n in ['DetectionConfig','PoseConfig','AssociationConfig','TrackingConfig','LutConfig','SyntheticConfig','ZDenoisingConfig','ReconstructionConfig','ReidConfig','PipelineConfig']]; text=open('docs/reference/config.md',encoding='utf-8').read(); missing=[x for x in names if x not in text]; print('LEAF_FIELDS', len(names)); print('MISSING', missing); assert not missing"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/config.md` contains an "Advanced" section with per-stage subsections ordered Detection, Pose, Tracking, Association, Reconstruction, LUT, Synthetic, Reid, plus a PipelineConfig subsection.
    - The verification script prints an empty `MISSING` list — every leaf field name across all 10 dataclasses appears somewhere in the page (Essential or Advanced), and the assert passes.
    - The page contains a cross-reference to `../api/engine`.
    - A note states that `z_denoising` is nested under `ReconstructionConfig`.
    - No drift-guard test was added; config.py and `docs/api/engine.rst` are unmodified.
  </acceptance_criteria>
  <done>Every leaf config field across all 10 dataclasses is documented with type/default/effect, split into Essential and Advanced tiers, cross-linked to the module API page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source config dataclasses → rendered docs | Field docstrings are manually transcribed (not introspected) into the reference page at authoring time. |

Documentation-only page. No runtime surface, no user input, no network. The content is hand-adapted from first-party docstrings.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03-T | Tampering | hand-transcribed field values drifting from source | accept | D-03 explicitly declined an automated drift-guard; reviewers catch config changes needing doc updates. The Task 2 verification script provides a lightweight name-coverage check (every field name present), bounding the drift risk to values/descriptions, not omissions. |
| T-112-03-I | Information disclosure | example values in the config tables | mitigate | Defaults and example values are transcribed from public source docstrings and the scaffold config; no secrets, credentials, or real dataset paths are embedded. |
| T-112-SC | Tampering | `sphinx-click` extension (inherited) | mitigate | Covered by Plan 01; this page uses no `sphinx-click` directive (pure MyST tables). |
</threat_model>

<verification>
- `docs/reference/config.md` exists with Essential and Advanced tiers and the loading-precedence intro.
- The Task 2 coverage script prints an empty MISSING list (every leaf field name present).
- Cross-reference to `../api/engine` present. (Full-build green is verified in Plan 04 once the page is in the toctree.)
</verification>

<success_criteria>
A reader can look up any AquaPose config field — its type, default, and effect — from `docs/reference/config.md`, with an Essential/Advanced tiering that spares a tutorial user the full inventory, satisfying DOCS-06 with complete field coverage (D-06).
</success_criteria>

<output>
Create `.planning/phases/112-config-cli-reference/112-03-SUMMARY.md` when done.
</output>
