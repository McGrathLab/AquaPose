---
phase: 111-example-dataset-reference-outputs
plan: 03
type: execute
wave: 3
depends_on: [111-01, 111-02]
files_modified:
  - scripts/package_tutorial_dataset.py
  - tests/scripts/test_package_tutorial_dataset.py
autonomous: false
requirements: [DATA-03]
user_setup:
  - service: zenodo
    why: "Manual deposit upload + DOI minting via the Zenodo web UI (D-11 — human-in-the-loop external publish, no automated upload)"
    env_vars: []
    account_setup: "A Zenodo account (log in via ORCID/GitHub) is required to create the deposit and mint the DOI."
    dashboard_config:
      - task: "Create a new deposit, upload the deposit tree, paste fields from zenodo-metadata.json, set license CC-BY-4.0, publish to mint the DOI"
        location: "https://zenodo.org/deposit/new"

must_haves:
  truths:
    - "The final checksums.sha256 manifest has exactly one line per deposit file (including reference_outputs/*), in sha256sum -c format"
    - "sha256sum -c checksums.sha256 verifies clean against the assembled tree"
    - "zenodo-metadata.json declares CC-BY-4.0 and the deposit README labels model weights as AGPL-3.0-derived, so the published deposit is correctly licensed"
    - "The dataset is uploaded to Zenodo and a citable DOI is minted (manual human step), and the DOI is recorded back into the deposit README citation block"
  artifacts:
    - path: "scripts/package_tutorial_dataset.py"
      provides: "Final checksum-manifest emission over the complete tree + a verify_deposit() completeness check"
      exports: ["write_checksums", "verify_deposit"]
    - path: "aquapose-tutorial-data/checksums.sha256"
      provides: "SHA-256 manifest of every deposit file"
      contains: "  "
  key_links:
    - from: "scripts/package_tutorial_dataset.py"
      to: "aquapose-tutorial-data/checksums.sha256"
      via: "write_checksums over the complete tree incl. reference_outputs"
      pattern: "checksums.sha256"
    - from: "aquapose-tutorial-data/zenodo-metadata.json"
      to: "Zenodo deposit"
      via: "manual web-UI upload + DOI mint (human step)"
      pattern: "cc-by-4.0"
---

<objective>
Deliver DATA-03: finalize the deposit for publication. Emit the
`checksums.sha256` manifest over the COMPLETE tree (now including the Plan-02
reference outputs), add a `verify_deposit()` completeness/integrity gate, and then
carry out the MANUAL Zenodo upload + DOI minting via the web UI (D-11 —
human-in-the-loop external publish; the script never auto-uploads). After the DOI
is minted, record it back into the deposit README citation block.

The script's automatable portion is: assemble the final manifest, verify tree
completeness + licensing, and produce a ready-to-upload tree + zenodo-metadata.json.
The external publish (upload + DOI) is modeled as a human/manual verification item,
NOT an automated task.

Purpose: A citable, checksum-verified, correctly licensed deposit on Zenodo is the
final deliverable of the phase and the anchor the Phase 113 tutorial + Phase 114
README/citation build on.
Output: `checksums.sha256`, a `verify_deposit()` gate, a minted Zenodo DOI, and the
DOI recorded in the README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-example-dataset-reference-outputs/111-CONTEXT.md
@.planning/phases/111-example-dataset-reference-outputs/111-PATTERNS.md
@.planning/phases/111-example-dataset-reference-outputs/111-01-SUMMARY.md
@.planning/phases/111-example-dataset-reference-outputs/111-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit the final checksums.sha256 manifest and add a verify_deposit() completeness + licensing gate</name>
  <files>scripts/package_tutorial_dataset.py, tests/scripts/test_package_tutorial_dataset.py</files>
  <read_first>
    - scripts/package_tutorial_dataset.py (the `_sha256`/`write_checksums` helper from Plan 01 and the `main` orchestration order)
    - .planning/phases/111-example-dataset-reference-outputs/111-PATTERNS.md (SHA-256 manifest pattern — sorted rglob, `f"{digest}  {rel}"`, skip manifest itself)
    - .planning/phases/111-example-dataset-reference-outputs/111-CONTEXT.md (D-06 tree, D-10 checksum format, D-12 licensing)
    - .planning/phases/111-example-dataset-reference-outputs/111-02-SUMMARY.md (which reference_outputs artifacts now exist)
  </read_first>
  <action>
    Ensure `write_checksums(deposit_dir)` is called from `main` as the FINAL step
    (after `--regenerate-outputs`, so the manifest covers `reference_outputs/*`).
    Confirm it uses the D-10 format: sorted `rglob("*")`, skip `checksums.sha256`
    itself, one `f"{_sha256(p)}  {p.relative_to(deposit_dir).as_posix()}"` line per
    file, utf-8, trailing newline — compatible with `sha256sum -c`.

    Add `verify_deposit(deposit_dir: Path) -> list[str]` returning a list of problem
    strings (empty == pass). It asserts the D-06 tree is complete and correctly
    licensed: (a) required paths exist — `videos/` has 12 `.mp4`, `geometry/calibration.json`,
    `models/yolo_obb.pt`, `models/yolo_pose.pt`, `config.yaml`, `README.md`,
    `zenodo-metadata.json`, and `reference_outputs/{outputs.h5, animation_3d.html,
    overlay_mosaic.mp4, timing.txt}`; (b) `config.yaml` contains no `/home/` or `D:\`
    absolute-path substring; (c) `README.md` contains both "CC-BY-4.0" and the exact
    string "AGPL-3.0-derived artifacts (trained with Ultralytics, AGPL-3.0)";
    (d) `zenodo-metadata.json` parses and has `"license": "cc-by-4.0"`. Call
    `verify_deposit` from `main` right before `write_checksums`; if it returns
    problems, print them and `return 1` (fail the run) so an incomplete/mislicensed
    tree cannot be checksummed as "verified".

    Add unit tests: build a minimal fake deposit tree in a tmp dir, run
    `write_checksums`, and assert the manifest has one line per file and lines match
    `^[0-9a-f]{64}  .+$`; run `verify_deposit` on a complete fake tree (expect empty)
    and on a tree missing a reference output / with an absolute path in config
    (expect the corresponding problem string).
  </action>
  <verify>
    <automated>hatch run test tests/scripts/test_package_tutorial_dataset.py</automated>
  </verify>
  <acceptance_criteria>
    - `write_checksums` output has one line per deposit file (excluding the manifest), each matching `^[0-9a-f]{64}  .+$` (asserted in test).
    - `verify_deposit` returns a non-empty problem list when a reference output is missing or an absolute path leaks into config.yaml, and an empty list on a complete correctly-licensed tree (asserted in test).
    - `main` calls `verify_deposit` before `write_checksums` and returns 1 on verification failure.
    - `hatch run test tests/scripts/test_package_tutorial_dataset.py` passes.
  </acceptance_criteria>
  <done>The final checksum manifest covers the complete tree (incl. reference outputs) in sha256sum -c format, and a verify_deposit gate blocks checksumming an incomplete or mislicensed deposit; both are unit-tested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify manifest integrity and deposit completeness before publish</name>
  <what-built>The packager now emits checksums.sha256 over the complete tree and runs verify_deposit() as a gate. This checkpoint confirms the manifest verifies and the tree is publication-ready before the irreversible upload.</what-built>
  <how-to-verify>
    1. Run the full packager end-to-end (or re-run the manifest step) so `aquapose-tutorial-data/checksums.sha256` reflects the complete tree including reference_outputs.
    2. From inside `aquapose-tutorial-data/`, run `sha256sum -c checksums.sha256` (on Windows use `hatch run python -c` with the script's verify path, or Git Bash) — confirm every line reports OK.
    3. Confirm the manifest has one line for every file under the deposit (count files via `Get-ChildItem -Recurse -File` minus the manifest itself equals the line count).
    4. Confirm `config.yaml` has no absolute paths and `README.md` has the CC-BY-4.0 + AGPL-derived-weights wording (verify_deposit already checks these; spot-confirm).
    5. Confirm total deposit size is a sane Zenodo size (~170-220 MB with reference outputs).
  </how-to-verify>
  <resume-signal>Type "approved" once the manifest verifies clean and the tree is complete + correctly licensed — or describe issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Manually upload the deposit to Zenodo, mint the DOI, and record it in the README</name>
  <what-built>The verified, checksum-manifested deposit tree + zenodo-metadata.json are ready to upload. The external publish is a human-only step (Zenodo web UI, no API automation per D-11): only a person with the Zenodo account can create the deposit and mint the DOI.</what-built>
  <how-to-verify>
    1. Log in to https://zenodo.org (ORCID or GitHub) and start a new deposit ("New upload").
    2. Upload the contents of `aquapose-tutorial-data/` (all files including checksums.sha256 and reference_outputs/). This is a SINGLE deposit / one DOI per D-11.
    3. Fill the metadata form from `zenodo-metadata.json`: title "AquaPose YH Tutorial Dataset", creator, description, keywords; set Upload type = Dataset; set License = "Creative Commons Attribution 4.0 International" (CC-BY-4.0); Access = Open.
    4. In the deposit description, confirm the note that bundled model weights are AGPL-3.0-derived artifacts (from the README) — the videos + calibration are CC-BY-4.0, the weights are labeled separately (D-12).
    5. Publish the deposit to mint the DOI. Copy the minted DOI.
    6. Record the DOI into the deposit `README.md` citation block (replace the DOI placeholder from Plan 01) and re-run the checksum step so `checksums.sha256` reflects the DOI-updated README, OR note that the README/DOI update is a post-publish revision (Zenodo versioning). Prefer: fill the DOI, re-emit the manifest, then upload the corrected README as a new deposit version if the DOI must live inside the published files.
    7. Confirm the deposit landing page is live and the DOI resolves.
  </how-to-verify>
  <resume-signal>Paste the minted Zenodo DOI (e.g. 10.5281/zenodo.XXXXXXX) and confirm the landing page is live. If you cannot upload right now, say "defer upload" and the DOI-dependent README line stays a placeholder for a follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembled deposit tree -> checksums.sha256 | Integrity manifest must cover every file or a corrupted/omitted file passes as verified |
| deposit tree -> public Zenodo DOI | Irreversible external exposure — once published, the DOI + files are permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-08 | Tampering / Integrity | checksums.sha256 | mitigate | Manifest is emitted over the COMPLETE tree (sorted rglob, one line per file incl. reference_outputs) as the final step; `sha256sum -c` verification is a human checkpoint before publish, closing the "corrupted deposit passes as verified" gap. |
| T-111-09 | Information Disclosure | public Zenodo publish | mitigate | `verify_deposit` blocks publish-readiness if config.yaml has absolute paths (username leak) or the tree is incomplete; upload is a deliberate human step (D-11) so nothing is auto-pushed to a permanent public DOI without review. |
| T-111-10 | Repudiation | deposit licensing | mitigate | `verify_deposit` asserts zenodo-metadata.json declares cc-by-4.0 AND README carries the AGPL-3.0-derived-weights label before checksumming — prevents shipping AGPL-derived weights mislabeled as CC-BY (D-12). |
| T-111-11 | Elevation / Tampering | oversized/wrong source publish | mitigate | Only the 30s trimmed re-encoded clips (Plan 01) + labeled models are in the tree; the 7.5 GB source is never in the deposit; the human checkpoint confirms deposit size before the irreversible upload. |
| T-111-SC | Tampering | pip/package installs | accept | No new package installs — stdlib `hashlib`/`json` only. No RESEARCH.md package audit required. |
</threat_model>

<verification>
- `hatch run test tests/scripts/test_package_tutorial_dataset.py` green.
- `hatch run check` clean on the modified script + test.
- Human checkpoint (Task 2): `sha256sum -c` verifies clean; tree complete + licensed.
- Human action (Task 3): deposit uploaded, DOI minted, DOI recorded in README.
</verification>

<success_criteria>
- `checksums.sha256` covers every deposit file (incl. reference outputs) in
  `sha256sum -c` format and verifies clean.
- `verify_deposit` gates publish-readiness on tree completeness + correct licensing.
- The dataset is live on Zenodo with a citable DOI; videos + calibration are
  CC-BY-4.0; bundled weights labeled as AGPL-derived (DATA-03 satisfied).
</success_criteria>

<output>
Create `.planning/phases/111-example-dataset-reference-outputs/111-03-SUMMARY.md` when done.
</output>
