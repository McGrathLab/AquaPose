---
phase: 108-branch-reconciliation-repo-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - LICENSE
  - LICENSING.md
  - pyproject.toml
  - README.md
  - CHANGELOG.md
  - docs/conf.py
autonomous: true
requirements: [FOUND-05]

must_haves:
  truths:
    - "D-07: every license declaration in the repo reads AGPL-3.0-or-later — `LICENSE`, the `pyproject.toml` license field, and the OSI classifier agree; no MIT declaration survives."
    - "D-08: the copyright holder is `Tucker Lancaster and the McGrath Lab at the Georgia Institute of Technology`; the string `tlancaster6` appears in no license or copyright context."
    - "D-09: a reader can find out why AquaPose is AGPL — `LICENSING.md` names Ultralytics (AGPL-3.0) and python-igraph / leidenalg (GPL-2.0+) as the forcing dependencies, and the README license section links to it."
    - "D-10: `CHANGELOG.md` records that AGPL-3.0-or-later applies as of 1.2.0, above the `<!-- version list -->` marker where python-semantic-release will not overwrite it, and the shipped v1.1.0 / v1.1.1 sections the 108-01 merge delivered are left untouched."
    - "D-11: the v1.1.0 and v1.1.1 PyPI releases are left alone — no yank, no retroactive relabel."
    - "D-12: no per-file AGPL header comments are added anywhere in `src/`."
    - "108-01's merged artifacts survive: `docs/conf.py` keeps `napoleon_use_ivar`, `autodoc_mock_imports`, and `_resolve_release()`; `pyproject.toml` keeps `version = 1.2.0-dev.0` and `detached = true`."
  artifacts:
    - path: "LICENSE"
      provides: "verbatim GNU AGPL v3 text under the D-08 copyright line"
      contains: "GNU AFFERO GENERAL PUBLIC LICENSE"
      min_lines: 600
    - path: "LICENSING.md"
      provides: "rationale naming the copyleft dependency chain"
      contains: "leidenalg"
    - path: "pyproject.toml"
      provides: "AGPL-3.0-or-later license field plus the AGPLv3+ OSI classifier"
      contains: "AGPL-3.0-or-later"
    - path: "README.md"
      provides: "license section pointing at AGPL and LICENSING.md"
      contains: "AGPL-3.0-or-later"
    - path: "CHANGELOG.md"
      provides: "license-boundary note above the semantic-release version marker"
      contains: "AGPL-3.0-or-later"
  key_links:
    - from: "README.md"
      to: "LICENSING.md"
      via: "markdown link in the License section"
      pattern: "\\(LICENSING\\.md\\)"
    - from: "pyproject.toml"
      to: "LICENSE"
      via: "SPDX identifier matching the LICENSE text"
      pattern: "AGPL-3\\.0-or-later"
---

<objective>
Relicense AquaPose from MIT to AGPL-3.0-or-later consistently across `LICENSE`, `pyproject.toml`,
`README.md`, and `docs/conf.py`, and add `LICENSING.md` explaining that the AGPL is a constraint
imposed by the dependency chain, not a preference (D-07 through D-12).

Purpose: satisfies FOUND-05. Ultralytics (AGPL-3.0) is the detection and pose pipeline and
python-igraph / leidenalg (GPL-2.0+) are the association layer — the MIT declaration is incorrect
and must be corrected before the repository is announced.

Output: an AGPL-3.0-or-later `LICENSE`, a new `LICENSING.md`, matching metadata in
`pyproject.toml`, a README pointer, and a license-boundary note in `CHANGELOG.md`.

**This plan owns `pyproject.toml` for the remainder of the phase.** No later plan may edit it.

**Do not push.** `release.yml` runs `semantic-release version` on every push to `dev`; the phase's
single push happens in plan 108-05, after the relicense has landed, so no prerelease tag is ever
cut against an MIT-declared tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**No worktree isolation.** This plan runs sequentially on the main working tree with HEAD on `dev`,
after 108-01's merge commit.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md

**Verbatim strings — copy these exactly, do not paraphrase:**

Copyright line (D-08):
`Copyright (C) 2026 Tucker Lancaster and the McGrath Lab at the Georgia Institute of Technology`

SPDX identifier (D-07): `AGPL-3.0-or-later`

OSI classifier (D-07):
`License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)`

**Current state after 108-01's merge (verified):**

- `LICENSE` is 21 lines: line 1 `MIT License`, line 3 `Copyright (c) 2026 tlancaster6`. The merge
  did not touch it (main and dev carried the same MIT text).
- `pyproject.toml` has exactly two whole-word `MIT` occurrences: line 11
  `license = {text = "MIT"}` and line 18 `"License :: OSI Approved :: MIT License",`. Line 7 is
  now `version = "1.2.0-dev.0"` and `[tool.hatch.envs.docs]` now carries `detached = true` — both
  set by 108-01 and both must survive this plan unchanged.
- `README.md` lines 63–65 are exactly `## License`, blank, `[MIT](LICENSE)`. That is the file's
  only whole-word `MIT`.
- `docs/conf.py` is now **main's** repaired version. Line 34 is `project = "Aquapose"`, line 35 is
  `copyright = f"{datetime.now().year}, Tucker Lancaster"`, line 36 is
  `author = "Tucker Lancaster"`. It contains no `MIT`.
- `CHANGELOG.md` is now **main's** version, delivered by the 108-01 merge. Line 1 is `# CHANGELOG`,
  line 3 is `<!-- version list -->`, and line 5 begins `## v1.1.1 (2026-08-12)`, followed by
  `## v1.1.0 (2026-03-06)` and then dev's original `## v1.1.0-dev.7` and older sections.
  python-semantic-release inserts new sections **after** the `<!-- version list -->` marker, so
  anything placed above that marker survives regeneration.
- `pyproject.toml` `[tool.hatch.build.targets.sdist] exclude` already lists `.planning/` — no
  packaging change is needed for the new `LICENSING.md` (it ships, which is correct).

**Grep hygiene:** the canonical AGPL v3 text contains the words `LIMITATION` and `LIMITED`, which
match a bare `grep MIT`. Every MIT-absence check in this plan therefore uses whole-word matching
(`grep -w`).

**Out of scope here:** the citation block (Phase 114), per-file AGPL headers (D-12, deferred as
PKG-02), yanking the MIT PyPI releases (D-11), and the `autodoc_mock_imports` list (plan 108-05).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the MIT license text and metadata with AGPL-3.0-or-later</name>
  <files>LICENSE, pyproject.toml, docs/conf.py</files>
  <read_first>
    - `LICENSE` — the 21-line MIT text being replaced; note the exact existing copyright line
      `Copyright (c) 2026 tlancaster6`.
    - `pyproject.toml` lines 1–25 — the `[project]` table: `version` (already `1.2.0-dev.0` from
      108-01), `license` on line 11, and the `classifiers` array containing the MIT classifier.
    - `pyproject.toml` `[tool.hatch.envs.docs]` — confirm `detached = true` is present before you
      start, so you can assert it is still present after.
    - `docs/conf.py` lines 33–41 — the project-information block containing `copyright` and
      `author`, now sourced from main.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` sections D-07, D-08,
      D-12.
  </read_first>
  <action>
    **LICENSE.** Overwrite it entirely. Line 1 is the D-08 copyright string verbatim, line 2 is
    blank, and from line 3 onward is the unmodified canonical GNU Affero General Public License
    version 3 text fetched from `https://www.gnu.org/licenses/agpl-3.0.txt`. That text begins with
    the centered title line `GNU AFFERO GENERAL PUBLIC LICENSE`, then
    `Version 3, 19 November 2007`, and ends with the "How to Apply These Terms to Your New
    Programs" appendix. Do not reflow, reindent, or trim the license body — GitHub's license
    detector hashes it. A leading copyright notice is stripped by the detector and is safe.

    **pyproject.toml.** Exactly two edits, both in the `[project]` table:
    - Replace `license = {text = "MIT"}` with `license = "AGPL-3.0-or-later"` (the PEP 639 SPDX
      string form, per D-07).
    - Replace the classifier line `"License :: OSI Approved :: MIT License",` with
      `"License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",`.

    Change nothing else in the file. In particular `version = "1.2.0-dev.0"`, the `dependencies`
    array, `detached = true`, and every `[tool.semantic_release]` table stay exactly as 108-01 left
    them.

    Then run `hatch project metadata` and discard its stdout, to make hatchling validate the
    metadata. **Fallback rule** (D-07's "where the build backend supports it"): if hatchling
    rejects the combination — some versions error when a PEP 639 SPDX string coexists with a
    `License :: OSI Approved ::` classifier — revert the license field to the table form
    `license = {text = "AGPL-3.0-or-later"}` and keep the AGPLv3+ classifier. Both forms satisfy
    FOUND-05; pick whichever makes `hatch project metadata` exit 0. Record which form you used in
    the SUMMARY.

    **docs/conf.py.** Change the `copyright` assignment on line 35 from the f-string
    `f"{datetime.now().year}, Tucker Lancaster"` to the literal string
    `"2026, Tucker Lancaster and the McGrath Lab at the Georgia Institute of Technology"`. Leave
    `author = "Tucker Lancaster"` unchanged (D-08 keeps the two consistent). If removing the
    f-string leaves `datetime` unused, remove the now-dead `from datetime import datetime` import
    so ruff stays clean; otherwise leave the import alone. Do not touch `autodoc_mock_imports`,
    `napoleon_use_ivar`, or `_resolve_release()` — plan 108-05 owns the mock list and 108-01
    already pinned the rest.

    Do **not** add AGPL header comments to any file under `src/` (D-12). Do **not** yank or modify
    anything on PyPI (D-11). Do **not** create or move a git tag.

    Commit as `feat(108-02): relicense AquaPose under AGPL-3.0-or-later`. **Do not add a `!`
    breaking-change marker or a `BREAKING CHANGE:` footer.** python-semantic-release parses these
    messages: a `!` would force a MAJOR bump to `2.0.0-dev.N` on the next push, contradicting D-03
    (the package stays on 1.x, ahead of the shipped 1.1.1). A plain `feat:` yields the intended
    `1.2.0-dev.N`. Do not push.
  </action>
  <verify>
    <automated>hatch project metadata &gt;/dev/null &amp;&amp; grep -q 'GNU AFFERO GENERAL PUBLIC LICENSE' LICENSE &amp;&amp; grep -q 'AGPL-3.0-or-later' pyproject.toml &amp;&amp; test "$(grep -cw MIT pyproject.toml)" -eq 0 &amp;&amp; hatch run lint</automated>
  </verify>
  <acceptance_criteria>
    - `hatch project metadata` exits 0.
    - `grep -c 'AGPL-3.0-or-later' pyproject.toml` returns at least `1`.
    - `grep -c 'License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)' pyproject.toml` returns `1`.
    - `grep -cw 'MIT' pyproject.toml` returns `0`.
    - `grep -cw 'MIT' LICENSE` returns `0`.
    - `grep -q 'GNU AFFERO GENERAL PUBLIC LICENSE' LICENSE` exits 0.
    - `grep -q 'Version 3, 19 November 2007' LICENSE` exits 0.
    - `grep -q 'How to Apply These Terms to Your New Programs' LICENSE` exits 0.
    - `wc -l < LICENSE` returns at least `600`.
    - `head -1 LICENSE` is exactly `Copyright (C) 2026 Tucker Lancaster and the McGrath Lab at the Georgia Institute of Technology`.
    - `grep -c 'tlancaster6' LICENSE` returns `0`.
    - `grep -c 'version = "1.2.0-dev.0"' pyproject.toml` returns `1` — 108-01's version survived.
    - `grep -c 'detached = true' pyproject.toml` returns `1` — 108-01's docs env survived.
    - `grep -q 'McGrath Lab at the Georgia Institute of Technology' docs/conf.py` exits 0.
    - `grep -c 'author = "Tucker Lancaster"' docs/conf.py` returns `1`.
    - `grep -c 'napoleon_use_ivar' docs/conf.py` returns `1` and `grep -q 'def _resolve_release' docs/conf.py` exits 0.
    - `git ls-files src | xargs grep -l 'SPDX-License-Identifier' | wc -l` returns `0` (D-12 respected).
    - `git diff --name-only HEAD~1 HEAD | sort | tr '\n' ' '` returns exactly `LICENSE docs/conf.py pyproject.toml ` — no other file changed.
    - `hatch run lint` exits 0.
    - `git status --porcelain` lists only `11.0` and `~/`.
  </acceptance_criteria>
  <done>`LICENSE` is the verbatim AGPL v3 text under the D-08 copyright line, `pyproject.toml` declares AGPL-3.0-or-later plus the AGPLv3+ OSI classifier with no whole-word MIT remaining, `docs/conf.py`'s copyright names the institution while its Sphinx repair is intact, and hatchling validates the metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Write LICENSING.md and point the README at it</name>
  <files>LICENSING.md, README.md</files>
  <read_first>
    - `README.md` lines 55–70 — the Documentation and License sections; the License section is
      exactly three lines (`## License`, blank, `[MIT](LICENSE)`), and that link is the file's only
      whole-word `MIT`.
    - `pyproject.toml` `[project] dependencies` — the authoritative list of the forcing
      dependencies (`ultralytics`, `igraph`, `leidenalg`). **Read only; task 1 owns this file.**
    - `.planning/REQUIREMENTS.md` section "Out of Scope" — records that the Ultralytics Enterprise
      License and replacing Ultralytics were both considered and rejected; `LICENSING.md` must
      reflect that reasoning.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` sections D-09, D-10,
      D-11, and `<specifics>`.
  </read_first>
  <action>
    Create `LICENSING.md` at the repository root, roughly 40–70 lines of markdown, in your own
    prose. It must:
    - State the license as `AGPL-3.0-or-later` and link to `LICENSE`.
    - Explain that the AGPL is **imposed by dependencies, not chosen as a preference**, naming them
      explicitly: **Ultralytics (AGPL-3.0)**, the detection and pose backbone, and **python-igraph
      and leidenalg (GPL-2.0+)**, the association / community-detection layer.
    - State plainly what this means for a reader who wanted a permissive license: derivative works
      and network-served deployments must also be AGPL-3.0-or-later.
    - Note that the two escape hatches were considered and rejected — an Ultralytics Enterprise
      License (costs money and does not address the GPL Leiden dependencies) and replacing
      Ultralytics to preserve MIT (it is the detection and pose pipeline).
    - Record the boundary: releases up to and including **v1.1.1** were published under MIT and
      that grant stands; AGPL-3.0-or-later applies from **1.2.0** onward. State this factually
      without adjudicating whether the earlier MIT releases were already non-compliant given the
      AGPL/GPL dependency chain (D-10).
    - Note that the MIT releases are **not** being yanked from PyPI (D-11).

    Then edit the README License section (three lines at 63–65): replace `[MIT](LICENSE)` with a
    statement that the project is licensed AGPL-3.0-or-later, linking `LICENSE`, plus a one-line
    pointer to `LICENSING.md` explaining that it covers why. Change nothing else in the README —
    the README refresh, badge row, and citation block are Phase 114's work.

    Commit as `docs(108-02): add LICENSING.md rationale and update README license section`.
    Do not push.
  </action>
  <verify>
    <automated>test -f LICENSING.md &amp;&amp; grep -q 'leidenalg' LICENSING.md &amp;&amp; grep -q 'LICENSING.md' README.md &amp;&amp; test "$(grep -cw MIT README.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSING.md` exits 0 and `wc -l < LICENSING.md` is between `25` and `120`.
    - `grep -q 'AGPL-3.0-or-later' LICENSING.md` exits 0.
    - `grep -qi 'ultralytics' LICENSING.md`, `grep -q 'leidenalg' LICENSING.md`, and `grep -qi 'igraph' LICENSING.md` each exit 0.
    - `grep -q '1.1.1' LICENSING.md` and `grep -q '1.2.0' LICENSING.md` each exit 0.
    - `grep -q 'LICENSE' LICENSING.md` exits 0 (it links the license file).
    - `grep -c 'LICENSING.md' README.md` returns at least `1`.
    - `grep -cw 'MIT' README.md` returns `0`.
    - `grep -c 'AGPL-3.0-or-later' README.md` returns at least `1`.
    - `git diff --numstat HEAD~1 HEAD -- README.md | awk '{print ($1+$2 <= 8)}'` prints `1` — the README change is confined to the License section.
    - `git diff --name-only HEAD~1 HEAD | sort | tr '\n' ' '` returns exactly `LICENSING.md README.md `.
    - `hatch run pre-commit run --files LICENSING.md README.md` exits 0.
  </acceptance_criteria>
  <done>`LICENSING.md` exists, names Ultralytics, igraph, and leidenalg as the forcing dependencies, and records the 1.1.1 / 1.2.0 boundary; the README License section declares AGPL-3.0-or-later and links to it; no other README content changed.</done>
</task>

<task type="auto">
  <name>Task 3: Record the license boundary in CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1–14 — confirm line 1 is `# CHANGELOG`, line 3 is
      `<!-- version list -->`, and that the first version section is now `## v1.1.1 (2026-08-12)`
      followed by `## v1.1.0 (2026-03-06)`, both delivered by the 108-01 merge.
    - `pyproject.toml` `[tool.semantic_release]` and `[tool.semantic_release.changelog]` — confirm
      `changelog_file = "CHANGELOG.md"` and that version sections are generated newest-first below
      the marker. **Read only.**
    - `LICENSING.md` (written in task 2) — the note must be consistent with it.
    - `.planning/phases/108-branch-reconciliation-repo-hygiene/108-CONTEXT.md` section D-10.
  </read_first>
  <action>
    Before writing, confirm the placement assumption empirically: run
    `git log --oneline -- CHANGELOG.md | head -5`, pick a `chore(release):` commit, and inspect
    `git show <sha> -- CHANGELOG.md` to verify that new version sections are inserted immediately
    **after** the `<!-- version list -->` marker and that content above the marker is preserved. If
    that does not hold, stop and report rather than writing a note that will be clobbered.

    Insert a short note between line 1 (`# CHANGELOG`) and the `<!-- version list -->` marker — in
    the region semantic-release does not regenerate. Use a small subheading such as `## Licensing`
    followed by two or three sentences stating factually that AquaPose is licensed
    `AGPL-3.0-or-later` as of version `1.2.0`, that releases up to and including `v1.1.1` were
    published under MIT and that grant is unaffected, and a link to `LICENSING.md` for the
    reasoning. State this without adjudicating whether the earlier MIT releases were compliant
    given the AGPL/GPL dependency chain (D-10).

    Do not edit, reorder, or reword any existing version section — in particular the `v1.1.1` and
    `v1.1.0` sections the merge just delivered. Do not hand-craft a release entry, bump
    `project.version`, or create a tag; python-semantic-release owns those.

    Commit as `docs(108-02): record AGPL license boundary in CHANGELOG`. Do not push.
  </action>
  <verify>
    <automated>test "$(grep -n 'AGPL-3.0-or-later' CHANGELOG.md | head -1 | cut -d: -f1)" -lt "$(grep -n '<!-- version list -->' CHANGELOG.md | cut -d: -f1)" &amp;&amp; test "$(git diff HEAD~1 HEAD -- CHANGELOG.md | grep -c '^-[^-]')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AGPL-3.0-or-later' CHANGELOG.md` returns at least `1`.
    - The line number of the first `AGPL-3.0-or-later` match is strictly less than the line number of `<!-- version list -->`.
    - `grep -q 'LICENSING.md' CHANGELOG.md` exits 0.
    - `grep -q '1.2.0' CHANGELOG.md` and `grep -q 'v1.1.1' CHANGELOG.md` each exit 0.
    - `head -1 CHANGELOG.md` is exactly `# CHANGELOG`.
    - `grep -c '<!-- version list -->' CHANGELOG.md` returns `1`.
    - `grep -c '^## v1.1.1 ' CHANGELOG.md` returns `1` and `grep -c '^## v1.1.0 ' CHANGELOG.md` returns `1` — the merged release history is intact.
    - `git diff HEAD~1 HEAD -- CHANGELOG.md | grep -c '^-[^-]'` returns `0` — insertion only, no existing line removed or altered.
    - `git diff --name-only HEAD~1 HEAD | tr '\n' ' '` returns exactly `CHANGELOG.md `.
    - `git status --porcelain` lists only `11.0` and `~/`.
    - `git log --oneline origin/dev..dev | wc -l` is greater than `0` — nothing was pushed.
  </acceptance_criteria>
  <done>`CHANGELOG.md` carries a licensing note above the `<!-- version list -->` marker stating AGPL-3.0-or-later applies as of 1.2.0 and that the v1.1.1 MIT grant stands, with no existing version section modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> downstream users | the declared license is a legally binding grant; once published it cannot be walked back |
| `LICENSE` file -> GitHub / PyPI license detectors | automated consumers hash the license body; mangled text silently misreports the license |
| `pyproject.toml` metadata -> PyPI | the license field is what a future release publishes as the package's legal terms |
| local commits -> `origin/dev` -> `release.yml` | pushing triggers `semantic-release version`, which can tag a release |
| this plan -> 108-01's merged artifacts | `pyproject.toml`, `docs/conf.py`, and `CHANGELOG.md` all carry merge content this plan must not regress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-06 | Repudiation | published license grant (irreversible once released) | mitigate | The relicense is confined to a documented, user-decided scope (D-07 through D-11); D-10's boundary note records that v1.1.1's MIT grant stands, so no reader can be misled into thinking a prior grant was revoked; the exact SPDX string and copyright holder are pinned verbatim rather than paraphrased |
| T-108-07 | Spoofing | license misattribution | mitigate | The copyright holder string is asserted byte-exact by acceptance criteria; `grep -c 'tlancaster6' LICENSE` must return 0 so the GitHub handle never stands in for the legal holder |
| T-108-08 | Tampering | canonical AGPL text integrity | mitigate | The LICENSE body is fetched from `gnu.org/licenses/agpl-3.0.txt` and asserted to contain the exact title line, version-date line, and closing appendix heading, with a 600-line floor; the action forbids reflowing or reindenting |
| T-108-09 | Information Disclosure | metadata inconsistency between `LICENSE`, `pyproject.toml`, and README | mitigate | Acceptance criteria assert `grep -cw MIT` returns 0 in `pyproject.toml`, `LICENSE`, and `README.md`; whole-word matching is mandated because the AGPL body contains `LIMITATION`, which a bare `grep MIT` would match and thereby mask a real MIT declaration |
| T-108-10 | Elevation of Privilege | `release.yml` cutting a tag against an MIT tree | mitigate | Every task ends with "do not push"; the phase's single push is deferred to plan 108-05, after this plan's commits exist |
| T-108-11 | Denial of Service | build backend rejecting a PEP 639 SPDX string alongside an OSI classifier | mitigate | Task 1 gates on `hatch project metadata` exiting 0 and specifies an explicit deterministic fallback to the table form |
| T-108-33 | Tampering | silently regressing 108-01's merged content while editing shared files | mitigate | Task 1 asserts `version = "1.2.0-dev.0"`, `detached = true`, `napoleon_use_ivar`, and `_resolve_release` all survive; task 3 asserts the merged `v1.1.1` and `v1.1.0` CHANGELOG sections are intact and that the diff is insertion-only; each task pins its exact changed-file set |
| T-108-SC | Tampering | npm/pip/cargo installs | accept | No package is installed by this plan. `hatch project metadata` reads existing metadata and does not resolve new dependencies |
</threat_model>

<verification>
- `hatch project metadata` exits 0 (FOUND-05 metadata is valid).
- `grep -cw MIT` returns 0 in `pyproject.toml`, `LICENSE`, and `README.md`.
- `head -1 LICENSE` matches the D-08 copyright string byte-for-byte.
- `LICENSING.md` exists and names Ultralytics, igraph, and leidenalg.
- The README License section links `LICENSING.md` and declares AGPL-3.0-or-later.
- The `CHANGELOG.md` licensing note sits above `<!-- version list -->` and the merged v1.1.0 / v1.1.1 sections are untouched.
- No file under `src/` gained a license header (D-12).
- 108-01's `version`, `detached = true`, `napoleon_use_ivar`, and `_resolve_release` all survive.
- `hatch run lint` passes; nothing pushed.
</verification>

<success_criteria>
- Every license declaration in the repository reads AGPL-3.0-or-later and they agree with each other.
- The copyright holder is the D-08 institution string everywhere it appears.
- A reader can answer "why can't I use this in a closed product?" from `LICENSING.md` alone.
- The v1.1.0 / v1.1.1 MIT boundary is recorded in a semantic-release-safe location.
- Nothing 108-01 delivered was regressed.
</success_criteria>

<output>
Create `.planning/phases/108-branch-reconciliation-repo-hygiene/108-02-SUMMARY.md` when done.
Record which `license` field form hatchling accepted (PEP 639 SPDX string vs the table form), and
whether the `datetime` import in `docs/conf.py` had to be removed.
</output>
