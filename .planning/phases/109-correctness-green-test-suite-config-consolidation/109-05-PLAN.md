---
phase: 109-correctness-green-test-suite-config-consolidation
plan: 05
type: execute
wave: 2
depends_on: ["109-01", "109-02", "109-03", "109-04"]
files_modified:
  - tests/unit/calibration/test_luts.py
autonomous: false
requirements: [QA-01, QA-02]
must_haves:
  truths:
    - "test_luts.py::test_forward_lut_cast_ray_matches_model is collected (not skipped/xfail) and passes with its original 1e-4 m / 0.01° tolerances intact (QA-01, D-01)"
    - "The reason STATE-108 believed QA-01 was failing is recorded (env/precision, or already fixed in Phase 108) — confirm-and-document, no code fix (D-01 superseded by baseline diagnosis)"
    - "hatch run test exits 0 (the full not-slow unit suite is green) (QA-02)"
    - "hatch run test-all exits 0 including the @slow re-ID tests (QA-02)"
    - "No @pytest.mark.skip or xfail was added anywhere in the phase to reach green, and no LUT tolerance was weakened (QA-01)"
  artifacts:
    - path: "tests/unit/calibration/test_luts.py"
      provides: "The tier-one LUT parity test, confirmed green, tolerances unchanged"
      contains: "def test_forward_lut_cast_ray_matches_model"
  key_links:
    - from: "hatch run test-all"
      to: "23 baseline failures across plans 01-04"
      via: "terminal green gate after all Wave-1 fixes land"
      pattern: "passed"
---

<objective>
Terminal green gate. Confirm QA-01 (the tier-one LUT parity test is reliably green,
resolved not skipped, with original tolerances) and record why STATE-108 believed it
was failing — a confirm-and-document task, NOT diagnose-and-fix (the baseline
diagnosis established it already passes on a fresh 3.12 env). Then verify the whole
suite is honestly green after plans 01–04 land: `hatch run test` exits 0 (per-push CI
cells) and `hatch run test-all` exits 0 (slow-tests). This plan depends on all four
Wave-1 plans and is the phase's honest-green sign-off.

Purpose: Prove the coverage badge is honest — zero failures, zero quarantined tests,
LUT tolerances untouched. This is the phase acceptance gate.

Output: A confirmed-green LUT test with a documented STATE-108 discrepancy note, and a
green full-suite run recorded, with an explicit no-skip/no-xfail/no-tolerance-weakening
attestation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-01-SUMMARY.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-02-SUMMARY.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-03-SUMMARY.md
@.planning/phases/109-correctness-green-test-suite-config-consolidation/109-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm QA-01 LUT test green and document the STATE-108 discrepancy (D-01)</name>
  <files>tests/unit/calibration/test_luts.py</files>
  <read_first>
    - tests/unit/calibration/test_luts.py — `test_forward_lut_cast_ray_matches_model` (line ~121); it asserts LUT `cast_ray()` vs `model.cast_ray()` within `1e-4 m` / `0.01°`, using the seeded `make_test_model()` fixture
    - .planning/STATE.md — line ~73, the STATE-108 claim that this test was one of "8 failing tests on dev" (a tier-one blocker)
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md — the `<baseline_diagnosis>` (QA-01 already GREEN on fresh 3.12 env; confirm-and-document, do not diagnose-fix) and D-01 (resolved-not-skipped; do not weaken tolerances)
  </read_first>
  <action>
    Run the single LUT test and confirm it is COLLECTED (not skipped, not xfail) and
    PASSES with the tolerances `1e-4 m` / `0.01°` exactly as written. Do NOT change the
    test, do NOT weaken tolerances, do NOT add any skip/xfail — this is confirm-only. If it
    passes (expected per baseline diagnosis), record in the SUMMARY the most likely reason
    STATE-108 believed it failed: it was the Linux/CI baseline estimate, or it was already
    resolved during Phase 108, or an env/precision difference — cite the baseline-diagnosis
    env (torch 2.5.1+cu121, numpy 2.5.2, pytest 9.1.1). Note that CI also runs 3.11/3.13; if
    the executor has those interpreters available it may spot-run there, but the phase
    acceptance is the local 3.12 env plus the CI matrix. In the UNEXPECTED event the test
    fails on the current env, STOP and surface it as a genuine tier-one issue for
    diagnosis under D-01 (diagnose the real cause; only adjust tolerances if the contract
    legitimately changed) — do not skip it to reach green.
  </action>
  <verify>
    <automated>hatch run test tests/unit/calibration/test_luts.py::test_forward_lut_cast_ray_matches_model -q</automated>
  </verify>
  <acceptance_criteria>
    - The LUT parity test passes, is collected (not skipped/xfail), and its `1e-4`/`0.01` tolerances are unchanged (`grep` shows the literals still present)
    - The SUMMARY records why STATE-108 believed QA-01 was failing (confirm-and-document)
    - No skip/xfail/tolerance change was introduced
  </acceptance_criteria>
  <done>QA-01 is confirmed reliably green with original tolerances and a documented STATE-108 discrepancy note; no code change beyond (at most) a clarifying comment.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green gate — hatch run test and hatch run test-all</name>
  <files>tests/unit/calibration/test_luts.py</files>
  <read_first>
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-01-SUMMARY.md, 109-02-SUMMARY.md, 109-03-SUMMARY.md, 109-04-SUMMARY.md — confirm each Wave-1 plan's fixes landed and no skip/xfail was introduced
    - CLAUDE.md — tests run via `hatch run test` (excludes @slow/@e2e) and `hatch run test-all` (includes @slow); never bare `pytest -k`
    - .planning/phases/109-correctness-green-test-suite-config-consolidation/109-CONTEXT.md — the "honest green" target: all CI cells green; the 23-failure baseline taxonomy
  </read_first>
  <action>
    Run `hatch run test` and confirm it exits 0 with zero failures (the per-push CI
    surface: the 3 CLI-help, the Windows path-sep/encoding, and the assemble failures are
    all in this not-slow set). Then run `hatch run test-all` and confirm it exits 0 with
    zero failures (adds the 2 @slow re-ID tests; the 5 e2e skips remain correctly
    marker-skipped, which is expected and NOT a violation). Audit the diff across the whole
    phase (all files_modified in plans 01-05) to attest that NO `@pytest.mark.skip` or
    `xfail` was added anywhere to reach green and NO LUT tolerance was weakened — use a
    repo-wide search for newly added skip/xfail markers and diff the `1e-4`/`0.01` literals
    against their pre-phase values. If any failure remains, do NOT quarantine it — surface
    it for a follow-up fix. This task modifies no production code; it is the sign-off gate
    (the `files_modified` entry is nominal for the LUT confirmation only).
  </action>
  <verify>
    <automated>hatch run test-all -q</automated>
  </verify>
  <acceptance_criteria>
    - `hatch run test` exits 0 (zero failures, only the intended e2e skips absent from this set)
    - `hatch run test-all` exits 0 (zero failures; exactly the 5 e2e marker-skips remain)
    - A repo-wide check confirms no `@pytest.mark.skip`/`xfail` was added in this phase and the LUT `1e-4`/`0.01` tolerances are unchanged
  </acceptance_criteria>
  <done>Both suites are green; the honest-green attestation (no added skip/xfail, no weakened tolerance) is recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full test suite is green: `hatch run test` and `hatch run test-all` both exit 0, QA-01 confirmed resolved (not skipped) with original tolerances, and no skip/xfail was introduced across the phase.</what-built>
  <how-to-verify>
    1. Run `hatch run test` — confirm it exits 0 with zero failures.
    2. Run `hatch run test-all` — confirm it exits 0 with zero failures and exactly 5 e2e skips.
    3. Confirm `test_forward_lut_cast_ray_matches_model` appears as PASSED (not skipped) and its `1e-4`/`0.01` tolerances are unchanged.
    4. Review the phase SUMMARYs' D-02 root-cause notes and the no-skip/no-xfail attestation.
    5. (Optional) Push to `dev` and confirm the test.yml matrix ({ubuntu,windows}×{3.11,3.12,3.13}) goes green, and trigger slow-tests.yml.
  </how-to-verify>
  <resume-signal>Type "approved" once both suites are green and the no-skip/no-tolerance-weakening attestation checks out, or describe any remaining failure.</resume-signal>
</task>

</tasks>

<threat_model>
Verification/sign-off work. No production code changes, no external attack surface,
no dependency installs. N/A.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05-01 | Repudiation | honest-green attestation | mitigate | Record the exact `hatch run test`/`test-all` summary lines and the no-skip/xfail search result in the SUMMARY so the green claim is auditable. |
</threat_model>

<verification>
- `hatch run test` exits 0
- `hatch run test-all` exits 0 (5 e2e skips only)
- LUT test passes, collected, tolerances unchanged
- No skip/xfail added anywhere in the phase
</verification>

<success_criteria>
QA-01 confirmed reliably green (resolved, not skipped, tolerances intact) with a
documented STATE-108 discrepancy; QA-02 satisfied — both suites green with no
quarantined tests; the coverage badge is honest.
</success_criteria>

<output>
Create `.planning/phases/109-correctness-green-test-suite-config-consolidation/109-05-SUMMARY.md` when done, including the STATE-108 discrepancy note, the full-suite green summary lines, and the no-skip/no-xfail/no-tolerance-weakening attestation.
</output>
